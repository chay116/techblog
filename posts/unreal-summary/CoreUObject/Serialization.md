---
title: "직렬화 시스템 (Serialization System)"
date: "2025-11-21"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "CoreUObject"
tags: ["unreal", "CoreUObject"]
---
# 직렬화 시스템 (Serialization System)

## 🧭 개요

**언리얼 엔진의 직렬화 시스템**은 메모리상의 데이터를 바이트 스트림으로 변환(직렬화)하고, 다시 메모리로 복원(역직렬화)하는 핵심 시스템입니다. 모든 에셋 저장/로드, 네트워크 리플리케이션, 클립보드 복사/붙여넣기가 이 시스템을 기반으로 작동합니다.

**핵심 구성 요소:**
- **FArchive** - 직렬화 추상 기본 클래스
- **ObjectReader/ObjectWriter** - UObject 직렬화
- **BulkData** - 대용량 데이터 스트리밍
- **AsyncLoading** - 비동기 에셋 로딩
- **StructuredArchive** - 구조화된 직렬화 (JSON, XML 등)
- **Custom Versioning** - 버전 관리 및 마이그레이션

**주요 기능:**
- 플랫폼 독립적 바이너리 포맷
- 리플렉션 기반 자동 직렬화 (UPROPERTY)
- 버전 관리 (FCustomVersion)
- 에디터 전용 데이터 제외 (쿠킹)
- 압축 및 암호화 지원
- 델타 직렬화 (네트워크 최적화)

**모듈 위치:**
- `Engine/Source/Runtime/Core/Public/Serialization/Archive.h`
- `Engine/Source/Runtime/CoreUObject/Public/Serialization/`

**핵심 파일:**
- `Archive.h` - FArchive 기본 클래스
- `ObjectReader.h`, `ObjectWriter.h` - UObject 직렬화
- `BulkData.h` - 대용량 데이터
- `AsyncLoading2.h` - 비동기 로딩

**엔진 버전:** Unreal Engine 5.6.1 (2025년 기준)

---

## 🧱 구조

### 직렬화 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Unreal Serialization System                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [직렬화 방향]                                                           │
│                                                                         │
│   메모리 오브젝트             직렬화              바이트 스트림           │
│   ┌──────────────┐          ────────>          ┌──────────────┐       │
│   │  UObject     │                              │  File        │       │
│   │  ┌────────┐  │   FArchive::Serialize()      │  Network     │       │
│   │  │Health  │  │   ────────────────────>      │  Memory      │       │
│   │  │Name    │  │                              │  Clipboard   │       │
│   │  │Items[] │  │   <────────────────────      └──────────────┘       │
│   │  └────────┘  │   FArchive::Serialize()                             │
│   └──────────────┘          <────────                                  │
│                             역직렬화                                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### FArchive 클래스 계층 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          FArchiveState                                  │
│  (기본 상태 및 플래그)                                                    │
├─────────────────────────────────────────────────────────────────────────┤
│  📂 위치: Core/Public/Serialization/Archive.h:69                        │
│                                                                         │
│  Protected:                                                             │
│    - bool ArIsLoading              // 로딩 중인가?                      │
│    - bool ArIsSaving               // 저장 중인가?                      │
│    - bool ArIsError                // 에러 발생?                        │
│    - bool ArIsPersistent           // 디스크 저장?                      │
│    - bool ArForceByteSwapping      // 엔디안 변환 강제?                 │
│    - bool ArContainsCode           // 코드 포함?                        │
│    - bool ArContainsMap            // 레벨/월드 포함?                   │
│                                                                         │
│  Public:                                                                │
│    + Tell() : int64                // 현재 위치                         │
│    + TotalSize() : int64           // 전체 크기                         │
│    + Seek(int64)                   // 위치 이동                         │
│    + AtEnd() : bool                // 끝에 도달?                        │
│    + SetError()                    // 에러 설정                         │
└─────────────────────────────────────────────────────────────────────────┘
                                      ▲
                                      │ 상속
                                      │
┌─────────────────────────────────────────────────────────────────────────┐
│                            FArchive                                     │
│  (직렬화 추상 기본 클래스)                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  📂 위치: Core/Public/Serialization/Archive.h:200+                      │
│                                                                         │
│  순수 가상 함수:                                                         │
│    + Serialize(void*, int64) = 0   // 핵심 직렬화 함수                  │
│                                                                         │
│  연산자 오버로드:                                                        │
│    + operator<<(int32&)            // 정수 직렬화                       │
│    + operator<<(FString&)          // 문자열 직렬화                     │
│    + operator<<(FName&)            // FName 직렬화                      │
│    + operator<<(UObject*&)         // 오브젝트 참조 직렬화              │
│    + operator<<(FText&)            // 지역화 텍스트 직렬화              │
│                                                                         │
│  커스텀 버전:                                                            │
│    + UsingCustomVersion(FGuid)     // 커스텀 버전 사용 선언            │
│    + CustomVer(FGuid) : int32      // 커스텀 버전 조회                  │
│                                                                         │
│  압축:                                                                  │
│    + SerializeCompressed(...)      // 압축 직렬화                       │
└─────────────────────────────────────────────────────────────────────────┘
                                      ▲
                      ┌───────────────┼───────────────┬─────────────────┐
                      │               │               │                 │
        ┌─────────────┴────┐   ┌─────┴──────┐   ┌───┴────────┐   ┌───┴────────┐
        │  FArchiveProxy   │   │ FMemoryArchive│  │ FLinkerLoad│   │FLinkerSave │
        │  (프록시)        │   │ (메모리)      │  │ (로딩)     │   │ (저장)     │
        └──────────────────┘   └───────────────┘  └────────────┘   └────────────┘
                │
        ┌───────┴────────┐
        │                │
┌───────┴────────┐  ┌───┴──────────┐
│ FObjectReader  │  │ FObjectWriter│
│ (오브젝트 로드)│  │(오브젝트 저장)│
└────────────────┘  └──────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                         메모리 기반 Archive                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  FMemoryReader                    FMemoryWriter                        │
│  ┌──────────────────┐            ┌──────────────────┐                 │
│  │ TArray<uint8>&   │            │ TArray<uint8>&   │                 │
│  │ Data Buffer      │            │ Data Buffer      │                 │
│  │                  │            │                  │                 │
│  │ - 읽기 전용      │            │ - 쓰기 전용      │                 │
│  │ - 포인터로 탐색  │            │ - 자동 확장      │                 │
│  └──────────────────┘            └──────────────────┘                 │
│                                                                         │
│  FBufferReader                    FBufferWriter                        │
│  ┌──────────────────┐            ┌──────────────────┐                 │
│  │ uint8* Buffer    │            │ uint8* Buffer    │                 │
│  │ int64 Size       │            │ int64 Size       │                 │
│  │                  │            │                  │                 │
│  │ - 외부 버퍼      │            │ - 외부 버퍼      │                 │
│  │ - 버퍼 소유 안함 │            │ - 버퍼 소유 안함 │                 │
│  └──────────────────┘            └──────────────────┘                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 직렬화 프로세스 (UObject 저장/로드)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    UObject 직렬화 프로세스                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [저장 (Saving)]                                                        │
│                                                                         │
│   게임 코드: SaveGame()                                                 │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  1. FObjectWriter 생성                                        │    │
│   │     - TArray<uint8> Buffer 준비                              │    │
│   │     - ArIsSaving = true                                      │    │
│   │     - ArIsPersistent = true                                  │    │
│   └──────────────────────────────────────────────────────────────┘    │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  2. UObject::Serialize(FArchive&) 호출                        │    │
│   │     - Super::Serialize(Ar) (부모 클래스)                     │    │
│   │     - 리플렉션 시스템이 UPROPERTY 순회                        │    │
│   │     - 각 프로퍼티의 SerializeItem() 호출                      │    │
│   └──────────────────────────────────────────────────────────────┘    │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  3. 프로퍼티별 직렬화                                         │    │
│   │                                                               │    │
│   │  UPROPERTY()                                                  │    │
│   │  int32 Health = 100;                                          │    │
│   │  ↓                                                            │    │
│   │  FIntProperty::SerializeItem()                                │    │
│   │  Ar << Health;  // 4 bytes 쓰기                              │    │
│   │  Buffer: [64 00 00 00]                                        │    │
│   │                                                               │    │
│   │  UPROPERTY()                                                  │    │
│   │  FString Name = "Player";                                     │    │
│   │  ↓                                                            │    │
│   │  FStrProperty::SerializeItem()                                │    │
│   │  Ar << Name;                                                  │    │
│   │  Buffer: [07 00 00 00] [50 6C 61 79 65 72 00]                │    │
│   │           │             │                                     │    │
│   │           Length        "Player\0" (UTF-16 or ASCII)          │    │
│   │                                                               │    │
│   │  UPROPERTY()                                                  │    │
│   │  UTexture2D* Avatar;  // 오브젝트 참조                        │    │
│   │  ↓                                                            │    │
│   │  FObjectProperty::SerializeItem()                             │    │
│   │  Ar << Avatar;  // 오브젝트 경로 또는 인덱스 저장             │    │
│   └──────────────────────────────────────────────────────────────┘    │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  4. 파일에 저장                                               │    │
│   │     - FFileHelper::SaveArrayToFile(Buffer, Path)             │    │
│   │     - 또는 네트워크 전송, 클립보드 저장                       │    │
│   └──────────────────────────────────────────────────────────────┘    │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [로드 (Loading)]                                                       │
│                                                                         │
│   게임 코드: LoadGame()                                                 │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  1. 파일에서 읽기                                             │    │
│   │     - FFileHelper::LoadFileToArray(Buffer, Path)             │    │
│   └──────────────────────────────────────────────────────────────┘    │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  2. FObjectReader 생성                                        │    │
│   │     - TArray<uint8> Buffer 참조                              │    │
│   │     - ArIsLoading = true                                     │    │
│   │     - ArIsPersistent = true                                  │    │
│   └──────────────────────────────────────────────────────────────┘    │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  3. UObject 생성                                              │    │
│   │     - NewObject<UMyObject>()                                  │    │
│   │     - 기본 생성자 실행                                        │    │
│   └──────────────────────────────────────────────────────────────┘    │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  4. UObject::Serialize(FArchive&) 호출                        │    │
│   │     - 리플렉션 시스템이 UPROPERTY 순회                        │    │
│   │     - 각 프로퍼티의 SerializeItem() 호출                      │    │
│   │     - Buffer에서 값 읽어 메모리에 복원                        │    │
│   └──────────────────────────────────────────────────────────────┘    │
│      ↓                                                                  │
│   ┌──────────────────────────────────────────────────────────────┐    │
│   │  5. PostLoad() 호출                                           │    │
│   │     - 로드 후 초기화                                          │    │
│   │     - 버전 마이그레이션                                       │    │
│   │     - 오래된 데이터 수정                                      │    │
│   └──────────────────────────────────────────────────────────────┘    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 상세 호출 스택 (Detailed Call Stack)

```
┌─────────────────────────────────────────────────────────────────────────┐
│          UPackage 저장 - 상세 호출 스택 (FLinkerSave)                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [Phase 1: FLinkerSave 생성 및 파일 준비]                                │
│  ────────────────────────────────────────────────────────────────────  │
│  UPackage::Save()                                                       │
│      ↓                                                                  │
│  UPackage::SavePackage()                                                │
│      ↓                                                                  │
│  FLinkerSave::FLinkerSave()                      // 생성자             │
│      - Linker 초기화                                                    │
│      - Export/Import Table 준비                                         │
│      ↓                                                                  │
│  FFileManagerGeneric::CreateFileWriter()         // 파일 Writer 생성   │
│      - 파일 핸들 열기                                                   │
│      - 버퍼 준비                                                        │
│                                                                         │
│  [Phase 2: 오브젝트 직렬화 시작]                                         │
│  ────────────────────────────────────────────────────────────────────  │
│  UDataAsset::Serialize(FArchive& Ar)             // 에셋 직렬화 시작   │
│      ↓                                                                  │
│  UObject::Serialize(FArchive& Ar)                // 기본 직렬화        │
│      ↓                                                                  │
│  UObject::SerializeScriptProperties(FArchive& Ar)                       │
│      - 스크립트 프로퍼티 (UPROPERTY) 직렬화                             │
│      ↓                                                                  │
│  FArchive::MarkScriptSerializationStart(UObject*)                       │
│      - 직렬화 시작 마킹                                                 │
│      ↓                                                                  │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  [분기: TPS vs UPS]                                             │  │
│  │                                                                 │  │
│  │  if (!Ar.UseUnversionedPropertySerialization())  // TPS (에디터) │  │
│  │  {                                                              │  │
│  │      UStruct::SerializeTaggedProperties(FArchive&, ...)         │  │
│  │          - Tagged Property 방식 직렬화                           │  │
│  │          - 각 프로퍼티마다 태그 + 데이터 저장                     │  │
│  │          ↓                                                      │  │
│  │      UStruct::SerializeVersionedTaggedProperties(FArchive&, ...) │ │
│  │          - 버전 정보 포함 직렬화                                 │  │
│  │          ↓                                                      │  │
│  │      FPropertyTag::SerializeTaggedProperty(FArchive&, ...)      │  │
│  │          - Property Tag 직렬화 (이름, GUID, 타입, 크기)          │  │
│  │  }                                                              │  │
│  │  else  // UPS (쿠킹 빌드)                                        │  │
│  │  {                                                              │  │
│  │      SerializeUnversionedProperties(Ar, ...)                    │  │
│  │          - 태그 없이 순서대로 직렬화                             │  │
│  │          - 빠른 성능 (검색 없음)                                 │  │
│  │  }                                                              │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  [Phase 3: 타입별 실제 데이터 직렬화]                                     │
│  ────────────────────────────────────────────────────────────────────  │
│  FTextProperty::SerializeItem(FArchive&, void*)  // FText 타입 예시    │
│      ↓                                                                  │
│  FStructuredArchiveSlot::operator<<()            // 구조화 직렬화       │
│      ↓                                                                  │
│  FArchive::operator<<(FText&)                    // FText 직렬화 연산자 │
│      ↓                                                                  │
│  FText::SerializeText(FArchive&, FText&)         // FText 직렬화       │
│      ↓                                                                  │
│  FTextHistory_Base::SerializeForDisplayString(FArchive&, ...)           │
│      - Display String 직렬화                                            │
│      ↓                                                                  │
│  FTextKey::SerializeAsString(FArchive&)          // Key 직렬화         │
│      ↓                                                                  │
│  FTextKey::SerializeAsString(FArchive&)          // Namespace 직렬화   │
│      ↓                                                                  │
│  TextKeyUtil::SaveKeyString(FArchive&, FString&)                        │
│      - 실제 문자열 저장                                                 │
│                                                                         │
│  [Phase 4: 바이트 수준 직렬화]                                           │
│  ────────────────────────────────────────────────────────────────────  │
│  FArchive::ByteOrderSerialize(void*, int64)      // 바이트 순서 처리   │
│      - 엔디안 변환 (필요 시)                                            │
│      ↓                                                                  │
│  FArchiveFileWriterGeneric::Serialize(void*, int64)                     │
│      - 파일에 실제 쓰기                                                 │
│      ↓                                                                  │
│  TArray<uint8>::Append()                         // 버퍼에 추가        │
│      ↓                                                                  │
│  FMemory::Memcpy(Dest, Src, Size)                // 메모리 복사        │
│      - 실제 바이트 복사                                                 │
│      ↓                                                                  │
│  FGenericPlatformMemory::Memcpy()                // 플랫폼 memcpy      │
│                                                                         │
│  [Phase 5: 직렬화 완료]                                                  │
│  ────────────────────────────────────────────────────────────────────  │
│  FArchive::MarkScriptSerializationEnd(UObject*)                         │
│      - 직렬화 완료 마킹                                                 │
│      ↓                                                                  │
│  FLinkerSave::CloseAndDestroySaver()             // Linker 정리        │
│      - 파일 닫기                                                        │
│      - 메모리 해제                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

```
┌─────────────────────────────────────────────────────────────────────────┐
│          UPackage 로드 - 상세 호출 스택 (FLinkerLoad)                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [Phase 1: FLinkerLoad 생성 및 파일 준비]                                │
│  ────────────────────────────────────────────────────────────────────  │
│  LoadPackage() / LoadObject()                                           │
│      ↓                                                                  │
│  FLinkerLoad::CreateLinker()                     // Linker 생성        │
│      - Package Summary 읽기                                             │
│      - Import/Export Table 파싱                                         │
│      ↓                                                                  │
│  FLinkerLoad::CreateLoader()                                            │
│      - 로더 초기화                                                      │
│      ↓                                                                  │
│  FFileManagerGeneric::CreateFileReader()         // 파일 Reader 생성   │
│      - 파일 핸들 열기                                                   │
│      ↓                                                                  │
│  FFileManagerGeneric::CreateFileReaderInternal()                        │
│      - 내부 Reader 생성                                                 │
│                                                                         │
│  [Phase 2: 오브젝트 역직렬화 시작]                                       │
│  ────────────────────────────────────────────────────────────────────  │
│  FLinkerLoad::Preload(UObject*)                  // Preload 단계       │
│      - Export 데이터 준비                                               │
│      ↓                                                                  │
│  UDataAsset::Serialize(FArchive& Ar)             // 에셋 역직렬화      │
│      ↓                                                                  │
│  UObject::Serialize(FArchive& Ar)                // 기본 역직렬화      │
│      ↓                                                                  │
│  UObject::SerializeScriptProperties(FArchive& Ar)                       │
│      - 스크립트 프로퍼티 역직렬화                                       │
│      ↓                                                                  │
│  FArchive::MarkScriptSerializationStart(UObject*)                       │
│      - 역직렬화 시작 마킹                                               │
│      ↓                                                                  │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │  [분기: TPS vs UPS]                                             │  │
│  │                                                                 │  │
│  │  if (!Ar.UseUnversionedPropertySerialization())  // TPS (에디터) │  │
│  │  {                                                              │  │
│  │      UStruct::SerializeTaggedProperties(FArchive&, ...)         │  │
│  │          - Tagged Property 복원                                 │  │
│  │          - 태그 읽고 해당 프로퍼티 찾아 값 복원                   │  │
│  │          ↓                                                      │  │
│  │      UStruct::SerializeVersionedTaggedProperties(FArchive&, ...) │ │
│  │          - 버전 정보 확인 및 마이그레이션                        │  │
│  │          ↓                                                      │  │
│  │      FPropertyTag::SerializeTaggedProperty(FArchive&, ...)      │  │
│  │          - Property Tag 읽기 (이름, 타입, 크기)                  │  │
│  │          - 해당 프로퍼티 검색 (이름/GUID로 매칭)                  │  │
│  │  }                                                              │  │
│  │  else  // UPS (쿠킹 빌드)                                        │  │
│  │  {                                                              │  │
│  │      SerializeUnversionedProperties(Ar, ...)                    │  │
│  │          - 태그 없이 순서대로 역직렬화                           │  │
│  │          - 검색 없이 순서대로 메모리에 배치                       │  │
│  │  }                                                              │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  [Phase 3: 타입별 실제 데이터 역직렬화]                                  │
│  ────────────────────────────────────────────────────────────────────  │
│  FTextProperty::SerializeItem(FArchive&, void*)  // FText 타입 예시    │
│      ↓                                                                  │
│  FStructuredArchiveSlot::operator<<()            // 구조화 역직렬화     │
│      ↓                                                                  │
│  FArchive::operator<<(FText&)                    // FText 역직렬화      │
│      ↓                                                                  │
│  FText::SerializeText(FArchive&, FText&)         // FText 복원         │
│      ↓                                                                  │
│  FTextHistory_Base::SerializeForDisplayString(FArchive&, ...)           │
│      - Display String 복원                                              │
│      ↓                                                                  │
│  FTextKey::SerializeAsString(FArchive&)          // Key 복원           │
│      ↓                                                                  │
│  FTextKey::SerializeAsString(FArchive&)          // Namespace 복원     │
│      ↓                                                                  │
│  TextKeyUtil::LoadKeyString(FArchive&, FString&)                        │
│      - 실제 문자열 읽기                                                 │
│                                                                         │
│  [Phase 4: 바이트 수준 역직렬화]                                         │
│  ────────────────────────────────────────────────────────────────────  │
│  FArchiveFileReaderGeneric::Serialize(void*, int64)                     │
│      - 파일에서 실제 읽기                                               │
│      ↓                                                                  │
│  FMemory::Memcpy(Dest, Src, Size)                // 메모리 복사        │
│      - 파일 버퍼 → 오브젝트 메모리                                      │
│      ↓                                                                  │
│  FGenericPlatformMemory::Memcpy()                // 플랫폼 memcpy      │
│                                                                         │
│  [Phase 5: 역직렬화 완료 및 초기화]                                      │
│  ────────────────────────────────────────────────────────────────────  │
│  FArchive::MarkScriptSerializationEnd(UObject*)                         │
│      - 역직렬화 완료 마킹                                               │
│      ↓                                                                  │
│  UObject::PostLoad()                             // PostLoad 단계      │
│      - 로드 후 초기화                                                   │
│      - 버전 마이그레이션 적용                                           │
│      - 참조 해결 (Fixup)                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔬 설계 철학

### 왜 커스텀 직렬화인가?

### C++ 표준 직렬화의 한계

```cpp
// ❌ C++ 표준 - 직렬화 지원 없음

class MyClass
{
    int Health;
    std::string Name;
};

// ❌ 불가능한 것들:
// - 자동 직렬화 (수동으로 모든 필드 작성 필요)
// - 버전 관리 (데이터 포맷 변경 시 마이그레이션 어려움)
// - 플랫폼 독립성 (엔디안, 정렬, 포인터 크기)
// - 오브젝트 참조 (포인터를 어떻게 저장?)
// - 리플렉션 활용 (어떤 필드가 있는지 알 수 없음)
```

```cpp
// ✅ Unreal 직렬화 - 모든 것이 자동

UCLASS()
class UMyClass : public UObject
{
    GENERATED_BODY()

    UPROPERTY()
    int32 Health;

    UPROPERTY()
    FString Name;

    UPROPERTY()
    UTexture2D* Avatar;  // 오브젝트 참조도 자동!

    // 직렬화 코드 작성 불필요!
    // 리플렉션이 자동으로 처리
};

// ✅ 가능한 것들:
// - 자동 직렬화 (UPROPERTY만 추가)
// - 버전 관리 (FCustomVersion으로 자동 마이그레이션)
// - 플랫폼 독립성 (모든 플랫폼에서 동일한 포맷)
// - 오브젝트 참조 (경로 또는 인덱스로 자동 변환)
// - 에디터 전용 데이터 제외 (쿠킹 시 자동)
```

### 직렬화 시스템 비교 테이블

| 특징 | **C++ 표준** | **Boost.Serialization** | **Unreal FArchive** |
|------|--------------|------------------------|---------------------|
| **자동 직렬화** | ❌ 수동 코드 필요 | ⚠️ 매크로 필요 | ✅ UPROPERTY만 추가 |
| **버전 관리** | ❌ 없음 | ⚠️ 제한적 | ✅ FCustomVersion (강력) |
| **오브젝트 참조** | ❌ 불가능 | ⚠️ 제한적 | ✅ 자동 경로 변환 |
| **플랫폼 독립성** | ❌ 플랫폼 의존적 | ✅ 지원 | ✅ 완전 독립적 |
| **압축** | ❌ 없음 | ✅ 지원 | ✅ 자동 압축 |
| **비동기 로딩** | ❌ 없음 | ❌ 없음 | ✅ AsyncLoading2 |
| **에디터 전용 데이터** | ❌ 없음 | ❌ 없음 | ✅ 자동 제외 (쿠킹) |
| **네트워크 최적화** | ❌ 없음 | ❌ 없음 | ✅ 델타 직렬화 |
| **리플렉션 통합** | ❌ 없음 | ❌ 없음 | ✅ 완전 통합 |

---

### 프로퍼티 직렬화 방식의 진화: TPS → UPS

언리얼 엔진은 프로퍼티 직렬화 성능을 극대화하기 위해 **UE 4.25**에서 혁신적인 변화를 도입했습니다.

#### TPS (Tagged Property Serialization) - UE 4.24 이전

**개념:**
- 각 프로퍼티마다 **태그(Tag)**를 함께 저장
- 태그 = 프로퍼티 이름, GUID, 타입 정보, 메타데이터
- 로드 시 태그를 사용해 프로퍼티를 **검색**하여 배치

**저장 구조:**
```
[Property Tag A] [Data A] [Property Tag B] [Data B] [Property Tag C] [Data C]
     ↑              ↑           ↑              ↑
  이름, GUID      실제 값    이름, GUID      실제 값
```

**장점:**
- ✅ 클래스 변경에 유연 (프로퍼티 추가/삭제/순서 변경 가능)
- ✅ 버전 호환성 우수 (이름/GUID로 검색)
- ✅ 개발 중 안전 (데이터 손실 최소화)

**단점:**
- ❌ 파일 크기 큼 (태그 오버헤드)
- ❌ 로딩 느림 (검색 오버헤드)
- ❌ CPU 병목 (태그 파싱 + 프로퍼티 검색)

**사용 영역 (현재):**
- 에디터 에셋 (.uasset)
- 개발 환경

#### UPS (Unversioned Property Serialization) - UE 4.25+

**개념:**
- 태그 없이 **프로퍼티 순서만 신뢰**
- 클래스 정의 순서대로 직렬화
- 로드 시 검색 없이 순서대로 배치

**저장 구조:**
```
[Header] [Data A] [Data B] [Data C]
   ↑        ↑        ↑        ↑
 스키마   순서대로 저장
```

**장점:**
- ✅ 파일 크기 **1/6 ~ 1/3** (태그 제거)
- ✅ 로딩 속도 **2배** (검색 제거)
- ✅ CPU 병목 해소 (단순 memcpy 수준)
- ✅ 차세대 SSD 활용 (IO 병목 → CPU 병목 해결)

**단점:**
- ❌ 클래스 변경 취약 (프로퍼티 순서 변경 불가)
- ❌ 쿠킹 빌드 전용 (에디터에서는 사용 불가)

**사용 영역 (현재):**
- 쿠킹된 빌드 (.uexp, .ubulk)
- 출시 빌드

#### 직렬화 방식 비교

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    TPS vs UPS 비교                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [TPS - Tagged Property Serialization]                                  │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  저장 (Save):                                                    │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  1. 클래스 → FProperty 참조                                      │  │
│  │     - FProperty (이름, 타입, GUID, 메타데이터)                   │  │
│  │  2. Property Tag 생성                                            │  │
│  │     - FPropertyTag { Name, GUID, Type, ... }                     │  │
│  │  3. 저장                                                         │  │
│  │     File: [Tag A][Data A][Tag B][Data B]...                     │  │
│  │                                                                  │  │
│  │  로드 (Load):                                                    │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  1. [Tag A][Data A] 추출                                         │  │
│  │  2. Tag A 사용해 클래스 내 프로퍼티 검색 (이름/GUID)             │  │
│  │     - 선형 검색 또는 해시 맵                                      │  │
│  │  3. 찾은 위치에 Data A 배치                                      │  │
│  │  4. 반복...                                                      │  │
│  │                                                                  │  │
│  │  ❌ 문제점:                                                       │  │
│  │    • 태그 크기: 프로퍼티당 ~40-80 bytes                          │  │
│  │    • 검색 비용: O(n) 또는 O(log n)                               │  │
│  │    • CPU 병목: 태그 파싱 + 검색                                  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  [UPS - Unversioned Property Serialization]                             │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  저장 (Save):                                                    │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  1. 클래스 정의 순서대로 값 수집                                 │  │
│  │     Property A, B, C → Data A, B, C                              │  │
│  │  2. 헤더 생성                                                    │  │
│  │     - 스키마 해시, 0 값 생략 정보                                │  │
│  │  3. 저장                                                         │  │
│  │     File: [Header][Data A][Data B][Data C]                      │  │
│  │                                                                  │  │
│  │  로드 (Load):                                                    │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  1. 헤더 읽기                                                    │  │
│  │  2. 순서대로 Data A, B, C 추출                                   │  │
│  │  3. 순서대로 메모리에 배치                                       │  │
│  │     - 검색 없음! 순서만 신뢰                                     │  │
│  │                                                                  │  │
│  │  ✅ 장점:                                                         │  │
│  │    • 태그 제거: 0 bytes 오버헤드                                 │  │
│  │    • 검색 제거: O(1) 직접 배치                                   │  │
│  │    • CPU 효율: memcpy 수준                                       │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 성능 측정 결과 (UE 4.25 기준)

**파일 크기 변화:**

| 에셋 타입 | TPS (UEXP) | UPS (UEXP) | 감소율 |
|----------|-----------|-----------|--------|
| **UMAP (레벨)** | 100 MB | 70 MB | **30%** |
| **블루프린트 (프로퍼티 많음)** | 18 MB | 3 MB | **83%** (1/6) |
| **서드 파티 템플릿** | 50 MB | 35 MB | **30%** |
| **머티리얼** | 10 MB | 8 MB | **20%** |

**로딩 시간 변화 (Async Loading Thread):**

| 측정 항목 | TPS | UPS | 개선율 |
|----------|-----|-----|--------|
| **프로퍼티 시리얼라이즈** (18개 액터) | 270 μs | 115 μs | **2.3배** |
| **레벨 액터 배치 + 프로퍼티** | 4.7 ms | 1.6 ms | **2.9배** |
| **전체 레벨 로딩** | 15 ms | 11 ms | **1.4배** |

**주의사항:**
- 블루프린트 노드 부분은 UPS 영향 없음 (프로퍼티만)
- 실제 게임에서는 로직 복잡도에 따라 차이 발생 가능

#### UPS 활성화 방법

**1. Engine.ini 설정**
```ini
[Core.System]
CanUseUnversionedPropertySerialization=True
```
→ **UE 5.x부터 기본값 True** (차세대 플랫폼)

**2. 패키징 옵션**
```
Project Settings → Packaging → Advanced
☑ Use Unversioned Property Serialization
☑ Use Unversioned Assets (권장)
```

또는 커맨드 라인:
```bash
# 쿠킹 시
RunUAT BuildCookRun -unversioned ...
```

**3. 플랫폼 독립성**
- 모든 플랫폼 지원 (하드웨어 독립적)
- PC, Console, Mobile 모두 사용 가능

#### UPS 사용 시 주의사항

**⚠️ 클래스 변경 제약:**

```cpp
// ❌ UPS에서 금지된 작업

// 예시: 에디터에서는 존재했으나 쿠킹 빌드에서 삭제
UCLASS()
class UMyActor : public AActor
{
    UPROPERTY()
    int32 PropertyA;  // 위치 0

#if WITH_EDITORONLY_DATA
    UPROPERTY()
    int32 PropertyB;  // 에디터: 위치 1, 쿠킹: 없음 ← 문제 발생!
#endif

    UPROPERTY()
    int32 PropertyC;  // 에디터: 위치 2, 쿠킹: 위치 1 ← 어긋남!
};

// 문제: 파일에는 B가 있지만 빌드에는 없음
// → PropertyC가 PropertyB 위치에 로드됨 (데이터 손상)
```

**✅ 올바른 패턴:**

```cpp
UCLASS()
class UMyActor : public AActor
{
    UPROPERTY()
    int32 PropertyA;

    // WITH_EDITORONLY_DATA는 괜찮음 (쿠킹 시 자동 제외)
    // 단, 순서 변경은 금지!
    UPROPERTY()
    int32 PropertyC;
};
```

**예외 처리:**
- 빌드 툴이 감시하여 경고
- C++ 전처리기 지시문으로 우회 가능 (비권장)
- 일반적인 사용에서는 문제 드묾

#### 현재 상태 (UE 5.7, 2025년)

| 환경 | 사용 방식 | 이유 |
|------|----------|------|
| **에디터** | TPS | 유연한 개발 환경 (클래스 변경 빈번) |
| **쿠킹 빌드** | **UPS (기본)** | 성능 최적화 (클래스 변경 없음) |
| **PIE (에디터 플레이)** | TPS | 에디터 환경과 동일 |

**Engine.ini 기본값:**
```ini
[Core.System]
CanUseUnversionedPropertySerialization=True  # UE 5.x 기본값
```

#### 권장 사항

**1. 신규 프로젝트:**
- UPS 기본 활성화 (이미 활성화됨)
- 추가 작업 불필요

**2. 기존 프로젝트 (UE 4.24 이하):**
- UE 4.25+ 업그레이드 시 자동 전환
- 클래스 구조 안정화 후 적용

**3. 성능 개선 목표:**
- CPU 병목 해소 (차세대 SSD 시대)
- 패치 크기 감소 (Unversioned Assets)
- 사용자 경험 개선 (로딩 시간 단축)

---

## 🧩 주요 API

### 기본 직렬화

```cpp
#include "Serialization/Archive.h"
#include "Serialization/MemoryWriter.h"
#include "Serialization/MemoryReader.h"

// 메모리 버퍼로 저장
TArray<uint8> Buffer;
FMemoryWriter Writer(Buffer);

int32 Health = 100;
FString Name = TEXT("Player");

Writer << Health;
Writer << Name;

// 메모리 버퍼에서 로드
FMemoryReader Reader(Buffer);

int32 LoadedHealth;
FString LoadedName;

Reader << LoadedHealth;
Reader << LoadedName;

check(LoadedHealth == 100);
check(LoadedName == TEXT("Player"));
```

### 커스텀 직렬화 구현

```cpp
USTRUCT()
struct FMyData
{
    GENERATED_BODY()

    UPROPERTY()
    int32 Score;

    UPROPERTY()
    TArray<FString> Names;

    // 커스텀 직렬화 연산자
    friend FArchive& operator<<(FArchive& Ar, FMyData& Data)
    {
        Ar << Data.Score;
        Ar << Data.Names;
        return Ar;
    }

    // 또는 Serialize 함수
    void Serialize(FArchive& Ar)
    {
        Ar << Score;
        Ar << Names;
    }
};

// UObject 커스텀 직렬화
UCLASS()
class UMyObject : public UObject
{
    GENERATED_BODY()

public:
    UPROPERTY()
    int32 Health;

    // 압축되지 않은 대용량 데이터 (UPROPERTY 외부)
    TArray<float> HeavyData;

    virtual void Serialize(FArchive& Ar) override
    {
        Super::Serialize(Ar);

        // UPROPERTY는 자동 직렬화됨
        // HeavyData는 수동 직렬화
        if (Ar.IsSaving() || Ar.IsLoading())
        {
            Ar << HeavyData;
        }
    }
};
```

### 파일 저장/로드

```cpp
#include "Misc/FileHelper.h"

// 파일에 저장
TArray<uint8> Buffer;
FMemoryWriter Writer(Buffer);

UMyObject* MyObject = NewObject<UMyObject>();
MyObject->Serialize(Writer);

FFileHelper::SaveArrayToFile(Buffer, TEXT("C:/SaveData.bin"));

// 파일에서 로드
TArray<uint8> LoadedBuffer;
FFileHelper::LoadFileToArray(LoadedBuffer, TEXT("C:/SaveData.bin"));

FMemoryReader Reader(LoadedBuffer);

UMyObject* LoadedObject = NewObject<UMyObject>();
LoadedObject->Serialize(Reader);
```

---

## 💡 버전 관리 및 마이그레이션

### FCustomVersion

```cpp
#include "Serialization/CustomVersion.h"

// 커스텀 버전 정의
struct FMyGameVersion
{
    enum Type
    {
        // 버전 히스토리
        BeforeCustomVersionWasAdded = 0,
        AddedPlayerStats = 1,
        AddedInventorySystem = 2,

        // 항상 마지막에 추가
        VersionPlusOne,
        LatestVersion = VersionPlusOne - 1
    };

    // GUID (고유 식별자)
    const static FGuid GUID;
};

// cpp 파일에서
const FGuid FMyGameVersion::GUID(0x12345678, 0x12345678, 0x12345678, 0x12345678);

// 버전 등록
FCustomVersionRegistration GRegisterMyGameVersion(
    FMyGameVersion::GUID,
    FMyGameVersion::LatestVersion,
    TEXT("MyGameVersion")
);
```

### 버전별 로드

```cpp
UCLASS()
class UMyObject : public UObject
{
    GENERATED_BODY()

public:
    UPROPERTY()
    int32 Health;

    UPROPERTY()
    int32 Stamina;  // v2에서 추가됨

    virtual void Serialize(FArchive& Ar) override
    {
        Super::Serialize(Ar);

        // 커스텀 버전 사용 선언
        Ar.UsingCustomVersion(FMyGameVersion::GUID);

        // Health는 항상 저장/로드
        Ar << Health;

        // Stamina는 v2부터 추가
        if (Ar.CustomVer(FMyGameVersion::GUID) >= FMyGameVersion::AddedPlayerStats)
        {
            Ar << Stamina;
        }
        else if (Ar.IsLoading())
        {
            // 구 버전 로드 시 기본값
            Stamina = 100;
        }
    }

    virtual void PostLoad() override
    {
        Super::PostLoad();

        // 로드 후 마이그레이션
        const int32 LoadedVersion = GetLinkerCustomVersion(FMyGameVersion::GUID);

        if (LoadedVersion < FMyGameVersion::AddedInventorySystem)
        {
            // 인벤토리 시스템 추가로 인한 마이그레이션
            UE_LOG(LogTemp, Log, TEXT("Migrating from old version"));
        }
    }
};
```

---

## 🚀 BulkData (대용량 데이터 스트리밍)

**📂 위치:** `CoreUObject/Public/Serialization/BulkData.h`

### FByteBulkData

```cpp
#include "Serialization/BulkData.h"

UCLASS()
class UMyTexture : public UObject
{
    GENERATED_BODY()

public:
    // BulkData - 별도 파일에 저장됨
    FByteBulkData ImageData;

    void SetImageData(const TArray<uint8>& Data)
    {
        // BulkData에 데이터 설정
        ImageData.Lock(LOCK_READ_WRITE);
        void* Dest = ImageData.Realloc(Data.Num());
        FMemory::Memcpy(Dest, Data.GetData(), Data.Num());
        ImageData.Unlock();
    }

    TArray<uint8> GetImageData() const
    {
        TArray<uint8> Result;

        // BulkData에서 데이터 읽기
        const void* Src = ImageData.LockReadOnly();
        int64 Size = ImageData.GetBulkDataSize();
        Result.SetNum(Size);
        FMemory::Memcpy(Result.GetData(), Src, Size);
        ImageData.Unlock();

        return Result;
    }

    virtual void Serialize(FArchive& Ar) override
    {
        Super::Serialize(Ar);

        // BulkData 직렬화 (자동으로 별도 파일에 저장)
        ImageData.Serialize(Ar, this);
    }
};
```

**BulkData 특징:**
- ✅ 대용량 데이터를 별도 파일에 저장 (.uexp, .ubulk)
- ✅ 스트리밍 지원 (필요할 때만 로드)
- ✅ 압축 지원
- ✅ 에디터에서 제외 가능 (쿠킹 최적화)

---

## ⚡ 비동기 로딩 (AsyncLoading)

**📂 위치:** `CoreUObject/Public/Serialization/AsyncLoading2.h`

### 비동기 에셋 로드

```cpp
#include "Engine/StreamableManager.h"

// StreamableManager 생성
FStreamableManager StreamableManager;

// 비동기 로드
FSoftObjectPath AssetPath(TEXT("/Game/Textures/MyTexture.MyTexture"));

StreamableManager.RequestAsyncLoad(
    AssetPath,
    []()
    {
        UE_LOG(LogTemp, Log, TEXT("Asset loaded!"));
    }
);

// 여러 에셋 동시 로드
TArray<FSoftObjectPath> AssetsToLoad;
AssetsToLoad.Add(FSoftObjectPath(TEXT("/Game/Textures/Texture1")));
AssetsToLoad.Add(FSoftObjectPath(TEXT("/Game/Meshes/Mesh1")));

StreamableManager.RequestAsyncLoad(
    AssetsToLoad,
    []()
    {
        UE_LOG(LogTemp, Log, TEXT("All assets loaded!"));
    }
);
```

### LoadPackageAsync

```cpp
#include "UObject/PackageAsyncLoading.h"

// 패키지 비동기 로드
LoadPackageAsync(
    TEXT("/Game/Maps/MyLevel"),
    FLoadPackageAsyncDelegate::CreateLambda([](const FName& PackageName, UPackage* LoadedPackage, EAsyncLoadingResult::Type Result)
    {
        if (Result == EAsyncLoadingResult::Succeeded)
        {
            UE_LOG(LogTemp, Log, TEXT("Package loaded: %s"), *PackageName.ToString());
        }
    })
);
```

---

## 🎨 StructuredArchive (구조화된 직렬화)

**📂 위치:** `Core/Public/Serialization/StructuredArchive.h`

### 개요

**StructuredArchive는 타입 안전하고 구조화된 직렬화 인터페이스**입니다. 기존 FArchive의 바이너리 스트림 방식에서 발전하여 JSON, XML, Binary 등 다양한 포맷을 지원합니다.

### 기존 FArchive의 문제점

```cpp
// ❌ 기존 FArchive - 바이너리 스트림
void Serialize(FArchive& Ar)
{
    Ar << Health;     // int32
    Ar << Name;       // FString
    Ar << Position;   // FVector

    // 문제점:
    // 1. 바이너리 스트림 - 디버깅 어려움
    // 2. 타입 정보 없음 - 실수 시 크래시
    // 3. JSON/XML 변환 불가능
    // 4. 필드 이름 없음 - 데이터 의미 파악 어려움
}
```

### StructuredArchive 계층 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    StructuredArchive 계층 구조                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  FStructuredArchive                                                     │
│      ↓                                                                  │
│  FStructuredArchive::FSlot (진입점)                                     │
│      - 아직 타입이 정해지지 않은 상태                                    │
│      - EnterXXX() 호출로 실제 타입 결정                                  │
│      ↓                                                                  │
│      ├─ EnterRecord() → FRecord                                         │
│      │      - 구조체, 오브젝트 (이름 있는 필드)                          │
│      │      - { "Name": "Value", "Level": 42 }                          │
│      │                                                                  │
│      ├─ EnterArray() → FArray                                           │
│      │      - 배열 (인덱스 기반 요소)                                    │
│      │      - [1, 2, 3, 4, 5]                                           │
│      │                                                                  │
│      ├─ EnterStream() → FStream                                         │
│      │      - 스트림 (순차 데이터)                                       │
│      │      - 연속된 바이트 또는 값                                      │
│      │                                                                  │
│      └─ EnterMap() → FMap                                               │
│             - 맵 (키-값 쌍)                                              │
│             - { "Key1": Value1, "Key2": Value2 }                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### FSlot - 구조 진입점

**FSlot은 "아직 결정되지 않은 구조"를 나타냅니다:**

```cpp
// Slot은 구조 진입점
FStructuredArchive::FSlot Slot;

// 이 Slot이 어떤 구조인지 선언:
FStructuredArchive::FRecord Record = Slot.EnterRecord();  // 구조체
FStructuredArchive::FArray Array = Slot.EnterArray();     // 배열
FStructuredArchive::FStream Stream = Slot.EnterStream();  // 스트림
FStructuredArchive::FMap Map = Slot.EnterMap();           // 맵
```

### 기본 사용법

```cpp
#include "Serialization/StructuredArchive.h"

USTRUCT()
struct FPlayerData
{
    GENERATED_BODY()

    UPROPERTY()
    FString Name;

    UPROPERTY()
    int32 Level;

    UPROPERTY()
    TArray<FString> Inventory;

    void Serialize(FStructuredArchive::FSlot Slot)
    {
        // Slot → Record 변환
        FStructuredArchive::FRecord Record = Slot.EnterRecord();

        // 필드 직렬화 (이름 기반)
        Record << SA_VALUE(TEXT("Name"), Name);
        Record << SA_VALUE(TEXT("Level"), Level);
        Record << SA_VALUE(TEXT("Inventory"), Inventory);
    }
};

// JSON 출력:
// {
//   "Name": "Player1",
//   "Level": 42,
//   "Inventory": ["Sword", "Shield", "Potion"]
// }
```

### 중첩 구조 직렬화

```cpp
USTRUCT()
struct FInventoryItem
{
    GENERATED_BODY()

    UPROPERTY()
    FString ItemName;

    UPROPERTY()
    int32 Quantity;

    void Serialize(FStructuredArchive::FSlot Slot)
    {
        FStructuredArchive::FRecord Record = Slot.EnterRecord();
        Record << SA_VALUE(TEXT("ItemName"), ItemName);
        Record << SA_VALUE(TEXT("Quantity"), Quantity);
    }
};

USTRUCT()
struct FPlayerInventory
{
    GENERATED_BODY()

    UPROPERTY()
    TArray<FInventoryItem> Items;

    void Serialize(FStructuredArchive::FSlot Slot)
    {
        FStructuredArchive::FRecord Record = Slot.EnterRecord();

        // 배열 필드 진입
        FStructuredArchive::FArray ItemsArray = Record.EnterArray(
            SA_FIELD_NAME(TEXT("Items")),
            Items
        );

        // 각 요소 직렬화
        for (int32 i = 0; i < Items.Num(); ++i)
        {
            FStructuredArchive::FSlot ElementSlot = ItemsArray.EnterElement();
            Items[i].Serialize(ElementSlot);
        }
    }
};

// JSON 출력:
// {
//   "Items": [
//     { "ItemName": "Sword", "Quantity": 1 },
//     { "ItemName": "Shield", "Quantity": 1 },
//     { "ItemName": "Potion", "Quantity": 5 }
//   ]
// }
```

### FRecord - 구조체/오브젝트

```cpp
// FRecord는 이름 있는 필드를 가진 구조
FStructuredArchive::FRecord Record = Slot.EnterRecord();

// 필드 추가
Record << SA_VALUE(TEXT("FieldName"), Value);
Record.EnterField(SA_FIELD_NAME(TEXT("FieldName")));

// 중첩 Record
FStructuredArchive::FRecord NestedRecord = Record.EnterRecord(
    SA_FIELD_NAME(TEXT("NestedObject"))
);
```

### FArray - 배열

```cpp
TArray<int32> Numbers = {1, 2, 3, 4, 5};

FStructuredArchive::FArray Array = Record.EnterArray(
    SA_FIELD_NAME(TEXT("Numbers")),
    Numbers
);

// 각 요소 직렬화
for (int32 i = 0; i < Numbers.Num(); ++i)
{
    FStructuredArchive::FSlot ElementSlot = Array.EnterElement();
    ElementSlot << Numbers[i];
}

// JSON 출력:
// "Numbers": [1, 2, 3, 4, 5]
```

### FStream - 순차 스트림

```cpp
// 스트림은 순차적으로 데이터 직렬화
FStructuredArchive::FStream Stream = Slot.EnterStream();

Stream.EnterElement() << Value1;
Stream.EnterElement() << Value2;
Stream.EnterElement() << Value3;

// 이름 없는 순차 데이터
```

### FMap - 키-값 맵

```cpp
TMap<FString, int32> Stats = {
    {TEXT("Health"), 100},
    {TEXT("Mana"), 50},
    {TEXT("Stamina"), 80}
};

FStructuredArchive::FMap MapArchive = Record.EnterMap(
    SA_FIELD_NAME(TEXT("Stats")),
    Stats
);

for (const TPair<FString, int32>& Pair : Stats)
{
    FStructuredArchive::FSlot ValueSlot = MapArchive.EnterElement(Pair.Key);
    ValueSlot << Pair.Value;
}

// JSON 출력:
// "Stats": {
//   "Health": 100,
//   "Mana": 50,
//   "Stamina": 80
// }
```

### 기존 FArchive와의 통합

```cpp
// UObject 직렬화에서 Structured Archive 사용
class UMyObject : public UObject
{
public:
    virtual void Serialize(FArchive& Ar) override
    {
        Super::Serialize(Ar);

        // FArchive → FStructuredArchive 변환
        FStructuredArchiveFromArchive StructuredArchive(Ar);

        // Root Slot 가져오기
        FStructuredArchive::FSlot RootSlot = StructuredArchive.GetSlot();

        // 구조화된 직렬화
        SerializeStructured(RootSlot);
    }

    void SerializeStructured(FStructuredArchive::FSlot Slot)
    {
        FStructuredArchive::FRecord Record = Slot.EnterRecord();

        Record << SA_VALUE(TEXT("Health"), Health);
        Record << SA_VALUE(TEXT("Mana"), Mana);
        Record << SA_VALUE(TEXT("Position"), Position);
    }

private:
    int32 Health = 100;
    int32 Mana = 50;
    FVector Position;
};
```

### 호출 스택에서의 Slot

**Phase 3의 타입별 직렬화에서 Slot이 사용됩니다:**

```
FTextProperty::SerializeItem(FArchive&, void*)
    ↓
FStructuredArchiveSlot::operator<<()  // ← FSlot이 타입 안전 인터페이스 제공
    ↓
FArchive::operator<<(FText&)
    ↓
FText::SerializeText(FArchive&, FText&)
```

**역할:**
1. `FTextProperty::SerializeItem`이 프로퍼티 직렬화 시작
2. **`FStructuredArchiveSlot::operator<<()`**가 구조화된 인터페이스 제공
3. 내부적으로 `FArchive::operator<<(FText&)` 호출
4. 최종적으로 바이너리 또는 JSON/XML로 변환

### 포맷 선택

```cpp
// Binary (기본)
FArchive* BinaryAr = ...;
FStructuredArchiveFromArchive(BinaryAr);

// JSON
FJsonArchiveOutputFormatter JsonFormatter;
FStructuredArchive JsonArchive(JsonFormatter);

// XML
FXmlArchiveOutputFormatter XmlFormatter;
FStructuredArchive XmlArchive(XmlFormatter);

// 동일한 Serialize() 코드로 모든 포맷 지원!
```

### 장점 정리

| 특징 | **FArchive** | **FStructuredArchive** |
|------|--------------|------------------------|
| **타입 안전성** | ❌ 없음 | ✅ 컴파일 타임 체크 |
| **디버깅** | ❌ 바이너리 스트림 | ✅ 이름 있는 필드 |
| **포맷** | Binary만 | ✅ Binary, JSON, XML |
| **필드 이름** | ❌ 없음 | ✅ SA_VALUE 매크로 |
| **중첩 구조** | ⚠️ 수동 관리 | ✅ EnterRecord/Array/Map |
| **성능** | ✅ 빠름 | ⚠️ 약간 느림 (구조 오버헤드) |

### 사용 시나리오

**FStructuredArchive 사용:**
- JSON/XML 직렬화 필요 시
- 디버깅 및 사람이 읽을 수 있는 포맷
- 복잡한 중첩 구조
- 타입 안전성 중요

**FArchive 사용:**
- 바이너리 성능이 중요
- 기존 코드와 호환성
- 간단한 직렬화

---

## 🔧 Archive 변형들

### 1. **ArchiveUObject** - UObject 직렬화

```cpp
#include "Serialization/ArchiveUObject.h"

FArchiveUObject Ar;
Ar.SetIsSaving(true);

UObject* MyObject = ...;
Ar << MyObject;  // 오브젝트 경로 또는 인덱스로 저장
```

### 2. **ArchiveCrc32** - 체크섬 계산

```cpp
#include "Serialization/ArchiveCrc32.h"

FArchiveCrc32 Ar;
Ar << MyData;

uint32 Checksum = Ar.GetCrc();
UE_LOG(LogTemp, Log, TEXT("Checksum: %u"), Checksum);
```

### 3. **ArchiveCountMem** - 메모리 사용량 계산

```cpp
#include "Serialization/ArchiveCountMem.h"

FArchiveCountMem Ar(MyObject);
MyObject->Serialize(Ar);

int64 MemoryUsage = Ar.GetNum();
UE_LOG(LogTemp, Log, TEXT("Memory: %lld bytes"), MemoryUsage);
```

### 4. **FindReferencersArchive** - 참조 찾기

```cpp
#include "Serialization/FindReferencersArchive.h"

TArray<UObject*> Referencers;
FFindReferencersArchive Ar(TargetObject, Referencers);

// 모든 오브젝트 스캔
for (TObjectIterator<UObject> It; It; ++It)
{
    It->Serialize(Ar);
}

// Referencers에 TargetObject를 참조하는 오브젝트들이 저장됨
```

---

## 💡 최적화 및 팁

### 성능 모범 사례

```cpp
// ✅ 좋음: 작은 데이터는 UPROPERTY
UCLASS()
class UMyClass : public UObject
{
    GENERATED_BODY()

    UPROPERTY()
    int32 Health;  // 4 bytes - UPROPERTY 사용

    UPROPERTY()
    FString Name;  // 작은 문자열 - UPROPERTY 사용
};

// ❌ 나쁨: 대용량 데이터에 UPROPERTY
UCLASS()
class UBadClass : public UObject
{
    GENERATED_BODY()

    UPROPERTY()
    TArray<uint8> HugeTexture;  // 10MB+ - 메모리 낭비!
};

// ✅ 좋음: BulkData 사용
UCLASS()
class UGoodClass : public UObject
{
    GENERATED_BODY()

    FByteBulkData HugeTexture;  // 별도 파일에 저장, 스트리밍 가능
};
```

### 직렬화 제외

```cpp
UCLASS()
class UMyClass : public UObject
{
    GENERATED_BODY()

    UPROPERTY()
    int32 Health;  // 저장됨

    UPROPERTY(Transient)  // 저장 안 됨!
    float CachedValue;

    int32 NotSerialized;  // UPROPERTY 없음 - 저장 안 됨
};
```

### 압축 직렬화

```cpp
// 압축 저장
TArray<uint8> CompressedData;
FArchiveSaveCompressedProxy Compressor(
    CompressedData,
    NAME_Zlib  // 압축 알고리즘
);

Compressor << MyData;
Compressor.Close();

// 압축 로드
FArchiveLoadCompressedProxy Decompressor(
    CompressedData,
    NAME_Zlib
);

Decompressor << LoadedData;
```

---

## 🚨 일반적인 함정

### ❌ Serialize 내에서 NewObject

```cpp
// ❌ 위험: GC 중 Serialize 호출 시 크래시
virtual void Serialize(FArchive& Ar) override
{
    Super::Serialize(Ar);

    if (Ar.IsLoading())
    {
        MyObject = NewObject<UMyObject>();  // 절대 안 됨!
    }
}

// ✅ 올바름: PostLoad에서 생성
virtual void PostLoad() override
{
    Super::PostLoad();

    if (!MyObject)
    {
        MyObject = NewObject<UMyObject>();  // 안전
    }
}
```

### ❌ 포인터 직접 저장

```cpp
// ❌ 위험: 포인터 주소는 런타임마다 다름!
FArchive& operator<<(FArchive& Ar, FMyData& Data)
{
    Ar << Data.SomePointer;  // 크래시!
    return Ar;
}

// ✅ 올바름: 오브젝트 경로 저장
FArchive& operator<<(FArchive& Ar, FMyData& Data)
{
    if (Ar.IsSaving())
    {
        FString Path = Data.SomeObject ? Data.SomeObject->GetPathName() : TEXT("");
        Ar << Path;
    }
    else if (Ar.IsLoading())
    {
        FString Path;
        Ar << Path;
        Data.SomeObject = LoadObject<UObject>(nullptr, *Path);
    }
    return Ar;
}
```

### ❌ 버전 관리 없이 구조 변경

```cpp
// ❌ 나쁨: 기존 저장 파일 깨짐
USTRUCT()
struct FMyData
{
    GENERATED_BODY()

    int32 Health;
    // int32 Stamina;  // 제거됨 - 기존 파일 로드 실패!
};

// ✅ 좋음: FCustomVersion 사용
virtual void Serialize(FArchive& Ar) override
{
    Super::Serialize(Ar);

    Ar.UsingCustomVersion(FMyGameVersion::GUID);

    Ar << Health;

    if (Ar.CustomVer(FMyGameVersion::GUID) >= FMyGameVersion::AddedStamina)
    {
        Ar << Stamina;
    }
    else if (Ar.IsLoading())
    {
        Stamina = 100;  // 기본값
    }
}
```

---

## 🔍 디버깅 팁

### 직렬화 추적

```cpp
// Serialize 호출 스택 추적
#if !UE_BUILD_SHIPPING
    UE_LOG(LogSerialization, Verbose, TEXT("Serializing %s"), *GetName());
#endif
```

### 콘솔 명령어

```bash
# 에셋 정보 출력
obj dump ClassName

# 참조 찾기
obj refs name=MyObject

# 메모리 사용량
memreport -full

# 직렬화 통계
stat archive
```

---

## 🆕 UE 5.7 주요 변경사항

### BulkData 플래그 오타 수정

**📂 위치:** `Engine/Source/Runtime/CoreUObject/Public/Serialization/BulkData.h:122`

**변경 내용:**

`BULKDATA_PayloadInSeperateFile` 플래그 이름의 철자 오류가 수정되었습니다:

```cpp
// ❌ UE 5.6 이전 (오타 - Deprecated)
UE_DEPRECATED(5.7, "This flag has been renamed to BULKDATA_PayloadInSeparateFile")
BULKDATA_PayloadInSeperateFile = 1 << 8,  // "Seperate" 오타

// ✅ UE 5.7 이후 (올바른 철자)
BULKDATA_PayloadInSeparateFile = 1 << 8,  // "Separate" 정확한 철자
```

**의미:**

이 플래그는 BulkData가 별도 파일(.ubulk, .uptnl, .m.ubulk)에 저장되었음을 나타냅니다:

```cpp
// BulkData 플래그 사용 예시
if (BulkData.GetBulkDataFlags() & BULKDATA_PayloadInSeparateFile)
{
    // Payload가 별도 파일에 저장됨
    UE_LOG(LogTemp, Log, TEXT("BulkData stored in separate file"));
}
```

**마이그레이션:**

코드에서 오타가 있는 플래그를 사용 중이라면 교체해야 합니다:

```cpp
// 검색 및 교체
BULKDATA_PayloadInSeperateFile → BULKDATA_PayloadInSeparateFile
```

**영향:**

- 기존 플래그는 동일한 값으로 alias되어 있으므로 즉시 작동 중단되지는 않음
- 하지만 UE 5.7에서 deprecated 경고가 발생
- 향후 버전에서 제거될 예정이므로 빠른 교체 권장

**관련 플래그:**

```cpp
enum EBulkDataFlags
{
    BULKDATA_None = 0,
    BULKDATA_PayloadAtEndOfFile = 1 << 0,       // 파일 끝에 저장
    BULKDATA_SerializeCompressedZLIB = 1 << 1,  // ZLib 압축
    BULKDATA_ForceSingleElementSerialization = 1 << 2,
    // ...
    BULKDATA_PayloadInSeparateFile = 1 << 8,    // ✅ 올바른 이름
    // ...
};
```

**테스트:**

```cpp
// BulkData 플래그 확인
FByteBulkData MyBulkData;

// 플래그 설정
MyBulkData.SetBulkDataFlags(BULKDATA_PayloadInSeparateFile);

// 플래그 확인
if (MyBulkData.GetBulkDataFlags() & BULKDATA_PayloadInSeparateFile)
{
    UE_LOG(LogTemp, Log, TEXT("✅ Using correct flag name"));
}
```

**마이그레이션 체크리스트:**

1. ✅ 프로젝트 전체 검색: `BULKDATA_PayloadInSeperateFile`
2. ✅ 모두 `BULKDATA_PayloadInSeparateFile`로 교체
3. ✅ 컴파일하여 deprecated 경고 제거 확인
4. ✅ 플래그 동작 테스트 (파일 저장/로드)

---

## 🔗 참고자료

- [Unreal Serialization Overview](https://docs.unrealengine.com/serialization-in-unreal-engine/)
- [FArchive API](https://docs.unrealengine.com/API/Runtime/Core/Serialization/FArchive/)
- [Asset Loading](https://docs.unrealengine.com/asset-loading-in-unreal-engine/)
- [Custom Versioning](https://docs.unrealengine.com/custom-versioning-in-unreal-engine/)
- [Archive.h Source](Engine/Source/Runtime/Core/Public/Serialization/Archive.h)
- [BulkData.h Source](Engine/Source/Runtime/CoreUObject/Public/Serialization/BulkData.h)

---

> 📅 생성: 2025-10-20 — 직렬화 시스템 문서화 (UE 5.6.1 검증 완료)
> 🔄 업데이트: 2025-11-06 — UE 5.7 BulkData 플래그 오타 수정 반영