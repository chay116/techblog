---
title: "Chaos Substepping & Async Physics"
date: "2025-12-09"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Physics"
tags: ["unreal", "Physics"]
engine_version: "** Unreal Engine 5.7"
---
# Chaos Substepping & Async Physics

## 🧭 개요

**Substepping**과 **Async Physics**는 물리 시뮬레이션의 정확도와 성능을 조절하는 핵심 시스템입니다. Substepping은 고정 타임스텝으로 안정성을 확보하고, Async Physics는 물리 연산을 별도 스레드에서 실행하여 게임 스레드와 병렬화합니다.

### 핵심 개념

| 개념 | 설명 |
|------|------|
| **Substepping** | 가변 프레임 시간을 고정 물리 스텝으로 분할 |
| **Async Physics** | 물리를 별도 스레드에서 비동기 실행 |
| **Fixed Timestep** | 일정한 시간 간격의 물리 업데이트 |
| **Interpolation** | 물리 상태를 렌더링에 부드럽게 보간 |

---

## 🧱 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                     Physics Timing Architecture                                  │
└─────────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────────┐
  │                          Without Substepping                                 │
  │                                                                              │
  │   Frame 1 (16ms)   Frame 2 (33ms)   Frame 3 (8ms)    Frame 4 (20ms)        │
  │   ┌──────────┐     ┌──────────────────┐  ┌────┐     ┌─────────────┐        │
  │   │ Physics  │     │    Physics       │  │Phys│     │  Physics    │        │
  │   │  16ms    │     │     33ms         │  │8ms │     │   20ms      │        │
  │   └──────────┘     └──────────────────┘  └────┘     └─────────────┘        │
  │                                                                              │
  │   문제: 프레임마다 다른 dt → 불안정한 시뮬레이션                             │
  └─────────────────────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────────────────────┐
  │                           With Substepping                                   │
  │                                                                              │
  │   Frame 1 (16ms)   Frame 2 (33ms)          Frame 3 (8ms)   Frame 4 (20ms)  │
  │   ┌────┬────┐     ┌────┬────┬────┬────┐   ┌────┐          ┌────┬────┐     │
  │   │ 8ms│ 8ms│     │8ms │8ms │8ms │8ms+│   │ 8ms│          │8ms │8ms+│     │
  │   │ P1 │ P2 │     │ P1 │ P2 │ P3 │ P4 │   │ P1 │          │ P1 │ P2 │     │
  │   └────┴────┘     └────┴────┴────┴────┘   └────┘          └────┴────┘     │
  │                                                                              │
  │   해결: 일정한 dt (8ms) → 안정적인 시뮬레이션                               │
  └─────────────────────────────────────────────────────────────────────────────┘
```

---

## 📂 주요 소스 파일

| 파일 | 역할 |
|------|------|
| `Engine/Classes/PhysicsEngine/PhysicsSettings.h` | 물리 설정 클래스 |
| `Chaos/Public/Chaos/Framework/PhysicsSolverBase.h` | 솔버 기본 클래스 |
| `Engine/Private/PhysicsEngine/PhysicsReplication.cpp` | 비동기 틱 콜백 |
| `Engine/Public/Physics/AsyncPhysicsTickCallback.h` | Async 콜백 인터페이스 |

---

## 🔷 Substepping 설정

### 1. Project Settings

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        Physics Settings (Substepping)                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  General Settings:                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  bSubstepping           = true        // Substepping 활성화             │   │
│  │  bSubsteppingAsync      = true        // Async와 결합                   │   │
│  │  bTickPhysicsAsync      = true        // Async Physics 활성화           │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  Timing Settings:                                                                │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  MaxSubstepDeltaTime    = 0.016667    // 최대 서브스텝 시간 (60Hz)      │   │
│  │  MaxSubsteps            = 6           // 프레임당 최대 서브스텝 수       │   │
│  │  AsyncFixedTimeStepSize = 0.01667     // Async 고정 스텝 (선택)         │   │
│  │  SyncSceneSmoothingFactor = 0.0       // 보간 팩터                      │   │
│  └─────────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2. UPhysicsSettings 클래스

```cpp
// PhysicsSettings.h
UCLASS(config=Engine, defaultconfig)
class ENGINE_API UPhysicsSettings : public UObject
{
    GENERATED_BODY()

public:
    // Substepping
    UPROPERTY(config, EditAnywhere, Category = Framerate)
    bool bSubstepping;

    UPROPERTY(config, EditAnywhere, Category = Framerate,
        meta = (ClampMin = "0.001", UIMin = "0.001"))
    float MaxSubstepDeltaTime;

    UPROPERTY(config, EditAnywhere, Category = Framerate,
        meta = (ClampMin = "1", UIMin = "1", ClampMax = "16", UIMax = "16"))
    int32 MaxSubsteps;

    // Async Physics
    UPROPERTY(config, EditAnywhere, Category = Async)
    bool bTickPhysicsAsync;

    UPROPERTY(config, EditAnywhere, Category = Async)
    float AsyncFixedTimeStepSize;

    // Interpolation
    UPROPERTY(config, EditAnywhere, Category = Async)
    float SyncSceneSmoothingFactor;
};
```

---

## 🔶 Async Physics

### 1. Async Physics 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         Async Physics Architecture                               │
└─────────────────────────────────────────────────────────────────────────────────┘

    Game Thread                        Physics Thread
        │                                    │
        │                                    │
   ┌────┴────┐                         ┌────┴────┐
   │ Frame N │                         │ Physics │
   │         │                         │ Step N  │
   │ Input   │ ────────────────────>   │         │
   │ Logic   │     Push Inputs         │ Solve   │
   │ Render  │                         │ Collide │
   └────┬────┘                         └────┬────┘
        │                                    │
        │     <──────────────────────        │
        │        Pull Results               │
   ┌────┴────┐                         ┌────┴────┐
   │ Frame N+1│                        │ Physics │
   │         │                         │ Step N+1│
   └─────────┘                         └─────────┘

  특징:
  - 게임 스레드는 물리 완료를 기다리지 않음
  - 결과는 1프레임 지연될 수 있음
  - Interpolation/Extrapolation으로 보정
```

### 2. Async Tick Callback

```cpp
// AsyncPhysicsTickCallback.h
class ENGINE_API FAsyncPhysicsTickCallback
{
public:
    // 비동기 물리 틱에서 호출되는 콜백
    virtual void AsyncPhysicsTickCallback(
        float DeltaTime,
        float SimTime
    ) = 0;

    // 물리 틱 후 처리
    virtual void OnPhysicsEndTick() {}
};

// 사용 예시
class AMyActor : public AActor, public FAsyncPhysicsTickCallback
{
    virtual void AsyncPhysicsTickCallback(float DeltaTime, float SimTime) override
    {
        // 물리 스레드에서 실행됨
        // 주의: 게임 스레드와 동기화 필요!
    }
};
```

---

## 🔷 Interpolation & Extrapolation

### 상태 보간 시스템

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Physics State Interpolation                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Physics Steps:    ●─────────●─────────●─────────●                              │
│                    T0        T1        T2        T3                             │
│                                                                                  │
│  Render Frames:      ○     ○     ○     ○     ○     ○                           │
│                      R0    R1    R2    R3    R4    R5                           │
│                                                                                  │
│  Interpolation:                                                                  │
│    R1의 위치 = Lerp(T0, T1, alpha)                                             │
│    where alpha = (R1_time - T0_time) / (T1_time - T0_time)                     │
│                                                                                  │
│  Extrapolation (미래 예측):                                                     │
│    R5의 위치 = T3 + Velocity * (R5_time - T3_time)                             │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 관련 CVars

```cpp
// Interpolation 설정
p.Chaos.AsyncInterpolationMultiplier = 2.0   // 보간 배수
p.Chaos.AsyncDeltaTimeThrottle = 0.3         // 최대 보간 범위

// 시각화
p.Chaos.DebugDraw.ShowAsyncState = 1         // Async 상태 표시
```

---

## 🔶 Substepping 구현 상세

### 1. Substep 계산

```cpp
void FPhysScene_Chaos::Tick(float DeltaTime)
{
    if (bSubstepping)
    {
        // 필요한 서브스텝 수 계산
        float AccumulatedTime = DeltaTime;
        int32 NumSubsteps = 0;

        while (AccumulatedTime > 0.f && NumSubsteps < MaxSubsteps)
        {
            float StepTime = FMath::Min(AccumulatedTime, MaxSubstepDeltaTime);

            // 서브스텝 실행
            PhysicsStep(StepTime);

            AccumulatedTime -= StepTime;
            NumSubsteps++;
        }

        // 남은 시간 처리 (누적 or 버림)
        if (AccumulatedTime > 0.f)
        {
            // 최대 서브스텝 초과 - 시간 버림 or 큰 스텝
            // 기본: 마지막 스텝에 남은 시간 추가
        }
    }
    else
    {
        // 가변 스텝
        PhysicsStep(DeltaTime);
    }
}
```

### 2. Substep Delta Time 권장값

| 대상 FPS | MaxSubstepDeltaTime | MaxSubsteps | 설명 |
|----------|--------------------:|------------:|------|
| 30 FPS | 1/60 (0.0167) | 2 | 기본값, 균형 |
| 60 FPS | 1/120 (0.0083) | 2 | 부드러운 시뮬레이션 |
| 120 FPS | 1/240 (0.0042) | 2 | VR/고정밀 |
| 가변 | 1/60 (0.0167) | 6 | 프레임 변동 대응 |

---

## 🔷 Physics Tick 흐름

### 동기 vs 비동기

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Synchronous Physics (bTickPhysicsAsync = false)               │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Game Thread:                                                                    │
│  ┌──────┬──────────────┬──────────────────┬──────┐                             │
│  │Input │   Physics    │      Render      │ Wait │                             │
│  │ 2ms  │    10ms      │       8ms        │ 0ms  │                             │
│  └──────┴──────────────┴──────────────────┴──────┘                             │
│                                                                                  │
│  총 프레임 시간: 20ms (50 FPS)                                                  │
│  특징: 물리가 완료될 때까지 대기                                                │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Asynchronous Physics (bTickPhysicsAsync = true)               │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Game Thread:                                                                    │
│  ┌──────┬──────────────────┬─────┐                                             │
│  │Input │      Render      │Sync │                                             │
│  │ 2ms  │       8ms        │ 2ms │                                             │
│  └──────┴──────────────────┴─────┘                                             │
│                                                                                  │
│  Physics Thread:                                                                 │
│       ┌──────────────────────────┐                                             │
│       │        Physics           │                                             │
│       │          10ms            │                                             │
│       └──────────────────────────┘                                             │
│                                                                                  │
│  총 프레임 시간: 12ms (83 FPS) - 병렬 실행!                                     │
│  특징: 물리와 렌더링 병렬 실행                                                  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 💡 실전 사용 예시

### 1. 프로젝트 설정

```cpp
// DefaultEngine.ini
[/Script/Engine.PhysicsSettings]
bSubstepping=True
MaxSubstepDeltaTime=0.016667
MaxSubsteps=6
bTickPhysicsAsync=True
AsyncFixedTimeStepSize=0.016667
SyncSceneSmoothingFactor=0.0
```

### 2. C++에서 설정 변경

```cpp
void AMyGameMode::BeginPlay()
{
    Super::BeginPlay();

    // 물리 설정 가져오기
    UPhysicsSettings* Settings = UPhysicsSettings::Get();

    // 런타임에 설정 변경 (주의: 일부는 초기화 시에만 적용)
    Settings->bSubstepping = true;
    Settings->MaxSubstepDeltaTime = 0.008333f;  // 120Hz
    Settings->MaxSubsteps = 4;
}
```

### 3. Async Physics Callback 구현

```cpp
// 비동기 물리 콜백을 사용하는 컴포넌트
UCLASS()
class UPhysicsCallbackComponent : public UActorComponent
{
    GENERATED_BODY()

public:
    virtual void BeginPlay() override
    {
        Super::BeginPlay();

        // Async Physics에 콜백 등록
        if (UWorld* World = GetWorld())
        {
            if (FPhysScene* PhysScene = World->GetPhysicsScene())
            {
                PhysScene->RegisterAsyncPhysicsTickCallback(this);
            }
        }
    }

    // 물리 스레드에서 호출됨
    void OnAsyncPhysicsTick(float DeltaTime)
    {
        // 주의: 스레드 안전성 확보 필요!
        // 게임 스레드 데이터 직접 접근 금지

        // 물리 연산 수행
        FVector Force = CalculateForce();

        // 물리 바디에 힘 적용
        if (BodyInstance)
        {
            BodyInstance->AddForce(Force);
        }
    }
};
```

### 4. 커스텀 서브스텝 로직

```cpp
void APhysicsActor::PhysicsSubstep(float DeltaTime)
{
    // 이 함수는 각 서브스텝마다 호출됨

    // 고정 타임스텝 기반 연산
    FVector Acceleration = TotalForce / Mass;
    Velocity += Acceleration * DeltaTime;
    Position += Velocity * DeltaTime;

    // 제약 해결
    SolveConstraints();
}
```

---

## ⚙️ 성능 고려사항

### 1. Substep 수와 성능

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Substep Count vs Performance                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Substeps   시뮬레이션 비용   안정성    정확도    권장 상황                     │
│  ────────────────────────────────────────────────────────────────────────────   │
│     1          1x             낮음       낮음     단순한 게임                   │
│     2          2x             중간       중간     일반 게임 (기본값)            │
│     4          4x             높음       높음     차량, 복잡한 물리             │
│     6          6x             매우높음   매우높음 시뮬레이션 게임               │
│     8+         8x+            최고       최고     물리 기반 퍼즐                │
│                                                                                  │
│  주의: MaxSubsteps 이상의 서브스텝이 필요하면 시뮬레이션 시간이 버려짐         │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2. Async Physics 트레이드오프

| 장점 | 단점 |
|------|------|
| 게임 스레드 부하 감소 | 1프레임 지연 가능 |
| 병렬 실행으로 FPS 향상 | 동기화 복잡성 증가 |
| 물리 품질 독립적 | 디버깅 어려움 |

### 3. CVars for Tuning

```cpp
// 서브스텝 디버깅
p.Chaos.Solver.Iterations = 8           // 솔버 반복 횟수
p.Chaos.Solver.PushOutIterations = 3    // 푸시아웃 반복

// Async 튜닝
p.Chaos.AsyncPhysics.WaitForResults = 0  // 결과 대기 여부
p.Chaos.AsyncPhysics.SkipFirstFrame = 1  // 첫 프레임 스킵

// 타이밍 디버깅
stat Physics                             // 물리 스탯 표시
p.Chaos.DebugDraw.Timing = 1             // 타이밍 시각화
```

---

## 🔧 일반적인 문제 및 해결

### 문제 해결 가이드

| 문제 | 원인 | 해결 |
|------|------|------|
| **물체 통과** | Substep 너무 큼 | MaxSubstepDeltaTime 감소 |
| **진동/불안정** | dt 변동 | Substepping 활성화 |
| **지연된 반응** | Async 지연 | Interpolation 조정 |
| **CPU 과부하** | Substep 과다 | MaxSubsteps 감소 |
| **비동기 충돌** | 스레드 경쟁 | 적절한 동기화 |

---

## 🔗 관련 문서

- [Overview.md](Overview.md) - 물리 시스템 개요
- [Chaos_Complete_Architecture.md](Chaos_Complete_Architecture.md) - Chaos 전체 구조
- [Chaos_Threading_And_Synchronization.md](Chaos_Threading_And_Synchronization.md) - 스레딩 상세
- [Chaos_Network_Physics_Replication.md](Chaos_Network_Physics_Replication.md) - 네트워크 동기화

---

> 이 문서는 Chaos Physics의 Substepping과 Async Physics 시스템을 설명합니다.