---
title: "Physics Simulation Methods - 종합 비교"
date: "2025-12-07"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Physics"
tags: ["unreal", "Physics"]
engine_version: "** Unreal Engine 5.7"
---
# Physics Simulation Methods - 종합 비교

## 🧭 개요

물리 시뮬레이션은 크게 **4가지 패러다임**으로 분류됩니다. 각 방식은 다른 문제 영역에서 장점을 가지며, 현대 물리 엔진들은 이들을 조합하여 사용합니다.

### 물리 시뮬레이션 패러다임 전체 맵

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Physics Simulation Paradigms                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   정확도 ↑                                                                       │
│      │                                                                           │
│      │  ┌───────────────────────────────────────────────────────────────────┐  │
│      │  │  Analytical / Lagrangian Methods (해석 역학)                      │  │
│      │  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                 │  │
│      │  │  │   MuJoCo    │ │    Drake    │ │  Pinocchio  │ ← 로보틱스/RL  │  │
│      │  │  │  Convex Opt │ │   LCP/QP    │ │   Rigid     │                 │  │
│      │  │  └─────────────┘ └─────────────┘ └─────────────┘                 │  │
│      │  └───────────────────────────────────────────────────────────────────┘  │
│      │                                                                           │
│      │  ┌───────────────────────────────────────────────────────────────────┐  │
│      │  │  Force-Based / Continuum Methods (힘/연속체 기반)                 │  │
│      │  │  ┌─────────────┐ ┌─────────────┐                                 │  │
│      │  │  │    FEM      │ │ Mass-Spring │ ← 정밀 시뮬레이션               │  │
│      │  │  │ 연속체 역학 │ │   시스템    │                                 │  │
│      │  │  └─────────────┘ └─────────────┘                                 │  │
│      │  └───────────────────────────────────────────────────────────────────┘  │
│      │                                                                           │
│      │  ┌───────────────────────────────────────────────────────────────────┐  │
│      │  │  Velocity/Impulse-Based Methods (속도/충격량 기반)                │  │
│      │  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                 │  │
│      │  │  │   Bullet    │ │   PhysX     │ │    Box2D    │ ← 범용 게임    │  │
│      │  │  │    SI/PGS   │ │   TGS/PGS   │ │    SI       │                 │  │
│      │  │  └─────────────┘ └─────────────┘ └─────────────┘                 │  │
│      │  └───────────────────────────────────────────────────────────────────┘  │
│      │                                                                           │
│      │  ┌───────────────────────────────────────────────────────────────────┐  │
│      │  │  Position-Based Methods (위치 기반)                               │  │
│      │  │  ┌─────────────┐ ┌─────────────┐                                 │  │
│      │  │  │    PBD      │ │    XPBD     │ ← 🎮 Chaos (UE5)               │  │
│      │  │  │  빠름/안정  │ │  + 정확도   │                                 │  │
│      │  │  └─────────────┘ └─────────────┘                                 │  │
│      │  └───────────────────────────────────────────────────────────────────┘  │
│      │                                                                           │
│      └────────────────────────────────────────────────────────────────→ 성능   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 각 패러다임 요약

| 패러다임 | 대표 엔진 | 핵심 아이디어 | 주 사용처 |
|----------|----------|--------------|----------|
| **Position-Based** | Chaos (UE5), Flex | 위치 직접 수정 | 게임, 실시간 |
| **Velocity-Based** | PhysX, Bullet, Havok | 충격량으로 속도 수정 | 게임, 시뮬레이션 |
| **Force-Based** | FEM 솔버들 | 힘/응력 계산 후 적분 | 공학, 오프라인 |
| **Analytical** | MuJoCo, Drake, Pinocchio | 해석적 동역학 | 로보틱스, RL |

### Unreal Engine 구현 현황

| 방식 | Chaos 구현 | 주요 사용 영역 |
|------|-----------|---------------|
| **PBD** | ✅ 완전 구현 | 리지드 바디, Destruction, Niagara |
| **XPBD** | ✅ 완전 구현 | Cloth, Soft Body, Hair |
| **FEM** | ✅ 부분 구현 | 고급 Soft Body (실험적) |
| **Velocity-Based** | ⚠️ 하이브리드 | 충돌 응답, 마찰 |

---

## 🎯 패러다임별 핵심 차이

### 핵심 차이점 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Constraint Solving Methods Comparison                         │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   정확도 ↑                                                                       │
│      │                                                                           │
│      │     ┌────────────────────────────────────────────┐                       │
│      │     │                  FEM                        │                       │
│      │     │  연속체 역학 기반 (Deformation Gradient)    │                       │
│      │     │  가장 물리적으로 정확                       │                       │
│      │     │  계산 비용: 🐢🐢🐢🐢🐢                      │                       │
│      │     └────────────────────────────────────────────┘                       │
│      │                                                                           │
│      │     ┌────────────────────────────────────────────┐                       │
│      │     │                 XPBD                        │                       │
│      │     │  라그랑주 승수 + Compliance 도입           │                       │
│      │     │  시간 스케일 고려 → 더 정확한 시뮬레이션   │                       │
│      │     │  계산 비용: 🐢🐢🐢                         │                       │
│      │     └────────────────────────────────────────────┘                       │
│      │                                                                           │
│      │     ┌────────────────────────────────────────────┐                       │
│      │     │                  PBD                        │                       │
│      │     │  위치 기반 제약 직접 해결                   │                       │
│      │     │  빠르고 안정적, 단 에너지 보존 불완전      │                       │
│      │     │  계산 비용: 🐢                              │ ← 🎮 게임 기본       │
│      │     └────────────────────────────────────────────┘                       │
│      │                                                                           │
│      └────────────────────────────────────────────────────────────→ 성능        │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔷 1. Position-Based Dynamics (PBD)

### 수학적 원리

- **위치 기반 제약 직접 해결**
- 비선형 **가우스-자이델 반복법** 사용
- 제약 위반을 위치 보정으로 직접 해소

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           PBD Algorithm                                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. 외력 적용 (Integrate)                                                        │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  v += dt * (F / m)                                                  │      │
│     │  x_predicted = x + dt * v                                           │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│  2. 제약 해결 (Solve Constraints) - N번 반복                                    │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  for each constraint C:                                             │      │
│     │      Δx = -C(x) / |∇C|² * ∇C   // 제약 위반량만큼 위치 보정        │      │
│     │      x += Δx * (1/m) / Σ(1/m)   // 질량 비율로 분배                │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│  3. 속도 역계산 (Update Velocity)                                                │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  v = (x_solved - x_old) / dt                                        │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 구현 예시

```cpp
// PBD 기본 알고리즘
void PBDStep(TArray<FParticle>& Particles, float Dt)
{
    // Step 1: Integrate (예측 위치 계산)
    for (FParticle& P : Particles)
    {
        P.OldPosition = P.Position;
        P.Velocity += Dt * P.Force / P.Mass;
        P.Position += Dt * P.Velocity;  // 예측 위치
    }

    // Step 2: Solve Constraints (반복)
    for (int32 Iter = 0; Iter < NumIterations; ++Iter)
    {
        for (FConstraint& Constraint : Constraints)
        {
            Constraint.Solve(Particles);  // 위치 직접 수정
        }
    }

    // Step 3: Update Velocity (역계산)
    for (FParticle& P : Particles)
    {
        P.Velocity = (P.Position - P.OldPosition) / Dt;
    }
}

// 거리 제약 예시
void FDistanceConstraint::Solve(TArray<FParticle>& Particles)
{
    FParticle& P1 = Particles[Index1];
    FParticle& P2 = Particles[Index2];

    FVector Delta = P2.Position - P1.Position;
    float CurrentLength = Delta.Size();
    float Error = CurrentLength - RestLength;

    // 제약 위반량을 질량 비율로 분배
    float W1 = P1.InvMass;
    float W2 = P2.InvMass;
    float WSum = W1 + W2;

    if (WSum > 0.0f)
    {
        FVector Correction = (Error / CurrentLength) * Delta;
        P1.Position += (W1 / WSum) * Correction;
        P2.Position -= (W2 / WSum) * Correction;
    }
}
```

### 장단점

| 장점 | 단점 |
|------|------|
| ⭐ 실시간성에 매우 적합 | ❌ 에너지 보존 불완전 |
| ⭐ 큰 time step에서도 안정적 | ❌ Stiffness 조절 어려움 |
| ⭐ 구현 매우 단순 | ❌ 물리적 정확도 낮음 |
| ⭐ 충돌 응답이 직관적 | ❌ 반복 횟수에 따라 강성 변화 |

### Unreal Engine에서의 사용

```cpp
// Chaos 리지드 바디 솔버
// Engine/Source/Runtime/Experimental/Chaos/Private/Chaos/PBDRigidsEvolution.cpp

class FPBDRigidsEvolutionGBF
{
    void Advance(FReal Dt)
    {
        // 1. Integrate
        IntegrateV(Particles, Dt);
        IntegrateX(Particles, Dt);

        // 2. Collision Detection
        CollisionDetection();

        // 3. Constraint Solving (PBD)
        for (int32 It = 0; It < PositionIterations; ++It)
        {
            ApplyConstraints();  // Joint, Collision 등
        }

        // 4. Velocity Update
        ApplyCorrections(Particles, Dt);
    }
};
```

---

## 🔶 2. Extended Position-Based Dynamics (XPBD)

### 수학적 원리

- **라그랑주 승수(λ) + Compliance(α̃) 도입**
- 시간 스케일을 고려하여 더 **물리적으로 정확**한 결과
- Stiffness와 Damping을 **독립적으로 제어** 가능

### 핵심 공식

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           XPBD Key Formula                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  라그랑주 승수 업데이트:                                                         │
│                                                                                  │
│              -C(x) - α̃ · λ                                                      │
│  Δλ = ─────────────────────────────                                              │
│         ∇C · M⁻¹ · ∇Cᵀ + α̃                                                     │
│                                                                                  │
│  여기서:                                                                         │
│  • C(x) = 제약 함수 (위반량)                                                     │
│  • α̃ = α / dt² = Compliance (시간 스케일 적용)                                 │
│  • λ = 누적 라그랑주 승수                                                        │
│  • M⁻¹ = 역질량 행렬                                                            │
│  • ∇C = 제약 그래디언트                                                          │
│                                                                                  │
│  Compliance α:                                                                   │
│  • α = 0: 무한히 뻣뻣함 (Rigid)                                                 │
│  • α 증가: 더 유연해짐 (Soft)                                                   │
│  • α = 1/k (k = stiffness) 관계                                                 │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### PBD vs XPBD 차이

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         PBD vs XPBD Comparison                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  PBD:                                                                            │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  Δx = -C(x) / |∇C|² * ∇C                                                │     │
│  │                                                                         │     │
│  │  • Stiffness는 반복 횟수로만 조절                                       │     │
│  │  • 반복 5회 vs 20회 → 완전히 다른 강성                                  │     │
│  │  • dt 변경 → 동작이 달라짐                                              │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
│  XPBD:                                                                           │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  Δλ = (-C - α̃λ) / (∇C·M⁻¹·∇Cᵀ + α̃)                                   │     │
│  │  Δx = M⁻¹ · ∇Cᵀ · Δλ                                                   │     │
│  │                                                                         │     │
│  │  • Compliance(α)로 Stiffness 직접 제어                                  │     │
│  │  • dt 변경해도 동작 일관성 유지                                         │     │
│  │  • 반복 횟수와 강성 독립                                                │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 구현 예시

```cpp
// XPBD 스프링 제약
void FXPBDSpringConstraint::Apply(TArray<FParticle>& Particles, float Dt)
{
    FParticle& P1 = Particles[Index1];
    FParticle& P2 = Particles[Index2];

    FVector Delta = P1.Position - P2.Position;
    float CurrentLength = Delta.Size();

    // 제약 함수: C = |x1 - x2| - RestLength
    float C = CurrentLength - RestLength;

    // Compliance (시간 스케일 적용)
    // Compliance = 1 / Stiffness, 값이 클수록 유연
    float AlphaTilde = Compliance / (Dt * Dt);

    // 역질량 합
    float W = P1.InvMass + P2.InvMass;

    // 라그랑주 승수 업데이트
    float DeltaLambda = -(C + AlphaTilde * Lambda) / (W + AlphaTilde);

    // 누적
    Lambda += DeltaLambda;

    // 위치 보정
    FVector Correction = DeltaLambda * Delta.GetSafeNormal();
    P1.Position += P1.InvMass * Correction;
    P2.Position -= P2.InvMass * Correction;
}

// XPBD 볼륨 보존 제약 (Tetrahedron)
void FXPBDVolumeConstraint::Apply(TArray<FParticle>& Particles, float Dt)
{
    // 4개 정점으로 사면체 볼륨 계산
    FVector& X0 = Particles[Indices[0]].Position;
    FVector& X1 = Particles[Indices[1]].Position;
    FVector& X2 = Particles[Indices[2]].Position;
    FVector& X3 = Particles[Indices[3]].Position;

    // 현재 볼륨 (6배)
    float Volume6 = FVector::DotProduct(
        FVector::CrossProduct(X1 - X0, X2 - X0),
        X3 - X0
    );

    // 제약: C = Volume - RestVolume
    float C = Volume6 - RestVolume6;

    // Gradients (∂C/∂xi)
    FVector Grad0 = FVector::CrossProduct(X1 - X2, X3 - X2);
    FVector Grad1 = FVector::CrossProduct(X2 - X0, X3 - X0);
    FVector Grad2 = FVector::CrossProduct(X0 - X1, X3 - X1);
    FVector Grad3 = FVector::CrossProduct(X1 - X0, X2 - X0);

    // XPBD 업데이트
    float AlphaTilde = Compliance / (Dt * Dt);
    float W = /* 역질량 가중 그래디언트 제곱합 */;
    float DeltaLambda = -(C + AlphaTilde * Lambda) / (W + AlphaTilde);

    Lambda += DeltaLambda;

    // 위치 보정
    X0 += Particles[Indices[0]].InvMass * DeltaLambda * Grad0;
    // ... X1, X2, X3도 동일
}
```

### 장단점

| 장점 | 단점 |
|------|------|
| ⭐ PBD보다 물리적으로 정확 | ❌ PBD보다 계산 비용 증가 |
| ⭐ Stiffness/Damping 독립 제어 | ❌ 구현 난이도 상승 |
| ⭐ dt 변경에 일관된 동작 | ❌ Compliance 튜닝 필요 |
| ⭐ 수렴성 우수 | |

### Unreal Engine에서의 사용

```cpp
// Chaos Cloth (XPBD 기반)
// Engine/Source/Runtime/Experimental/Chaos/Private/Chaos/PBDEvolution.cpp

class FXPBDSpringConstraints : public FPBDSpringConstraintsBase
{
    void Apply(FSolverParticles& Particles, const FSolverReal Dt) const
    {
        for (int32 i = 0; i < Constraints.Num(); ++i)
        {
            const int32 i1 = Constraints[i][0];
            const int32 i2 = Constraints[i][1];

            FSolverVec3 Delta = Particles.P(i1) - Particles.P(i2);
            FSolverReal Distance = Delta.Size();
            FSolverReal C = Distance - Dists[i];

            // XPBD: Compliance 적용
            FSolverReal AlphaTilde = Stiffness[i] > 0.0f
                ? (1.0f / Stiffness[i]) / (Dt * Dt)
                : 0.0f;

            FSolverReal DeltaLambda = -(C + AlphaTilde * Lambdas[i])
                / (Particles.InvM(i1) + Particles.InvM(i2) + AlphaTilde);

            Lambdas[i] += DeltaLambda;

            FSolverVec3 Correction = (DeltaLambda / Distance) * Delta;
            Particles.P(i1) += Particles.InvM(i1) * Correction;
            Particles.P(i2) -= Particles.InvM(i2) * Correction;
        }
    }
};
```

---

## 🔷 3. Finite Element Method (FEM)

### 수학적 원리

- **연속체 역학(Continuum Mechanics)** 기반
- **Deformation Gradient Tensor (F)** 사용
- **에너지 함수(W)**로부터 내부 응력 유도

### 핵심 공식

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           FEM Key Concepts                                       │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. Deformation Gradient (변형 기울기):                                          │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  F = ∂x / ∂X                                                        │      │
│     │                                                                     │      │
│     │  x = 변형된 위치 (World Space)                                      │      │
│     │  X = 원래 위치 (Material Space / Rest Pose)                         │      │
│     │                                                                     │      │
│     │  F는 3x3 행렬로, 국소적 변형을 완전히 기술                          │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│  2. Strain Tensor (변형률 텐서):                                                 │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  Green Strain: E = ½(FᵀF - I)                                       │      │
│     │                                                                     │      │
│     │  회전 불변 (Rotation Invariant) → 물리적으로 의미 있음              │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│  3. Stress Tensor (응력 텐서):                                                   │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  First Piola-Kirchhoff Stress: P = ∂W/∂F                            │      │
│     │                                                                     │      │
│     │  W = 탄성 에너지 밀도 함수 (예: Neo-Hookean, St. Venant-Kirchhoff) │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│  4. 내부 힘 계산:                                                                │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  f_internal = -∫ P · ∇N dV                                          │      │
│     │                                                                     │      │
│     │  N = Shape Function (요소 내 보간 함수)                             │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 재료 모델

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         Material Models for FEM                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. St. Venant-Kirchhoff (선형 탄성):                                           │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  W = μ·tr(E²) + (λ/2)·tr(E)²                                        │      │
│     │                                                                     │      │
│     │  • 가장 간단한 비선형 탄성 모델                                     │      │
│     │  • 큰 압축에서 불안정 (뒤집힘 가능)                                 │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│  2. Neo-Hookean (비선형 탄성):                                                   │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  W = (μ/2)·(tr(FᵀF) - 3) - μ·ln(J) + (λ/2)·ln²(J)                  │      │
│     │  J = det(F)                                                         │      │
│     │                                                                     │      │
│     │  • 큰 변형에서도 안정적                                             │      │
│     │  • 고무, 젤리 같은 재료에 적합                                      │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
│  3. Corotated Linear Elasticity:                                                 │
│     ┌────────────────────────────────────────────────────────────────────┐      │
│     │  F = R·S (Polar Decomposition)                                      │      │
│     │  W = μ·||F - R||² + (λ/2)·(tr(S) - 3)²                              │      │
│     │                                                                     │      │
│     │  • 회전 분리 → 선형 탄성 적용                                       │      │
│     │  • 계산 효율적이면서 안정적                                         │      │
│     └────────────────────────────────────────────────────────────────────┘      │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 구현 예시

```cpp
// FEM 탄성 내부 힘 계산
// Engine/Source/Runtime/Experimental/Chaos/Public/Chaos/Deformable/ChaosDeformableSolverProxy.h

template<class T>
void ElasticFEM<T>::AddInternalElasticForce(
    const TArray<FTetMesh>& Mesh,
    const TArray<TVec3<T>>& X,         // 현재 위치
    const TArray<TVec3<T>>& RestX,     // 원래 위치
    TArray<TVec3<T>>& Force)           // 출력: 내부 힘
{
    for (int32 e = 0; e < Mesh.Num(); ++e)
    {
        // 1. Deformation Gradient 계산
        PMatrix<T, 3, 3> Fe;
        ComputeDeformationGradient(Mesh[e], X, RestX, Fe);

        // 2. First Piola-Kirchhoff Stress 계산
        PMatrix<T, 3, 3> Pe;
        ComputeStress(Fe, Pe, e);  // 재료 모델에 따라 P 계산

        // 3. 요소 힘 계산: f = -V₀ · DmInv^T · P
        // V₀ = 원래 요소 체적
        // DmInv = 원래 형상의 역행렬
        PMatrix<T, 3, 3> H = -Measure[e] * (DmInv[e].GetTransposed() * Pe);

        // 4. 노드별 힘 분배
        const TArray<int32>& Elem = Mesh[e].Indices;

        // 정점 0의 힘 = -(다른 3개 정점 힘의 합)
        Force[Elem[0]] -= H.GetColumn(0) + H.GetColumn(1) + H.GetColumn(2);

        // 정점 1, 2, 3의 힘
        for (int32 i = 0; i < 3; ++i)
        {
            Force[Elem[i + 1]] += H.GetColumn(i);
        }
    }
}

// Neo-Hookean 응력 계산
template<class T>
void NeoHookean<T>::ComputeStress(
    const PMatrix<T, 3, 3>& F,
    PMatrix<T, 3, 3>& P,
    int32 ElementIndex)
{
    T J = F.Determinant();  // det(F)
    PMatrix<T, 3, 3> FinvT = F.Inverse().GetTransposed();

    // P = μ(F - F^(-T)) + λ·ln(J)·F^(-T)
    P = Mu * (F - FinvT) + Lambda * FMath::Loge(J) * FinvT;
}
```

### 장단점

| 장점 | 단점 |
|------|------|
| ⭐ 물리적 정확성 최고 | ❌ 계산 비용 매우 높음 |
| ⭐ 복잡한 재료 모델 가능 | ❌ 실시간 부적합 (게임) |
| ⭐ 정량적 분석 가능 | ❌ 구현 복잡 |
| ⭐ 큰 변형에서도 정확 | ❌ 메시 품질 의존적 |

---

## 🔶 4. Velocity/Impulse-Based Methods (속도/충격량 기반)

### 개요

PBD가 **위치를 직접 수정**하는 반면, Velocity-Based 방식은 **충격량(Impulse)**으로 **속도를 수정**합니다. PhysX, Bullet, Havok 등 대부분의 전통적 물리 엔진이 이 방식을 사용합니다.

### 핵심 개념

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Velocity-Based vs Position-Based                              │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Position-Based (PBD):                                                          │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  1. 예측 위치 계산: x_pred = x + v*dt                                   │     │
│  │  2. 제약 위반 → 위치 직접 수정: x += Δx                                │     │
│  │  3. 속도 역계산: v = (x_new - x_old) / dt                               │     │
│  │                                                                         │     │
│  │  특징: 항상 제약 만족, 하지만 비물리적 힘 발생 가능                     │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
│  Velocity-Based (Impulse):                                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  1. 충돌 감지 (현재 위치에서)                                           │     │
│  │  2. 제약 위반 → 충격량 계산: J = λ * n                                 │     │
│  │  3. 속도 수정: v += J / m                                               │     │
│  │  4. 위치 적분: x += v * dt                                              │     │
│  │                                                                         │     │
│  │  특징: 물리적으로 정확한 충격량, 하지만 침투 발생 가능                  │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### Sequential Impulse (SI) 알고리즘

```cpp
// Bullet Physics, Box2D 등에서 사용하는 Sequential Impulse
void SequentialImpulseSolver(TArray<FContact>& Contacts, float Dt, int32 Iterations)
{
    // 충격량 캐시 초기화 (Warm Starting)
    for (FContact& C : Contacts)
    {
        // 이전 프레임의 충격량으로 초기화 → 수렴 가속
        ApplyImpulse(C.BodyA, C.BodyB, C.CachedImpulse);
    }

    // 반복 솔버
    for (int32 Iter = 0; Iter < Iterations; ++Iter)
    {
        for (FContact& C : Contacts)
        {
            // 상대 속도 계산
            FVector RelVel = C.BodyB->Velocity - C.BodyA->Velocity;
            float VelAlongNormal = FVector::DotProduct(RelVel, C.Normal);

            // 이미 분리 중이면 스킵
            if (VelAlongNormal > 0) continue;

            // 충격량 크기 계산 (반발 계수 적용)
            float e = FMath::Min(C.BodyA->Restitution, C.BodyB->Restitution);
            float J = -(1.0f + e) * VelAlongNormal;
            J /= C.BodyA->InvMass + C.BodyB->InvMass;

            // 누적 충격량 (음수 방지)
            float NewImpulse = FMath::Max(C.AccumulatedImpulse + J, 0.0f);
            J = NewImpulse - C.AccumulatedImpulse;
            C.AccumulatedImpulse = NewImpulse;

            // 충격량 적용
            FVector Impulse = J * C.Normal;
            C.BodyA->Velocity -= Impulse * C.BodyA->InvMass;
            C.BodyB->Velocity += Impulse * C.BodyB->InvMass;
        }
    }
}
```

### Projected Gauss-Seidel (PGS)

```cpp
// PhysX, ODE 등에서 사용하는 PGS 솔버
// LCP (Linear Complementarity Problem)를 반복적으로 풀이
void PGSSolver(TArray<FConstraint>& Constraints, float Dt, int32 Iterations)
{
    // Ax + b = w, x >= 0, w >= 0, x·w = 0 형태의 LCP
    // A = J * M^(-1) * J^T (유효 질량 행렬)
    // b = J * v + (bias/dt) (속도 오차)

    // 유효 질량 미리 계산
    for (FConstraint& C : Constraints)
    {
        C.EffectiveMass = 1.0f / (C.BodyA->InvMass + C.BodyB->InvMass +
                                  /* 회전 관성 항 */);
        C.Bias = C.Penetration * BiasFactor / Dt;  // Baumgarte Stabilization
    }

    // Gauss-Seidel 반복
    for (int32 Iter = 0; Iter < Iterations; ++Iter)
    {
        for (FConstraint& C : Constraints)
        {
            // 제약 속도 계산
            float Cdot = FVector::DotProduct(
                C.BodyB->Velocity - C.BodyA->Velocity, C.Normal) + C.Bias;

            // 충격량 계산
            float Lambda = -C.EffectiveMass * Cdot;

            // 클램핑 (Projection)
            float OldLambda = C.AccumulatedLambda;
            C.AccumulatedLambda = FMath::Max(OldLambda + Lambda, 0.0f);
            Lambda = C.AccumulatedLambda - OldLambda;

            // 속도 업데이트
            FVector Impulse = Lambda * C.Normal;
            C.BodyA->Velocity -= Impulse * C.BodyA->InvMass;
            C.BodyB->Velocity += Impulse * C.BodyB->InvMass;
        }
    }
}
```

### Temporal Gauss-Seidel (TGS) - PhysX 4/5

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Temporal Gauss-Seidel (TGS)                                   │
│                    PhysX 4.0+에서 사용                                           │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  기존 PGS:                                                                       │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  for (iter = 0; iter < N; iter++)                                       │     │
│  │      for (constraint : constraints)                                     │     │
│  │          SolveVelocity(constraint)     // 속도만 해결                   │     │
│  │  IntegratePositions()                   // 마지막에 위치 적분           │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
│  TGS (개선):                                                                     │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  for (iter = 0; iter < N; iter++)                                       │     │
│  │      for (constraint : constraints)                                     │     │
│  │          SolveVelocity(constraint)                                      │     │
│  │      IntegratePositions(dt / N)         // ⭐ 매 반복마다 위치 적분    │     │
│  │      UpdateContactPositions()           // ⭐ 접촉 정보 갱신           │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
│  장점:                                                                           │
│  • 더 빠른 수렴                                                                  │
│  • 더 적은 반복으로 높은 품질                                                    │
│  • 스태킹 안정성 향상                                                            │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 장단점

| 장점 | 단점 |
|------|------|
| ⭐ 물리적으로 정확한 충격량 | ❌ 침투(Penetration) 발생 가능 |
| ⭐ 에너지 보존 양호 | ❌ Stiff 제약에서 수렴 느림 |
| ⭐ 복잡한 관절/모터 지원 | ❌ 많은 반복 필요 |
| ⭐ 오랜 역사의 안정적 구현 | ❌ 스태킹에서 떨림 가능 |

---

## 🔷 5. Analytical/Lagrangian Methods (해석 역학 기반)

### MuJoCo (Multi-Joint dynamics with Contact)

MuJoCo는 **해석 역학(Analytical Mechanics)**과 **볼록 최적화(Convex Optimization)**를 결합한 물리 엔진입니다. 로보틱스와 강화학습에서 주로 사용됩니다.

### 핵심 특징

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                         MuJoCo Architecture                                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  1. 일반화 좌표 (Generalized Coordinates)                                       │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  q = [θ₁, θ₂, θ₃, ...]   // 관절 각도 직접 사용                        │     │
│  │  v = [ω₁, ω₂, ω₃, ...]   // 관절 각속도                                │     │
│  │                                                                         │     │
│  │  vs. Cartesian: x = [x, y, z, qw, qx, qy, qz, ...]                     │     │
│  │                                                                         │     │
│  │  장점: 더 적은 변수, 관절 제약 자동 만족                               │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
│  2. Featherstone 알고리즘 (Articulated Body Algorithm)                          │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  O(n) 복잡도로 관절체 동역학 계산                                       │     │
│  │                                                                         │     │
│  │  Forward Pass (Recursive Newton-Euler):                                 │     │
│  │    링크 0 → 1 → 2 → ... → n (속도/가속도 전파)                         │     │
│  │                                                                         │     │
│  │  Backward Pass:                                                         │     │
│  │    링크 n → ... → 2 → 1 → 0 (힘/토크 역전파)                           │     │
│  │                                                                         │     │
│  │  결과: 관절 토크 ↔ 관절 가속도 정확히 매핑                             │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
│  3. Convex 접촉 모델                                                            │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  기존 (LCP): f ≥ 0, d ≥ 0, f·d = 0 (상보성 조건)                       │     │
│  │            → 비볼록, 불연속, 해 존재 보장 안 됨                         │     │
│  │                                                                         │     │
│  │  MuJoCo (Convex): minimize ½ v^T M v + v^T τ                            │     │
│  │                   subject to contact constraints                        │     │
│  │            → 볼록 QP, 항상 유일한 해, 미분 가능                         │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
│  4. 해석적 미분 (Analytical Derivatives)                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐     │
│  │  ∂(가속도)/∂(상태) 를 해석적으로 계산                                   │     │
│  │  → 미분 가능 시뮬레이션                                                 │     │
│  │  → 그래디언트 기반 최적화/학습 가능                                     │     │
│  └────────────────────────────────────────────────────────────────────────┘     │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### MuJoCo 핵심 방정식

```cpp
// MuJoCo의 운동 방정식
// M(q) * v̇ + c(q, v) = τ + J^T * f
//
// M(q): 질량 행렬 (Configuration-dependent)
// c(q, v): 코리올리/원심력 + 중력
// τ: 제어 토크 (actuator)
// J: 접촉 자코비안
// f: 접촉력

// Forward Dynamics: τ → v̇
void MuJoCoForwardDynamics(const MjModel* m, MjData* d)
{
    // 1. 질량 행렬 M 계산 (Composite Rigid Body Algorithm)
    mj_crb(m, d);

    // 2. 바이어스 힘 c 계산 (Recursive Newton-Euler)
    mj_rne(m, d, /* computeForward */ 1);

    // 3. 접촉력 f 계산 (Convex Optimization)
    mj_contactForce(m, d);

    // 4. 가속도 계산: v̇ = M^(-1) * (τ + J^T*f - c)
    mj_factorM(m, d);           // M = L*D*L^T (Cholesky-like)
    mj_solveM(m, d, d->qacc);   // 선형 시스템 풀이
}

// Inverse Dynamics: v̇ → τ
void MuJoCoInverseDynamics(const MjModel* m, MjData* d)
{
    // Recursive Newton-Euler (O(n))
    mj_rne(m, d, /* computeForward */ 0);

    // τ = M * v̇ + c (접촉 없는 경우)
}
```

### MuJoCo vs PBD 구현 비교

```cpp
// ===== PBD 구현 (Chaos) =====
void PBDSolve(Particles& P, float Dt)
{
    // 1. 예측 위치
    for (auto& p : P)
    {
        p.x_pred = p.x + p.v * Dt;
    }

    // 2. 제약 투영 (반복)
    for (int i = 0; i < Iterations; ++i)
    {
        for (auto& c : Constraints)
        {
            // 위치 직접 수정
            c.Project(P);  // x_pred += Δx
        }
    }

    // 3. 속도 역계산
    for (auto& p : P)
    {
        p.v = (p.x_pred - p.x) / Dt;
        p.x = p.x_pred;
    }
}

// ===== MuJoCo 구현 (개념적) =====
void MuJoCoStep(Model& M, State& S, float Dt)
{
    // 1. 질량 행렬 계산 (일반화 좌표)
    Matrix MassMatrix = ComputeMassMatrix(M, S.q);

    // 2. 바이어스 힘 (중력, 코리올리)
    Vector Bias = ComputeBias(M, S.q, S.v);

    // 3. 접촉력 (볼록 최적화)
    Vector ContactForce = SolveContactQP(M, S);

    // 4. 가속도 계산
    Vector Acc = MassMatrix.Solve(S.tau + ContactForce - Bias);

    // 5. Semi-Implicit 적분
    S.v += Acc * Dt;
    S.q += S.v * Dt;  // 또는 적절한 일반화 좌표 적분
}
```

### Drake / Pinocchio / RBDL 비교

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                 Robotics Dynamics Libraries Comparison                           │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ┌─────────────────────┬──────────────────────────────────────────────────┐    │
│  │       MuJoCo        │ • DeepMind에서 오픈소스화                         │    │
│  │                     │ • Convex 접촉 모델                                │    │
│  │                     │ • XML 기반 모델 정의                              │    │
│  │                     │ • GPU 가속 (MJX)                                  │    │
│  │                     │ • 🎯 RL, MPC에 최적화                            │    │
│  └─────────────────────┴──────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌─────────────────────┬──────────────────────────────────────────────────┐    │
│  │        Drake        │ • MIT/Toyota Research에서 개발                    │    │
│  │                     │ • LCP/QP 기반 접촉                                │    │
│  │                     │ • 수학적 최적화 통합 (SNOPT, Ipopt)              │    │
│  │                     │ • 시스템 모델링 프레임워크                        │    │
│  │                     │ • 🎯 궤적 최적화, 제어 설계                      │    │
│  └─────────────────────┴──────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌─────────────────────┬──────────────────────────────────────────────────┐    │
│  │      Pinocchio      │ • INRIA에서 개발 (C++)                            │    │
│  │                     │ • 순수 동역학 라이브러리                          │    │
│  │                     │ • 해석적 미분 제공                                │    │
│  │                     │ • URDF 표준 지원                                  │    │
│  │                     │ • 🎯 빠른 동역학 계산                            │    │
│  └─────────────────────┴──────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌─────────────────────┬──────────────────────────────────────────────────┐    │
│  │        RBDL         │ • Rigid Body Dynamics Library                     │    │
│  │                     │ • Featherstone 알고리즘 구현                      │    │
│  │                     │ • 경량, 임베디드 적합                             │    │
│  │                     │ • 🎯 교육/연구용                                 │    │
│  └─────────────────────┴──────────────────────────────────────────────────┘    │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### Analytical 방식의 장단점

| 장점 | 단점 |
|------|------|
| ⭐ 물리적으로 가장 정확 | ❌ 구현 매우 복잡 |
| ⭐ 미분 가능 (Differentiable) | ❌ 관절체에 특화 (일반 형상 제한적) |
| ⭐ 제어 이론과 직접 연결 | ❌ 대규모 시뮬레이션에 부적합 |
| ⭐ 로봇 시뮬레이션에 최적 | ❌ 파괴, Cloth 등 지원 제한 |

---

## 📊 전체 방식 비교

### 종합 비교표

| 항목 | PBD | XPBD | FEM | Velocity-Based | Analytical (MuJoCo) |
|------|-----|------|-----|----------------|---------------------|
| **정확도** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **성능** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **안정성** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **구현 복잡도** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **에너지 보존** | ❌ | ⚠️ | ✅ | ✅ | ✅ |
| **미분 가능** | ❌ | ❌ | ⚠️ | ❌ | ✅ |
| **대규모 장면** | ✅ | ✅ | ❌ | ✅ | ❌ |
| **Cloth/SoftBody** | ✅ | ✅ | ✅ | ⚠️ | ❌ |
| **로봇 시뮬** | ⚠️ | ⚠️ | ❌ | ⚠️ | ✅ |

### 사용 영역별 추천

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                       Use Case Recommendations                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  🎮 실시간 게임 물리                                                            │
│     └─ PBD (Chaos) - 빠르고 안정적                                              │
│                                                                                  │
│  👗 의상/머리카락 시뮬레이션                                                    │
│     └─ XPBD (Chaos Cloth) - 품질과 성능 균형                                   │
│                                                                                  │
│  🤖 로봇 시뮬레이션 / 강화학습                                                  │
│     └─ MuJoCo, Drake - 정확한 동역학, 미분 가능                                │
│                                                                                  │
│  🚗 차량/기계 시뮬레이션                                                        │
│     └─ Velocity-Based (PhysX) - 복잡한 관절/모터 지원                          │
│                                                                                  │
│  🏗️ 정밀 공학 시뮬레이션                                                       │
│     └─ FEM - 물리적 정확도 최고                                                 │
│                                                                                  │
│  💥 파괴 시뮬레이션                                                             │
│     └─ PBD + XPBD (Chaos Destruction) - 실시간 파괴                            │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🎮 Chaos에서의 하이브리드 접근

Unreal Engine의 Chaos는 **여러 방식을 조합**하여 사용합니다.

### Chaos 시스템별 사용 방식

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                    Chaos Physics - Hybrid Approach                               │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Rigid Body Dynamics (FPBDRigidsEvolution)                               │   │
│  │  ├── 위치 적분: Semi-Implicit Euler                                      │   │
│  │  ├── 제약 해결: PBD (Position Iteration)                                 │   │
│  │  ├── 충돌 응답: Velocity-Based (충격량)                                  │   │
│  │  └── 마찰: Velocity-Based                                                │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Cloth Simulation (FPBDEvolution)                                        │   │
│  │  ├── 스프링 제약: XPBD (Compliance 기반)                                 │   │
│  │  ├── 볼륨 보존: XPBD                                                     │   │
│  │  ├── 굽힘 제약: XPBD                                                     │   │
│  │  └── 충돌: PBD (위치 보정)                                               │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Soft Body / Flesh (Experimental)                                        │   │
│  │  ├── 탄성: FEM (Neo-Hookean / Corotated)                                 │   │
│  │  ├── 체적 보존: FEM                                                      │   │
│  │  └── 충돌: PBD                                                           │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Destruction (Geometry Collection)                                       │   │
│  │  ├── 파편 동역학: PBD                                                    │   │
│  │  ├── 연결 강도: Strain-based (힘 기반)                                   │   │
│  │  └── 충돌: BVH + PBD                                                     │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 코드 레벨 비교

```cpp
// ===== Chaos Rigid Body (PBD + Velocity Hybrid) =====
// Engine/Source/Runtime/Experimental/Chaos/Private/Chaos/PBDRigidsEvolution.cpp

void FPBDRigidsEvolution::Advance(FReal Dt)
{
    // 1. 힘 적용 → 속도 (Force-Based)
    IntegrateV(Particles, Dt, Gravity);

    // 2. 속도 → 예측 위치 (Semi-Implicit)
    IntegrateX(Particles, Dt);

    // 3. 충돌 감지
    CollisionDetection();

    // 4. Position Iteration (PBD)
    for (int32 It = 0; It < PositionIterations; ++It)
    {
        ApplyPositionConstraints();  // 관절 제약
        ApplyCollisionConstraints(); // 충돌 (위치 보정)
    }

    // 5. Velocity Iteration (Impulse-Based)
    for (int32 It = 0; It < VelocityIterations; ++It)
    {
        ApplyVelocityConstraints();  // 마찰, 반발
    }
}

// ===== Chaos Cloth (XPBD) =====
// Engine/Source/Runtime/Experimental/Chaos/Private/Chaos/PBDEvolution.cpp

void FPBDEvolution::AdvanceOneTimeStep(FSolverReal Dt)
{
    // 1. 외력 적용
    ApplyExternalForces(Dt);

    // 2. 예측 위치
    IntegratePositions(Dt);

    // 3. XPBD 제약 해결 (람다 누적)
    for (int32 It = 0; It < NumIterations; ++It)
    {
        // 거리 제약 (스프링)
        SpringConstraints->Apply(Particles, Dt);

        // 굽힘 제약
        BendingConstraints->Apply(Particles, Dt);

        // 볼륨 보존
        VolumeConstraints->Apply(Particles, Dt);

        // 충돌
        CollisionConstraints->Apply(Particles, Dt);
    }

    // 4. 속도 업데이트
    UpdateVelocities(Dt);
}
```

---

## ⚡ 성능 최적화 기법

### 1. 병렬 처리 (Graph Coloring)

```cpp
// 독립적인 제약들을 병렬로 처리
// Engine/Source/Runtime/Experimental/Chaos/Private/Chaos/PBDRigidsEvolution.cpp

// Graph Coloring으로 충돌 없는 제약 그룹 생성
FGraphColoring::ComputeGraphColoringParticlesOrRange(
    Constraints,
    Particles,
    OutColoredConstraints
);

// 같은 색상의 제약들은 병렬 처리 가능
ParallelFor(ColoredConstraints.Num(), [&](int32 ColorIdx)
{
    for (FConstraint& C : ColoredConstraints[ColorIdx])
    {
        C.Apply(Particles, Dt);
    }
});
```

### 2. SIMD (ISPC)

```cpp
// Intel ISPC를 사용한 벡터화
#if INTEL_ISPC
export void ApplyXPBDSpringConstraints(
    uniform float Positions[],
    uniform float InvMasses[],
    uniform int Indices[],
    uniform float RestLengths[],
    uniform float Stiffness[],
    uniform float Lambdas[],
    uniform float Dt,
    uniform int NumConstraints)
{
    foreach (i = 0 ... NumConstraints)
    {
        // SIMD 벡터화된 제약 계산
        // 8개 또는 16개 제약을 동시에 처리
    }
}
#endif
```

### 3. 메모리 최적화 (SOA)

```cpp
// Structure of Arrays (SOA) - 캐시 친화적
class FPBDRigidsSOAs
{
    // AOS (비효율): struct { Vec3 Pos; Vec3 Vel; float Mass; } Particles[N];

    // SOA (효율): 연속된 메모리에 같은 타입 배치
    TArray<FVec3> Positions;      // [P0, P1, P2, ..., PN]
    TArray<FVec3> Velocities;     // [V0, V1, V2, ..., VN]
    TArray<FReal> InvMasses;      // [M0, M1, M2, ..., MN]

    // SIMD와 프리페치에 최적화됨
};
```

---

## 🔗 관련 문서

- [Overview.md](Overview.md) - 물리 시스템 개요
- [Integration_Methods.md](Integration_Methods.md) - 적분 방법
- [Chaos_Physics_Solver_And_Constraints_Deep_Dive.md](Chaos_Physics_Solver_And_Constraints_Deep_Dive.md) - Chaos 솔버 상세
- [Soft_Body_Physics_Deep_Dive.md](Soft_Body_Physics_Deep_Dive.md) - 소프트 바디
- [Chaos_Cloth_Simulation_Deep_Dive.md](Chaos_Cloth_Simulation_Deep_Dive.md) - Cloth 시뮬레이션

---

## 📚 참고 자료

- Müller et al., "Position Based Dynamics" (SIGGRAPH 2006)
- Macklin et al., "XPBD: Position-Based Simulation of Compliant Constrained Dynamics" (MIG 2016)
- Sifakis & Barbic, "FEM Simulation of 3D Deformable Solids" (SIGGRAPH Course)
- Unreal Engine 5 Source Code - Chaos Physics

---

> 이 문서는 PBD, XPBD, FEM 세 가지 제약 해결 방법을 비교합니다.
> Unreal Engine 5에서는 사용 목적에 따라 적절한 방식을 선택할 수 있습니다.