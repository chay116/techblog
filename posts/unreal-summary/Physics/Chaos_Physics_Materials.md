---
title: "Chaos Physics Materials"
date: "2025-12-09"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Physics"
tags: ["unreal", "Physics"]
engine_version: "** Unreal Engine 5.7"
---
# Chaos Physics Materials

## 🧭 개요

**Physics Material**은 물체의 물리적 특성(마찰, 반발력, 밀도 등)을 정의하는 에셋입니다. 표면 유형에 따라 다른 물리 반응을 구현할 수 있습니다.

### 핵심 개념

| 개념 | 설명 |
|------|------|
| **Friction** | 마찰 계수 (미끄러짐 저항) |
| **Restitution** | 반발 계수 (탄성) |
| **Density** | 질량 밀도 |
| **Surface Type** | 표면 유형 (효과, 사운드용) |
| **Physical Material Mask** | 삼각형별 재질 지정 |

---

## 🧱 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        Physics Material Architecture                             │
└─────────────────────────────────────────────────────────────────────────────────┘

  UPhysicalMaterial (Asset)
         │
         ├── 기본 속성
         │     ├── Friction (마찰)
         │     ├── FrictionCombineMode
         │     ├── Restitution (반발)
         │     ├── RestitutionCombineMode
         │     └── Density (밀도)
         │
         ├── Surface 속성
         │     ├── SurfaceType
         │     └── Physical Material (Chaos용)
         │
         └── 런타임 사용
               │
               ├── StaticMesh → Simple Collision → PhysMaterial
               │
               ├── SkeletalMesh → Physics Asset → Body → PhysMaterial
               │
               └── Landscape → Layer → Physical Material
                                      또는
                                      Physical Material Mask Texture

```

---

## 📂 주요 소스 파일

| 파일 | 역할 |
|------|------|
| `PhysicsCore/Public/PhysicalMaterials/PhysicalMaterial.h` | UPhysicalMaterial 클래스 |
| `Engine/Classes/PhysicsEngine/PhysicsSettings.h` | 기본 물리 설정 |
| `Chaos/Public/Chaos/Framework/PhysicalMaterialProxy.h` | Chaos 프록시 |
| `Engine/Public/CollisionQueryParams.h` | 머티리얼 쿼리 |

---

## 🔷 UPhysicalMaterial

### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           UPhysicalMaterial                                      │
│  (물리 재질 에셋)                                                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Friction (마찰):                                                                │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Friction           : float = 0.7       // 마찰 계수 (0-1+)             │   │
│  │  StaticFriction     : float             // 정지 마찰 (선택)             │   │
│  │  FrictionCombineMode: EFrictionCombineMode                              │   │
│  │    - Average  : (A + B) / 2                                            │   │
│  │    - Min      : min(A, B)                                              │   │
│  │    - Multiply : A * B                                                  │   │
│  │    - Max      : max(A, B)                                              │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  Restitution (반발):                                                            │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Restitution           : float = 0.3    // 반발 계수 (0-1)              │   │
│  │  RestitutionCombineMode: EFrictionCombineMode                           │   │
│  │    - 마찰과 동일한 결합 모드                                           │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  Density (밀도):                                                                │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  Density : float = 1.0                  // g/cm³ (물 = 1.0)             │   │
│  │  - 자동 질량 계산에 사용: Mass = Volume * Density                      │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  Surface Type:                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  SurfaceType : EPhysicalSurface         // Surface Type 열거형          │   │
│  │    - Default, Concrete, Grass, Metal, Wood, Water, etc.                │   │
│  │    - 프로젝트 설정에서 커스텀 정의 가능                                │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
│  Override Properties:                                                           │
│  ┌─────────────────────────────────────────────────────────────────────────┐   │
│  │  bOverrideFrictionCombineMode : bool                                    │   │
│  │  bOverrideRestitutionCombineMode : bool                                 │   │
│  │  RaiseMassToPower : float = 0.75        // 질량 스케일링               │   │
│  └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 물리 속성 예시값

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          Material Properties Examples                            │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Surface          │ Friction │ Restitution │ Density │ Notes                   │
│  ─────────────────┼──────────┼─────────────┼─────────┼────────────────────────  │
│  Ice              │   0.05   │    0.05     │   0.9   │ 매우 미끄러움           │
│  Rubber           │   1.0    │    0.8      │   1.2   │ 높은 마찰, 탄성         │
│  Wood             │   0.5    │    0.3      │   0.6   │ 일반적                  │
│  Metal            │   0.4    │    0.2      │   7.8   │ 철 밀도                 │
│  Concrete         │   0.8    │    0.1      │   2.4   │ 거친 표면               │
│  Glass            │   0.4    │    0.5      │   2.5   │ 약간 탄성               │
│  Sand             │   0.6    │    0.0      │   1.5   │ 비탄성                  │
│  Mud              │   0.3    │    0.0      │   1.8   │ 미끄럽고 비탄성         │
│  Grass            │   0.6    │    0.2      │   0.1   │ 지형용                  │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔶 Friction Combine Modes

### 마찰 결합 방식

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          Friction Combine Modes                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  두 물체 A(friction=0.8)와 B(friction=0.2) 충돌 시:                            │
│                                                                                  │
│  1. Average (평균) - 기본값                                                     │
│     ────────────────────────────                                                │
│     Result = (0.8 + 0.2) / 2 = 0.5                                             │
│     가장 일반적, 균형 잡힌 결과                                                 │
│                                                                                  │
│  2. Min (최소)                                                                  │
│     ────────────                                                                │
│     Result = min(0.8, 0.2) = 0.2                                               │
│     얼음 위 타이어 = 미끄러움                                                   │
│                                                                                  │
│  3. Multiply (곱)                                                               │
│     ────────────                                                                │
│     Result = 0.8 * 0.2 = 0.16                                                  │
│     더욱 미끄러운 결과                                                          │
│                                                                                  │
│  4. Max (최대)                                                                  │
│     ────────────                                                                │
│     Result = max(0.8, 0.2) = 0.8                                               │
│     높은 마찰 표면이 지배                                                       │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔷 Physical Material Mask

### Per-Triangle Material

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                     Physical Material Mask (Per-Triangle)                        │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  목적: 하나의 메시에서 삼각형마다 다른 물리 재질 적용                          │
│                                                                                  │
│  작동 방식:                                                                     │
│                                                                                  │
│       UV Space                          Mask Texture                            │
│    ┌───────────────┐                ┌───────────────┐                          │
│    │   ╱╲    ╱╲    │                │ R G B = 재질  │                          │
│    │  ╱  ╲  ╱  ╲   │   ─────────>   │   0 1 2...    │                          │
│    │ ╱────╲╱────╲  │                │               │                          │
│    │╱      ╲      ╲│                └───────────────┘                          │
│    └───────────────┘                                                            │
│                                                                                  │
│  구현:                                                                          │
│  1. 마스크 텍스처 생성 (각 색상 = 재질 인덱스)                                 │
│  2. 메시 UV 매핑                                                                │
│  3. Physical Material 배열 설정                                                 │
│  4. 런타임에 삼각형별 재질 조회                                                 │
│                                                                                  │
│  장점:                                                                          │
│  - 단일 메시로 다양한 표면 표현                                                 │
│  - 메모리 효율적                                                                │
│  - 복잡한 지형에 적합                                                           │
│                                                                                  │
│  단점:                                                                          │
│  - 추가 텍스처 메모리                                                           │
│  - 약간의 런타임 오버헤드                                                       │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔶 Surface Type

### EPhysicalSurface

```cpp
// 기본 Surface Type (프로젝트 설정에서 확장)
UENUM(BlueprintType)
enum EPhysicalSurface
{
    SurfaceType_Default = 0,
    SurfaceType1,       // 커스텀: Concrete
    SurfaceType2,       // 커스텀: Grass
    SurfaceType3,       // 커스텀: Metal
    SurfaceType4,       // 커스텀: Wood
    SurfaceType5,       // 커스텀: Water
    // ... SurfaceType62까지 정의 가능
};

// Project Settings > Physics > Physical Surface에서 이름 지정
```

### Surface Type 활용

```cpp
// Hit Result에서 Surface Type 가져오기
EPhysicalSurface GetSurfaceType(const FHitResult& Hit)
{
    if (Hit.PhysMaterial.IsValid())
    {
        return Hit.PhysMaterial->SurfaceType;
    }
    return SurfaceType_Default;
}

// Surface Type에 따른 효과 재생
void PlayFootstepEffect(const FHitResult& Hit)
{
    EPhysicalSurface Surface = GetSurfaceType(Hit);

    switch (Surface)
    {
    case EPhysicalSurface::SurfaceType1:  // Concrete
        PlaySound(ConcreteFootstep);
        SpawnParticle(ConcreteDust);
        break;

    case EPhysicalSurface::SurfaceType2:  // Grass
        PlaySound(GrassFootstep);
        SpawnParticle(GrassBits);
        break;

    case EPhysicalSurface::SurfaceType5:  // Water
        PlaySound(WaterSplash);
        SpawnParticle(WaterSplash);
        break;

    default:
        PlaySound(DefaultFootstep);
        break;
    }
}
```

---

## 💡 실전 사용 예시

### 1. Physical Material 생성

```cpp
// C++에서 Physical Material 생성
UPhysicalMaterial* CreateIceMaterial()
{
    UPhysicalMaterial* IceMat = NewObject<UPhysicalMaterial>();

    IceMat->Friction = 0.05f;
    IceMat->StaticFriction = 0.1f;
    IceMat->FrictionCombineMode = EFrictionCombineMode::Min;

    IceMat->Restitution = 0.05f;
    IceMat->RestitutionCombineMode = EFrictionCombineMode::Min;

    IceMat->Density = 0.92f;  // 얼음 밀도

    IceMat->SurfaceType = EPhysicalSurface::SurfaceType_Ice;

    return IceMat;
}
```

### 2. Static Mesh에 적용

```cpp
void ApplyPhysicalMaterial(UStaticMeshComponent* MeshComp, UPhysicalMaterial* PhysMat)
{
    if (MeshComp && PhysMat)
    {
        // Simple Collision에 적용
        if (UBodySetup* BodySetup = MeshComp->GetBodySetup())
        {
            BodySetup->PhysMaterial = PhysMat;
        }

        // 또는 런타임에 BodyInstance로 적용
        FBodyInstance* BodyInstance = MeshComp->GetBodyInstance();
        if (BodyInstance)
        {
            BodyInstance->SetPhysMaterialOverride(PhysMat);
        }
    }
}
```

### 3. Line Trace로 Material 쿼리

```cpp
void QuerySurfaceMaterial(FVector Start, FVector End)
{
    FHitResult Hit;
    FCollisionQueryParams QueryParams;
    QueryParams.bReturnPhysicalMaterial = true;  // 중요!

    if (GetWorld()->LineTraceSingleByChannel(Hit, Start, End, ECC_Visibility, QueryParams))
    {
        if (Hit.PhysMaterial.IsValid())
        {
            UPhysicalMaterial* PhysMat = Hit.PhysMaterial.Get();

            float Friction = PhysMat->Friction;
            float Restitution = PhysMat->Restitution;
            EPhysicalSurface SurfaceType = PhysMat->SurfaceType;

            UE_LOG(LogTemp, Log, TEXT("Surface: %s, Friction: %f"),
                *UEnum::GetValueAsString(SurfaceType), Friction);
        }
    }
}
```

### 4. Landscape Physical Material

```cpp
// Landscape Layer에 Physical Material 설정
// 1. Landscape Material에서 각 Layer마다 Physical Material 지정
// 2. 또는 Physical Material Mask Texture 사용

// 런타임에 Landscape에서 Material 쿼리
UPhysicalMaterial* GetLandscapeMaterial(ALandscapeProxy* Landscape, FVector Location)
{
    if (Landscape)
    {
        FHitResult Hit;
        FCollisionQueryParams Params;
        Params.bReturnPhysicalMaterial = true;

        FVector Start = Location + FVector(0, 0, 1000);
        FVector End = Location - FVector(0, 0, 1000);

        if (Landscape->LineTraceSingle(Hit, Start, End, Params))
        {
            return Hit.PhysMaterial.Get();
        }
    }
    return nullptr;
}
```

---

## ⚙️ 성능 고려사항

### 1. Physical Material 캐싱

```cpp
// Material 조회 캐싱
class FPhysicalMaterialCache
{
    TMap<FName, TWeakObjectPtr<UPhysicalMaterial>> Cache;

public:
    UPhysicalMaterial* GetMaterial(FName Name)
    {
        if (TWeakObjectPtr<UPhysicalMaterial>* Found = Cache.Find(Name))
        {
            if (Found->IsValid())
            {
                return Found->Get();
            }
        }

        // 캐시 미스 - 로드
        UPhysicalMaterial* Mat = LoadObject<UPhysicalMaterial>(
            nullptr, *Name.ToString());

        if (Mat)
        {
            Cache.Add(Name, Mat);
        }
        return Mat;
    }
};
```

### 2. 관련 CVars

```cpp
// Physical Material 디버깅
p.Chaos.DebugDraw.PhysicalMaterial = 1    // 재질 시각화
show Collision                             // 충돌체 표시

// Line Trace 디버깅
p.Chaos.SceneQuery.DrawPhysMaterial = 1   // 쿼리 결과 표시
```

---

## 🔧 일반적인 문제 및 해결

| 문제 | 원인 | 해결 |
|------|------|------|
| **Material이 적용 안 됨** | BodySetup 미설정 | Simple Collision 확인 |
| **Hit에 Material 없음** | bReturnPhysicalMaterial = false | 쿼리 파라미터 확인 |
| **마찰이 이상함** | Combine Mode 문제 | FrictionCombineMode 확인 |
| **Landscape Material 안 나옴** | Layer 설정 누락 | Landscape Material 확인 |

---

## 🔗 관련 문서

- [Overview.md](Overview.md) - 물리 시스템 개요
- [Collision_And_SceneQuery.md](Collision_And_SceneQuery.md) - 충돌 및 쿼리
- [Chaos_Scene_Query_Deep_Dive.md](Chaos_Scene_Query_Deep_Dive.md) - Scene Query

---

> 이 문서는 Chaos Physics Materials 시스템을 설명합니다.