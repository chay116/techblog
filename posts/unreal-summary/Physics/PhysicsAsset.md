---
title: "Physics Asset (물리 에셋)"
date: "2025-12-07"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Physics"
tags: ["unreal", "Physics"]
engine_version: "** Unreal Engine 5.7"
---
# Physics Asset (물리 에셋)

## 🧭 개요

**Physics Asset**은 SkeletalMesh의 물리 시뮬레이션을 정의하는 에셋입니다. Ragdoll, 물리 애니메이션, 충돌 설정 등에 사용됩니다.

### 핵심 구성요소

| 구성요소 | 설명 |
|----------|------|
| **Bodies** | 각 본에 대한 물리 바디 (형상, 질량, 제약) |
| **Constraints** | 본 간의 물리 제약 (조인트) |
| **Profiles** | 물리 설정 프리셋 (런타임 전환 가능) |

---

## 🧱 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        Physics Asset Architecture                                │
└─────────────────────────────────────────────────────────────────────────────────┘

  USkeletalMesh
       │
       └── UPhysicsAsset* PhysicsAsset
                │
                ├── SkeletalBodySetups : TArray<USkeletalBodySetup*>
                │         │
                │         └── 각 본별 물리 바디 정의
                │               ├── BoneName
                │               ├── Collision Shapes
                │               ├── Mass Properties
                │               └── Collision Settings
                │
                ├── ConstraintSetups : TArray<UPhysicsConstraintTemplate*>
                │         │
                │         └── 본 간 제약 정의
                │               ├── JointName
                │               ├── Bone1 / Bone2
                │               ├── Linear Limits
                │               ├── Angular Limits
                │               └── Motors
                │
                └── PhysicsAssetSolverSettings
                          │
                          └── 솔버 설정 (반복 횟수, 안정화 등)


  Runtime:
  USkeletalMeshComponent
       │
       ├── Bodies : TArray<FBodyInstance*>
       │         │
       │         └── 각 본의 런타임 물리 바디
       │
       └── Constraints : TArray<FConstraintInstance*>
                 │
                 └── 각 조인트의 런타임 인스턴스
```

---

## 📂 주요 소스 파일

| 파일 | 역할 |
|------|------|
| `Engine/Public/PhysicsEngine/PhysicsAsset.h` | UPhysicsAsset 정의 |
| `Engine/Public/PhysicsEngine/BodySetup.h` | UBodySetup/USkeletalBodySetup |
| `Engine/Public/PhysicsEngine/ConstraintInstance.h` | FConstraintInstance |
| `Engine/Public/PhysicsEngine/PhysicsConstraintTemplate.h` | 제약 템플릿 |

---

## 🔷 USkeletalBodySetup

### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          USkeletalBodySetup                                      │
│  (본별 물리 바디 설정)                                                          │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Identity:                                                                       │
│  ├── BoneName : FName                     // 연결된 본 이름                     │
│  └── PhysicsType : EPhysicsType           // 물리 타입                          │
│        ├── PhysType_Default               //  - 기본 (시뮬레이션)               │
│        ├── PhysType_Fixed                 //  - 고정 (Kinematic)                │
│        └── PhysType_Simulated             //  - 시뮬레이션                       │
│                                                                                  │
│  Collision Shapes (AggGeom):                                                     │
│  ├── SphereElems : TArray<FKSphereElem>   // 구 형상들                          │
│  ├── BoxElems : TArray<FKBoxElem>         // 박스 형상들                        │
│  ├── CapsuleElems : TArray<FKSphylElem>   // 캡슐 형상들                        │
│  ├── ConvexElems : TArray<FKConvexElem>   // 컨벡스 형상들                      │
│  └── TaperedCapsuleElems                  // 테이퍼 캡슐                        │
│                                                                                  │
│  Mass Properties:                                                                │
│  ├── DefaultInstance.MassInKgOverride     // 질량 오버라이드                    │
│  ├── DefaultInstance.bOverrideMass        // 질량 오버라이드 사용               │
│  ├── DefaultInstance.LinearDamping        // 선형 감쇠                          │
│  └── DefaultInstance.AngularDamping       // 각 감쇠                            │
│                                                                                  │
│  Collision Settings:                                                             │
│  ├── DefaultInstance.CollisionEnabled     // 충돌 활성화                        │
│  ├── DefaultInstance.ObjectType           // 충돌 채널                          │
│  └── DefaultInstance.CollisionResponses   // 채널별 응답                        │
│                                                                                  │
│  Profile:                                                                        │
│  └── CurrentProfile : FName               // 현재 활성 프로파일                 │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 형상 정의

```cpp
// 구 형상
struct FKSphereElem
{
    FVector Center;      // 로컬 중심
    float Radius;        // 반지름
    FName Name;          // 형상 이름 (선택)
};

// 박스 형상
struct FKBoxElem
{
    FVector Center;      // 로컬 중심
    FRotator Rotation;   // 로컬 회전
    float X, Y, Z;       // 반크기
    FName Name;
};

// 캡슐 형상
struct FKSphylElem
{
    FVector Center;
    FRotator Rotation;
    float Radius;        // 반지름
    float Length;        // 길이 (반구 사이)
    FName Name;
};
```

---

## 🔶 Constraint (제약/조인트)

### 제약 타입

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          Physics Constraint Types                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐           │
│  │ Fixed (고정)                                                      │           │
│  │                                                                   │           │
│  │ ●───────────● 모든 자유도 잠금                                   │           │
│  │              (두 본이 완전히 고정)                               │           │
│  └──────────────────────────────────────────────────────────────────┘           │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐           │
│  │ Hinge (힌지)                                                      │           │
│  │                                                                   │           │
│  │     ↻                                                             │           │
│  │ ●───●───● 단일 축 회전만 허용 (문, 팔꿈치)                       │           │
│  │                                                                   │           │
│  └──────────────────────────────────────────────────────────────────┘           │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐           │
│  │ Ball and Socket (볼소켓)                                          │           │
│  │                                                                   │           │
│  │   ↻↺↻                                                             │           │
│  │ ●──◉──● 모든 축 회전 허용 (어깨, 고관절)                         │           │
│  │                                                                   │           │
│  └──────────────────────────────────────────────────────────────────┘           │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐           │
│  │ Prismatic (프리즈매틱)                                            │           │
│  │                                                                   │           │
│  │ ●═══════● 단일 축 이동만 허용 (슬라이더)                         │           │
│  │ ↔                                                                 │           │
│  └──────────────────────────────────────────────────────────────────┘           │
│                                                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐           │
│  │ Cone (원뿔)                                                       │           │
│  │                                                                   │           │
│  │    ╱╲                                                             │           │
│  │ ●─◀  ▶─● 원뿔 범위 내 회전 (목)                                  │           │
│  │    ╲╱                                                             │           │
│  └──────────────────────────────────────────────────────────────────┘           │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### UPhysicsConstraintTemplate

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                       UPhysicsConstraintTemplate                                 │
│  (조인트 설정 템플릿)                                                           │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│  Identity:                                                                       │
│  ├── JointName : FName                    // 조인트 이름                        │
│  ├── ConstraintBone1 : FName              // 첫 번째 본                         │
│  └── ConstraintBone2 : FName              // 두 번째 본                         │
│                                                                                  │
│  Linear Limits:                                                                  │
│  ├── LinearXMotion : ELinearConstraintMotion  // X축 이동 모드                  │
│  │     ├── LCM_Free                           //  - 자유                        │
│  │     ├── LCM_Limited                        //  - 제한                        │
│  │     └── LCM_Locked                         //  - 잠금                        │
│  ├── LinearYMotion                                                              │
│  ├── LinearZMotion                                                              │
│  └── LinearLimit : float                  // 제한 거리                          │
│                                                                                  │
│  Angular Limits:                                                                 │
│  ├── AngularSwing1Motion                  // Swing1 (Y축 회전)                  │
│  ├── AngularSwing2Motion                  // Swing2 (Z축 회전)                  │
│  ├── AngularTwistMotion                   // Twist (X축 회전)                   │
│  ├── Swing1LimitAngle : float             // Swing1 제한 각도                   │
│  ├── Swing2LimitAngle : float             // Swing2 제한 각도                   │
│  └── TwistLimitAngle : float              // Twist 제한 각도                    │
│                                                                                  │
│  Drive (모터):                                                                   │
│  ├── AngularDriveMode : EAngularDriveMode                                       │
│  │     ├── SLERP                              //  - 구면 보간                   │
│  │     └── TwistAndSwing                      //  - 개별 제어                   │
│  ├── bEnableSwingDrive                                                          │
│  ├── bEnableTwistDrive                                                          │
│  ├── AngularDrivePositionStrength         // 목표 위치 강도                     │
│  ├── AngularDriveVelocityStrength         // 목표 속도 강도                     │
│  └── AngularVelocityTarget : FVector      // 목표 각속도                        │
│                                                                                  │
│  Contact:                                                                        │
│  ├── bDisableCollision : bool             // 연결된 본 간 충돌 비활성화         │
│  └── ContactDistance : float              // 접촉 거리                          │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔷 Profiles (프로파일)

### 프로파일 시스템

```cpp
// 물리 에셋의 프로파일 정의
USTRUCT()
struct FPhysicsAssetProfile
{
    FName ProfileName;                    // 프로파일 이름

    // 본별 물리 타입 오버라이드
    TMap<FName, EPhysicsType> BonePhysicsTypes;

    // 본별 충돌 설정 오버라이드
    TMap<FName, FBodyInstanceCollisionProfile> CollisionProfiles;
};
```

### 런타임 프로파일 전환

```cpp
// Ragdoll 프로파일로 전환
USkeletalMeshComponent* SkelComp = GetMesh();

// 모든 바디를 시뮬레이션으로
SkelComp->SetAllBodiesSimulatePhysics(true);

// 또는 특정 프로파일 적용
SkelComp->SetPhysicsAsset(PhysicsAsset);
SkelComp->SetConstraintProfileForAll(FName("Ragdoll"));
```

---

## 🔶 Ragdoll 구현

### 기본 Ragdoll

```cpp
// 캐릭터 사망 시 Ragdoll 활성화
void AMyCharacter::Die()
{
    USkeletalMeshComponent* Mesh = GetMesh();

    // 애니메이션 중지
    Mesh->SetAnimationMode(EAnimationMode::AnimationSingleNode);

    // 충돌 프리셋 변경
    Mesh->SetCollisionProfileName(TEXT("Ragdoll"));

    // 물리 시뮬레이션 활성화
    Mesh->SetAllBodiesSimulatePhysics(true);
    Mesh->SetSimulatePhysics(true);
    Mesh->WakeAllRigidBodies();

    // 블렌드 가중치 (애니메이션 → 물리)
    Mesh->SetAllBodiesPhysicsBlendWeight(1.0f);

    // 옵션: 초기 충격량
    Mesh->AddImpulse(LastHitDirection * ImpulseStrength, LastHitBone);
}
```

### 부분 Ragdoll (Hit Reaction)

```cpp
// 피격 시 해당 사지만 물리 활성화
void AMyCharacter::OnHit(FName HitBone, FVector HitDirection)
{
    USkeletalMeshComponent* Mesh = GetMesh();

    // 특정 본부터 하위 본만 물리 활성화
    Mesh->SetAllBodiesBelowSimulatePhysics(HitBone, true);
    Mesh->SetAllBodiesBelowPhysicsBlendWeight(HitBone, 1.0f);

    // 충격량 적용
    Mesh->AddImpulseAtLocation(
        HitDirection * ImpulseStrength,
        Mesh->GetBoneLocation(HitBone),
        HitBone
    );

    // 일정 시간 후 복구
    GetWorldTimerManager().SetTimer(
        RecoveryTimer,
        this,
        &AMyCharacter::RecoverFromHit,
        RecoveryTime
    );
}

void AMyCharacter::RecoverFromHit()
{
    // 점진적으로 애니메이션으로 복귀
    GetMesh()->SetAllBodiesPhysicsBlendWeight(0.0f, true);  // bIncludeSelf
}
```

### Physical Animation

```cpp
// 물리 기반 애니메이션 (PhysicalAnimationComponent)
UPhysicalAnimationComponent* PhysAnimComp = FindComponentByClass<UPhysicalAnimationComponent>();

// 프로파일 설정
FPhysicalAnimationProfile Profile;
Profile.StrengthMultiplyer = 1.0f;
Profile.WorldSpaceAlpha = 0.0f;
Profile.LocalSpaceAlpha = 1.0f;
Profile.OrientationAlpha = 0.8f;
Profile.AngularVelocityAlpha = 0.0f;
Profile.PositionAlpha = 0.8f;
Profile.VelocityAlpha = 0.0f;

PhysAnimComp->ApplyPhysicalAnimationProfileBelow(
    FName("spine_01"),     // 시작 본
    Profile,
    true                   // bIncludeSelf
);
```

---

## ⚡ 성능 고려사항

### 1. 바디 수 최소화

```cpp
// 나쁜 예: 모든 본에 물리 바디
// 손가락 하나당 3개 본 = 30개 바디 추가

// 좋은 예: 주요 본만 물리 바디
// Pelvis, Spine, Head, UpperArm x2, LowerArm x2,
// UpperLeg x2, LowerLeg x2 = 약 12개 바디
```

### 2. 형상 단순화

```cpp
// 캡슐/박스 > 구 > 컨벡스
// 가능하면 캡슐/박스 사용

// 컨벡스 형상은 버텍스 수 제한
// 32 버텍스 이하 권장
```

### 3. Sleep 활용

```cpp
// 정지 상태에서 빠르게 Sleep
for (int32 i = 0; i < SkelComp->Bodies.Num(); ++i)
{
    FBodyInstance* Body = SkelComp->Bodies[i];
    Body->SetSleepFamily(ESleepFamily::Normal);
}
```

---

## 💡 Tips & 디버깅

### Physics Asset 에디터

```
에디터에서 Physics Asset 열기:
- 더블클릭 또는 우클릭 > Edit

시뮬레이션 테스트:
- 툴바 > Simulate 버튼
- 마우스로 바디 드래그하여 테스트

바디 추가:
- 스켈레톤 트리에서 본 선택 > Add Body

제약 추가:
- 두 바디 선택 > Add Constraint
```

### 디버그 시각화

```cpp
// 콘솔 명령
show physics          // 물리 바디 표시
show constraints      // 제약 표시

// C++에서
SkelComp->SetDisplayDebugMode(EPhysicsAssetPreviewMode::Bodies);
```

### 일반적인 문제

| 문제 | 원인 | 해결 |
|------|------|------|
| Ragdoll이 폭발함 | 초기 침투/잘못된 제약 | 바디 크기 조정, 제약 확인 |
| 팔다리가 늘어남 | 제약 한계가 너무 넓음 | Angular Limit 조정 |
| 떨림/불안정 | 솔버 반복 부족 | 반복 횟수 증가 |
| 성능 저하 | 너무 많은 바디 | 바디 수 줄이기 |

---

## 🔗 관련 문서

- [Overview.md](Overview.md) - 물리 시스템 개요
- [PhysicsInterface.md](PhysicsInterface.md) - FBodyInstance 상세
- [Chaos_Physics_Solver_And_Constraints_Deep_Dive.md](Chaos_Physics_Solver_And_Constraints_Deep_Dive.md) - 제약 솔버

---

> 이 문서는 Physics Asset과 스켈레탈 메시 물리를 설명합니다.