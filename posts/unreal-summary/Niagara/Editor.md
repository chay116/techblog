---
title: "Editor - Niagara 에디터 시스템 (Niagara Editor System)"
date: "2026-02-18"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Niagara"
tags: ["unreal", "Niagara"]
---
# Editor - Niagara 에디터 시스템 (Niagara Editor System)

## 🧭 개요 (Overview)

**Niagara Editor**는 **아티스트와 디자이너가 코드 없이 파티클 이펙트를 제작할 수 있는 비주얼 에디팅 시스템**입니다. MVVM (Model-View-ViewModel) 아키텍처를 기반으로 하여 복잡한 Niagara 시스템을 직관적으로 편집하고, 실시간 프리뷰하며, 디버깅할 수 있습니다.

**핵심 역할:**
- **비주얼 편집**: 노드 기반 그래프 에디터로 로직 작성
- **스택 UI**: 모듈 기반 파라미터 편집 (Blueprint와 유사)
- **실시간 프리뷰**: 에디터 내 라이브 시뮬레이션 및 렌더링
- **파라미터 관리**: User 파라미터, System/Emitter 파라미터 통합 관리
- **컴파일 피드백**: 즉각적인 에러/경고 표시 및 수정 가이드
- **버전 관리**: 에셋 버전 관리 및 업그레이드
- **디버깅 도구**: Attribute Spreadsheet, GPU Profiler, Debug HUD

**MVVM 아키텍처:**

```
[View (Slate UI)]
      ↕
[ViewModel (Logic)]
      ↕
[Model (Data)]
```

**📂 위치:**
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/` (에디터 모듈 전체)
- `Private/Toolkits/NiagaraSystemToolkit.h:48` (FNiagaraSystemToolkit - 메인 에디터)
- `Public/ViewModels/NiagaraSystemViewModel.h:83` (FNiagaraSystemViewModel)
- `Public/ViewModels/NiagaraEmitterViewModel.h:33` (FNiagaraEmitterViewModel)

---

## 🎯 설계 철학: 왜 MVVM인가?

### 문제: 직접 UI-데이터 결합의 한계

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    직접 UI-데이터 결합 시 문제점                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ❌ 문제 1: 강한 결합 (Tight Coupling)                                  │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - UI 위젯이 UNiagaraSystem/Emitter 직접 참조           │          │
│  │  - 데이터 변경 시 UI 수동 업데이트 필요                  │          │
│  │  - 테스트 불가능 (UI 없이 로직 테스트 불가)              │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ❌ 문제 2: 상태 관리 어려움                                             │
│  - Undo/Redo 구현 복잡                                                  │
│  - 다수 UI 간 동기화 어려움 (Stack UI, Graph, Parameter Panel)          │
│  - 변경 추적 불가능                                                     │
│                                                                         │
│  ❌ 문제 3: 재사용성 부족                                                │
│  - UI 코드에 비즈니스 로직 혼재                                          │
│  - 다른 UI로 재사용 불가능                                               │
│  - 유닛 테스트 작성 불가능                                               │
│                                                                         │
│  ❌ 문제 4: 복잡도 폭발                                                  │
│  - System → Emitter → Script → Graph 계층 구조                         │
│  - 각 계층의 상태 동기화 매우 복잡                                       │
│  - 버그 발생 위험 높음                                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                            ↓
                   MVVM 아키텍처 솔루션:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ✅ 해결 1: 명확한 책임 분리 (Separation of Concerns)                   │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  [Model] - 순수 데이터                                   │          │
│  │  └─ UNiagaraSystem, UNiagaraEmitter, UNiagaraScript     │          │
│  │      └─ 비즈니스 로직 없음, 직렬화 가능                  │          │
│  │                                                          │          │
│  │  [ViewModel] - 프레젠테이션 로직                         │          │
│  │  └─ FNiagaraSystemViewModel, FNiagaraEmitterViewModel   │          │
│  │      ├─ 데이터 변환 (Model → UI 친화적 형태)            │          │
│  │      ├─ 명령 처리 (Add Emitter, Delete Module 등)       │          │
│  │      ├─ 상태 관리 (Selection, Expansion 등)             │          │
│  │      └─ 변경 알림 (Delegates/Events)                    │          │
│  │                                                          │          │
│  │  [View] - 순수 UI                                        │          │
│  │  └─ SNiagaraSystemEditor, SNiagaraStackWidget           │          │
│  │      └─ ViewModel 바인딩만 수행                          │          │
│  │          └─ 비즈니스 로직 없음                           │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ✅ 해결 2: 자동 동기화 (Reactive Programming)                          │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  ViewModel 변경 → Delegate 발생 → 모든 View 자동 갱신   │          │
│  │                                                          │          │
│  │  예시:                                                   │          │
│  │  SystemViewModel->OnSystemCompiled()                    │          │
│  │  └─ Stack UI 갱신                                        │          │
│  │  └─ Parameter Panel 갱신                                │          │
│  │  └─ Graph Editor 갱신                                   │          │
│  │  └─ Preview Viewport 갱신                               │          │
│  │                                                          │          │
│  │  → 단일 이벤트로 모든 UI 동기화!                         │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ✅ 해결 3: 테스트 가능 (Testability)                                   │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  ViewModel은 UI 없이 테스트 가능:                        │          │
│  │  ```cpp                                                  │          │
│  │  TEST(NiagaraEditor, AddEmitterTest)                    │          │
│  │  {                                                       │          │
│  │      auto ViewModel = MakeShared<FNiagaraSystemViewModel>();│      │
│  │      ViewModel->AddEmitter(...);                         │          │
│  │      ASSERT_EQ(ViewModel->GetEmitterHandles().Num(), 1);│          │
│  │  }                                                       │          │
│  │  ```                                                     │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ✅ 해결 4: Undo/Redo 통합                                              │
│  - FEditorUndoClient 상속                                               │
│  - PostUndo() 호출 시 ViewModel 자동 갱신                               │
│  - UI는 ViewModel만 보므로 자동으로 이전 상태로 복원됨                  │
│                                                                         │
│  ✅ 해결 5: 계층적 ViewModel 관리                                       │
│  FNiagaraSystemViewModel                                               │
│  └─ TArray<FNiagaraEmitterHandleViewModel>                            │
│      └─ FNiagaraEmitterViewModel                                      │
│          ├─ FNiagaraScriptViewModel (Spawn/Update Scripts)            │
│          │   └─ FNiagaraScriptGraphViewModel (Graph)                  │
│          └─ FNiagaraStackViewModel (Stack UI)                         │
│                                                                         │
│  → 각 레벨이 독립적으로 관리되며 자동 동기화                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 설계 결정

| 결정 사항 | 이유 | 트레이드오프 |
|----------|------|-------------|
| **MVVM 패턴** | 관심사 분리, 테스트 가능, 재사용성 | 초기 아키텍처 복잡도 증가 |
| **Slate UI** | 크로스 플랫폼, 고성능, 유연성 | WPF/Qt보다 학습 곡선 높음 |
| **Delegate 기반 동기화** | 느슨한 결합, 확장 가능 | 디버깅 어려움 (이벤트 체인 추적) |
| **계층적 ViewModel** | 책임 분리, 독립적 생명주기 | 메모리 사용량 증가 |
| **EditorUndoClient 통합** | Undo/Redo 자동 지원 | PostUndo 구현 필수 |

---

## 🏗️ 시스템 아키텍처 (System Architecture)

### 전체 아키텍처 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     Niagara Editor 아키텍처                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [1] FNiagaraSystemToolkit (메인 에디터 윈도우)                         │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: 에디터 윈도우 통합 및 라이프사이클 관리            │          │
│  │  - FWorkflowCentricApplication 상속 (탭 기반 UI)         │          │
│  │  - FGCObject: ViewModel들의 GC 방지                      │          │
│  │  - FEditorUndoClient: Undo/Redo 처리                     │          │
│  │                                                          │          │
│  │  소유하는 주요 컴포넌트:                                  │          │
│  │  ├─ SystemViewModel (TSharedPtr)                        │          │
│  │  ├─ GraphSelectionViewModel                             │          │
│  │  ├─ ParameterPanelViewModel                             │          │
│  │  ├─ SimCacheViewModel                                   │          │
│  │  └─ BakerViewModel                                      │          │
│  │                                                          │          │
│  │  제공하는 UI 탭:                                          │          │
│  │  ├─ Viewport (3D Preview)                               │          │
│  │  ├─ Timeline (Sequencer)                                │          │
│  │  ├─ System Overview (Emitter 목록)                      │          │
│  │  ├─ Selection (선택된 Emitter/Module 상세)              │          │
│  │  ├─ Parameters (파라미터 패널)                           │          │
│  │  ├─ Script Stats (성능 통계)                            │          │
│  │  └─ Message Log (컴파일 에러/경고)                       │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 소유                                                          │
│  [2] FNiagaraSystemViewModel (시스템 레벨 ViewModel)                   │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: UNiagaraSystem 편집 로직 및 상태 관리             │          │
│  │  - System 추가/삭제/수정                                 │          │
│  │  - Emitter 추가/삭제/순서 변경                           │          │
│  │  - 컴파일 트리거 및 상태 추적                            │          │
│  │  - Selection 관리                                       │          │
│  │  - Undo/Redo 통합                                        │          │
│  │                                                          │          │
│  │  Private:                                                │          │
│  │  - System : TObjectPtr<UNiagaraSystem>                  │          │
│  │  - EmitterHandleViewModels : TArray<TSharedRef<...>>   │          │
│  │  - EditorData : TObjectPtr<UNiagaraSystemEditorData>   │          │
│  │  - SelectionViewModel                                   │          │
│  │  - SystemScriptViewModel                                │          │
│  │                                                          │          │
│  │  Delegates (알림 시스템):                                 │          │
│  │  - OnSystemCompiled                                     │          │
│  │  - OnEmitterHandleViewModelsChanged                     │          │
│  │  - OnPreSave, OnPostSave                                │          │
│  │  - OnRequestFocusTab                                    │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 소유 (TArray)                                                │
│  [3] FNiagaraEmitterHandleViewModel (Emitter 핸들 래퍼)                │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: System 내 Emitter의 메타 정보 관리                │          │
│  │  - Enabled/Disabled 상태                                │          │
│  │  - Isolation (단독 표시) 모드                            │          │
│  │  - 이름 변경                                             │          │
│  │  - 순서 변경                                             │          │
│  │                                                          │          │
│  │  소유:                                                   │          │
│  │  - EmitterViewModel : TSharedPtr                        │          │
│  │  - EmitterHandle : const FNiagaraEmitterHandle*         │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 소유                                                          │
│  [4] FNiagaraEmitterViewModel (Emitter 레벨 ViewModel)                 │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: UNiagaraEmitter 편집 로직                          │          │
│  │  - Emitter 속성 수정 (SimTarget, SpawnRate 등)          │          │
│  │  - Parent Emitter 관리                                  │          │
│  │  - Event Handler 추가/삭제                               │          │
│  │  - Stats 표시                                            │          │
│  │                                                          │          │
│  │  Private:                                                │          │
│  │  - EmitterWeakPtr : FVersionedNiagaraEmitterWeakPtr     │          │
│  │  - Simulation : TWeakPtr<FNiagaraEmitterInstance>      │          │
│  │  - SharedScriptViewModel                                │          │
│  │  - SummaryViewHierarchyViewModel                        │          │
│  │                                                          │          │
│  │  Delegates:                                              │          │
│  │  - OnEmitterChanged                                     │          │
│  │  - OnPropertyChanged                                    │          │
│  │  - OnScriptCompiled                                     │          │
│  │  - OnParentRemoved                                      │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 소유                                                          │
│  [5] FNiagaraScriptViewModel (스크립트 ViewModel)                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: UNiagaraScript 편집 (Spawn/Update/Event)          │          │
│  │  - 그래프 ViewModel 관리                                 │          │
│  │  - 변수 추가/삭제/수정                                    │          │
│  │  - 컴파일 상태 추적                                      │          │
│  │                                                          │          │
│  │  소유:                                                   │          │
│  │  - GraphViewModel : TSharedRef<FNiagaraScriptGraphViewModel>│      │
│  │  - InputCollectionViewModel                             │          │
│  │  - OutputCollectionViewModel                            │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 소유                                                          │
│  [6] FNiagaraScriptGraphViewModel (그래프 에디터 ViewModel)             │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: UNiagaraGraph 편집                                 │          │
│  │  - 노드 선택 관리                                         │          │
│  │  - 복사/붙여넣기/삭제 명령                                │          │
│  │  - Undo/Redo                                             │          │
│  │                                                          │          │
│  │  Private:                                                │          │
│  │  - ScriptSource : TWeakObjectPtr<UNiagaraScriptSource> │          │
│  │  - NodeSelection : TSharedRef<FNiagaraObjectSelection> │          │
│  │  - Commands : TSharedRef<FUICommandList>                │          │
│  │                                                          │          │
│  │  Delegates:                                              │          │
│  │  - OnNodesPasted                                        │          │
│  │  - OnGraphChanged                                       │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  [7] INiagaraParameterPanelViewModel (파라미터 패널 ViewModel)          │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: 파라미터 통합 관리                                 │          │
│  │  - User 파라미터 추가/삭제/수정                           │          │
│  │  - System/Emitter/Particle 파라미터 표시                 │          │
│  │  - 네임스페이스 관리                                      │          │
│  │  - Parameter Definitions 연동                           │          │
│  │                                                          │          │
│  │  주요 메서드:                                             │          │
│  │  - AddParameter()                                        │          │
│  │  - DeleteParameters()                                    │          │
│  │  - RenameParameter()                                     │          │
│  │  - GetEditableGraphsConst()                             │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  [8] UNiagaraStackEntry (Stack UI 베이스 클래스)                        │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  역할: 모듈 스택 UI의 각 항목 (재귀적 트리 구조)          │          │
│  │  - 계층 구조: System → Emitter → Script → Module → Parameter│      │
│  │  - Expand/Collapse 상태 관리                             │          │
│  │  - 검색 및 필터링                                         │          │
│  │  - Issue 표시 (에러/경고)                                 │          │
│  │                                                          │          │
│  │  파생 클래스:                                             │          │
│  │  - UNiagaraStackRoot                                    │          │
│  │  - UNiagaraStackEmitter                                 │          │
│  │  - UNiagaraStackRenderItemGroup                         │          │
│  │  - UNiagaraStackModuleItem                              │          │
│  │  - UNiagaraStackFunctionInput                           │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1. FNiagaraSystemToolkit (메인 에디터 윈도우)

**역할:** Niagara System/Emitter 편집을 위한 통합 에디터 윈도우

**구조:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   FNiagaraSystemToolkit                                 │
│  (메인 에디터 윈도우 - System 또는 Emitter 편집)                         │
├─────────────────────────────────────────────────────────────────────────┤
│  상속: FWorkflowCentricApplication, FGCObject, FEditorUndoClient        │
│                                                                         │
│  Private:                                                               │
│    - SystemToolkitMode : ESystemToolkitMode                            │
│      // System 또는 Emitter 편집 모드                                   │
│      //   System: 전체 시스템 편집                                      │
│      //   Emitter: 단일 에미터 편집 (Emitter Asset)                     │
│                                                                         │
│    - System : TObjectPtr<UNiagaraSystem>                               │
│      // 편집 중인 시스템 (System 모드)                                  │
│                                                                         │
│    - Emitter : TObjectPtr<UNiagaraEmitter>                             │
│      // 편집 중인 에미터 (Emitter 모드)                                 │
│                                                                         │
│    - SystemViewModel : TSharedPtr<FNiagaraSystemViewModel>             │
│      // 시스템 ViewModel (양쪽 모드에서 사용)                           │
│                                                                         │
│    - SystemGraphSelectionViewModel : TSharedPtr<...>                   │
│      // 선택 상태 관리 ViewModel                                        │
│                                                                         │
│    - ParameterPanelViewModel : TSharedPtr<...>                         │
│      // 파라미터 패널 ViewModel                                         │
│                                                                         │
│    - SimCacheViewModel : TSharedPtr<FNiagaraSimCacheViewModel>         │
│      // Simulation Cache ViewModel (녹화/재생)                         │
│                                                                         │
│    - BakerViewModel : TSharedPtr<FNiagaraBakerViewModel>               │
│      // Baker ViewModel (Texture/Mesh 베이킹)                          │
│                                                                         │
│  주요 메서드:                                                            │
│    + InitializeWithSystem(Mode, Host, System) : void                   │
│      // System 편집 모드로 초기화                                        │
│                                                                         │
│    + InitializeWithEmitter(Mode, Host, Emitter) : void                 │
│      // Emitter 편집 모드로 초기화                                       │
│                                                                         │
│    + CompileSystem(bFullRebuild) : void                                │
│      // 시스템 컴파일 (Ctrl+S 또는 Apply 버튼)                          │
│                                                                         │
│    + GetSystemViewModel() : TSharedPtr<FNiagaraSystemViewModel>        │
│      // SystemViewModel 반환                                            │
│                                                                         │
│    + GetCompileStatusImage() : FSlateIcon                              │
│      // 컴파일 상태 아이콘 (Success, Warning, Error)                    │
│                                                                         │
│    + GetCompileStatusTooltip() : FText                                 │
│      // 컴파일 상태 툴팁                                                 │
│                                                                         │
│    + ResetSimulation() : void                                          │
│      // 시뮬레이션 리셋 (Space 키)                                       │
│                                                                         │
│    + ToggleBounds() : void                                             │
│      // Bounds 표시 토글                                                │
│                                                                         │
│    + ToggleStatPerformance() : void                                    │
│      // 성능 통계 표시 토글                                              │
│                                                                         │
│  UI 탭 생성 메서드:                                                      │
│    - GetSystemOverview() : TSharedPtr<SWidget>                         │
│      // System Overview 탭 (Emitter 목록)                              │
│                                                                         │
│    - GetScriptScratchpadManager() : TSharedPtr<SWidget>                │
│      // Script Scratchpad 탭 (모듈 스크립트 편집)                       │
│                                                                         │
│    - GetVersioningWidget() : TSharedPtr<SWidget>                       │
│      // Versioning 위젯 (버전 전환)                                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraSystemToolkit.h:48-200` - FNiagaraSystemToolkit 클래스

**UI 탭 구성:**

```
FNiagaraSystemToolkit 윈도우 레이아웃:
┌─────────────────────────────────────────────────────────────────────────┐
│  [File] [Edit] [Window] [Help]                           [Compile ✓]    │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────┬──────────────────────────────────────────────┐ │
│  │ System Overview     │  Viewport (3D Preview)                       │ │
│  │ ┌─────────────────┐ │  ┌────────────────────────────────────────┐ │ │
│  │ │ Emitter 1 (On)  │ │  │                                        │ │ │
│  │ │ Emitter 2 (Off) │ │  │        [Particle Preview]              │ │ │
│  │ │ Emitter 3 (On)  │ │  │                                        │ │ │
│  │ └─────────────────┘ │  └────────────────────────────────────────┘ │ │
│  │                     │  ┌────────────────────────────────────────┐ │ │
│  │ Parameters          │  │ Timeline (Sequencer)                   │ │ │
│  │ ┌─────────────────┐ │  │ [0────────5────────10──────15───]      │ │ │
│  │ │ User.Speed      │ │  └────────────────────────────────────────┘ │ │
│  │ │ User.Color      │ │                                              │ │
│  │ └─────────────────┘ │                                              │ │
│  └─────────────────────┴──────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ Selection (Selected Emitter/Module Details)                      │   │
│  │ ┌──────────────────────────────────────────────────────────────┐ │   │
│  │ │ [Stack UI]                                                   │ │   │
│  │ │ ├─ Emitter Properties                                        │ │   │
│  │ │ ├─ Emitter Spawn                                             │ │   │
│  │ │ ├─ Emitter Update                                            │ │   │
│  │ │ ├─ Particle Spawn                                            │ │   │
│  │ │ │  ├─ Initialize Particle                                    │ │   │
│  │ │ │  ├─ Add Velocity                                           │ │   │
│  │ │ │  └─ [Add Module ▼]                                         │ │   │
│  │ │ ├─ Particle Update                                           │ │   │
│  │ │ │  ├─ Gravity Force                                          │ │   │
│  │ │ │  ├─ Solve Forces and Velocity                              │ │   │
│  │ │ │  └─ [Add Module ▼]                                         │ │   │
│  │ │ └─ Render                                                    │ │   │
│  │ │    └─ Sprite Renderer                                        │ │   │
│  │ └──────────────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ Message Log                                                      │   │
│  │ [Info] Compilation successful (0.5s)                             │   │
│  │ [Warning] Module "CustomForce" is experimental                   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2. FNiagaraSystemViewModel (시스템 ViewModel)

**역할:** UNiagaraSystem의 편집 로직 및 상태 관리

**구조:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   FNiagaraSystemViewModel                               │
│  (시스템 레벨 ViewModel)                                                 │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: System 편집, Emitter 관리, 컴파일 조율                            │
│                                                                         │
│  Private:                                                               │
│    - System : TObjectPtr<UNiagaraSystem>                               │
│      // 편집 중인 시스템 에셋                                           │
│                                                                         │
│    - EmitterHandleViewModels : TArray<TSharedRef<...>>                 │
│      // 시스템 내 모든 Emitter의 ViewModel 목록                         │
│                                                                         │
│    - SystemScriptViewModel : TSharedPtr<FNiagaraScriptViewModel>       │
│      // System Spawn/Update Script ViewModel                           │
│                                                                         │
│    - SelectionViewModel : TSharedPtr<FNiagaraSystemGraphSelectionViewModel>│
│      // 선택 상태 관리                                                  │
│                                                                         │
│    - EditorData : TObjectPtr<UNiagaraSystemEditorData>                 │
│      // 에디터 전용 데이터 (UI 상태 등)                                  │
│                                                                         │
│    - PreviewComponent : TObjectPtr<UNiagaraComponent>                  │
│      // 프리뷰용 컴포넌트                                                │
│                                                                         │
│    - EditMode : ENiagaraSystemViewModelEditMode                        │
│      // SystemAsset: 에셋 직접 편집                                     │
│      // SystemInstance: 레벨에 배치된 인스턴스 편집                      │
│                                                                         │
│    - bCompilePendingCompletion : bool                                  │
│      // 컴파일 대기 중 플래그                                            │
│                                                                         │
│  주요 메서드:                                                            │
│    + AddEmitter(Emitter) : void                                        │
│      // 에미터 추가 (드래그앤드롭 또는 버튼)                             │
│                                                                         │
│    + DeleteEmitters(EmitterHandles) : void                             │
│      // 에미터 삭제                                                     │
│                                                                         │
│    + DuplicateEmitters(EmitterHandles) : void                          │
│      // 에미터 복제                                                     │
│                                                                         │
│    + RequestCompile(bForce) : void                                     │
│      // 컴파일 요청                                                     │
│                                                                         │
│    + ResetSystem(ResetMode, TimeRange) : void                          │
│      // 시뮬레이션 리셋                                                  │
│                                                                         │
│    + GetEmitterHandles() : TArray<TSharedRef<...>>                     │
│      // 모든 EmitterHandleViewModel 반환                               │
│                                                                         │
│    + GetSelectionViewModel() : TSharedPtr<...>                         │
│      // SelectionViewModel 반환                                         │
│                                                                         │
│    + GetPreviewComponent() : UNiagaraComponent*                        │
│      // 프리뷰 컴포넌트 반환                                             │
│                                                                         │
│  Delegates (알림 시스템):                                                │
│    DECLARE_MULTICAST_DELEGATE(FOnSystemCompiled);                      │
│    FOnSystemCompiled OnSystemCompiled;                                 │
│    // 컴파일 완료 시 발생                                                │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE(FOnEmitterHandleViewModelsChanged);      │
│    FOnEmitterHandleViewModelsChanged OnEmitterHandleViewModelsChanged; │
│    // Emitter 추가/삭제 시 발생                                          │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE(FOnPreSave);                             │
│    FOnPreSave OnPreSave;                                               │
│    // 저장 직전 발생 (정리 작업 수행)                                    │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE(FOnPostSave);                            │
│    FOnPostSave OnPostSave;                                             │
│    // 저장 직후 발생                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraSystemViewModel.h:83-300` - FNiagaraSystemViewModel 클래스

### 3. FNiagaraEmitterViewModel (에미터 ViewModel)

**역할:** UNiagaraEmitter의 편집 로직 및 Script ViewModel 관리

**구조:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   FNiagaraEmitterViewModel                              │
│  (에미터 레벨 ViewModel)                                                 │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: Emitter 속성 편집, Script ViewModel 관리                          │
│                                                                         │
│  Private:                                                               │
│    - EmitterWeakPtr : FVersionedNiagaraEmitterWeakPtr                  │
│      // 편집 중인 에미터 (Weak Pointer로 GC 안전)                       │
│                                                                         │
│    - Simulation : TWeakPtr<FNiagaraEmitterInstance>                    │
│      // 런타임 시뮬레이션 인스턴스 (프리뷰용)                            │
│                                                                         │
│    - SharedScriptViewModel : TSharedPtr<FNiagaraScriptViewModel>       │
│      // Spawn/Update/Event Script ViewModel (공유)                     │
│                                                                         │
│    - SummaryViewHierarchyViewModel : TObjectPtr<...>                   │
│      // Summary View (간략 표시) ViewModel                             │
│                                                                         │
│    - bUpdatingSelectionInternally : bool                               │
│      // 재진입 방지 플래그 (Selection 업데이트 시)                       │
│                                                                         │
│  주요 메서드:                                                            │
│    + Initialize(Emitter, Simulation) : bool                            │
│      // ViewModel 초기화                                                │
│                                                                         │
│    + GetEmitter() : FVersionedNiagaraEmitter                           │
│      // 에미터 반환                                                     │
│                                                                         │
│    + HasParentEmitter() : bool                                         │
│      // Parent Emitter 존재 여부                                       │
│                                                                         │
│    + GetParentEmitter() : FVersionedNiagaraEmitter                     │
│      // Parent Emitter 반환                                            │
│                                                                         │
│    + RemoveParentEmitter() : void                                      │
│      // Parent 제거 (독립 Emitter로 전환)                               │
│                                                                         │
│    + GetStatsText() : FText                                            │
│      // 통계 텍스트 (파티클 개수, 메모리 등)                             │
│                                                                         │
│    + GetSharedScriptViewModel() : TSharedRef<...>                      │
│      // Script ViewModel 반환                                           │
│                                                                         │
│    + GetLatestCompileStatus() : ENiagaraScriptCompileStatus            │
│      // 최신 컴파일 상태 반환                                            │
│                                                                         │
│    + AddEventHandler(EventProperties, bResetGraph) : void              │
│      // Event Script 추가                                              │
│                                                                         │
│    + GetEditorData() : UNiagaraEmitterEditorData&                      │
│      // 에디터 전용 데이터 반환                                          │
│                                                                         │
│  Delegates:                                                             │
│    DECLARE_MULTICAST_DELEGATE(FOnEmitterChanged);                      │
│    FOnEmitterChanged OnEmitterChanged;                                 │
│    // Emitter 에셋 변경 시 (다른 에셋으로 교체)                          │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE(FOnPropertyChanged);                     │
│    FOnPropertyChanged OnPropertyChanged;                               │
│    // Emitter 속성 변경 시 (SimTarget, SpawnRate 등)                    │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE_TwoParams(FOnScriptCompiled, ...);       │
│    FOnScriptCompiled OnScriptCompiled;                                 │
│    // Script 컴파일 완료 시                                             │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE(FOnParentRemoved);                       │
│    FOnParentRemoved OnParentRemoved;                                   │
│    // Parent Emitter 제거 시                                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraEmitterViewModel.h:33-219` - FNiagaraEmitterViewModel 클래스

### 4. FNiagaraScriptGraphViewModel (그래프 에디터 ViewModel)

**역할:** UNiagaraGraph의 그래프 편집 로직 및 노드 선택 관리

**구조:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                 FNiagaraScriptGraphViewModel                            │
│  (그래프 에디터 ViewModel)                                               │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 그래프 노드 편집, 복사/붙여넣기/삭제 명령 처리                     │
│                                                                         │
│  Private:                                                               │
│    - ScriptSource : TWeakObjectPtr<UNiagaraScriptSource>               │
│      // UNiagaraGraph를 소유한 ScriptSource                             │
│                                                                         │
│    - DisplayName : TAttribute<FText>                                   │
│      // 그래프 표시 이름 (탭 제목)                                       │
│                                                                         │
│    - Commands : TSharedRef<FUICommandList>                             │
│      // 편집 명령 (Copy, Paste, Delete 등)                              │
│                                                                         │
│    - NodeSelection : TSharedRef<FNiagaraObjectSelection>               │
│      // 선택된 노드 목록                                                │
│                                                                         │
│    - ErrorMsg : FString                                                │
│      // 에러 메시지 (컴파일 에러 등)                                     │
│                                                                         │
│    - ErrorColor : FSlateColor                                          │
│      // 에러 색상 (빨강, 노랑 등)                                        │
│                                                                         │
│    - bIsForDataProcessingOnly : bool                                   │
│      // UI 없이 데이터 처리만 하는 ViewModel인지 여부                    │
│                                                                         │
│  주요 메서드:                                                            │
│    + SetScriptSource(ScriptSource) : void                              │
│      // 그래프 소스 설정                                                │
│                                                                         │
│    + GetGraph() : UNiagaraGraph*                                       │
│      // 그래프 반환                                                     │
│                                                                         │
│    + GetCommands() : TSharedRef<FUICommandList>                        │
│      // 명령 목록 반환 (Copy, Paste 등)                                 │
│                                                                         │
│    + GetNodeSelection() : TSharedRef<FNiagaraObjectSelection>          │
│      // 선택된 노드 목록 반환                                            │
│                                                                         │
│    + SetSelectedNodes(InSelectedNodes) : void                          │
│      // 노드 선택 설정                                                  │
│                                                                         │
│    + ClearSelectedNodes() : void                                       │
│      // 선택 해제                                                       │
│                                                                         │
│  명령 핸들러 (Private):                                                  │
│    - SelectAllNodes() : void                                           │
│      // Ctrl+A 처리                                                    │
│                                                                         │
│    - DeleteSelectedNodes() : void                                      │
│      // Delete 키 처리                                                 │
│                                                                         │
│    - CanDeleteNodes() : bool                                           │
│      // 삭제 가능 여부 체크                                              │
│                                                                         │
│    - CutSelectedNodes() : void                                         │
│      // Ctrl+X 처리 (Copy + Delete)                                    │
│                                                                         │
│    - CopySelectedNodes() : void                                        │
│      // Ctrl+C 처리                                                    │
│                                                                         │
│    - PasteNodes() : void                                               │
│      // Ctrl+V 처리                                                    │
│                                                                         │
│    - DuplicateNodes() : void                                           │
│      // Ctrl+D 처리                                                    │
│                                                                         │
│  Delegates:                                                             │
│    DECLARE_MULTICAST_DELEGATE_OneParam(FOnNodesPasted, ...);           │
│    FOnNodesPasted OnNodesPasted;                                       │
│    // 노드 붙여넣기 완료 시 발생                                         │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE(FOnGraphChanged);                        │
│    FOnGraphChanged OnGraphChanged;                                     │
│    // 그래프 변경 시 발생 (노드 추가/삭제 등)                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraScriptGraphViewModel.h:16-121` - FNiagaraScriptGraphViewModel 클래스

### 5. UNiagaraStackEntry (Stack UI 베이스)

**역할:** 모듈 스택 UI의 각 항목 (재귀적 트리 구조)

**구조:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    UNiagaraStackEntry                                   │
│  (Stack UI 베이스 클래스 - 재귀적 트리)                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: Stack UI의 각 항목 (System, Emitter, Script, Module, Parameter)  │
│                                                                         │
│  상속: UObject → UNiagaraStackEntry                                     │
│                                                                         │
│  Protected:                                                             │
│    - bIsExpanded : bool                                                │
│      // Expand/Collapse 상태                                           │
│                                                                         │
│    - bIsExpandedByDefault : bool                                       │
│      // 기본 Expand 상태                                                │
│                                                                         │
│    - bIsSearchResult : bool                                            │
│      // 검색 결과인지 여부                                               │
│                                                                         │
│    - StackIssues : TArray<FStackIssue>                                 │
│      // Issue 목록 (에러, 경고, 정보)                                    │
│                                                                         │
│    - Children : TArray<UNiagaraStackEntry*>                            │
│      // 자식 항목 (재귀적 트리)                                          │
│                                                                         │
│  주요 메서드:                                                            │
│    + GetDisplayName() : FText (virtual)                                │
│      // 항목 이름 반환                                                  │
│                                                                         │
│    + GetTooltipText() : FText (virtual)                                │
│      // 툴팁 텍스트                                                     │
│                                                                         │
│    + GetChildren() : TArray<UNiagaraStackEntry*>                       │
│      // 자식 항목 반환                                                  │
│                                                                         │
│    + GetIsExpanded() : bool                                            │
│      // Expand 상태 반환                                                │
│                                                                         │
│    + SetIsExpanded(bExpanded) : void                                   │
│      // Expand 상태 설정                                                │
│                                                                         │
│    + GetStackIssues() : TArray<FStackIssue>                            │
│      // Issue 목록 반환                                                 │
│                                                                         │
│    + GetHighestStackIssueSeverity() : EStackIssueSeverity              │
│      // 가장 심각한 Issue 레벨 반환 (Error > Warning > Info)            │
│                                                                         │
│  파생 클래스:                                                            │
│    - UNiagaraStackRoot                                                 │
│      // 최상위 Root (System 또는 Emitter)                               │
│                                                                         │
│    - UNiagaraStackSystemPropertiesItem                                 │
│      // System 속성 항목                                                │
│                                                                         │
│    - UNiagaraStackEmitterPropertiesItem                                │
│      // Emitter 속성 항목                                               │
│                                                                         │
│    - UNiagaraStackScriptItemGroup                                      │
│      // Script 그룹 (Spawn, Update, Event)                             │
│                                                                         │
│    - UNiagaraStackModuleItem                                           │
│      // 모듈 항목 (Add Velocity, Gravity Force 등)                     │
│                                                                         │
│    - UNiagaraStackFunctionInput                                        │
│      // 모듈의 입력 파라미터 항목                                        │
│                                                                         │
│    - UNiagaraStackRenderItemGroup                                      │
│      // Render 그룹 (Sprite Renderer, Mesh Renderer 등)                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraStackEntry.h:24-250` - UNiagaraStackEntry 클래스 및 파생 클래스들

**Stack UI 계층 구조:**

```
Stack UI 트리 구조 예시:
┌─────────────────────────────────────────────────────────────────────────┐
│  [UNiagaraStackRoot]                                                    │
│  └─ [UNiagaraStackEmitterPropertiesItem] "Emitter Properties"          │
│      ├─ SimTarget: GPUComputeSim                                       │
│      ├─ bLocalSpace: false                                             │
│      └─ ...                                                             │
│                                                                         │
│  └─ [UNiagaraStackScriptItemGroup] "Emitter Spawn"                     │
│      └─ (모듈 없음)                                                     │
│                                                                         │
│  └─ [UNiagaraStackScriptItemGroup] "Emitter Update"                    │
│      ├─ [UNiagaraStackModuleItem] "Spawn Rate"                         │
│      │   └─ [UNiagaraStackFunctionInput] "SpawnRate" = 100.0          │
│      └─ [Add Module ▼]                                                 │
│                                                                         │
│  └─ [UNiagaraStackScriptItemGroup] "Particle Spawn"                    │
│      ├─ [UNiagaraStackModuleItem] "Initialize Particle"                │
│      │   ├─ [UNiagaraStackFunctionInput] "Lifetime" = 5.0             │
│      │   ├─ [UNiagaraStackFunctionInput] "Color" = (1, 1, 1, 1)       │
│      │   └─ [UNiagaraStackFunctionInput] "Sprite Size" = (100, 100)   │
│      │                                                                 │
│      ├─ [UNiagaraStackModuleItem] "Add Velocity"                       │
│      │   └─ [UNiagaraStackFunctionInput] "Velocity" = (0, 0, 100)     │
│      │                                                                 │
│      └─ [Add Module ▼]                                                 │
│                                                                         │
│  └─ [UNiagaraStackScriptItemGroup] "Particle Update"                   │
│      ├─ [UNiagaraStackModuleItem] "Gravity Force"                      │
│      │   └─ [UNiagaraStackFunctionInput] "Gravity" = (0, 0, -980)     │
│      │                                                                 │
│      ├─ [UNiagaraStackModuleItem] "Solve Forces and Velocity"          │
│      │   └─ (No inputs)                                               │
│      │                                                                 │
│      └─ [Add Module ▼]                                                 │
│                                                                         │
│  └─ [UNiagaraStackRenderItemGroup] "Render"                            │
│      └─ [UNiagaraStackRendererItem] "Sprite Renderer"                  │
│          ├─ Material: M_Particle_Default                               │
│          ├─ Alignment: Facing Camera                                  │
│          └─ ...                                                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 에디터 워크플로우 (Editor Workflow)

### 1. 시스템 생성 및 편집 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   Niagara 시스템 생성 및 편집 흐름                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [1] 새 Niagara System 생성                                             │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  Content Browser → 우클릭                                │          │
│  │  └─ FX → Niagara System                                 │          │
│  │      └─ 템플릿 선택:                                      │          │
│  │          ├─ Empty (빈 시스템)                            │          │
│  │          ├─ Simple Sprite Burst (스프라이트 버스트)      │          │
│  │          ├─ Fountain (분수)                              │          │
│  │          └─ ...                                          │          │
│  └──────────────────────────────────────────────────────────┘          │
│         ↓                                                               │
│  [2] FNiagaraSystemToolkit 열림                                         │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  InitializeWithSystem() 호출                             │          │
│  │  ├─ SystemViewModel 생성                                │          │
│  │  ├─ EmitterHandleViewModels 생성                        │          │
│  │  ├─ PreviewComponent 생성 및 월드 배치                  │          │
│  │  └─ UI 탭 생성 (Viewport, Stack, Parameters 등)         │          │
│  └──────────────────────────────────────────────────────────┘          │
│         ↓                                                               │
│  [3] Emitter 추가                                                       │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  System Overview 패널 → [+] 버튼 클릭                    │          │
│  │  또는 Content Browser에서 Emitter 드래그앤드롭           │          │
│  │                                                          │          │
│  │  SystemViewModel->AddEmitter(EmitterAsset)              │          │
│  │  ├─ System->AddEmitterHandle() (Model 변경)             │          │
│  │  ├─ FNiagaraEmitterHandleViewModel 생성                 │          │
│  │  │   └─ FNiagaraEmitterViewModel 생성                   │          │
│  │  │       └─ FNiagaraScriptViewModel 생성                │          │
│  │  │           └─ FNiagaraScriptGraphViewModel 생성        │          │
│  │  │                                                       │          │
│  │  └─ OnEmitterHandleViewModelsChanged 발생               │          │
│  │      └─ UI 자동 갱신 (System Overview, Stack)           │          │
│  └──────────────────────────────────────────────────────────┘          │
│         ↓                                                               │
│  [4] 모듈 추가                                                          │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  Selection 패널 (Stack UI) → [Add Module ▼] 클릭        │          │
│  │  └─ 모듈 라이브러리 표시                                 │          │
│  │      ├─ Forces (Gravity, Drag, Curl Noise 등)          │          │
│  │      ├─ Location (Set Position, Orbit 등)              │          │
│  │      ├─ Velocity (Add Velocity, Velocity Cone 등)      │          │
│  │      └─ ...                                             │          │
│  │                                                          │          │
│  │  모듈 선택 → Script에 Function Call 노드 추가            │          │
│  │  ├─ UNiagaraGraph 수정                                  │          │
│  │  ├─ OnScriptGraphChanged 발생                           │          │
│  │  └─ 자동 재컴파일 트리거 (Auto Compile 켜져있으면)      │          │
│  └──────────────────────────────────────────────────────────┘          │
│         ↓                                                               │
│  [5] 파라미터 수정                                                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  Stack UI에서 모듈 확장 → 파라미터 편집                 │          │
│  │  예: "Gravity Force" → Gravity = (0, 0, -980)           │          │
│  │                                                          │          │
│  │  UNiagaraStackFunctionInput 수정                        │          │
│  │  └─ FNiagaraVariable 값 변경                            │          │
│  │      └─ RapidIterationParameters 업데이트               │          │
│  │          └─ 즉시 프리뷰에 반영 (재컴파일 불필요!)        │          │
│  └──────────────────────────────────────────────────────────┘          │
│         ↓                                                               │
│  [6] 컴파일                                                             │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  자동 컴파일 (Auto Compile 활성화 시):                   │          │
│  │  - 그래프 변경 감지 → 자동으로 RequestCompile()          │          │
│  │                                                          │          │
│  │  수동 컴파일:                                             │          │
│  │  - Ctrl+S 또는 [Apply] 버튼                             │          │
│  │      └─ SystemViewModel->RequestCompile(bForce=false)   │          │
│  │          └─ System->RequestCompile()                    │          │
│  │              ├─ 모든 Script 컴파일 요청                  │          │
│  │              ├─ DDC 확인 → 히트 시 즉시 로드             │          │
│  │              ├─ 미스 시 Compiler 실행                    │          │
│  │              └─ 컴파일 완료 후 OnSystemCompiled 발생     │          │
│  │                  └─ UI 갱신                              │          │
│  └──────────────────────────────────────────────────────────┘          │
│         ↓                                                               │
│  [7] 프리뷰 및 테스트                                                   │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  Viewport 탭:                                             │          │
│  │  - Space 키: 시뮬레이션 리셋                             │          │
│  │  - Pause/Play: 일시정지/재생                            │          │
│  │  - Bounds 표시 토글                                      │          │
│  │                                                          │          │
│  │  Timeline (Sequencer):                                   │          │
│  │  - 시간 스크러빙 (Scrubbing)                             │          │
│  │  - 특정 시간으로 이동                                    │          │
│  │  - 재생 속도 조절                                        │          │
│  │                                                          │          │
│  │  Script Stats:                                           │          │
│  │  - CPU/GPU 시간 측정                                     │          │
│  │  - 모듈별 성능 비용 표시                                 │          │
│  └──────────────────────────────────────────────────────────┘          │
│         ↓                                                               │
│  [8] 저장                                                               │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  Ctrl+S 또는 File → Save                                │          │
│  │  └─ Toolkit->SaveAsset_Execute()                        │          │
│  │      ├─ OnPreSave 발생 (정리 작업)                       │          │
│  │      ├─ System->PreSave() (에셋 저장)                    │          │
│  │      ├─ EditorData 저장 (UI 상태 등)                     │          │
│  │      └─ OnPostSave 발생                                  │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2. 실시간 프리뷰 시스템

```
실시간 프리뷰 동작 원리:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [SystemViewModel]                                                      │
│  └─ PreviewComponent : TObjectPtr<UNiagaraComponent>                   │
│      // 에디터 전용 프리뷰 컴포넌트                                      │
│                                                                         │
│  컴포넌트 생성:                                                          │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  FNiagaraSystemViewModel::Initialize()                  │          │
│  │  {                                                       │          │
│  │      // 1. 프리뷰 월드 생성 (독립된 UWorld)              │          │
│  │      PreviewWorld = FNiagaraEditorModule::Get()         │          │
│  │          .GetOrCreatePreviewWorld();                    │          │
│  │                                                          │          │
│  │      // 2. 프리뷰 컴포넌트 생성                          │          │
│  │      PreviewComponent = NewObject<UNiagaraComponent>(); │          │
│  │      PreviewComponent->SetAsset(System);                │          │
│  │      PreviewComponent->RegisterComponentWithWorld(      │          │
│  │          PreviewWorld);                                 │          │
│  │                                                          │          │
│  │      // 3. 자동 활성화                                   │          │
│  │      PreviewComponent->Activate(true);                  │          │
│  │  }                                                       │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  실시간 업데이트:                                                        │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  매 프레임 (Slate Tick):                                 │          │
│  │  SNiagaraSystemViewport::Tick(DeltaTime)                │          │
│  │  └─ PreviewWorld->Tick(DeltaTime)                       │          │
│  │      └─ PreviewComponent->TickComponent(DeltaTime)      │          │
│  │          └─ SystemInstance->Tick_GameThread()           │          │
│  │              └─ SystemInstance->Tick_Concurrent()       │          │
│  │                  └─ EmitterInstance->Tick()             │          │
│  │                      └─ VectorVM::Exec() (CPU)          │          │
│  │                          또는 GPU Compute Dispatch       │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  파라미터 즉시 반영:                                                     │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  Stack UI에서 파라미터 변경:                             │          │
│  │  UNiagaraStackFunctionInput::SetValue(NewValue)         │          │
│  │  └─ RapidIterationParameters 업데이트                   │          │
│  │      └─ PreviewComponent->SetVariableFloat(...)         │          │
│  │          └─ SystemInstance->ParameterStore 업데이트      │          │
│  │              └─ 다음 프레임부터 즉시 반영!               │          │
│  │                  (재컴파일 불필요!)                       │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  리셋 기능:                                                              │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  Space 키 누를 때:                                        │          │
│  │  SystemViewModel->ResetSystem(...)                      │          │
│  │  └─ PreviewComponent->ResetSystem()                     │          │
│  │      └─ SystemInstance->Reset()                         │          │
│  │          ├─ 모든 Emitter 리셋                            │          │
│  │          ├─ 파티클 데이터 초기화                         │          │
│  │          └─ Age = 0.0으로 리셋                           │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 💡 실전 예시 (Practical Examples)

### ✅ 좋은 예: ViewModel Delegate 활용

```cpp
#if WITH_EDITOR
// ✅ ViewModel 변경 이벤트 구독
void SMyCustomWidget::Construct(const FArguments& InArgs)
{
    SystemViewModel = InArgs._SystemViewModel;

    // Emitter 변경 시 자동 갱신
    SystemViewModel->OnEmitterHandleViewModelsChanged().AddSP(
        this, &SMyCustomWidget::OnEmittersChanged
    );

    // 컴파일 완료 시 자동 갱신
    SystemViewModel->OnSystemCompiled().AddSP(
        this, &SMyCustomWidget::OnSystemCompiled
    );

    // UI 구성...
}

void SMyCustomWidget::OnEmittersChanged()
{
    // Emitter 목록 갱신
    RefreshEmitterList();
}

void SMyCustomWidget::OnSystemCompiled()
{
    // 컴파일 상태 갱신
    RefreshCompileStatus();
}
#endif
```

### ✅ 좋은 예: Stack UI 계층 탐색

```cpp
#if WITH_EDITOR
// ✅ Stack UI 계층 순회하여 특정 항목 찾기
UNiagaraStackModuleItem* FindModuleInStack(
    UNiagaraStackEntry* RootEntry,
    FName ModuleName)
{
    // 재귀적으로 자식 탐색
    TArray<UNiagaraStackEntry*> Children = RootEntry->GetChildren();

    for (UNiagaraStackEntry* Child : Children)
    {
        // 모듈 항목인지 확인
        if (UNiagaraStackModuleItem* ModuleItem = Cast<UNiagaraStackModuleItem>(Child))
        {
            if (ModuleItem->GetDisplayName().ToString().Contains(ModuleName.ToString()))
            {
                return ModuleItem;  // 찾음!
            }
        }

        // 재귀적으로 자식 탐색
        if (UNiagaraStackModuleItem* Found = FindModuleInStack(Child, ModuleName))
        {
            return Found;
        }
    }

    return nullptr;  // 못 찾음
}

// 사용 예시:
UNiagaraStackModuleItem* GravityModule = FindModuleInStack(
    StackViewModel->GetRootEntry(),
    TEXT("Gravity Force")
);

if (GravityModule)
{
    UE_LOG(LogNiagara, Log, TEXT("Found Gravity Force module!"));
}
#endif
```

### ✅ 좋은 예: 프로그래매틱 모듈 추가

```cpp
#if WITH_EDITOR
// ✅ 코드로 모듈 추가 (에디터에서만)
void AddModuleToEmitter(
    FNiagaraEmitterViewModel* EmitterViewModel,
    UNiagaraScript* ModuleScript)
{
    // 1. Script 가져오기
    UNiagaraScript* ParticleUpdateScript = EmitterViewModel->GetEmitter()
        .GetEmitterData()->GetScript(ENiagaraScriptUsage::ParticleUpdateScript, FGuid());

    if (!ParticleUpdateScript)
        return;

    // 2. Graph 가져오기
    UNiagaraScriptSource* ScriptSource = Cast<UNiagaraScriptSource>(
        ParticleUpdateScript->GetLatestSource()
    );

    if (!ScriptSource)
        return;

    UNiagaraGraph* Graph = ScriptSource->NodeGraph;

    // 3. Function Call 노드 생성
    UNiagaraNodeFunctionCall* FunctionCallNode = NewObject<UNiagaraNodeFunctionCall>(Graph);
    FunctionCallNode->FunctionScript = ModuleScript;

    // 4. Graph에 추가
    Graph->AddNode(FunctionCallNode, /*bSelectNewNode=*/ true);

    // 5. 자동 연결 (이전 노드 → 새 노드 → 출력 노드)
    // ... 연결 로직 ...

    // 6. 재컴파일 트리거
    Graph->NotifyGraphNeedsRecompile();
}
#endif
```

### ✅ 좋은 예: 파라미터 오버라이드

```cpp
#if WITH_EDITOR
// ✅ User 파라미터 추가 및 기본값 설정
void AddUserParameter(
    FNiagaraSystemViewModel* SystemViewModel,
    FName ParameterName,
    FNiagaraTypeDefinition Type,
    float DefaultValue)
{
    UNiagaraSystem* System = SystemViewModel->GetSystem();

    // 1. User 파라미터 추가
    FNiagaraVariable NewVariable(Type, ParameterName);
    System->GetExposedParameters().AddParameter(NewVariable);

    // 2. 기본값 설정
    System->GetExposedParameters().SetParameterValue(DefaultValue, NewVariable);

    // 3. UI 갱신
    SystemViewModel->RefreshAll();

    UE_LOG(LogNiagara, Log, TEXT("Added User Parameter: %s = %f"),
        *ParameterName.ToString(), DefaultValue);
}

// 사용 예시:
AddUserParameter(
    SystemViewModel,
    TEXT("User.FireIntensity"),
    FNiagaraTypeDefinition::GetFloatDef(),
    1.0f
);
#endif
```

### ❌ 나쁜 예: ViewModel 없이 Model 직접 수정

```cpp
#if WITH_EDITOR
// ❌ 잘못된 방법: Model을 직접 수정
void BadAddEmitter(UNiagaraSystem* System, UNiagaraEmitter* Emitter)
{
    // ❌ ViewModel 없이 직접 추가
    FNiagaraEmitterHandle Handle = System->AddEmitterHandle(*Emitter, TEXT("MyEmitter"));

    // 문제점:
    // 1. UI가 갱신되지 않음! (Delegate 발생 안 함)
    // 2. Undo/Redo 안 됨
    // 3. ViewModel 상태와 불일치
    // 4. 다른 UI 패널들이 동기화 안 됨
}
```

**올바른 방법:**

```cpp
#if WITH_EDITOR
// ✅ 올바른 방법: ViewModel 사용
void GoodAddEmitter(
    FNiagaraSystemViewModel* SystemViewModel,
    UNiagaraEmitter* Emitter)
{
    // ✅ ViewModel의 메서드 사용
    SystemViewModel->AddEmitter(*Emitter);

    // 자동으로:
    // 1. Model 업데이트
    // 2. Delegate 발생 → UI 자동 갱신
    // 3. Undo/Redo 스택 등록
    // 4. 모든 UI 패널 동기화
}
#endif
```

### ❌ 나쁜 예: 런타임에서 에디터 API 호출

```cpp
// ❌ 절대 런타임에서 에디터 API 호출하지 마세요!
void BadRuntimeEditing(UNiagaraSystem* System)
{
    // ❌ 컴파일되지 않음! (WITH_EDITOR 없음)
    // ❌ 패키징된 게임에서 크래시!
    #if WITH_EDITOR
    FNiagaraSystemViewModel* ViewModel = ...; // 에디터 전용 클래스
    ViewModel->AddEmitter(...);
    #endif
}
```

**올바른 방법:**

```cpp
// ✅ 런타임에서는 이미 완성된 에셋만 사용
void GoodRuntimeUsage(AActor* Owner)
{
    // ✅ 이미 편집 완료된 에셋 로드
    UNiagaraSystem* System = LoadObject<UNiagaraSystem>(
        nullptr,
        TEXT("/Game/VFX/Explosion.Explosion")
    );

    // ✅ 컴포넌트로 생성
    UNiagaraComponent* NiagaraComp = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
        Owner->GetWorld(),
        System,
        FVector::ZeroVector
    );

    // ✅ 파라미터 오버라이드는 가능 (ViewModel 불필요)
    NiagaraComp->SetVariableFloat(TEXT("User.Speed"), 2.0f);
}
```

---

## 📊 에디터 성능 최적화 (Editor Performance Optimization)

### 1. 대규모 시스템 편집 최적화

```cpp
#if WITH_EDITOR
// ✅ 다수 변경 작업 시 일괄 처리
void OptimizedBulkChanges(FNiagaraSystemViewModel* SystemViewModel)
{
    // 1. 업데이트 일시 정지
    SystemViewModel->PauseAllSystemInstances();

    // 2. 변경 작업 일괄 처리
    for (int32 i = 0; i < 100; ++i)
    {
        // Emitter 추가, 모듈 수정 등...
    }

    // 3. 한 번에 갱신
    SystemViewModel->RefreshAll();

    // 4. 재개
    SystemViewModel->UnpauseAllSystemInstances();
}
#endif
```

### 2. Stack UI 가상화

```
Stack UI 최적화:
- 수천 개 항목이 있는 Stack UI는 느려질 수 있음
- Slate의 SListView 가상화 활용
- 보이는 항목만 위젯 생성
- 스크롤 시 재활용

성능:
- 가상화 전: 1000개 항목 = ~500ms 생성 시간
- 가상화 후: 1000개 항목 = ~50ms 생성 시간 (10배 빠름)
```

### 3. 컴파일 최적화

```
에디터 컴파일 최적화 옵션:
┌─────────────────────────────────────────────────────────────────────────┐
│  Edit → Project Settings → Niagara                                      │
│                                                                         │
│  [✓] Auto Compile                                                       │
│      // 자동 컴파일 (그래프 변경 시)                                     │
│                                                                         │
│  [✓] Auto Compile Delay (초)                                            │
│      // 컴파일 지연 시간 (0.5초 기본값)                                  │
│      // 연속 타이핑 시 불필요한 재컴파일 방지                            │
│                                                                         │
│  [✓] Cache Scripts for Cooking                                         │
│      // DDC에 스크립트 캐싱                                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 디버깅 및 트러블슈팅 (Debugging & Troubleshooting)

### 흔한 에러와 해결 방법

**1. "Stack UI not refreshing"**

```
원인: Delegate가 제대로 연결되지 않음

해결책:
1. ViewModel의 OnPropertyChanged Delegate 확인
2. Widget이 Delegate를 구독했는지 확인:
   SystemViewModel->OnEmitterHandleViewModelsChanged()
       .AddSP(this, &SMyWidget::OnRefresh);

3. Widget 소멸 시 Unsubscribe 확인:
   SystemViewModel->OnEmitterHandleViewModelsChanged()
       .RemoveAll(this);
```

**2. "UI와 데이터 불일치"**

```
원인: Model을 직접 수정하고 ViewModel을 거치지 않음

해결책:
❌ System->AddEmitterHandle(...);  // Model 직접 수정
✅ SystemViewModel->AddEmitter(...);  // ViewModel 사용

ViewModel이 자동으로:
- Model 업데이트
- Delegate 발생
- UI 갱신
```

**3. "Undo/Redo not working"**

```
원인: Transaction 없이 변경

해결책:
// ✅ FScopedTransaction 사용
FScopedTransaction Transaction(LOCTEXT("AddEmitter", "Add Emitter"));
System->Modify();  // Undo 스택 등록
SystemViewModel->AddEmitter(...);
```

**4. "Preview not updating"**

```
원인: PreviewComponent가 비활성화되었거나 컴파일 에러

해결책:
1. 컴파일 상태 확인:
   SystemViewModel->GetLatestCompileStatus()

2. PreviewComponent 활성화 확인:
   PreviewComponent->IsActive()

3. 강제 리셋:
   SystemViewModel->ResetSystem(ETimeResetMode::AllowResetTime, ...)
```

### 에디터 디버깅 도구

**1. Niagara Debugger**

```
콘솔 명령어:
fx.Niagara.Debug.Enabled 1  // 디버거 활성화

Window → Niagara Debugger
- 모든 활성 System/Emitter 표시
- 실시간 파티클 개수
- 성능 통계
- Attribute 값 확인
```

**2. Attribute Spreadsheet**

```
Niagara Editor → Tools → Attribute Spreadsheet

기능:
- 모든 파티클의 Attribute를 테이블로 표시
- 실시간 업데이트
- 필터링 및 정렬
- 특정 Attribute 추적
```

**3. GPU Profiler**

```
r.Niagara.GpuComputeDebug.Enabled 1

Window → GPU Visualizer
- Compute Shader 실행 시간
- 메모리 대역폭 사용량
- Dispatch 횟수
```

---

## 🎨 Slate UI 위젯 상세 (Slate UI Widgets in Detail)

### SNiagaraParameterPanel 위젯 구조

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/Widgets/SNiagaraParameterPanel.h`

**역할:** 파라미터를 카테고리별로 계층화하여 표시하고, Drag & Drop, 검색, 필터링을 지원하는 패널

#### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   SNiagaraParameterPanel                                │
│  (SCompoundWidget 상속 - 파라미터 패널)                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  Private:                                                               │
│    - bPendingRefresh : bool                                            │
│      // 다음 Tick에 새로고침 예약                                         │
│                                                                         │
│    - bPendingSelectionRestore : bool                                   │
│      // 선택 복원 대기                                                   │
│                                                                         │
│    - bParameterItemsPendingChange : bool                               │
│      // 파라미터 아이템 변경 대기                                         │
│                                                                         │
│    - ParametersWithSelectionPending : TArray<FName>                    │
│      // 선택 대기 중인 파라미터 이름                                      │
│                                                                         │
│    - ParametersWithRenamePending : TArray<FName>                       │
│      // 이름 변경 대기 중인 파라미터                                      │
│                                                                         │
│    - ParametersWithNamespaceModifierRenamePending :                    │
│        TSharedPtr<TArray<FNiagaraVariable>>                            │
│      // Namespace Modifier 변경 대기                                    │
│                                                                         │
│    - CategoryToButtonMap : TMap<FNiagaraParameterPanelCategory,        │
│                                  TSharedPtr<SButton>>                  │
│      // 카테고리별 버튼 매핑                                              │
│                                                                         │
│    - ToolkitCommands : TSharedPtr<FUICommandList>                      │
│      // 편집 명령 (Copy, Paste, Delete 등)                              │
│                                                                         │
│    - ParameterPanelViewModel :                                         │
│        TSharedPtr<INiagaraParameterPanelViewModel>                     │
│      // ViewModel 참조                                                  │
│                                                                         │
│    - ItemSelector : TSharedPtr<SNiagaraParameterPanelSelector>         │
│      // 아이템 선택 위젯 (계층 구조 렌더링)                               │
│                                                                         │
│    - ItemSelectorSearchBox : TSharedPtr<SSearchBox>                    │
│      // 검색 박스                                                       │
│                                                                         │
│  Public 핵심 메서드:                                                     │
│    + Construct(Args) : void                                            │
│      // 위젯 생성 및 초기화                                              │
│                                                                         │
│    + Refresh(bRefreshMenu, bRefreshExternalSelection) : void           │
│      // 패널 전체 새로고침                                               │
│                                                                         │
│    + Tick(AllottedGeometry, InCurrentTime, InDeltaTime) : void         │
│      // 대기 중인 작업 처리 (Rename, Selection 등)                       │
│                                                                         │
│    + OnGetCategoriesForItem(Item) : TArray<Category>                   │
│      // 아이템의 카테고리 목록 반환                                       │
│                                                                         │
│    + OnGenerateWidgetForCategory(Category) : TSharedRef<SWidget>       │
│      // 카테고리 헤더 위젯 생성                                          │
│                                                                         │
│    + OnGenerateWidgetForItem(Item) : TSharedRef<SWidget>               │
│      // 파라미터 아이템 위젯 생성                                         │
│                                                                         │
│    + OnParameterItemSelected(Item, SelectInfo) : void                  │
│      // 파라미터 선택 이벤트 처리                                         │
│                                                                         │
│    + OnParameterItemsDragged(Items) : FReply                           │
│      // Drag & Drop 시작 처리                                           │
│                                                                         │
│    + OnParameterItemActived(Item, ActivationType) : void               │
│      // 파라미터 활성화 (더블클릭 등)                                     │
│                                                                         │
│  Private 헬퍼:                                                           │
│    - OnGetMenuContent() : TSharedRef<SWidget>                          │
│      // "Add Parameter" 메뉴 생성                                       │
│                                                                         │
│    - RefreshExternalSelection() : void                                 │
│      // 외부 선택 변경 동기화                                            │
│                                                                         │
│    - RefreshMenu() : void                                              │
│      // 메뉴 새로고침                                                   │
│                                                                         │
│    - HandleDragDropOperation(DropOp) : FReply                          │
│      // Drag & Drop 완료 처리                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### Parameter Panel Item 위젯

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Private/Widgets/SNiagaraParameterPanelPaletteItem.h`

```
┌─────────────────────────────────────────────────────────────────────────┐
│              SNiagaraParameterPanelPaletteItem                          │
│  (개별 파라미터 표시 위젯)                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  구성 요소:                                                              │
│  ┌──────────────────────────────────────────────────────┐              │
│  │  [Icon] [ParameterName] [Type] [Actions]            │              │
│  │   ├─ GPU/CPU 호환성 아이콘                            │              │
│  │   ├─ Static Switch 아이콘                            │              │
│  │   ├─ 파라미터 이름 (편집 가능)                        │              │
│  │   ├─ 타입 표시 (Float, Vector, Texture 등)           │              │
│  │   └─ 액션 버튼 (Delete, Copy Reference 등)           │              │
│  └──────────────────────────────────────────────────────┘              │
│                                                                         │
│  기능:                                                                  │
│  - Inline Rename: F2 키 또는 느린 더블클릭                              │
│  - Drag & Drop: 다른 패널이나 그래프로 드래그                            │
│  - Context Menu: 우클릭 메뉴 (Rename, Delete, Copy Reference)          │
│  - Namespace Modifier: 접두사 변경 (User. → Emitter. 등)                │
│  - Type Conversion: 타입 변경 요청                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Parameter Category 시스템

**기본 카테고리:**

| 카테고리 | 설명 | 아이콘 |
|---------|------|--------|
| **User Parameters** | 사용자 정의 파라미터 | 👤 |
| **Engine Parameters** | 엔진 기본 파라미터 (Engine.Owner.Location 등) | ⚙️ |
| **Emitter Parameters** | 이미터 속성 (Emitter.Age, Emitter.SpawnRate) | 🔥 |
| **Particle Attributes** | 파티클 속성 (Particles.Position, Particles.Color) | ⚫ |
| **Data Interface** | 데이터 인터페이스 인스턴스 | 🔌 |
| **Static Switch** | 컴파일 타임 스위치 | 🔀 |
| **Advanced** | 고급 파라미터 (기본 숨김) | 🔧 |

**카테고리 필터링 규칙:**

```cpp
// NiagaraParameterPanelViewModel.cpp의 카테고리 분류 로직
bool ShouldShowInCategory(const FNiagaraVariable& Var, ECategory Category)
{
    // User Parameters: User.* namespace
    if (Category == ECategory::User)
        return Var.GetName().ToString().StartsWith(TEXT("User."));

    // Engine Parameters: Engine.*, System.* namespace
    if (Category == ECategory::Engine)
        return Var.GetName().ToString().StartsWith(TEXT("Engine.")) ||
               Var.GetName().ToString().StartsWith(TEXT("System."));

    // Emitter Parameters: Emitter.* namespace
    if (Category == ECategory::Emitter)
        return Var.GetName().ToString().StartsWith(TEXT("Emitter."));

    // Particle Attributes: Particles.* namespace
    if (Category == ECategory::Particle)
        return Var.GetName().ToString().StartsWith(TEXT("Particles."));

    // Data Interface: UNiagaraDataInterface 타입
    if (Category == ECategory::DataInterface)
        return Var.GetType().IsDataInterface();

    // Static Switch: UNiagaraStaticSwitchType
    if (Category == ECategory::StaticSwitch)
        return Var.GetType().IsStatic();

    return false;
}
```

### SNiagaraStackWidget 계층 렌더링

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditorWidgets/Private/SNiagaraStack.h`

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    SNiagaraStack (SCompoundWidget)                      │
│  (Stack UI - 모듈 계층 렌더링)                                           │
├─────────────────────────────────────────────────────────────────────────┤
│  구조:                                                                  │
│  ┌──────────────────────────────────────────────────────┐              │
│  │  [SScrollBox]                                        │              │
│  │    └─ [SVerticalBox] - 동적 생성                     │              │
│  │        ├─ UNiagaraStackEntry (Root)                 │              │
│  │        │   ├─ UNiagaraStackItemGroup (Spawn)        │              │
│  │        │   │   ├─ UNiagaraStackModuleItem (Module1) │              │
│  │        │   │   └─ UNiagaraStackModuleItem (Module2) │              │
│  │        │   ├─ UNiagaraStackItemGroup (Update)       │              │
│  │        │   └─ UNiagaraStackItemGroup (Render)       │              │
│  │        │                                             │              │
│  │        └─ 각 Entry별로 GenerateWidget() 호출         │              │
│  └──────────────────────────────────────────────────────┘              │
│                                                                         │
│  핵심 메서드:                                                            │
│    + Construct(Args, InStackViewModel) : void                          │
│    + Tick(AllottedGeometry, InCurrentTime, InDeltaTime) : void         │
│    + OnStructureChanged(ChangeFlags) : void                            │
│    + RefreshEntryWidgetMap() : void                                    │
│                                                                         │
│  Private:                                                               │
│    - StackViewModel : TSharedPtr<UNiagaraStackViewModel>               │
│    - EntryWidgetMap : TMap<UNiagaraStackEntry*, TSharedRef<SWidget>>   │
│    - EntryBoxLayout : TSharedPtr<SVerticalBox>                         │
│    - ScrollBox : TSharedPtr<SScrollBox>                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**렌더링 흐름:**

```
[1] UNiagaraStackViewModel::GetRootEntry()
     ↓
[2] UNiagaraStackEntry::RefreshChildren() (재귀 호출)
     ↓ 계층 구축
[3] UNiagaraStackEntry::GetChildren() 반환
     ↓
[4] SNiagaraStack::RefreshEntryWidgetMap()
     ├─ 각 Entry에 대해 GenerateStackEntryWidget() 호출
     │   ├─ Row Style에 따라 다른 위젯 생성
     │   │   ├─ GroupHeader → SExpandableArea
     │   │   ├─ ItemHeader → SNiagaraStackItemGroupHeader
     │   │   ├─ ItemContent → SNiagaraStackTableRow
     │   │   └─ Spacer → SSpacer
     │   └─ Indent 레벨 적용
     └─ EntryBoxLayout->AddSlot() 추가
     ↓
[5] 최종 UI 렌더링
```

---

## 📊 Parameter Panel ViewModel 상세 (Parameter Panel ViewModel in Detail)

### INiagaraParameterPanelViewModel 인터페이스 계층

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/ViewModels/NiagaraParameterPanelViewModel.h:79-141`

```
┌─────────────────────────────────────────────────────────────────────────┐
│           INiagaraImmutableParameterPanelViewModel                      │
│  (Read-only 파라미터 패널 인터페이스)                                     │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 파라미터 읽기 및 표시 전용 인터페이스                              │
│                                                                         │
│  Delegates:                                                             │
│    DECLARE_MULTICAST_DELEGATE(FOnRequestRefresh);                      │
│    FOnRequestRefresh OnRequestRefreshDelegate;                         │
│    // 패널 새로고침 요청                                                 │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE_OneParam(FOnRequestRefreshNextTick, bool);│
│    FOnRequestRefreshNextTick OnRequestRefreshNextTickDelegate;         │
│    // 다음 Tick에 새로고침 (bool: 카테고리 확장 여부)                     │
│                                                                         │
│  Pure Virtual Methods:                                                 │
│    + GetEditableGraphsConst() : const TArray<UNiagaraGraph*>&          │
│      // 편집 가능한 그래프 목록                                          │
│                                                                         │
│    + GetEditableScriptVariablesWithName(Name) :                        │
│        const TArray<UNiagaraScriptVariable*>&                          │
│      // 이름으로 Script Variable 검색                                   │
│                                                                         │
│    + GetGraphParameterReferencesForItem(Item) :                        │
│        const TArray<UNiagaraParameterPanelItemReference*>&             │
│      // 파라미터 아이템의 그래프 참조 목록                                │
│                                                                         │
│  Implemented Methods:                                                  │
│    + CopyParameterReference(ItemToCopy) : void                         │
│      // 파라미터 레퍼런스를 클립보드에 복사                               │
│                                                                         │
│    + CopyParameterMetaData(ItemToCopy) : void                          │
│      // 파라미터 메타데이터 복사                                         │
│                                                                         │
│    + RefreshFull(bDoCategoryExpansion) : void                          │
│      // 즉시 전체 새로고침                                               │
│                                                                         │
│    + RefreshFullNextTick(bDoCategoryExpansion) : void                  │
│      // 다음 Tick에 새로고침 예약                                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                                  ▲
                                  │ 상속
                                  │
┌─────────────────────────────────────────────────────────────────────────┐
│              INiagaraParameterPanelViewModel                            │
│  (편집 기능 추가 인터페이스)                                              │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 파라미터 추가/삭제/수정 기능 제공                                  │
│                                                                         │
│  핵심 Virtual Methods:                                                  │
│    + AddParameter(Variable, Category, bRequestRename, ...) : void      │
│      // 새 파라미터 추가                                                 │
│                                                                         │
│    + DeleteParameters(Items) : void                                    │
│      // 파라미터 삭제                                                   │
│                                                                         │
│    + RenameParameter(Item, NewName) : void                             │
│      // 파라미터 이름 변경                                               │
│                                                                         │
│    + GetViewedParameterItems() : TArray<FNiagaraParameterPanelItem>    │
│      // 현재 표시 중인 파라미터 목록                                      │
│                                                                         │
│    + GetParameterMenu(Category) : FMenuAndSearchBoxWidgets             │
│      // "Add Parameter" 메뉴 생성                                       │
│                                                                         │
│    + HandleDragDropOperation(DropOp) : FReply                          │
│      // Drag & Drop 처리                                                │
│                                                                         │
│    + SetParameterNamespace(Item, Namespace, TransferDefinition, ...)   │
│        : void                                                           │
│      // Namespace 변경 (User. → Emitter. 등)                            │
│                                                                         │
│    + SetParameterNamespaceModifier(Item, Modifier, RequestRename, ...)  │
│        : void                                                           │
│      // Namespace Modifier 변경 (User.MyParam → User.Module.MyParam)    │
│                                                                         │
│    + OnParameterItemActivated(Item) : void                             │
│      // 파라미터 활성화 (더블클릭 등)                                     │
│                                                                         │
│  Delegates:                                                             │
│    DECLARE_MULTICAST_DELEGATE_OneParam(                                │
│        FOnSelectParameterItemByName, FName);                           │
│    FOnSelectParameterItemByName SelectParameterItemByNameDelegate;     │
│    // 이름으로 파라미터 선택 요청                                         │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE_TwoParams(                               │
│        FOnNotifyParameterPendingRename, FName, FName);                 │
│    FOnNotifyParameterPendingRename NotifyParameterPendingRenameDelegate;│
│    // Rename 대기 알림                                                  │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE_OneParam(                                │
│        FOnNotifyParameterPendingNamespaceModifierRename,               │
│        TSharedPtr<TArray<FNiagaraVariable>>);                          │
│    FOnNotifyParameterPendingNamespaceModifierRename                    │
│        NotifyParameterPendingNamespaceModifierRenameDelegate;          │
│    // Namespace Modifier 변경 대기                                      │
│                                                                         │
│    DECLARE_MULTICAST_DELEGATE(FOnInvalidateCachedDependencies);        │
│    FOnInvalidateCachedDependencies OnInvalidateCachedDependenciesDelegate;│
│    // 캐시 무효화 알림                                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### FNiagaraSystemToolkitParameterPanelViewModel 구현

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/ViewModels/NiagaraParameterPanelViewModel.h:324-458`

```
┌─────────────────────────────────────────────────────────────────────────┐
│         FNiagaraSystemToolkitParameterPanelViewModel                    │
│  (시스템 에디터용 파라미터 패널 ViewModel)                                │
├─────────────────────────────────────────────────────────────────────────┤
│  Private 핵심 멤버:                                                      │
│    - SystemScriptGraph : TWeakObjectPtr<UNiagaraGraph>                 │
│      // System Script 그래프 참조                                        │
│                                                                         │
│    - SelectedVariable : FNiagaraVariable                               │
│      // 현재 선택된 변수                                                 │
│                                                                         │
│    - SystemViewModel : TSharedPtr<FNiagaraSystemViewModel>              │
│      // 시스템 ViewModel 참조                                           │
│                                                                         │
│    - SystemGraphSelectionViewModelWeak :                               │
│        TWeakPtr<FNiagaraSystemGraphSelectionViewModel>                 │
│      // 그래프 선택 ViewModel                                            │
│                                                                         │
│    - UIContext : FSystemToolkitUIContext (mutable)                     │
│      // UI 컨텍스트 (카테고리, 필터 등)                                  │
│                                                                         │
│    - GraphIdToOnGraphChangedHandleMap :                                │
│        TMap<uint32, FDelegateHandle>                                   │
│      // 그래프별 OnGraphChanged 핸들러 매핑                               │
│                                                                         │
│    - DefaultCategories : static TArray<FNiagaraParameterPanelCategory> │
│      // User, Engine 기본 카테고리                                       │
│                                                                         │
│    - UserCategories : static TArray<FNiagaraParameterPanelCategory>    │
│      // User 전용 카테고리                                               │
│                                                                         │
│    - DefaultAdvancedCategories :                                       │
│        static TArray<FNiagaraParameterPanelCategory>                   │
│      // Advanced 카테고리                                                │
│                                                                         │
│    - VariableObjectSelection : TSharedPtr<FNiagaraObjectSelection>     │
│      // Script Variable 선택 추적                                        │
│                                                                         │
│  Public 핵심 메서드:                                                     │
│    + GetEditableScriptVariablesWithName(VariableName) :                │
│        const TArray<UNiagaraScriptVariable*>&                          │
│      // System + Emitter Script Variables 모두 반환                     │
│                                                                         │
│    + GetGraphParameterReferencesForItem(Item) :                        │
│        const TArray<UNiagaraParameterPanelItemReference*>&             │
│      // 그래프 내 모든 참조 노드 반환                                     │
│                                                                         │
│    + GetAvailableParameterDefinitions(bSkipSubscribedDefs) :           │
│        const TArray<UNiagaraParameterDefinitions*>&                    │
│      // 사용 가능한 Parameter Definition 목록                            │
│                                                                         │
│    + AddParameter(NewVar, Category, bRequestRename, ...) : void        │
│      // 파라미터 추가 (System/Emitter 스크립트에 추가)                    │
│                                                                         │
│    + DeleteParameters(ItemsToDelete) : void                            │
│      // 파라미터 삭제 (참조 노드도 함께 제거)                             │
│                                                                         │
│    + RenameParameter(ItemToRename, NewName) : void                     │
│      // 파라미터 이름 변경 (그래프 내 모든 참조 업데이트)                  │
│                                                                         │
│    + OnParameterItemSelected(SelectedItems, SelectInfo) : void         │
│      // 선택 동기화 (Graph Selection ViewModel과 연동)                   │
│                                                                         │
│    + OnParameterItemsDragged(DraggedItems) : FReply                    │
│      // Drag 시작 (FNiagaraParameterGraphDragOperation 생성)             │
│                                                                         │
│    + SetActiveSection(NewSection) : void                               │
│      // 활성 섹션 변경 (Parameters, Attributes 등)                       │
│                                                                         │
│    + IsCategoryExpandedByDefault(Category) : bool                      │
│      // 카테고리 기본 확장 여부                                           │
│                                                                         │
│    + UsesCategoryFilteringForInitialExpansion() : bool                 │
│      // 초기 확장 시 필터링 사용 여부                                     │
│                                                                         │
│    + UpdateCategoryExpansionState(Category, bExpanded) : void          │
│      // 카테고리 확장 상태 저장 (EditorData에 persist)                   │
│                                                                         │
│  Private 핵심 헬퍼:                                                      │
│    - ShouldRouteThroughScratchParameterMap(Graph) : bool               │
│      // Scratch Pad 그래프 여부 확인                                     │
│                                                                         │
│    - GetEditableEmitterScriptGraphs() :                                │
│        TArray<TWeakObjectPtr<UNiagaraGraph>>                           │
│      // 편집 가능한 Emitter 그래프 목록                                  │
│                                                                         │
│    - OnGraphChanged(InAction) : void                                   │
│      // 그래프 변경 시 파라미터 목록 갱신                                 │
│                                                                         │
│    - ReconcileOnGraphChangedBindings() : void                          │
│      // 그래프 변경 핸들러 재동기화                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Parameter Namespace 시스템

**Namespace 규칙:**

| Namespace | 의미 | 예시 | 스코프 |
|-----------|------|------|--------|
| `User.` | 사용자 정의 파라미터 | `User.MyFloat` | System 전역 |
| `Engine.` | 엔진 제공 파라미터 | `Engine.Owner.Location` | 시스템별 |
| `System.` | 시스템 파라미터 | `System.Age` | 시스템 인스턴스 |
| `Emitter.` | 이미터 파라미터 | `Emitter.SpawnRate` | 이미터별 |
| `Particles.` | 파티클 속성 | `Particles.Position` | 파티클별 |
| `Transient.` | 일시적 파라미터 | `Transient.Temp` | 스크립트 로컬 |
| `Module.` | 모듈 로컬 | `Module.MyInput` | 모듈 내부 |

**Namespace Modifier:**

```cpp
// User.MyParam → User.Module.MyParam
SetParameterNamespaceModifier(Item, "Module", true);

// Emitter.Age → System.Age (Namespace 변경)
SetParameterNamespace(Item, FNiagaraEditorUtilities::GetSystemNamespaceMetaData());
```

---

## 🔧 컴파일 시스템 상세 (Compilation System in Detail)

### Async Compilation Pipeline

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Private/NiagaraCompiler.h:64-124`

```
┌─────────────────────────────────────────────────────────────────────────┐
│              FNiagaraShaderMapCompiler                                  │
│  (비동기 셰이더 컴파일 관리)                                              │
├─────────────────────────────────────────────────────────────────────────┤
│  Nested Struct:                                                         │
│  ┌──────────────────────────────────────────────────────┐              │
│  │  FActiveCompilation {                                │              │
│  │    - ShaderMapId : FNiagaraShaderMapId              │              │
│  │      // DDC 키 및 고유 식별자                         │              │
│  │                                                      │              │
│  │    - ShaderPlatform : EShaderPlatform               │              │
│  │      // PC, PS5, XSX 등                              │              │
│  │                                                      │              │
│  │    - ShaderMap : FNiagaraShaderMapRef               │              │
│  │      // 결과 ShaderMap                               │              │
│  │                                                      │              │
│  │    - ShaderCompileJobs :                            │              │
│  │        TArray<FShaderCommonCompileJobPtr>           │              │
│  │      // 병렬 컴파일 Job 목록                          │              │
│  │  }                                                   │              │
│  └──────────────────────────────────────────────────────┘              │
│                                                                         │
│  Public Methods:                                                        │
│    + CompileScript(CompileId, GroupName, Options, ...) : void          │
│      // 스크립트 컴파일 시작                                             │
│      ├─ ShaderMapId 생성                                                │
│      ├─ DDC 검색 (캐시 히트 시 즉시 반환)                                │
│      └─ Miss 시 비동기 컴파일 시작                                       │
│                                                                         │
│    + AddShaderPlatform(ShaderMapId, Platform) : void                   │
│      // 추가 플랫폼 컴파일 (Cross-compile)                               │
│                                                                         │
│    + ProcessCompileResults(bWait) : bool                               │
│      // 비동기 컴파일 결과 수집                                          │
│      ├─ Pending Jobs 확인                                               │
│      ├─ 완료된 Job → CompletedCompilations 이동                        │
│      └─ ShaderMap 빌드 및 DDC 저장                                      │
│                                                                         │
│    + GetShaderMap(bWaitForCompile) : bool                              │
│      // ShaderMap 가져오기 (대기 옵션)                                   │
│                                                                         │
│    + ReadScriptMetaData() : TSharedPtr<FNiagaraVMExecutableData>       │
│      // VM Executable Data 읽기 (CPU용)                                 │
│                                                                         │
│    + GetShaderParameters() :                                           │
│        TSharedPtr<FNiagaraShaderScriptParameters>                      │
│      // 셰이더 파라미터 가져오기                                         │
│                                                                         │
│  Private Members:                                                       │
│    - ShaderType : const FNiagaraShaderType*                            │
│      // NiagaraComputeShader 타입                                       │
│                                                                         │
│    - ShaderParameters :                                                │
│        TSharedPtr<FNiagaraShaderScriptParameters>                      │
│      // 셰이더 파라미터 (버퍼, 텍스처 등)                                │
│                                                                         │
│    - ActiveCompilations : TArray<FActiveCompilation>                   │
│      // 진행 중인 컴파일 목록                                            │
│                                                                         │
│    - CompletedCompilations : TArray<FCompletedCompilation>             │
│      // 완료된 컴파일 결과                                               │
│                                                                         │
│    - ScriptExeData : TSharedPtr<FNiagaraVMExecutableData>               │
│      // CPU 실행 데이터                                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 컴파일 흐름 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Niagara 컴파일 파이프라인                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [1] 사용자 액션 (에디터 변경, 명시적 Compile)                           │
│       │                                                                 │
│       ↓                                                                 │
│  [2] FNiagaraSystemViewModel::RequestCompile(bForce)                   │
│       ├─ InvalidateCachedData()                                        │
│       │   └─ 모든 Emitter의 Cached Data 무효화                          │
│       ├─ CompileSystem(bForce)                                         │
│       │   ├─ UNiagaraSystem::RequestCompile()                          │
│       │   │   ├─ FNiagaraCompileRequestDataBase 생성                   │
│       │   │   └─ FNiagaraCompilationQueue::AddCompilationRequest()     │
│       │   │                                                             │
│       │   └─ DDC Key 생성:                                              │
│       │       FNiagaraCompileHash::FromString(SourceGraph, Settings, ...)│
│       │                                                                 │
│       ↓                                                                 │
│  [3] FNiagaraCompilationQueue::ProcessQueue()                          │
│       ├─ GetAsyncBatch() → 병렬 처리 가능 그룹                          │
│       └─ For each Request:                                             │
│           ├─ [3-1] CPU 컴파일 (FHlslNiagaraCompiler)                   │
│           │   ├─ HLSL 생성 (FNiagaraHlslTranslator::Compile)           │
│           │   ├─ VM 바이트코드 변환                                     │
│           │   ├─ FNiagaraVMExecutableData 생성                         │
│           │   └─ DDC 저장 (NiagaraScript.cpp:992)                      │
│           │                                                             │
│           └─ [3-2] GPU 컴파일 (FNiagaraShaderMapCompiler)              │
│               ├─ FNiagaraShaderMapId 생성                              │
│               ├─ DDC 검색 (히트 시 [6]으로)                             │
│               ├─ FNiagaraShaderType::BeginCompileShader()              │
│               │   ├─ HLSL → FShaderCompilerInput 변환                  │
│               │   └─ GShaderCompilingManager::AddJobs()                │
│               │       ├─ [병렬] DXC.exe / FXC.exe 실행                 │
│               │       └─ [플랫폼] PS5/XSX 크로스 컴파일                 │
│               │                                                         │
│               └─ [비동기 대기]                                           │
│                                                                         │
│       ↓                                                                 │
│  [4] [별도 스레드] Shader Compilation Workers                           │
│       ├─ DXC: HLSL → DXIL (DirectX Intermediate Language)              │
│       ├─ FXC: HLSL → D3D Bytecode (Legacy)                             │
│       ├─ PS5: HLSL → PSSL                                              │
│       └─ XSX: HLSL → DXIL (Xbox Optimized)                             │
│           ↓                                                             │
│           [워커 완료 대기]                                               │
│                                                                         │
│       ↓                                                                 │
│  [5] FNiagaraShaderMapCompiler::ProcessCompileResults(bWait)           │
│       ├─ GShaderCompilingManager::GetAsyncResults() 호출               │
│       ├─ FShaderCompileJob 결과 수집                                   │
│       ├─ FNiagaraShaderMap 빌드                                        │
│       │   ├─ FNiagaraShader 인스턴스 생성                              │
│       │   └─ RHI Resource 바인딩 정보 저장                             │
│       └─ DDC에 ShaderMap 저장                                          │
│                                                                         │
│       ↓                                                                 │
│  [6] CompletedCompilations → FNiagaraSystemViewModel::OnSystemCompiled()│
│       ├─ PreviewComponent->SetAsset(System) 갱신                        │
│       ├─ MessageLog에 에러/경고 표시                                     │
│       ├─ SystemViewModel->OnSystemCompiled.Broadcast()                 │
│       │   └─ 모든 View에 컴파일 완료 알림                               │
│       │       ├─ Stack UI 갱신                                          │
│       │       ├─ Parameter Panel 갱신                                  │
│       │       ├─ Graph Editor 갱신                                     │
│       │       └─ Script Stats 업데이트                                 │
│       │                                                                 │
│       └─ [완료]                                                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### DDC (Derived Data Cache) 통합

**DDC 키 생성:**

```cpp
// NiagaraScript.cpp:992
FString FNiagaraScriptDerivedData::BuildNiagaraDDCKeyString(
    const FNiagaraVMExecutableDataId& CompileId)
{
    FString KeyString = FString::Printf(
        TEXT("%s_%s_%s"),
        NIAGARA_DERIVEDDATA_VER,         // 버전 문자열
        *CompileId.CompilerVersionID.ToString(),  // 컴파일러 버전
        *CompileId.ScriptUsageTypeID.ToString()   // Script Usage
    );

    // 추가 요소:
    // - BaseScriptID (부모 스크립트)
    // - BaseScriptCompileHash
    // - ReferencedCompileHashes (의존성)
    // - AdditionalDefines (매크로)
    // - AdditionalVariables (파라미터)

    return FDerivedDataCacheInterface::BuildCacheKey(
        TEXT("NiagaraScript"),
        KeyString,
        TEXT("")
    );
}
```

**컴파일 캐시 효율:**
- **Cache Hit**: DDC에서 즉시 로드 (~1ms)
- **Cache Miss**: 전체 컴파일 (~500ms ~ 5s)
- **Invalidation**: 그래프 변경 시 ChangeID 업데이트 → DDC 키 변경

---

## 🔗 관련 문서 (Related Documents)

- **[Overview.md](Overview.md)** - Niagara 시스템 전체 개요
- **[Core/NiagaraSystem.md](Core/NiagaraSystem.md)** - System 에셋
- **[Core/NiagaraEmitter.md](Core/NiagaraEmitter.md)** - Emitter 에셋
- **[Core/NiagaraScript.md](Core/NiagaraScript.md)** - Script 컴파일 시스템
- **[Compiler.md](Compiler.md)** - 컴파일러 시스템
- **[DDC_and_Cooking.md](DDC_and_Cooking.md)** - DDC 및 쿠킹 시스템

---

## 📚 참고 자료 (References)

### 소스 파일

- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Private/Toolkits/NiagaraSystemToolkit.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/ViewModels/NiagaraSystemViewModel.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/ViewModels/NiagaraEmitterViewModel.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Private/ViewModels/NiagaraScriptGraphViewModel.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/ViewModels/Stack/NiagaraStackEntry.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/ViewModels/NiagaraParameterPanelViewModel.h`

### 공식 문서

- [Niagara Editor Overview](https://docs.unrealengine.com/5.7/en-US/niagara-editor-overview/)
- [Niagara Visual Effects](https://docs.unrealengine.com/5.7/en-US/niagara-visual-effects/)

### 주요 API

```cpp
// FNiagaraSystemToolkit (메인 에디터)
void InitializeWithSystem(EToolkitMode::Type Mode, TSharedPtr<IToolkitHost> Host, UNiagaraSystem& System);
void InitializeWithEmitter(EToolkitMode::Type Mode, TSharedPtr<IToolkitHost> Host, UNiagaraEmitter& Emitter);
void CompileSystem(bool bFullRebuild);
TSharedPtr<FNiagaraSystemViewModel> GetSystemViewModel() const;

// FNiagaraSystemViewModel
void AddEmitter(UNiagaraEmitter& Emitter);
void DeleteEmitters(TSet<FGuid> EmitterHandleIds);
void DuplicateEmitters(TSet<FGuid> EmitterHandleIds);
void RequestCompile(bool bForce);
void ResetSystem(ETimeResetMode ResetMode, TOptional<FFrameTime> TimeRange);
TArray<TSharedRef<FNiagaraEmitterHandleViewModel>> GetEmitterHandles();

// FNiagaraEmitterViewModel
bool Initialize(const FVersionedNiagaraEmitter& InEmitter, TWeakPtr<FNiagaraEmitterInstance> InSimulation);
FVersionedNiagaraEmitter GetEmitter();
bool HasParentEmitter() const;
void RemoveParentEmitter();
TSharedRef<FNiagaraScriptViewModel> GetSharedScriptViewModel();
ENiagaraScriptCompileStatus GetLatestCompileStatus();

// FNiagaraScriptGraphViewModel
void SetScriptSource(UNiagaraScriptSource* InScriptSource);
UNiagaraGraph* GetGraph() const;
TSharedRef<FUICommandList> GetCommands();
TSharedRef<FNiagaraObjectSelection> GetNodeSelection();
void SetSelectedNodes(const TSet<UObject*>& InSelectedNodes);

// UNiagaraStackEntry
virtual FText GetDisplayName() const;
TArray<UNiagaraStackEntry*> GetChildren() const;
bool GetIsExpanded() const;
void SetIsExpanded(bool bExpanded);
TArray<FStackIssue> GetStackIssues() const;
EStackIssueSeverity GetHighestStackIssueSeverity() const;
```

---

> 🔄 **작성일**: 2025-11-10
> 📝 **문서 버전**: v1.0
> ✅ **소스 검증**: UE 5.7.0
> 📂 **Niagara 문서 완료**: 10/10 문서 작성 완료 (Overview, Core 7개, Compiler, Editor)
