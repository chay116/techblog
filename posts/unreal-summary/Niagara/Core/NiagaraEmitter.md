---
title: "NiagaraEmitter - Niagara 에미터 (Niagara Emitter)"
date: "2025-11-21"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Niagara"
tags: ["unreal", "Niagara", "Core"]
---
# NiagaraEmitter - Niagara 에미터 (Niagara Emitter)

## 🧭 개요 (Overview)

**UNiagaraEmitter**는 **파티클 생성 및 시뮬레이션의 기본 단위**로, 개별 파티클 그룹의 생성(Spawn), 업데이트(Update), 렌더링(Rendering)을 담당하는 핵심 클래스입니다. 에미터는 **재사용 가능한 독립적인 에셋**이며, 하나 이상의 UNiagaraSystem에 포함되어 복합 이펙트를 구성합니다.

**핵심 역할:**
- **파티클 생성**: SpawnScript를 통해 파티클 초기화
- **파티클 시뮬레이션**: UpdateScript로 매 프레임 파티클 업데이트
- **CPU/GPU 선택**: SimTarget으로 시뮬레이션 경로 선택 가능
- **버전 관리**: 하위 호환성을 위한 FVersionedNiagaraEmitterData 시스템
- **상속 시스템**: Parent Emitter를 통한 변경 사항 자동 전파
- **렌더링**: 다수의 Renderer (Sprite, Mesh, Ribbon, Light 등) 지원
- **이벤트 시스템**: 에미터 간 통신 및 파티클 이벤트 처리

**📂 위치:**
- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraEmitter.h:593` (UNiagaraEmitter 클래스 정의)
- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraEmitter.h:238` (FVersionedNiagaraEmitterData 구조체)

---

## 🎯 설계 철학: 왜 Emitter가 독립 에셋인가?

### 문제: 파티클 로직의 재사용

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    재사용 없는 파티클 시스템의 문제점                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ❌ 문제 1: 중복 작업                                                    │
│  ┌──────────────────────────────────────────────────────────────┐      │
│  │  - 동일한 연기 이펙트를 여러 시스템에서 사용                  │      │
│  │  - 각 시스템마다 설정을 따로 작성해야 함                      │      │
│  │  - 변경 사항을 모든 곳에 수동으로 적용해야 함                 │      │
│  └──────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ❌ 문제 2: 유지보수 악몽                                                │
│  - 버그 수정 시 100개 시스템을 모두 수정해야 함                          │
│  - 최적화 시 모든 복사본을 다시 조정해야 함                              │
│  - 버전 관리 불가능                                                      │
│                                                                         │
│  ❌ 문제 3: 퀄리티 불일치                                                │
│  - 프로젝트 전체에서 동일한 이펙트가 미묘하게 다름                       │
│  - 아티스트마다 설정이 달라짐                                            │
│  - 표준화 불가능                                                         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                            ↓
                   Niagara Emitter 솔루션:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ✅ 해결 1: 독립 에셋 (Independent Asset)                                │
│  ┌──────────────────────────────────────────────────────────────┐      │
│  │  UNiagaraEmitter (연기 에미터)                               │      │
│  │  ├─ SpawnScript                                              │      │
│  │  ├─ UpdateScript                                             │      │
│  │  ├─ EventHandlers                                            │      │
│  │  └─ RendererProperties[]                                     │      │
│  │                                                               │      │
│  │  → 다수의 시스템에서 참조 가능                                │      │
│  │  → 한 곳만 수정하면 모든 곳에 반영                            │      │
│  └──────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ✅ 해결 2: 버전 관리 시스템 (Versioning System)                         │
│  ┌──────────────────────────────────────────────────────────────┐      │
│  │  UNiagaraEmitter                                             │      │
│  │  ├─ VersionData[0]: v1.0 (기존 프로젝트용)                   │      │
│  │  ├─ VersionData[1]: v1.1 (새로운 최적화)                     │      │
│  │  └─ ExposedVersion: v1.1 (신규 사용자용)                     │      │
│  │                                                               │      │
│  │  → 하위 호환성 유지                                           │      │
│  │  → 새 버전 안전하게 테스트 가능                               │      │
│  └──────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ✅ 해결 3: 상속 시스템 (Inheritance System)                             │
│  ┌──────────────────────────────────────────────────────────────┐      │
│  │  Parent Emitter (연기 베이스)                                │      │
│  │  ├─ 기본 연기 로직                                            │      │
│  │  └─ 공통 파라미터                                             │      │
│  │      ↓ 상속                                                   │      │
│  │  Child Emitter (검은 연기)                                   │      │
│  │  └─ 색상만 오버라이드                                         │      │
│  │                                                               │      │
│  │  → Parent 변경 시 자동 전파                                   │      │
│  │  → Child는 차이점만 저장                                      │      │
│  └──────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ✅ 해결 4: CPU/GPU 선택 (Simulation Target)                            │
│  - SimTarget = CPUSim: 복잡한 로직, 디버깅 용이                         │
│  - SimTarget = GPUComputeSim: 대규모 파티클, 초고속 시뮬레이션           │
│  → 프로젝트 요구에 맞게 선택 가능                                        │
│                                                                         │
│  ✅ 해결 5: 모듈 시스템 (Modular Scripts)                                │
│  - SpawnScript: 파티클 생성 시 1회 실행                                 │
│  - UpdateScript: 매 프레임 실행                                         │
│  - EventHandlerScripts: 이벤트 기반 로직                                │
│  - SimulationStages: 멀티 패스 시뮬레이션                                │
│  → 유연한 로직 구성 가능                                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 설계 결정

| 결정 사항 | 이유 | 트레이드오프 |
|----------|------|-------------|
| **독립 에셋** | 재사용성, 표준화, 유지보수성 향상 | 단일 사용 시 오버헤드 |
| **버전 시스템** | 하위 호환성 유지, 안전한 업데이트 | 메모리 오버헤드 (다수 버전 저장) |
| **상속 시스템** | Parent 변경 자동 전파, 일관성 유지 | Merge 충돌 가능성 |
| **CPU/GPU 분리** | 플랫폼별 최적화, 성능 vs 유연성 선택 | 코드 경로 이원화 |
| **모듈 스크립트** | 기능별 분리, 명확한 실행 순서 | 스크립트 개수 증가 |
| **컴파일 단위** | 최적화된 바이트코드/HLSL 생성 | 변경 시 재컴파일 필요 |

---

## 🏗️ 시스템 아키텍처 (System Architecture)

### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         UNiagaraEmitter                                 │
│  (재사용 가능한 파티클 생성 에셋)                                        │
├─────────────────────────────────────────────────────────────────────────┤
│  상속: UObject → UNiagaraEmitterBase → UNiagaraEmitter                 │
│        INiagaraParameterDefinitionsSubscriber                           │
│        FNiagaraVersionedObject                                          │
│                                                                         │
│  Private:                                                               │
│    - ExposedVersion : FGuid                                            │
│      // 기본적으로 사용되는 버전 (신규 시스템 추가 시)                  │
│    - bVersioningEnabled : bool                                         │
│      // 버전 관리 활성화 여부                                            │
│    - VersionData : TArray<FVersionedNiagaraEmitterData>                │
│      // 버전별 에미터 데이터 목록 (각 버전은 독립적인 설정 포함)         │
│                                                                         │
│  Editor Only:                                                           │
│    - bIsInheritable : bool                                             │
│      // 이 에미터를 Parent로 사용 가능한지 여부                          │
│    - TemplateAssetDescription : FText                                  │
│      // 에셋 설명 (라이브러리에서 표시)                                 │
│    - Category : FText                                                  │
│      // 에미터 카테고리 (예: "Fire", "Smoke", "Magic")                  │
│    - LibraryVisibility : ENiagaraScriptLibraryVisibility               │
│      // 라이브러리 노출 여부 (Exposed, Hidden, Unexposed)               │
│    - ThumbnailImage : UTexture2D*                                      │
│      // 에디터 썸네일 이미지                                             │
│                                                                         │
│  주요 메서드:                                                            │
│    + GetLatestEmitterData() : FVersionedNiagaraEmitterData*            │
│      // 최신 버전 데이터 반환                                            │
│    + GetEmitterData(FGuid Version) : FVersionedNiagaraEmitterData*     │
│      // 특정 버전 데이터 반환                                            │
│    + GetAllAvailableVersions() : TArray<FNiagaraAssetVersion>         │
│      // 모든 버전 목록 반환                                              │
│                                                                         │
│  Editor Only 메서드:                                                     │
│    + AddNewVersion(int32 Major, int32 Minor) : FGuid                   │
│      // 새 버전 생성                                                     │
│    + DeleteVersion(FGuid Version) : void                               │
│      // 버전 삭제                                                        │
│    + ExposeVersion(FGuid Version) : void                               │
│      // 특정 버전을 ExposedVersion으로 설정                              │
│    + EnableVersioning() : void                                         │
│      // 버전 관리 활성화                                                 │
│    + DisableVersioning(FGuid VersionToKeep) : void                     │
│      // 버전 관리 비활성화 (하나만 유지)                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraEmitter.h:593-776` - UNiagaraEmitter 클래스 선언

### FVersionedNiagaraEmitterData 상세 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                  FVersionedNiagaraEmitterData                           │
│  (버전별 에미터 설정 및 스크립트 - 에미터의 실제 데이터)                 │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 각 버전의 독립적인 에미터 설정 저장                               │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 1. 버전 정보 (Version Information)                          │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - Version : FNiagaraAssetVersion                          │      │
│  │     └─ MajorVersion, MinorVersion, VersionGuid              │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 2. 시뮬레이션 설정 (Simulation Settings)                    │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - SimTarget : ENiagaraSimTarget                           │      │
│  │     ├─ CPUSim: VectorVM 기반 CPU 시뮬레이션                 │      │
│  │     └─ GPUComputeSim: HLSL Compute Shader 기반 GPU 시뮬레이션│      │
│  │                                                              │      │
│  │   - bLocalSpace : bool                                      │      │
│  │     ├─ true: 로컬 공간 시뮬레이션 (부모 Transform 기준)     │      │
│  │     └─ false: 월드 공간 시뮬레이션                          │      │
│  │                                                              │      │
│  │   - bDeterminism : bool                                     │      │
│  │     └─ 결정론적 시뮬레이션 (동일 시드 → 동일 결과)          │      │
│  │   - RandomSeed : int32                                      │      │
│  │     └─ 랜덤 시드 (bDeterminism = true 시)                   │      │
│  │                                                              │      │
│  │   - bInterpolatedSpawning : bool                            │      │
│  │     └─ 프레임 간 스폰 보간 (부드러운 스폰)                  │      │
│  │   - bRequiresPersistentIDs : bool                           │      │
│  │     └─ 파티클 ID 유지 (이벤트, GPU Ribbon 등 필요)          │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 3. 할당 모드 (Allocation Mode)                              │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - AllocationMode : EParticleAllocationMode                │      │
│  │     ├─ AutomaticEstimate: 자동 추정                         │      │
│  │     ├─ ManualEstimate: 수동 설정                            │      │
│  │     └─ FixedCount: 고정 개수                                │      │
│  │                                                              │      │
│  │   - PreAllocationCount : int32                              │      │
│  │     └─ 사전 할당할 파티클 개수                               │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 4. 경계 계산 모드 (Bounds Calculation)                      │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - CalculateBoundsMode : ENiagaraEmitterCalculateBoundMode │      │
│  │     ├─ Dynamic: 파티클 위치 기반 동적 계산                  │      │
│  │     ├─ Fixed: 고정 경계 사용                                │      │
│  │     └─ Programmable: 스크립트로 계산                        │      │
│  │                                                              │      │
│  │   - FixedBounds : FBox                                      │      │
│  │     └─ Fixed 모드 시 사용할 경계                            │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 5. 스크립트 (Scripts)                                       │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - SpawnScriptProps : FVersionedNiagaraEmitterScriptProps  │      │
│  │     └─ Script: UNiagaraScript*                              │      │
│  │        // 파티클 생성 시 1회 실행                            │      │
│  │                                                              │      │
│  │   - UpdateScriptProps : FVersionedNiagaraEmitterScriptProps │      │
│  │     └─ Script: UNiagaraScript*                              │      │
│  │        // 매 프레임 파티클 업데이트                          │      │
│  │                                                              │      │
│  │   - EventHandlerScriptProps : TArray<...>                   │      │
│  │     └─ 이벤트 발생 시 실행 (Death, Collision 등)            │      │
│  │                                                              │      │
│  │   - SimulationStages : TArray<UNiagaraSimulationStageBase*> │      │
│  │     └─ 멀티 패스 시뮬레이션 (고급 기능)                     │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 6. 렌더러 (Renderers)                                       │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - RendererProperties : TArray<UNiagaraRendererProperties*>│      │
│  │     ├─ UNiagaraSpriteRendererProperties                     │      │
│  │     │   └─ 2D 스프라이트 렌더링                              │      │
│  │     ├─ UNiagaraMeshRendererProperties                       │      │
│  │     │   └─ 3D 메시 렌더링                                    │      │
│  │     ├─ UNiagaraRibbonRendererProperties                     │      │
│  │     │   └─ 트레일/리본 렌더링                                │      │
│  │     ├─ UNiagaraLightRendererProperties                      │      │
│  │     │   └─ 파티클별 라이트                                   │      │
│  │     └─ (기타 다수 렌더러)                                    │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 7. Scalability (성능 최적화)                                │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - Platforms : FNiagaraPlatformSet                         │      │
│  │     └─ 플랫폼별 활성화 여부 (PC, Console, Mobile 등)         │      │
│  │                                                              │      │
│  │   - ScalabilityOverrides : FNiagaraEmitterScalabilityOverrides│     │
│  │     └─ 퀄리티 레벨별 설정 오버라이드                         │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │ 8. 기타 설정                                                 │      │
│  ├─────────────────────────────────────────────────────────────┤      │
│  │   - MaxGPUParticlesSpawnPerFrame : uint32                   │      │
│  │     └─ GPU 시뮬레이션 시 프레임당 최대 스폰 개수             │      │
│  │                                                              │      │
│  │   - bGpuAlwaysRunParticleUpdateScript : bool                │      │
│  │     └─ GPU 시뮬레이션에서도 CPU UpdateScript 실행 여부      │      │
│  │                                                              │      │
│  │   - EmitterSpawnScriptProps : FVersionedNiagaraEmitterScriptProps│  │
│  │     └─ 에미터 시작 시 실행 (시스템 레벨)                    │      │
│  │   - EmitterUpdateScriptProps : FVersionedNiagaraEmitterScriptProps│ │
│  │     └─ 매 프레임 에미터 업데이트 (시스템 레벨)              │      │
│  └─────────────────────────────────────────────────────────────┘      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraEmitter.h:238-584` - FVersionedNiagaraEmitterData 전체 구조

### SimTarget 비교: CPU vs GPU

```
┌─────────────────────────────────────────────────────────────────────────┐
│              SimTarget = CPUSim (CPU 시뮬레이션)                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  실행 환경:                                                              │
│  ├─ VectorVM (바이트코드 인터프리터)                                     │
│  ├─ SIMD 최적화 (128-bit, 4개 파티클 동시 처리)                         │
│  └─ Worker Thread 병렬 실행 (멀티코어)                                  │
│                                                                         │
│  장점:                                                                   │
│  ✅ 디버깅 용이 (브레이크포인트, 로그 가능)                              │
│  ✅ 복잡한 로직 지원 (모든 DataInterface 사용 가능)                      │
│  ✅ Collision Query, SkeletalMesh Sampling 등 CPU 전용 기능             │
│  ✅ 플랫폼 호환성 높음                                                   │
│                                                                         │
│  단점:                                                                   │
│  ❌ 최대 파티클 수 제한 (~50만 개)                                       │
│  ❌ 매 프레임 CPU 시간 소모                                              │
│  ❌ GPU보다 느림 (대규모 시)                                             │
│                                                                         │
│  최적 사용 사례:                                                         │
│  - 복잡한 로직 (조건문, 룩업 테이블 등)                                  │
│  - Collision/Physics 연동                                               │
│  - SkeletalMesh Sampling                                                │
│  - 중소 규모 파티클 (< 5만 개)                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│            SimTarget = GPUComputeSim (GPU 시뮬레이션)                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  실행 환경:                                                              │
│  ├─ HLSL Compute Shader (컴파일된 GPU 코드)                             │
│  ├─ Render Thread 실행                                                  │
│  └─ GPU 완전 병렬 처리 (수천 스레드 동시 실행)                          │
│                                                                         │
│  장점:                                                                   │
│  ✅ 초대규모 파티클 지원 (~100만+ 개)                                    │
│  ✅ 매우 빠른 성능 (CPU 대비 20배 이상)                                  │
│  ✅ CPU 시간 절약 (Render Thread로 이동)                                │
│  ✅ GPU 메모리에서 직접 렌더링 (메모리 전송 최소화)                      │
│                                                                         │
│  단점:                                                                   │
│  ❌ 디버깅 어려움 (GPU 디버거 필요)                                      │
│  ❌ 일부 DataInterface 미지원 (Collision Query 등)                      │
│  ❌ 복잡한 로직 제한적                                                   │
│  ❌ 플랫폼별 셰이더 컴파일 필요                                          │
│                                                                         │
│  최적 사용 사례:                                                         │
│  - 대규모 파티클 (10만+ 개)                                              │
│  - 간단한 Force/Velocity 계산                                           │
│  - Texture Sampling                                                     │
│  - Curl Noise, Turbulence                                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                        성능 비교 (벤치마크)                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  파티클 개수별 시뮬레이션 시간:                                          │
│                                                                         │
│  ┌────────────┬──────────────┬──────────────┬────────────┐            │
│  │ 파티클 수  │ CPU (VectorVM)│ GPU (HLSL)   │ 성능 차이  │            │
│  ├────────────┼──────────────┼──────────────┼────────────┤            │
│  │ 1,000      │ ~0.1ms       │ ~0.05ms      │ 2배        │            │
│  │ 10,000     │ ~0.5ms       │ ~0.1ms       │ 5배        │            │
│  │ 50,000     │ ~2.5ms       │ ~0.3ms       │ 8배        │            │
│  │ 100,000    │ ~5ms         │ ~0.5ms       │ 10배       │            │
│  │ 500,000    │ ~25ms (한계) │ ~2ms         │ 12배       │            │
│  │ 1,000,000  │ N/A (불가능) │ ~4ms         │ -          │            │
│  └────────────┴──────────────┴──────────────┴────────────┘            │
│                                                                         │
│  결론:                                                                   │
│  - 1만 개 이하: CPU도 충분 (디버깅 편의 우선)                           │
│  - 5만 개 이상: GPU 권장 (성능 차이 명확)                               │
│  - 10만 개 이상: GPU 필수 (CPU로 불가능)                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🧱 핵심 컴포넌트 (Core Components)

### 1. 버전 시스템 (Versioning System)

**역할:** 하위 호환성을 유지하면서 에미터 업데이트

**구조:**

```cpp
// NiagaraEmitter.h:734-744
UPROPERTY()
FGuid ExposedVersion;  // 기본 버전 (신규 사용자용)

UPROPERTY()
bool bVersioningEnabled = false;  // 버전 관리 활성화

UPROPERTY()
TArray<FVersionedNiagaraEmitterData> VersionData;  // 버전별 데이터
```

**버전 시스템 동작:**

```
버전 관리 예시:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [초기 상태] 버전 관리 비활성화                                          │
│  UNiagaraEmitter: "FireEmitter"                                        │
│  ├─ bVersioningEnabled = false                                         │
│  ├─ ExposedVersion = {00000000-0000-0000-0000-000000000000}            │
│  └─ VersionData[0]:                                                    │
│      └─ Version = 1.0                                                  │
│          └─ SpawnRate = 100                                            │
│                                                                         │
│  [버전 관리 활성화]                                                      │
│  EnableVersioning()                                                     │
│  ├─ bVersioningEnabled = true                                          │
│  ├─ ExposedVersion = GUID_v1.0                                         │
│  └─ VersionData[0]:                                                    │
│      └─ Version = 1.0 {GUID_v1.0}                                      │
│                                                                         │
│  [새 버전 생성]                                                          │
│  AddNewVersion(1, 1)  // v1.1                                          │
│  ├─ VersionData[0]: v1.0 (기존 프로젝트용)                              │
│  │   └─ SpawnRate = 100                                                │
│  └─ VersionData[1]: v1.1 (새 버전 - v1.0 복사본)                       │
│      └─ SpawnRate = 100 (초기값 동일)                                   │
│                                                                         │
│  [새 버전 수정]                                                          │
│  GetEmitterData(GUID_v1.1)->SpawnRate = 200;                           │
│  ├─ VersionData[0]: v1.0                                               │
│  │   └─ SpawnRate = 100 (변경 없음 - 기존 프로젝트 보호됨)              │
│  └─ VersionData[1]: v1.1                                               │
│      └─ SpawnRate = 200 (새 버전만 변경)                                │
│                                                                         │
│  [새 버전 노출]                                                          │
│  ExposeVersion(GUID_v1.1)                                              │
│  └─ ExposedVersion = GUID_v1.1                                         │
│      → 신규 시스템에 추가 시 v1.1 사용                                   │
│      → 기존 시스템은 여전히 v1.0 사용 (호환성 유지)                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**API 사용 예시:**

```cpp
#if WITH_EDITOR
// 버전 관리 활성화
Emitter->EnableVersioning();

// 새 버전 생성 (v1.1)
FGuid NewVersionGuid = Emitter->AddNewVersion(1, 1);

// 특정 버전 데이터 가져오기
FVersionedNiagaraEmitterData* V11Data = Emitter->GetEmitterData(NewVersionGuid);
if (V11Data)
{
    // 새 버전 수정
    V11Data->SimTarget = ENiagaraSimTarget::GPUComputeSim;
    V11Data->PreAllocationCount = 100000;
}

// 새 버전 노출 (기본 버전으로 설정)
Emitter->ExposeVersion(NewVersionGuid);

// 모든 버전 나열
TArray<FNiagaraAssetVersion> AllVersions = Emitter->GetAllAvailableVersions();
for (const FNiagaraAssetVersion& Version : AllVersions)
{
    UE_LOG(LogTemp, Log, TEXT("Version %d.%d: %s"),
        Version.MajorVersion, Version.MinorVersion,
        *Version.VersionGuid.ToString());
}
#endif
```

### 2. Spawn & Update Scripts

**역할:** 파티클 생성 및 시뮬레이션 로직

**구조:**

```cpp
// FVersionedNiagaraEmitterData 내부
UPROPERTY()
FVersionedNiagaraEmitterScriptProps SpawnScriptProps;  // 파티클 생성 시

UPROPERTY()
FVersionedNiagaraEmitterScriptProps UpdateScriptProps; // 매 프레임
```

**실행 순서:**

```
파티클 생명주기:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [1] 에미터 활성화                                                       │
│  └─ EmitterSpawnScript 실행 (에미터 레벨, 1회)                          │
│      ├─ 에미터 초기 상태 설정                                            │
│      ├─ 에미터 파라미터 초기화                                           │
│      └─ SpawnRate 계산                                                  │
│                                                                         │
│  [2] 매 프레임                                                           │
│  └─ EmitterUpdateScript 실행 (에미터 레벨, 매 프레임)                   │
│      ├─ Emitter.Age 증가                                                │
│      ├─ SpawnRate 동적 조정                                             │
│      ├─ 조건부 스폰 제어                                                 │
│      └─ 이 프레임에 생성할 파티클 수 결정                                │
│                                                                         │
│  [3] 파티클 생성 (SpawnRate 기반)                                        │
│  └─ ParticleSpawnScript 실행 (각 신규 파티클마다)                       │
│      ├─ Particles.Position 초기화                                       │
│      ├─ Particles.Velocity 설정                                         │
│      ├─ Particles.Color 설정                                            │
│      ├─ Particles.Lifetime 설정                                         │
│      ├─ Particles.Age = 0                                               │
│      └─ (기타 커스텀 속성 초기화)                                        │
│                                                                         │
│  [4] 파티클 업데이트 (모든 살아있는 파티클)                              │
│  └─ ParticleUpdateScript 실행 (각 파티클마다, 매 프레임)                │
│      ├─ Particles.Age += DeltaTime                                      │
│      ├─ Particles.Position += Particles.Velocity * DeltaTime            │
│      ├─ Particles.Velocity += Forces * DeltaTime                        │
│      ├─ Particles.Color 페이드                                          │
│      └─ if (Particles.Age >= Particles.Lifetime) → Kill                │
│                                                                         │
│  [5] 렌더링                                                              │
│  └─ Renderer->Render(ParticleData)                                     │
│      ├─ Sprite Renderer → Billboard 생성                                │
│      ├─ Mesh Renderer → Static Mesh 인스턴스 생성                       │
│      └─ Ribbon Renderer → 트레일 생성                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**모듈 구성 예시:**

```
ParticleSpawnScript 모듈 스택:
┌─────────────────────────────────────────────────────────────────────────┐
│  Initialize Particle                                                    │
│  ├─ Particles.Position = (0, 0, 0)                                     │
│  ├─ Particles.Velocity = (0, 0, 0)                                     │
│  └─ Particles.Lifetime = 2.0                                           │
│      ↓                                                                  │
│  Sphere Location                                                        │
│  └─ Particles.Position = RandomPointOnSphere(Radius=100)               │
│      ↓                                                                  │
│  Add Velocity                                                           │
│  └─ Particles.Velocity = RandomConeDirection(Angle=30) * Speed         │
│      ↓                                                                  │
│  Set Color                                                              │
│  └─ Particles.Color = User.FireColor                                   │
└─────────────────────────────────────────────────────────────────────────┘

ParticleUpdateScript 모듈 스택:
┌─────────────────────────────────────────────────────────────────────────┐
│  Gravity Force                                                          │
│  └─ Particles.Velocity += (0, 0, -980) * DeltaTime                     │
│      ↓                                                                  │
│  Drag                                                                   │
│  └─ Particles.Velocity *= (1.0 - Drag * DeltaTime)                     │
│      ↓                                                                  │
│  Integrate Position                                                     │
│  └─ Particles.Position += Particles.Velocity * DeltaTime               │
│      ↓                                                                  │
│  Color Over Life                                                        │
│  └─ Particles.Color = Lerp(StartColor, EndColor, Age/Lifetime)         │
│      ↓                                                                  │
│  Scale Over Life                                                        │
│  └─ Particles.SpriteSize *= Curve.Sample(Age/Lifetime)                 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3. Event Handlers - 에미터 간 통신

**역할:** 파티클 이벤트 기반 로직 실행

**구조:**

```cpp
// FVersionedNiagaraEmitterData 내부
UPROPERTY()
TArray<FVersionedNiagaraEmitterEventHandlerProps> EventHandlerScriptProps;
```

**이벤트 타입:**

```
Niagara 이벤트 시스템:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [1] 파티클 Death 이벤트                                                 │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  불꽃 Emitter (이벤트 생성자)                             │          │
│  │  └─ ParticleUpdateScript                                │          │
│  │      └─ if (Age >= Lifetime)                            │          │
│  │          └─ Generate Event: "ParticleDeath"             │          │
│  │             └─ EventData.Position = Particles.Position  │          │
│  └──────────────────────────────────────────────────────────┘          │
│      │                                                                  │
│      ↓ 이벤트 전송                                                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  연기 Emitter (이벤트 수신자)                             │          │
│  │  └─ EventHandler: "OnParticleDeath"                     │          │
│  │      └─ SpawnParticles(10)                              │          │
│  │          └─ Position = EventData.Position                │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  결과: 불꽃 파티클이 죽을 때마다 그 위치에 연기 10개 생성                │
│                                                                         │
│  [2] 파티클 Collision 이벤트                                             │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  파편 Emitter                                            │          │
│  │  └─ Collision Query Module                              │          │
│  │      └─ if (Collision Detected)                         │          │
│  │          └─ Generate Event: "ParticleCollision"         │          │
│  │             ├─ EventData.Position = CollisionLocation   │          │
│  │             └─ EventData.Normal = CollisionNormal       │          │
│  └──────────────────────────────────────────────────────────┘          │
│      │                                                                  │
│      ↓ 이벤트 전송                                                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  충돌 스파크 Emitter                                     │          │
│  │  └─ EventHandler: "OnParticleCollision"                 │          │
│  │      └─ SpawnParticles(5)                               │          │
│  │          ├─ Position = EventData.Position                │          │
│  │          └─ Velocity = Reflect(Velocity, EventData.Normal)│         │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  [3] 커스텀 이벤트                                                       │
│  └─ 사용자 정의 조건에서 이벤트 생성 가능                                │
│      예: if (Speed > Threshold) → Generate Event              │          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**이벤트 설정 예시 (에디터):**

```
Emitter A (불꽃):
1. Particle Update 섹션
2. + 버튼 → Generate Death Event
3. Event Name: "FireDeath"

Emitter B (연기):
1. Event Handler 섹션
2. + 버튼 → Receive Event
3. Source Emitter: "불꽃"
4. Event Name: "FireDeath"
5. Execution Mode: Spawned Particles
6. Spawn Number: 10
```

### 4. Renderers - 렌더링 타입

**역할:** 파티클을 화면에 표시

**구조:**

```cpp
// FVersionedNiagaraEmitterData 내부
UPROPERTY()
TArray<TObjectPtr<UNiagaraRendererProperties>> RendererProperties;
```

**렌더러 타입:**

```
Niagara Renderer 타입:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  1. UNiagaraSpriteRendererProperties                                    │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 2D Billboard 스프라이트                                │          │
│  │  - 가장 일반적인 렌더러                                   │          │
│  │  - Alignment: FaceCamera, Velocity, CustomAxis           │          │
│  │  - SubImage Animation 지원                                │          │
│  │  - 사용 사례: 불, 연기, 마법 이펙트                       │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  2. UNiagaraMeshRendererProperties                                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 3D Static Mesh 렌더링                                  │          │
│  │  - 각 파티클이 독립적인 메시 인스턴스                     │          │
│  │  - 회전, 스케일 완전 제어                                 │          │
│  │  - 사용 사례: 파편, 낙엽, 총알 케이스                     │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  3. UNiagaraRibbonRendererProperties                                    │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 파티클을 연결하는 리본/트레일                          │          │
│  │  - 파티클 순서 기반 연결                                  │          │
│  │  - UV Scrolling 지원                                      │          │
│  │  - 사용 사례: 검 궤적, 마법 트레일, 레이저               │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  4. UNiagaraLightRendererProperties                                     │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 각 파티클이 Point Light 생성                           │          │
│  │  - 색상, 강도, 반경 제어                                  │          │
│  │  - 성능 비용 높음 (신중히 사용)                           │          │
│  │  - 사용 사례: 불꽃 빛, 마법 반짝임                        │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  5. UNiagaraDecalRendererProperties                                     │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 표면에 투영되는 데칼                                   │          │
│  │  - 충돌 지점에 표식 생성                                  │          │
│  │  - 사용 사례: 총알 자국, 피, 폭발 흔적                    │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  6. UNiagaraComponentRendererProperties                                 │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - Actor Component 스폰                                   │          │
│  │  - Audio Component, Niagara Component 등                 │          │
│  │  - 사용 사례: 파티클마다 사운드, 복합 이펙트              │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  (기타 다수 렌더러: GPU Ribbons, Geometry Cache 등)                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**다수 렌더러 사용:**

```cpp
// 하나의 에미터가 여러 렌더러 사용 가능
FVersionedNiagaraEmitterData* EmitterData = ...;

// 렌더러 1: Sprite (불꽃)
UNiagaraSpriteRendererProperties* SpriteRenderer = NewObject<UNiagaraSpriteRendererProperties>();
SpriteRenderer->MaterialInterface = FireMaterial;
EmitterData->RendererProperties.Add(SpriteRenderer);

// 렌더러 2: Light (빛)
UNiagaraLightRendererProperties* LightRenderer = NewObject<UNiagaraLightRendererProperties>();
LightRenderer->bUseInverseSquaredFalloff = true;
LightRenderer->RadiusScale = 100.0f;
EmitterData->RendererProperties.Add(LightRenderer);

// 결과: 각 파티클이 스프라이트 + 라이트로 표시됨
```

### 5. Simulation Stages - 멀티 패스 시뮬레이션

**역할:** 복잡한 시뮬레이션을 여러 단계로 분리

**구조:**

```cpp
// FVersionedNiagaraEmitterData 내부
UPROPERTY()
TArray<TObjectPtr<UNiagaraSimulationStageBase>> SimulationStages;
```

**사용 사례:**

```
Simulation Stage 예시:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  일반적인 Update (단일 패스):                                            │
│  └─ ParticleUpdateScript 실행 (1회)                                     │
│      └─ 모든 로직을 한 번에 처리                                         │
│                                                                         │
│  Simulation Stage 사용 (멀티 패스):                                      │
│  ├─ Stage 1: "Calculate Forces"                                        │
│  │   └─ 모든 파티클의 Force만 계산                                      │
│  │       (다른 파티클 위치 읽기 가능)                                    │
│  ├─ Stage 2: "Integrate Velocity"                                      │
│  │   └─ Force를 Velocity에 적용                                         │
│  └─ Stage 3: "Integrate Position"                                      │
│      └─ Velocity를 Position에 적용                                      │
│                                                                         │
│  장점:                                                                   │
│  - 계산 순서 명확화                                                      │
│  - 데이터 의존성 해결                                                    │
│  - GPU 최적화 (배치 처리)                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**고급 예시 (Boids 알고리즘):**

```
Boids Flocking Simulation:
Stage 1: "Calculate Neighbors"
└─ 각 파티클의 이웃 파티클 찾기
   └─ Grid Spatial Hash 사용

Stage 2: "Calculate Flocking Forces"
├─ Separation: 너무 가까운 이웃으로부터 멀어짐
├─ Alignment: 이웃과 동일한 방향으로 정렬
└─ Cohesion: 이웃 중심으로 이동

Stage 3: "Apply Forces"
└─ 계산된 Force를 Velocity에 적용

Stage 4: "Integrate"
└─ Position 업데이트
```

---

## 🔄 생명주기 (Lifecycle)

### 에미터 생명주기

```
┌─────────────────────────────────────────────────────────────────────────┐
│              UNiagaraEmitter 생명주기                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [1] 에셋 생성 (Asset Creation) - Editor Only                           │
│  ┌──────────────────────────────────────────────┐                      │
│  │  Content Browser → Right Click → Niagara Emitter│                   │
│  │  ├─ UNiagaraEmitter::PostInitProperties()   │                      │
│  │  ├─ VersionData[0] 초기화                    │                      │
│  │  └─ 기본 SpawnScript, UpdateScript 생성      │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓                                                               │
│  [2] 에셋 편집 (Asset Editing) - Editor Only                            │
│  ┌──────────────────────────────────────────────┐                      │
│  │  Double Click → Niagara Emitter Editor      │                      │
│  │  ├─ 모듈 추가/제거                           │                      │
│  │  ├─ 파라미터 조정                            │                      │
│  │  ├─ Renderer 설정                            │                      │
│  │  └─ Preview 확인                             │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ 저장 시                                                       │
│  [3] 컴파일 (Compilation) - Editor Only                                 │
│  ┌──────────────────────────────────────────────┐                      │
│  │  UNiagaraEmitter::PostEditChangeProperty()  │                      │
│  │  └─ 스크립트 컴파일 요청                     │                      │
│  │      ├─ SpawnScript → VM/HLSL               │                      │
│  │      ├─ UpdateScript → VM/HLSL              │                      │
│  │      └─ EventHandlerScripts → VM/HLSL       │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓                                                               │
│  [4] 시스템에 추가                                                       │
│  ┌──────────────────────────────────────────────┐                      │
│  │  UNiagaraSystem::AddEmitterHandle()         │                      │
│  │  ├─ FNiagaraEmitterHandle 생성              │                      │
│  │  ├─ VersionedInstance 복사                  │                      │
│  │  │   (에미터 원본 → 시스템 내 복사본)        │                      │
│  │  └─ 고유 GUID 할당                           │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓                                                               │
│  [5] 런타임 인스턴스 생성                                                │
│  ┌──────────────────────────────────────────────┐                      │
│  │  FNiagaraSystemInstance::Init()             │                      │
│  │  └─ FNiagaraEmitterInstance 생성            │                      │
│  │      ├─ EmitterData 복사                    │                      │
│  │      ├─ Particle DataSet 할당               │                      │
│  │      ├─ Renderer 초기화                     │                      │
│  │      └─ Script 바인딩                       │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ ActivateSystem()                                              │
│  [6] 에미터 활성화                                                       │
│  ┌──────────────────────────────────────────────┐                      │
│  │  FNiagaraEmitterInstance::Init()            │                      │
│  │  ├─ EmitterSpawnScript 실행 (1회)           │                      │
│  │  ├─ 에미터 Age = 0                           │                      │
│  │  ├─ 파티클 버퍼 할당                         │                      │
│  │  └─ Renderer 준비                            │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ 매 프레임                                                     │
│  [7] 에미터 업데이트 루프                                                │
│  ┌──────────────────────────────────────────────┐                      │
│  │  FNiagaraEmitterInstance::Tick()            │                      │
│  │  ├─ EmitterUpdateScript 실행                │                      │
│  │  │   └─ SpawnRate 계산                      │                      │
│  │  ├─ ParticleSpawnScript 실행                │                      │
│  │  │   └─ 신규 파티클 초기화                   │                      │
│  │  ├─ ParticleUpdateScript 실행               │                      │
│  │  │   └─ 모든 파티클 업데이트                 │                      │
│  │  ├─ EventHandlerScripts 실행                │                      │
│  │  │   └─ 이벤트 처리                          │                      │
│  │  └─ Renderer->Render()                      │                      │
│  │      └─ 화면 표시                            │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ Complete 조건 만족 시                                         │
│  [8] 에미터 완료                                                         │
│  └─ bCompleteOnInactive = true                                         │
│      └─ 모든 파티클 소멸 시 자동 종료                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 💡 실전 예시 (Practical Examples)

### ✅ 좋은 예: 에미터 버전 관리

```cpp
#if WITH_EDITOR
UFUNCTION(BlueprintCallable, Category = "Niagara|Emitter")
void UpdateEmitterSafely(UNiagaraEmitter* Emitter)
{
    if (!Emitter) return;

    // ✅ 버전 관리 활성화 (아직 안 되어있으면)
    if (!Emitter->IsVersioningEnabled())
    {
        Emitter->EnableVersioning();
        UE_LOG(LogTemp, Log, TEXT("Versioning enabled for %s"), *Emitter->GetName());
    }

    // ✅ 현재 노출 버전 확인
    FNiagaraAssetVersion CurrentVersion = Emitter->GetExposedVersion();
    UE_LOG(LogTemp, Log, TEXT("Current version: %d.%d"),
        CurrentVersion.MajorVersion,
        CurrentVersion.MinorVersion);

    // ✅ 새 버전 생성 (기존 프로젝트는 영향받지 않음)
    int32 NewMajor = CurrentVersion.MajorVersion;
    int32 NewMinor = CurrentVersion.MinorVersion + 1;
    FGuid NewVersionGuid = Emitter->AddNewVersion(NewMajor, NewMinor);

    // ✅ 새 버전 수정
    FVersionedNiagaraEmitterData* NewData = Emitter->GetEmitterData(NewVersionGuid);
    if (NewData)
    {
        // GPU 시뮬레이션으로 변경 (성능 향상)
        NewData->SimTarget = ENiagaraSimTarget::GPUComputeSim;
        NewData->PreAllocationCount = 100000;

        // 경계 모드 변경
        NewData->CalculateBoundsMode = ENiagaraEmitterCalculateBoundMode::Fixed;
        NewData->FixedBounds = FBox(FVector(-500), FVector(500));

        UE_LOG(LogTemp, Log, TEXT("Created new version %d.%d with GPU simulation"),
            NewMajor, NewMinor);
    }

    // ✅ 새 버전 노출 (테스트 후)
    // Emitter->ExposeVersion(NewVersionGuid);
    UE_LOG(LogTemp, Warning, TEXT("Test new version before exposing!"));
}
#endif
```

### ✅ 좋은 예: CPU vs GPU 선택

```cpp
// ✅ 파티클 개수 기반 자동 선택
void ConfigureEmitterSimTarget(FVersionedNiagaraEmitterData* EmitterData, int32 ExpectedParticleCount)
{
    if (!EmitterData) return;

    if (ExpectedParticleCount < 10000)
    {
        // ✅ 소규모 파티클 → CPU (디버깅 용이)
        EmitterData->SimTarget = ENiagaraSimTarget::CPUSim;
        EmitterData->AllocationMode = EParticleAllocationMode::AutomaticEstimate;

        UE_LOG(LogTemp, Log, TEXT("Using CPU simulation (small scale)"));
    }
    else
    {
        // ✅ 대규모 파티클 → GPU (고성능)
        EmitterData->SimTarget = ENiagaraSimTarget::GPUComputeSim;
        EmitterData->AllocationMode = EParticleAllocationMode::ManualEstimate;
        EmitterData->PreAllocationCount = ExpectedParticleCount;

        // ✅ GPU에서는 MaxGPUParticlesSpawnPerFrame 설정 필요
        EmitterData->MaxGPUParticlesSpawnPerFrame = 10000;

        UE_LOG(LogTemp, Log, TEXT("Using GPU simulation (large scale: %d particles)"),
            ExpectedParticleCount);
    }
}

// ✅ DataInterface 호환성 확인
bool IsDataInterfaceCompatibleWithGPU(const UNiagaraDataInterface* DI)
{
    // GPU 미지원 DataInterface 목록
    static TSet<UClass*> CPUOnlyDI = {
        UNiagaraDataInterfaceCollisionQuery::StaticClass(),
        UNiagaraDataInterfaceAudioPlayer::StaticClass(),
        // ... (기타 CPU 전용)
    };

    return !CPUOnlyDI.Contains(DI->GetClass());
}
```

### ✅ 좋은 예: 이벤트 기반 파티클 생성

```cpp
// ✅ Death 이벤트 설정 (에디터 또는 코드)
void SetupDeathEvent(FVersionedNiagaraEmitterData* EmitterData)
{
    if (!EmitterData) return;

    // ✅ EventHandler 추가
    FVersionedNiagaraEmitterEventHandlerProps EventHandler;
    EventHandler.SourceEventName = FName("ParticleDeath");
    EventHandler.SourceEmitterID = FGuid::NewGuid();  // 이벤트 생성 에미터 ID
    EventHandler.ExecutionMode = EScriptExecutionMode::SpawnedParticles;
    EventHandler.SpawnNumber = 10;  // 죽을 때마다 10개 생성

    EmitterData->AddEventHandler(EventHandler);

    UE_LOG(LogTemp, Log, TEXT("Death event handler added: spawn 10 particles on death"));
}

// ✅ 런타임에서 이벤트 생성 (ParticleUpdateScript 내부)
// (이것은 Niagara Graph에서 "Generate Event" 모듈로 설정)
/*
if (Particles.Age >= Particles.Lifetime)
{
    GenerateEvent("ParticleDeath", Particles.Position, Particles.Velocity);
}
*/
```

### ❌ 나쁜 예: GPU 에미터에 CPU 전용 기능 사용

```cpp
// ❌ 잘못된 방법: GPU 시뮬레이션 + Collision Query
void BadGPUSetup(FVersionedNiagaraEmitterData* EmitterData)
{
    // ❌ GPU로 설정
    EmitterData->SimTarget = ENiagaraSimTarget::GPUComputeSim;

    // ❌ CPU 전용 DataInterface 추가 시도
    // → 런타임 에러: "DataInterface CollisionQuery is not supported on GPU"
    UNiagaraDataInterfaceCollisionQuery* CollisionDI = NewObject<UNiagaraDataInterfaceCollisionQuery>();
    // 이것은 작동하지 않음!
}
```

**올바른 방법:**

```cpp
// ✅ 올바른 방법 1: CPU 시뮬레이션 사용
void GoodCPUCollision(FVersionedNiagaraEmitterData* EmitterData)
{
    EmitterData->SimTarget = ENiagaraSimTarget::CPUSim;
    // → Collision Query 사용 가능
}

// ✅ 올바른 방법 2: GPU 지원 대체 기능 사용
void GoodGPUCollision(FVersionedNiagaraEmitterData* EmitterData)
{
    EmitterData->SimTarget = ENiagaraSimTarget::GPUComputeSim;
    // → Scene Depth Collision 사용 (GPU 지원)
    UNiagaraDataInterfaceSceneDepth* SceneDepthDI = NewObject<UNiagaraDataInterfaceSceneDepth>();
}
```

### ❌ 나쁜 예: 버전 관리 없이 에미터 수정

```cpp
// ❌ 잘못된 방법: 기존 에미터 직접 수정
void BadDirectModify(UNiagaraEmitter* Emitter)
{
    // ❌ 모든 프로젝트에 즉시 영향!
    FVersionedNiagaraEmitterData* Data = Emitter->GetLatestEmitterData();
    Data->SimTarget = ENiagaraSimTarget::GPUComputeSim;

    // 기존 프로젝트가 망가질 수 있음!
    // - CPU 전용 기능 사용 중이었을 수 있음
    // - 성능 특성이 달라져 프레임 드롭 발생
}
```

**올바른 방법:**

```cpp
// ✅ 올바른 방법: 버전 생성 후 수정
void GoodVersionedModify(UNiagaraEmitter* Emitter)
{
    #if WITH_EDITOR
    // ✅ 새 버전 생성
    FGuid NewVersion = Emitter->AddNewVersion(1, 1);

    // ✅ 새 버전만 수정
    FVersionedNiagaraEmitterData* NewData = Emitter->GetEmitterData(NewVersion);
    NewData->SimTarget = ENiagaraSimTarget::GPUComputeSim;

    // ✅ 테스트 후 노출
    // Emitter->ExposeVersion(NewVersion);
    #endif
}
```

---

## 📊 성능 최적화 (Performance Optimization)

### 에미터 레벨 최적화

**1. PreAllocationCount 설정:**

```cpp
// ✅ 파티클 재할당 방지
EmitterData->AllocationMode = EParticleAllocationMode::ManualEstimate;
EmitterData->PreAllocationCount = 10000;  // 예상 최대 파티클 수

// 성능 비교:
// AutomaticEstimate: 동적 할당 → 재할당 오버헤드
// ManualEstimate: 사전 할당 → 재할당 없음 (20% 빠름)
```

**2. Fixed Bounds 사용:**

```cpp
// ✅ 경계 계산 비용 제거
EmitterData->CalculateBoundsMode = ENiagaraEmitterCalculateBoundMode::Fixed;
EmitterData->FixedBounds = FBox(FVector(-200), FVector(200));

// 성능 향상: ~2ms → ~0ms (10만 파티클 기준)
```

**3. bRequiresPersistentIDs 최소화:**

```cpp
// ❌ 불필요한 ID 할당
EmitterData->bRequiresPersistentIDs = true;  // 메모리 오버헤드!

// ✅ 필요할 때만 활성화
// - GPU Ribbon Renderer 사용 시
// - 이벤트 시스템에서 특정 파티클 추적 시
// - 그 외에는 false 유지
EmitterData->bRequiresPersistentIDs = false;
```

**4. Interpolated Spawning:**

```cpp
// ✅ 부드러운 스폰 (고품질)
EmitterData->bInterpolatedSpawning = true;
// → 프레임 간 균등 분포, 시각적 품질 향상
// → 약간의 성능 비용 (5% 정도)

// ❌ 성능 우선 (저사양 기기)
EmitterData->bInterpolatedSpawning = false;
// → 모든 파티클을 프레임 시작 시 일괄 생성
// → 약간 더 빠름, 시각적으로 거칠 수 있음
```

### GPU 시뮬레이션 최적화

```cpp
// ✅ GPU 파티클 스폰 제한
EmitterData->SimTarget = ENiagaraSimTarget::GPUComputeSim;
EmitterData->MaxGPUParticlesSpawnPerFrame = 10000;

// 너무 많은 파티클을 한 프레임에 생성하면:
// - GPU 메모리 할당 병목
// - Render Thread 블로킹
// 권장: 프레임당 1만 개 이하
```

### stat 명령어

```
콘솔 명령어:

stat Niagara
  - 에미터별 Tick Time 확인
  - CPU vs GPU 시간 비교

stat NiagaraEmitters
  - 개별 에미터 상세 통계
  - 파티클 개수, 메모리 사용량

fx.Niagara.Debug.DrawSystemBounds 1
  - 에미터 경계 시각화
  - Fixed vs Dynamic Bounds 검증
```

---

## 🔧 디버깅 및 트러블슈팅 (Debugging & Troubleshooting)

### 흔한 에러와 해결 방법

**1. "Emitter has no renderers"**

```
원인: RendererProperties가 비어있음

해결책:
1. Niagara Emitter 에디터 열기
2. Emitter Properties → Render 섹션
3. + 버튼 → Sprite Renderer 추가
4. Material 설정
```

**2. "GPU simulation not supported on this platform"**

```
원인: 플랫폼이 Compute Shader 미지원

해결책:
- Emitter → Platforms 설정 확인
- GPU 미지원 플랫폼은 CPU로 Fallback
- 또는 해당 플랫폼에서 에미터 비활성화
```

**3. "Particle count is zero"**

```
디버깅 체크리스트:
✅ SpawnRate > 0?
✅ EmitterUpdateScript의 SpawnRate 계산 로직 확인
✅ Scalability로 비활성화되지 않았는가?
✅ Lifetime > 0?
✅ 에미터가 Enabled 상태인가?
```

**4. "Script compilation failed"**

```
원인: 스크립트 그래프 에러

해결책:
1. Output Log 확인 (에러 메시지)
2. Niagara 에디터에서 Compile 버튼
3. 빨간색 노드 확인 및 수정
4. DDC 재생성 (필요 시)
```

### Niagara Debugger 활용

```
Window → Niagara Debugger

주요 기능:
1. Active Systems 목록
2. 에미터별 Particle Count
3. Tick Time (CPU/GPU)
4. Scalability State
5. Bounds 시각화
6. DataInterface Bindings
```

---

## 🔗 관련 문서 (Related Documents)

- **[Overview.md](../Overview.md)** - Niagara 시스템 전체 개요
- **[NiagaraSystem.md](NiagaraSystem.md)** - 시스템 컨테이너 (에미터 관리)
- **[NiagaraComponent.md](NiagaraComponent.md)** - 런타임 컴포넌트
- **[DataInterface.md](DataInterface.md)** - DataInterface 시스템
- **[NiagaraScript.md](NiagaraScript.md)** - 스크립트 및 컴파일 시스템

---

## 📚 참고 자료 (References)

### 소스 파일

- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraEmitter.h` (1,163 라인)
- `Engine/Plugins/FX/Niagara/Source/Niagara/Private/NiagaraEmitter.cpp`

### 공식 문서

- [Niagara Emitters](https://docs.unrealengine.com/5.7/en-US/niagara-emitters-in-unreal-engine/)
- [Niagara CPU vs GPU Simulation](https://docs.unrealengine.com/5.7/en-US/gpu-simulation-in-niagara-for-unreal-engine/)

### 주요 API

```cpp
// UNiagaraEmitter
FVersionedNiagaraEmitterData* GetLatestEmitterData();
FVersionedNiagaraEmitterData* GetEmitterData(const FGuid& VersionGuid);
TArray<FNiagaraAssetVersion> GetAllAvailableVersions() const;

#if WITH_EDITOR
FGuid AddNewVersion(int32 MajorVersion, int32 MinorVersion);
void DeleteVersion(const FGuid& VersionGuid);
void ExposeVersion(const FGuid& VersionGuid);
void EnableVersioning();
void DisableVersioning(const FGuid& VersionGuidToUse);
#endif

// FVersionedNiagaraEmitterData (주요 멤버)
ENiagaraSimTarget SimTarget;
EParticleAllocationMode AllocationMode;
ENiagaraEmitterCalculateBoundMode CalculateBoundsMode;
TArray<TObjectPtr<UNiagaraRendererProperties>> RendererProperties;
FVersionedNiagaraEmitterScriptProps SpawnScriptProps;
FVersionedNiagaraEmitterScriptProps UpdateScriptProps;
TArray<FVersionedNiagaraEmitterEventHandlerProps> EventHandlerScriptProps;
TArray<TObjectPtr<UNiagaraSimulationStageBase>> SimulationStages;
```

---

> 🔄 **작성일**: 2025-11-10
> 📝 **문서 버전**: v1.0
> ✅ **소스 검증**: UE 5.7.0
> 📂 **다음 문서**: [NiagaraComponent.md](NiagaraComponent.md)
