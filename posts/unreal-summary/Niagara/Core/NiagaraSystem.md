---
title: "NiagaraSystem - Niagara 시스템 컨테이너 (Niagara System Container)"
date: "2025-11-21"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Niagara"
tags: ["unreal", "Niagara", "Core"]
---
# NiagaraSystem - Niagara 시스템 컨테이너 (Niagara System Container)

## 🧭 개요 (Overview)

**UNiagaraSystem**은 **Niagara 이펙트의 최상위 컨테이너**로, 하나 이상의 에미터(Emitter)를 포함하고 전체 이펙트의 실행, 시간, 파라미터 흐름을 관리하는 핵심 클래스입니다. 이것은 **실제로 월드에 배치되고 실행되는 VFX 에셋**이며, UNiagaraComponent를 통해 게임 월드에 인스턴스화됩니다.

**핵심 역할:**
- **에미터 컨테이너**: 다수의 UNiagaraEmitter를 포함하여 복합 이펙트 구성
- **파라미터 통합**: 시스템 레벨 파라미터를 모든 하위 에미터에 전파
- **생명주기 관리**: 시스템 단위로 Activate, Deactivate, Reset 제어
- **컴파일 단위**: 시스템 전체가 하나의 컴파일 단위로 최적화됨
- **Scalability 제어**: Effect Type과 연동하여 성능 예산 관리
- **Warmup 시스템**: 루프 이펙트의 초기 안정 상태 생성

**📂 위치:**
- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraSystem.h:233` (UNiagaraSystem 클래스 정의)
- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraEmitterHandle.h:26` (FNiagaraEmitterHandle 구조체)

---

## 🎯 설계 철학: 왜 System 중심인가?

### 문제: 개별 에미터 관리의 한계

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    개별 에미터만으로는 부족한 이유                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ❌ 문제 1: 복합 이펙트 구성 어려움                                      │
│  ┌──────────────────────────────────────────┐                          │
│  │  - 불꽃 + 연기 + 파편 = 3개의 개별 에셋   │                          │
│  │  - 각각을 따로 배치하고 동기화해야 함      │                          │
│  │  - 파라미터 전달 메커니즘 없음            │                          │
│  └──────────────────────────────────────────┘                          │
│                                                                         │
│  ❌ 문제 2: 최적화 한계                                                  │
│  - 개별 에미터는 독립적으로 컴파일됨                                     │
│  - Cross-emitter 최적화 불가능                                          │
│  - Scalability 제어 분산됨                                              │
│                                                                         │
│  ❌ 문제 3: 아티스트 워크플로우 복잡성                                   │
│  - 여러 에셋을 따로 관리해야 함                                          │
│  - 버전 관리 어려움                                                      │
│  - 이펙트 조합 재사용 불가능                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                            ↓
                   Niagara System 솔루션:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ✅ 해결 1: 통합 컨테이너 (Unified Container)                            │
│  ┌──────────────────────────────────────────┐                          │
│  │  UNiagaraSystem                          │                          │
│  │  ├─ 불꽃 Emitter                         │                          │
│  │  ├─ 연기 Emitter                         │                          │
│  │  └─ 파편 Emitter                         │                          │
│  │                                          │                          │
│  │  → 하나의 에셋으로 관리                   │                          │
│  │  → 동기화된 생명주기                      │                          │
│  └──────────────────────────────────────────┘                          │
│                                                                         │
│  ✅ 해결 2: 통합 파라미터 시스템 (Unified Parameters)                     │
│  ┌──────────────────────────────────────────┐                          │
│  │  ExposedParameters                       │                          │
│  │  ├─ User.ExplosionSize (Float)           │                          │
│  │  ├─ User.ImpactPoint (Vector)            │                          │
│  │  └─ User.FireColor (LinearColor)         │                          │
│  │                                          │                          │
│  │  → 모든 에미터가 자동 접근 가능           │                          │
│  │  → Blueprint/C++에서 한 곳에서 제어       │                          │
│  └──────────────────────────────────────────┘                          │
│                                                                         │
│  ✅ 해결 3: 시스템 레벨 스크립트 (System-Level Scripts)                   │
│  ┌──────────────────────────────────────────┐                          │
│  │  SystemSpawnScript:                      │                          │
│  │  - 전체 이펙트 초기화 로직                │                          │
│  │  - 공통 파라미터 계산                     │                          │
│  │                                          │                          │
│  │  SystemUpdateScript:                     │                          │
│  │  - 매 프레임 시스템 전체 업데이트          │                          │
│  │  - LOD 계산, Scalability 제어            │                          │
│  └──────────────────────────────────────────┘                          │
│                                                                         │
│  ✅ 해결 4: 통합 컴파일 및 최적화                                         │
│  - 시스템 전체를 하나의 단위로 컴파일                                     │
│  - Cross-emitter 데이터 흐름 최적화                                      │
│  - DDC 캐싱으로 재컴파일 최소화                                          │
│                                                                         │
│  ✅ 해결 5: Effect Type 연동                                             │
│  - 시스템 단위로 성능 예산 할당                                          │
│  - 중요도 기반 Culling                                                  │
│  - Scalability Override 적용                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 설계 결정

| 결정 사항 | 이유 | 트레이드오프 |
|----------|------|-------------|
| **시스템 = 에셋 단위** | 아티스트가 하나의 파일로 복합 이펙트 관리 | 단일 에미터 재사용 시 오버헤드 |
| **에미터 = 참조 (Handle)** | 동일 에미터를 다수 시스템에서 공유 가능 | 버전 관리 복잡도 증가 |
| **통합 컴파일** | Cross-emitter 최적화, 일관된 성능 | 부분 변경 시 전체 재컴파일 필요 |
| **Exposed Parameters** | 외부에서 동적 제어 가능 | 파라미터 네임스페이스 관리 필요 |
| **Warmup 시스템** | 루프 이펙트의 즉각적인 안정 상태 | 초기 로딩 시간 증가 |
| **Effect Type 통합** | 전역 성능 예산 제어 | 세밀한 개별 제어 어려움 |

---

## 🏗️ 시스템 아키텍처 (System Architecture)

### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         UNiagaraSystem                                  │
│  (Niagara 이펙트의 최상위 컨테이너 에셋)                                 │
├─────────────────────────────────────────────────────────────────────────┤
│  상속: UObject → UFXSystemAsset → UNiagaraSystem                        │
│        INiagaraParameterDefinitionsSubscriber                           │
│                                                                         │
│  Public:                                                                │
│    - EmitterHandles : TArray<FNiagaraEmitterHandle>                    │
│      // 시스템에 포함된 에미터 목록                                      │
│                                                                         │
│    - SystemSpawnScript : UNiagaraScript*                               │
│      // 시스템 시작 시 1회 실행되는 스크립트                             │
│    - SystemUpdateScript : UNiagaraScript*                              │
│      // 매 프레임 실행되는 시스템 레벨 로직                              │
│                                                                         │
│    - ExposedParameters : FNiagaraUserRedirectionParameterStore         │
│      // 외부(Blueprint/C++)에서 제어 가능한 파라미터                      │
│                                                                         │
│    - SystemCompiledData : FNiagaraSystemCompiledData                   │
│      // 컴파일된 시스템 데이터 (바이트코드, 바인딩 정보)                 │
│                                                                         │
│    - EffectType : UNiagaraEffectType*                                  │
│      // 성능 예산, Scalability, Significance 설정                       │
│    - SystemScalabilityOverrides : FNiagaraSystemScalabilityOverrides   │
│      // 시스템별 Scalability 오버라이드                                 │
│                                                                         │
│    - bFixedBounds : bool                                               │
│      // 고정 경계 사용 여부 (성능 최적화)                                │
│    - FixedBounds : FBox                                                │
│      // 고정 경계 크기                                                   │
│                                                                         │
│    - WarmupTime : float                                                │
│      // 워밍업 시간 (초 단위)                                            │
│    - WarmupTickCount : int32                                           │
│      // 워밍업 틱 횟수                                                   │
│    - WarmupTickDelta : float                                           │
│      // 워밍업 틱 간격 (초 단위)                                         │
│                                                                         │
│    - bFixedTickDelta : bool                                            │
│      // 고정 틱 델타 사용 여부                                           │
│    - FixedTickDeltaTime : float                                        │
│      // 고정 틱 델타 값                                                  │
│                                                                         │
│    - bDeterminism : bool                                               │
│      // 결정론적 시뮬레이션 활성화 여부                                  │
│    - RandomSeed : int32                                                │
│      // 랜덤 시드 (결정론적 시뮬레이션 시)                               │
│                                                                         │
│    - MaxDeltaTime : TOptional<float>                                   │
│      // 최대 델타 타임 제한                                              │
│                                                                         │
│  주요 메서드:                                                            │
│    + GetEmitterHandles() : TArray<FNiagaraEmitterHandle>&             │
│    + GetSystemSpawnScript() : UNiagaraScript*                          │
│    + GetSystemUpdateScript() : UNiagaraScript*                         │
│    + GetExposedParameters() : FNiagaraUserRedirectionParameterStore&  │
│    + IsReadyToRun() : bool                                             │
│    + NeedsWarmup() : bool                                              │
│    + GetWarmupTime() : float                                           │
│    + NeedsDeterminism() : bool                                         │
│    + GetRandomSeed() : int32                                           │
│                                                                         │
│  Editor Only:                                                           │
│    + AddEmitterHandle(UNiagaraEmitter&, FName, FGuid) : FNiagaraEmitterHandle │
│    + DuplicateEmitterHandle(const FNiagaraEmitterHandle&, FName) : FNiagaraEmitterHandle │
│    + RemoveEmitterHandle(const FNiagaraEmitterHandle&) : void          │
│    + RequestCompile(bool bForce) : bool                                │
│    + WaitForCompilationComplete(bool bGPU) : void                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraSystem.h:233-500` - UNiagaraSystem 클래스 선언

### FNiagaraEmitterHandle 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       FNiagaraEmitterHandle                             │
│  (시스템 내 에미터 참조 및 설정)                                         │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 시스템이 에미터를 포함하는 방식                                   │
│                                                                         │
│  Private:                                                               │
│    - Id : FGuid                                                        │
│      // 에미터 핸들의 고유 식별자                                        │
│    - IdName : FName                                                    │
│      // 에미터 이름 (DataSet 호환용)                                    │
│    - Name : FName                                                      │
│      // 시스템 내 표시 이름                                              │
│                                                                         │
│    - VersionedInstance : FVersionedNiagaraEmitter                      │
│      // 버전 관리되는 에미터 인스턴스 (복사본)                           │
│    - StatelessEmitter : UNiagaraStatelessEmitter*                      │
│      // Stateless 모드 에미터 (최적화된 경량 에미터)                     │
│                                                                         │
│    - bIsEnabled : bool                                                 │
│      // 에미터 활성화 여부 (비활성화 시 시뮬레이션 안 함)                │
│    - EmitterMode : ENiagaraEmitterMode                                 │
│      // Standard 또는 Stateless                                        │
│                                                                         │
│  Editor Only:                                                           │
│    - bIsolated : bool                                                  │
│      // 에디터에서 이 에미터만 독립적으로 프리뷰                          │
│    - bDebugShowBounds : bool                                           │
│      // 에미터 경계 디버그 표시                                          │
│                                                                         │
│  Public 메서드:                                                          │
│    + GetId() : FGuid                                                   │
│    + GetIdName() : FName                                               │
│    + GetName() : FName                                                 │
│    + SetName(FName, UNiagaraSystem&) : void                            │
│    + GetIsEnabled() : bool                                             │
│    + SetIsEnabled(bool, UNiagaraSystem&, bool) : bool                  │
│    + GetInstance() : FVersionedNiagaraEmitter                          │
│    + GetEmitterData() : FVersionedNiagaraEmitterData*                  │
│    + IsValid() : bool                                                  │
│    + IsAllowedByScalability() : bool                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraEmitterHandle.h:26-181` - FNiagaraEmitterHandle 구조체 전체

**핵심 설계:**
- 에미터는 **복사본(Instance)**으로 시스템에 포함됨
- 원본(Source) 에미터는 별도로 유지되어 병합(Merge) 가능
- 버전 시스템으로 하위 호환성 유지

### 컴파일 데이터 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FNiagaraSystemCompiledData                           │
│  (시스템 컴파일 결과 데이터)                                             │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 시스템 전체의 컴파일된 바이트코드 및 바인딩 정보 저장              │
│                                                                         │
│  구조:                                                                   │
│    - InstanceParamStore : FNiagaraParameterStore                       │
│      // 시스템 인스턴스 파라미터 저장소                                  │
│                                                                         │
│    - DataSetCompiledData : FNiagaraDataSetCompiledData                 │
│      // 시스템 DataSet 컴파일 정보                                       │
│                                                                         │
│    - SpawnInstanceParamsDataSetCompiledData : FNiagaraDataSetCompiledData │
│      // Spawn 시점 인스턴스 파라미터 DataSet                             │
│    - UpdateInstanceParamsDataSetCompiledData : FNiagaraDataSetCompiledData │
│      // Update 시점 인스턴스 파라미터 DataSet                            │
│                                                                         │
│    - SpawnInstanceGlobalBinding : FNiagaraParameterDataSetBindingCollection │
│      // Spawn 시 Global 파라미터 바인딩                                  │
│    - SpawnInstanceSystemBinding : FNiagaraParameterDataSetBindingCollection │
│      // Spawn 시 System 파라미터 바인딩                                  │
│    - SpawnInstanceOwnerBinding : FNiagaraParameterDataSetBindingCollection │
│      // Spawn 시 Owner 파라미터 바인딩                                   │
│    - SpawnInstanceEmitterBindings : TArray<FNiagaraParameterDataSetBindingCollection> │
│      // Spawn 시 각 Emitter 파라미터 바인딩                              │
│                                                                         │
│    - UpdateInstanceGlobalBinding : FNiagaraParameterDataSetBindingCollection │
│      // Update 시 Global 파라미터 바인딩                                 │
│    - UpdateInstanceSystemBinding : FNiagaraParameterDataSetBindingCollection │
│      // Update 시 System 파라미터 바인딩                                 │
│    - UpdateInstanceOwnerBinding : FNiagaraParameterDataSetBindingCollection │
│      // Update 시 Owner 파라미터 바인딩                                  │
│    - UpdateInstanceEmitterBindings : TArray<FNiagaraParameterDataSetBindingCollection> │
│      // Update 시 각 Emitter 파라미터 바인딩                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                  FNiagaraEmitterCompiledData                            │
│  (에미터별 컴파일 결과 데이터)                                           │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 개별 에미터의 컴파일된 데이터 및 변수 바인딩                       │
│                                                                         │
│  구조:                                                                   │
│    - SpawnAttributes : TArray<FName>                                   │
│      // 파티클 스폰을 구동하는 속성 이름 목록                            │
│                                                                         │
│    - EmitterSpawnIntervalVar : FNiagaraVariable                        │
│      // 에미터 스폰 간격 변수                                            │
│    - EmitterInterpSpawnStartDTVar : FNiagaraVariable                   │
│      // 보간 스폰 시작 DeltaTime 변수                                    │
│    - EmitterSpawnGroupVar : FNiagaraVariable                           │
│      // 에미터 스폰 그룹 변수                                            │
│    - EmitterAgeVar : FNiagaraVariable                                  │
│      // 에미터 Age 변수                                                  │
│    - EmitterRandomSeedVar : FNiagaraVariable                           │
│      // 에미터 랜덤 시드 변수                                            │
│    - EmitterInstanceSeedVar : FNiagaraVariable                         │
│      // 에미터 인스턴스 시드 변수                                        │
│    - EmitterTotalSpawnedParticlesVar : FNiagaraVariable                │
│      // 에미터 총 스폰된 파티클 수 변수                                  │
│                                                                         │
│    - DataSetCompiledData : FNiagaraDataSetCompiledData                 │
│      // 에미터별 DataSet 컴파일 정보                                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraSystem.h:141-174` - FNiagaraSystemCompiledData
- `NiagaraSystem.h:54-96` - FNiagaraEmitterCompiledData

---

## 🧱 핵심 컴포넌트 (Core Components)

### 1. EmitterHandles - 에미터 관리

**역할:** 시스템이 포함하는 모든 에미터를 관리

**구조:**

```cpp
// NiagaraSystem.h:916-918
UPROPERTY()
TArray<FNiagaraEmitterHandle> EmitterHandles;
```

**주요 기능:**

```
EmitterHandles 배열의 각 요소:
┌─────────────────────────────────────────────────────────────────────────┐
│  FNiagaraEmitterHandle                                                  │
│  ├─ Id (FGuid)                   // 고유 식별자                          │
│  ├─ Name (FName)                 // 표시 이름                            │
│  ├─ bIsEnabled (bool)            // 활성화 여부                          │
│  ├─ EmitterMode (ENiagaraEmitterMode)  // Standard/Stateless            │
│  └─ VersionedInstance (FVersionedNiagaraEmitter)  // 실제 에미터 인스턴스 │
└─────────────────────────────────────────────────────────────────────────┘
```

**사용 예시:**

```cpp
// 에미터 순회
UNiagaraSystem* System = ...;
for (const FNiagaraEmitterHandle& Handle : System->GetEmitterHandles())
{
    if (Handle.GetIsEnabled())
    {
        FVersionedNiagaraEmitterData* EmitterData = Handle.GetEmitterData();
        // 에미터 데이터 처리
    }
}

// 특정 에미터 찾기
for (const FNiagaraEmitterHandle& Handle : System->GetEmitterHandles())
{
    if (Handle.GetName() == FName("SmokeEmitter"))
    {
        // 찾음!
    }
}
```

**에디터 전용 기능:**

```cpp
#if WITH_EDITOR
// 에미터 추가
FNiagaraEmitterHandle NewHandle = System->AddEmitterHandle(
    *SourceEmitter,
    FName("MyEmitter"),
    EmitterVersion
);

// 에미터 복제
FNiagaraEmitterHandle DupHandle = System->DuplicateEmitterHandle(
    ExistingHandle,
    FName("MyEmitter_Copy")
);

// 에미터 제거
System->RemoveEmitterHandle(HandleToDelete);
#endif
```

### 2. SystemSpawnScript & SystemUpdateScript

**역할:** 시스템 레벨 로직 실행

**구조:**

```cpp
// NiagaraSystem.h:936-942
UPROPERTY()
TObjectPtr<UNiagaraScript> SystemSpawnScript;  // 시스템 시작 시 1회 실행

UPROPERTY()
TObjectPtr<UNiagaraScript> SystemUpdateScript; // 매 프레임 실행
```

**실행 시점:**

```
시스템 생명주기:
┌─────────────────────────────────────────────────────────────────────────┐
│  [생성 및 초기화]                                                        │
│  └─ UNiagaraComponent::ActivateSystem()                                │
│      └─ FNiagaraSystemInstance::Init()                                 │
│          ├─ SystemSpawnScript 실행 ◀── 1회만 실행                       │
│          │   ├─ 시스템 파라미터 초기화                                   │
│          │   ├─ 에미터 개수 설정                                         │
│          │   └─ 전역 변수 초기화                                         │
│          │                                                              │
│          └─ 각 EmitterSpawnScript 실행                                  │
│                                                                         │
│  [매 프레임 업데이트]                                                    │
│  └─ FNiagaraSystemInstance::Tick()                                     │
│      ├─ SystemUpdateScript 실행 ◀── 매 프레임                           │
│      │   ├─ System.Age 업데이트                                         │
│      │   ├─ DeltaTime 전파                                              │
│      │   ├─ LOD 계산                                                    │
│      │   └─ Scalability 제어                                            │
│      │                                                                  │
│      └─ 각 EmitterUpdateScript 실행                                     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**주요 사용 사례:**

```
SystemSpawnScript:
- 시스템 전체 초기화
- 에미터 활성화 조건 설정
- 공통 파라미터 계산 (예: 폭발 크기 기반 에미터 배치)

SystemUpdateScript:
- 시스템 Age 기반 동적 제어
- 환경 변화 감지 (예: 카메라 거리 기반 LOD)
- 에미터 간 조율 (예: 연기는 불꽃이 끝난 후 시작)
```

### 3. ExposedParameters - 외부 파라미터

**역할:** Blueprint/C++에서 제어 가능한 시스템 파라미터

**구조:**

```cpp
// NiagaraSystem.h:952-953
UPROPERTY()
FNiagaraUserRedirectionParameterStore ExposedParameters;
```

**파라미터 네임스페이스:**

```
Niagara 파라미터 계층:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  User.XXX                      ← ExposedParameters (외부에서 설정)       │
│  │                                                                      │
│  ├─ User.ExplosionSize          (Float)                                │
│  ├─ User.ImpactPoint            (Vector)                               │
│  ├─ User.FireColor              (LinearColor)                          │
│  └─ User.TargetActor            (Actor)                                │
│      ↓ 자동 전파                                                         │
│  ┌────────────────────────────────────────────────────────┐            │
│  │  System.XXX  (시스템 내부 변수)                         │            │
│  │  ├─ System.Age                                         │            │
│  │  ├─ System.ExecutionState                              │            │
│  │  └─ System.NumEmittersAlive                            │            │
│  │      ↓ 전파                                             │            │
│  │  ┌──────────────────────────────────────────────────┐  │            │
│  │  │  Emitter.XXX  (에미터 변수)                       │  │            │
│  │  │  ├─ Emitter.Age                                   │  │            │
│  │  │  ├─ Emitter.SpawnRate                             │  │            │
│  │  │  └─ Emitter.ExecutionState                        │  │            │
│  │  │      ↓ 전파                                        │  │            │
│  │  │  ┌────────────────────────────────────────────┐   │  │            │
│  │  │  │  Particles.XXX  (파티클 속성)               │   │  │            │
│  │  │  │  ├─ Particles.Position                      │   │  │            │
│  │  │  │  ├─ Particles.Velocity                      │   │  │            │
│  │  │  │  ├─ Particles.Color                         │   │  │            │
│  │  │  │  └─ Particles.Age                           │   │  │            │
│  │  │  └────────────────────────────────────────────┘   │  │            │
│  │  └──────────────────────────────────────────────────┘  │            │
│  └────────────────────────────────────────────────────────┘            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**사용 예시:**

```cpp
// C++에서 파라미터 설정
UNiagaraComponent* NiagaraComp = ...;
NiagaraComp->SetFloatParameter(FName("User.ExplosionSize"), 5.0f);
NiagaraComp->SetVectorParameter(FName("User.ImpactPoint"), ImpactLocation);
NiagaraComp->SetColorParameter(FName("User.FireColor"), FLinearColor::Red);

// 에디터에서:
// 1. Niagara System 에셋 열기
// 2. User Parameters 패널에서 "+" 버튼
// 3. 타입 선택 (Float, Vector, Color, etc.)
// 4. 이름 입력 (예: "ExplosionSize")
// 5. 에미터의 모듈에서 User.ExplosionSize 사용
```

### 4. EffectType - 성능 관리

**역할:** 시스템의 성능 예산 및 Scalability 제어

**구조:**

```cpp
UPROPERTY(EditAnywhere, Category="Effect")
TObjectPtr<UNiagaraEffectType> EffectType;
```

**UNiagaraEffectType 주요 기능:**

```
Effect Type 설정:
┌─────────────────────────────────────────────────────────────────────────┐
│  Performance Settings:                                                  │
│  ├─ NumInstancesToActivate           // 동시 활성 개수 제한             │
│  ├─ CullProxyMode                    // 인스턴싱 Culling 모드            │
│  ├─ SignificanceBudget               // 중요도 기반 예산                 │
│  └─ CullReaction                     // 예산 초과 시 반응               │
│                                                                         │
│  Scalability:                                                           │
│  ├─ QualityLevelMask                 // 퀄리티 레벨별 활성화 여부        │
│  ├─ DistanceScaling                  // 거리 기반 스케일링               │
│  └─ VisibilityCulling                // 가시성 기반 컬링                 │
│                                                                         │
│  Validation:                                                            │
│  └─ ValidationRules[]                // 에셋 검증 규칙                   │
└─────────────────────────────────────────────────────────────────────────┘
```

**성능 제어 흐름:**

```
Runtime 성능 제어:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [1] 시스템 Activate 시도                                                │
│  └─ FNiagaraWorldManager::ActivateSystem()                             │
│      ├─ Effect Type 확인                                                │
│      ├─ 현재 활성 개수 체크                                              │
│      │   └─ NumInstancesToActivate 초과?                                │
│      │       ├─ Yes → Culling 수행                                      │
│      │       │   ├─ Significance 계산 (카메라 거리, 중요도 등)           │
│      │       │   └─ 가장 낮은 Significance 시스템 비활성화               │
│      │       └─ No → Activate 허용                                      │
│      │                                                                  │
│  [2] 매 프레임 Scalability 평가                                          │
│  └─ FNiagaraSystemInstance::Tick()                                     │
│      ├─ 카메라 거리 계산                                                 │
│      ├─ 현재 퀄리티 레벨 확인                                            │
│      ├─ Effect Type의 Scalability Settings 적용                         │
│      │   ├─ 거리 임계값 초과 → Culling                                  │
│      │   ├─ 퀄리티 레벨 미달 → 비활성화                                 │
│      │   └─ 가시성 없음 → Culling                                       │
│      │                                                                  │
│      └─ Tick 허용 여부 결정                                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5. Warmup 시스템

**역할:** 루프 이펙트의 초기 안정 상태 생성

**구조:**

```cpp
UPROPERTY(EditAnywhere, Category = "Warmup")
float WarmupTime;              // 워밍업 시간 (초)

UPROPERTY(EditAnywhere, Category = "Warmup")
int32 WarmupTickCount;         // 워밍업 틱 횟수

UPROPERTY(EditAnywhere, Category = "Warmup")
float WarmupTickDelta;         // 워밍업 틱 간격 (초)
```

**Warmup 실행 흐름:**

```
Warmup 프로세스:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [Activate 시]                                                          │
│  UNiagaraComponent::ActivateSystem(bReset = true)                      │
│  └─ FNiagaraSystemInstance::Init()                                     │
│      │                                                                  │
│      ├─ NeedsWarmup() 체크                                              │
│      │   └─ (WarmupTickCount > 0 && WarmupTickDelta > 0)               │
│      │                                                                  │
│      ├─ Yes → Warmup 실행                                               │
│      │   ┌───────────────────────────────────────────────────────────┐ │
│      │   │  for (int32 i = 0; i < WarmupTickCount; ++i)             │ │
│      │   │  {                                                        │ │
│      │   │      // 시간 진행                                         │ │
│      │   │      Age += WarmupTickDelta;                             │ │
│      │   │                                                           │ │
│      │   │      // SystemUpdateScript 실행                          │ │
│      │   │      ExecuteSystemUpdateScript(WarmupTickDelta);         │ │
│      │   │                                                           │ │
│      │   │      // 각 에미터 Tick                                    │ │
│      │   │      for (Emitter in Emitters)                           │ │
│      │   │      {                                                    │ │
│      │   │          Emitter->Tick(WarmupTickDelta);                 │ │
│      │   │          // 파티클 스폰, 업데이트                         │ │
│      │   │      }                                                    │ │
│      │   │  }                                                        │ │
│      │   └───────────────────────────────────────────────────────────┘ │
│      │   결과: Age = WarmupTickCount * WarmupTickDelta               │
│      │                                                                  │
│      └─ 정상 Tick 시작                                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**Warmup 설정 예시:**

```
루프 연기 이펙트:
- Lifetime: 5초
- Spawn Rate: 100 particles/sec
- 목표: 즉시 안정된 상태로 보이기

Warmup 설정:
- WarmupTime = 5.0f         // 5초 워밍업
- WarmupTickCount = 150      // 150틱
- WarmupTickDelta = 1/30     // 30 FPS (0.0333초)

계산:
- 150틱 × 0.0333초 = 5초
- 5초 × 100 particles/sec = 500개 파티클 생성
- 결과: Activate 즉시 500개 파티클이 5초 동안의 상태로 보임
```

**주의사항:**

```cpp
// ❌ 너무 긴 Warmup → 초기 로딩 시간 증가
WarmupTime = 30.0f;  // 30초는 너무 김!

// ✅ 적절한 Warmup (Lifetime * 1~2배)
WarmupTime = Lifetime * 1.5f;

// ✅ Warmup 필요 여부 확인
inline bool NeedsWarmup() const
{
    return WarmupTickCount > 0 && WarmupTickDelta > SMALL_NUMBER;
}
```

### 6. Fixed Bounds - 경계 최적화

**역할:** 동적 경계 계산 비용 제거

**구조:**

```cpp
UPROPERTY(EditAnywhere, Category = "Performance")
bool bFixedBounds;             // 고정 경계 사용 여부

UPROPERTY(EditAnywhere, Category = "Performance", meta=(EditCondition="bFixedBounds"))
FBox FixedBounds;              // 고정 경계 크기
```

**Fixed vs Dynamic Bounds:**

```
Dynamic Bounds (bFixedBounds = false):
┌─────────────────────────────────────────────────────────────────────────┐
│  매 프레임:                                                              │
│  └─ FNiagaraSystemInstance::Tick()                                     │
│      └─ CalculateBounds()                                              │
│          ├─ 모든 파티클 위치 순회                                        │
│          ├─ Min/Max 계산                                                │
│          └─ FBox 생성                                                   │
│                                                                         │
│  비용: O(N) 파티클 개수에 비례                                           │
│  예: 10만 파티클 → ~2ms                                                 │
└─────────────────────────────────────────────────────────────────────────┘

Fixed Bounds (bFixedBounds = true):
┌─────────────────────────────────────────────────────────────────────────┐
│  매 프레임:                                                              │
│  └─ GetBounds() → return FixedBounds;  // 즉시 반환                     │
│                                                                         │
│  비용: O(1) 상수 시간                                                    │
│  예: ~0ms                                                               │
└─────────────────────────────────────────────────────────────────────────┘
```

**사용 가이드:**

```
Fixed Bounds 적합한 경우:
✅ 파티클이 일정 영역 내에서만 움직임 (예: 바닥 불)
✅ 정적 배치된 이펙트 (예: 횃불)
✅ Culling Distance가 명확한 경우
✅ 대규모 파티클 (10만+)

Dynamic Bounds 필요한 경우:
❌ 파티클이 넓게 퍼짐 (예: 폭발 파편)
❌ 이동하는 캐릭터에 부착 (예: 버프 오라)
❌ 파티클 이동 경로 예측 불가
```

### 7. Determinism - 결정론적 시뮬레이션

**역할:** 재현 가능한 시뮬레이션 (Replay, 네트워크 동기화)

**구조:**

```cpp
UPROPERTY(EditAnywhere, Category = "Random")
bool bDeterminism;             // 결정론적 시뮬레이션 활성화

UPROPERTY(EditAnywhere, Category = "Random", meta=(EditCondition="bDeterminism"))
int32 RandomSeed;              // 랜덤 시드
```

**Determinism 동작:**

```
Non-Deterministic (bDeterminism = false):
┌─────────────────────────────────────────────────────────────────────────┐
│  랜덤 시드 생성:                                                         │
│  Seed = FPlatformTime::Cycles() + InstanceID + EmitterIndex             │
│                                                                         │
│  결과:                                                                   │
│  - 매 실행마다 다른 결과                                                 │
│  - Replay 불가능                                                        │
│  - 네트워크 동기화 불가능                                                │
└─────────────────────────────────────────────────────────────────────────┘

Deterministic (bDeterminism = true):
┌─────────────────────────────────────────────────────────────────────────┐
│  랜덤 시드 생성:                                                         │
│  Seed = RandomSeed (사용자 지정 고정값)                                  │
│                                                                         │
│  결과:                                                                   │
│  - 동일 입력 → 동일 결과                                                │
│  - Replay 가능                                                          │
│  - 네트워크 동기화 가능 (동일 시드 사용 시)                              │
│                                                                         │
│  주의: 모든 입력이 동일해야 함!                                          │
│  - DeltaTime                                                            │
│  - 파라미터 값                                                           │
│  - DataInterface 데이터 (Texture, Mesh 등)                              │
└─────────────────────────────────────────────────────────────────────────┘
```

**사용 사례:**

```cpp
// ✅ Replay 시스템
UNiagaraSystem* System = ...;
System->bDeterminism = true;
System->RandomSeed = 12345;
// → 리플레이 재생 시 동일한 이펙트

// ✅ 네트워크 동기화
// 서버와 클라이언트에 동일한 시드 전달
UNiagaraComponent* Comp = ...;
Comp->SetIntParameter(FName("User.NetworkSeed"), ServerProvidedSeed);
```

---

## 🔄 생명주기 (Lifecycle)

### 시스템 생명주기 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│              UNiagaraSystem 생명주기                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [1] 에셋 로딩 (Asset Loading)                                          │
│  ┌──────────────────────────────────────────────┐                      │
│  │  UNiagaraSystem::PostLoad()                  │                      │
│  │  ├─ EmitterHandles[] 역직렬화                │                      │
│  │  ├─ Scripts 로드                             │                      │
│  │  ├─ ExposedParameters 로드                   │                      │
│  │  └─ EffectType 로드                          │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ 에디터에서만                                                   │
│  ┌──────────────────────────────────────────────┐                      │
│  │  [2] 컴파일 (Compilation) - Editor Only      │                      │
│  │  ├─ RequestCompile()                         │                      │
│  │  │   ├─ 그래프 → HLSL/VM 바이트코드 변환      │                      │
│  │  │   ├─ SystemCompiledData 생성              │                      │
│  │  │   ├─ EmitterCompiledData[] 생성           │                      │
│  │  │   └─ DDC 캐싱                             │                      │
│  │  └─ WaitForCompilationComplete()             │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓                                                               │
│  [3] 런타임 인스턴스 생성 (Runtime Instantiation)                        │
│  ┌──────────────────────────────────────────────┐                      │
│  │  UNiagaraComponent 생성                      │                      │
│  │  ├─ SetAsset(UNiagaraSystem*)               │                      │
│  │  ├─ OverrideParameters 설정                  │                      │
│  │  └─ RegisterComponent()                      │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ ActivateSystem() 호출                                         │
│  ┌──────────────────────────────────────────────┐                      │
│  │  [4] 활성화 (Activation)                     │                      │
│  │  └─ FNiagaraSystemInstance::Init()           │                      │
│  │      ├─ SystemSpawnScript 실행 (1회)         │                      │
│  │      ├─ EmitterInstances[] 생성              │                      │
│  │      ├─ DataInterfaceBindings 설정           │                      │
│  │      └─ Warmup 실행 (설정 시)                │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ 매 프레임                                                     │
│  ┌──────────────────────────────────────────────┐                      │
│  │  [5] 업데이트 루프 (Update Loop)             │                      │
│  │  └─ FNiagaraSystemInstance::Tick()           │                      │
│  │      ├─ Scalability 평가                     │                      │
│  │      ├─ SystemUpdateScript 실행              │                      │
│  │      ├─ 각 EmitterInstance Tick              │                      │
│  │      │   ├─ EmitterUpdateScript              │                      │
│  │      │   ├─ ParticleSpawnScript              │                      │
│  │      │   ├─ ParticleUpdateScript             │                      │
│  │      │   └─ EventHandlerScripts              │                      │
│  │      └─ Rendering Commands 생성              │                      │
│  └──────────────────────────────────────────────┘                      │
│         │                                                               │
│         ↓ Deactivate() 호출                                             │
│  ┌──────────────────────────────────────────────┐                      │
│  │  [6] 비활성화 (Deactivation)                 │                      │
│  │  ├─ DeactivateImmediate()                    │                      │
│  │  │   └─ 모든 파티클 즉시 제거                 │                      │
│  │  └─ Deactivate()                             │                      │
│  │      ├─ 신규 파티클 생성 중단                 │                      │
│  │      └─ 기존 파티클 Lifetime까지 유지         │                      │
│  └──────────────────────────────────────────────┘                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 컴파일 프로세스 (Compilation Process)

```
에디터 전용 컴파일 흐름:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [그래프 변경 감지]                                                      │
│  └─ NiagaraScript->MarkDirty()                                          │
│      └─ UNiagaraSystem::RequestCompile(bForce = false)                 │
│                                                                         │
│  [컴파일 요청]                                                           │
│  └─ FNiagaraActiveCompilation 생성                                      │
│      ├─ SystemSpawnScript → 컴파일 대기열                               │
│      ├─ SystemUpdateScript → 컴파일 대기열                              │
│      └─ 각 Emitter의 Scripts → 컴파일 대기열                            │
│                                                                         │
│  [컴파일 실행]                                                           │
│  └─ FNiagaraCompilationQueue::ProcessQueue()                           │
│      ├─ 그래프 → HLSL 변환 (GPU 에미터)                                 │
│      │   ├─ FNiagaraHlslTranslator::Translate()                        │
│      │   ├─ HLSL Compute Shader 생성                                   │
│      │   └─ Shader Compiler 호출                                       │
│      │                                                                  │
│      ├─ 그래프 → VM 바이트코드 변환 (CPU 에미터)                         │
│      │   ├─ FNiagaraScriptExecutionContext::CompileScript()            │
│      │   ├─ VectorVM 바이트코드 생성                                    │
│      │   └─ Bytecode 최적화                                             │
│      │                                                                  │
│      └─ FNiagaraSystemCompiledData 생성                                 │
│          ├─ Parameter 바인딩 정보                                       │
│          ├─ DataSet 구조 정보                                           │
│          └─ Emitter 실행 순서                                           │
│                                                                         │
│  [DDC 캐싱]                                                              │
│  └─ GetDerivedDataCacheRef().GetSynchronous()                          │
│      ├─ 컴파일 결과를 DDC에 저장                                         │
│      └─ 다음 로드 시 재컴파일 스킵 가능                                  │
│                                                                         │
│  [완료 콜백]                                                             │
│  └─ OnSystemCompiled.Broadcast(this)                                   │
│      └─ UI 갱신, 에러 표시 등                                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**컴파일 API:**

```cpp
#if WITH_EDITOR
// 컴파일 요청
bool bSuccess = System->RequestCompile(
    /*bForce=*/ false,  // Dirty만 컴파일
    /*OptionalUpdateContext=*/ nullptr,
    /*TargetPlatform=*/ nullptr
);

// 컴파일 완료 대기
System->WaitForCompilationComplete(
    /*bIncludingGPUShaders=*/ true,
    /*bShowProgress=*/ true
);

// 컴파일 상태 확인
if (System->HasOutstandingCompilationRequests())
{
    // 아직 컴파일 중...
}

// 컴파일 취소
System->InvalidateActiveCompiles();
#endif
```

---

## 💡 실전 예시 (Practical Examples)

### ✅ 좋은 예: 시스템 생성 및 설정

```cpp
UCLASS()
class AExplosionActor : public AActor
{
    GENERATED_BODY()

public:
    // ✅ Niagara System 에셋 참조
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "VFX")
    TObjectPtr<UNiagaraSystem> ExplosionSystemAsset;

    // ✅ 컴포넌트로 관리
    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "VFX")
    TObjectPtr<UNiagaraComponent> ExplosionComponent;

protected:
    virtual void BeginPlay() override
    {
        Super::BeginPlay();

        // ✅ 시스템 검증
        if (!ExplosionSystemAsset)
        {
            UE_LOG(LogTemp, Warning, TEXT("ExplosionSystemAsset is null!"));
            return;
        }

        // ✅ 컴포넌트 생성
        ExplosionComponent = NewObject<UNiagaraComponent>(this, TEXT("ExplosionVFX"));
        ExplosionComponent->SetupAttachment(RootComponent);
        ExplosionComponent->RegisterComponent();

        // ✅ 에셋 할당
        ExplosionComponent->SetAsset(ExplosionSystemAsset);

        // ✅ 자동 활성화 비활성화 (수동 제어)
        ExplosionComponent->SetAutoActivate(false);
        ExplosionComponent->SetAutoDestroy(false);
    }

public:
    // ✅ 폭발 트리거
    UFUNCTION(BlueprintCallable, Category = "VFX")
    void TriggerExplosion(FVector Location, float Magnitude, FLinearColor Color)
    {
        if (!ExplosionComponent) return;

        // ✅ 위치 설정
        ExplosionComponent->SetWorldLocation(Location);

        // ✅ 파라미터 설정 (활성화 전에 설정!)
        ExplosionComponent->SetFloatParameter(FName("User.ExplosionSize"), Magnitude);
        ExplosionComponent->SetVectorParameter(
            FName("User.ExplosionColor"),
            FVector(Color.R, Color.G, Color.B)
        );

        // ✅ 시스템 활성화 (bReset=true로 처음부터 재생)
        ExplosionComponent->ActivateSystem(true);

        // ✅ 완료 콜백 등록
        if (!ExplosionComponent->OnSystemFinished.IsBound())
        {
            ExplosionComponent->OnSystemFinished.AddDynamic(
                this,
                &AExplosionActor::OnExplosionFinished
            );
        }
    }

private:
    UFUNCTION()
    void OnExplosionFinished(UNiagaraComponent* PSystem)
    {
        UE_LOG(LogTemp, Log, TEXT("Explosion finished: %s"), *GetName());

        // ✅ 이후 작업 (예: 데미지 적용, 사운드 재생 등)
        // ...
    }
};
```

### ✅ 좋은 예: 동적 시스템 제어

```cpp
UCLASS()
class AFireActor : public AActor
{
    GENERATED_BODY()

public:
    UPROPERTY(VisibleAnywhere)
    UNiagaraComponent* FireComponent;

    UPROPERTY(EditAnywhere, Category = "Fire")
    float MaxIntensity = 1.0f;

    UPROPERTY(EditAnywhere, Category = "Fire")
    float DecayRate = 0.5f; // 초당 감소율

private:
    float CurrentIntensity = 1.0f;

protected:
    virtual void Tick(float DeltaTime) override
    {
        Super::Tick(DeltaTime);

        if (!FireComponent || !FireComponent->IsActive()) return;

        // ✅ 시간에 따라 강도 감소
        CurrentIntensity = FMath::Max(0.0f, CurrentIntensity - DecayRate * DeltaTime);

        // ✅ 파라미터 업데이트
        FireComponent->SetFloatParameter(FName("User.FireIntensity"), CurrentIntensity);

        // ✅ SpawnRate 동적 조절
        float SpawnRate = FMath::Lerp(0.0f, 1000.0f, CurrentIntensity);
        FireComponent->SetFloatParameter(FName("User.SpawnRate"), SpawnRate);

        // ✅ 색상 변화 (강렬한 흰색 → 약한 주황색)
        FLinearColor FireColor = FMath::Lerp(
            FLinearColor(1.0f, 0.5f, 0.0f),  // 주황색
            FLinearColor(1.0f, 1.0f, 0.9f),  // 흰색
            CurrentIntensity
        );
        FireComponent->SetColorParameter(FName("User.FireColor"), FireColor);

        // ✅ 강도가 0에 가까우면 비활성화
        if (CurrentIntensity <= 0.01f)
        {
            // Deactivate(): 기존 파티클은 Lifetime까지 유지
            FireComponent->Deactivate();
            UE_LOG(LogTemp, Log, TEXT("Fire extinguished"));
        }
    }

public:
    UFUNCTION(BlueprintCallable)
    void AddFuel(float Amount)
    {
        // ✅ 연료 추가 시 강도 증가
        CurrentIntensity = FMath::Min(MaxIntensity, CurrentIntensity + Amount);

        // ✅ 꺼진 불 다시 켜기
        if (!FireComponent->IsActive())
        {
            FireComponent->ActivateSystem(false);  // bReset=false (현재 상태 유지)
        }
    }
};
```

### ✅ 좋은 예: Effect Type 활용

```cpp
// EffectType 생성 및 설정
void SetupFireEffectType()
{
    // ✅ Effect Type 에셋 생성 (에디터)
    UNiagaraEffectType* FireEffectType = NewObject<UNiagaraEffectType>(
        GetTransientPackage(),
        UNiagaraEffectType::StaticClass(),
        TEXT("FireEffectType")
    );

    // ✅ 성능 제한 설정
    FireEffectType->NumInstancesToActivate = 20;  // 동시 활성 20개 제한

    // ✅ Culling 설정
    FireEffectType->CullProxyMode = ENiagaraCullProxyMode::Instanced;

    // ✅ Significance 설정
    FireEffectType->SignificanceBudget = 0.5f;  // 중간 중요도

    // ✅ 거리 기반 Scalability
    FireEffectType->bUseDistanceBasedScalability = true;
    FireEffectType->MaxDistance = 5000.0f;  // 50m 이상 비활성화

    // ✅ 시스템에 적용
    UNiagaraSystem* FireSystem = ...;
    FireSystem->SetEffectType(FireEffectType);
}

// 런타임에서 중요도 설정
void SpawnImportantFire(FVector Location)
{
    UNiagaraComponent* Comp = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
        GetWorld(),
        FireSystem,
        Location
    );

    // ✅ 중요한 이펙트는 Culling 방지
    Comp->SetAsset(FireSystem);
    // Significance를 높게 설정하여 우선순위 상승
    // (실제 구현은 Effect Type의 Significance Handler에서 처리)
}
```

### ❌ 나쁜 예: Emitter 직접 접근

```cpp
// ❌ 잘못된 방법: Emitter 직접 수정 시도
void BadModifyEmitter()
{
    UNiagaraSystem* System = ...;

    // ❌ EmitterHandle을 통해 직접 수정 (런타임에서 불가능)
    TArray<FNiagaraEmitterHandle>& Handles = System->GetEmitterHandles();
    FNiagaraEmitterHandle& Handle = Handles[0];

    // ❌ 에미터 데이터 직접 변경 시도 (크래시 위험!)
    FVersionedNiagaraEmitterData* Data = Handle.GetEmitterData();
    Data->SimTarget = ENiagaraSimTarget::GPUComputeSim;  // 런타임에 안 됨!

    // ❌ SpawnRate 직접 변경 시도 (반영 안 됨)
    // Data->SpawnScriptProps.Script의 파라미터를 직접 변경하려는 시도
}
```

**올바른 방법:**

```cpp
// ✅ 올바른 방법: User Parameters로 제어
void GoodModifySpawnRate()
{
    UNiagaraComponent* Comp = ...;

    // ✅ User Parameter를 통해 SpawnRate 제어
    // (에미터 스크립트에서 User.SpawnRateMultiplier를 SpawnRate에 연결)
    Comp->SetFloatParameter(FName("User.SpawnRateMultiplier"), 2.0f);

    // ✅ 또는 Dynamic Input으로 설정
    // (Niagara 에디터에서 SpawnRate를 Dynamic Input으로 설정)
}
```

### ❌ 나쁜 예: 매 프레임 시스템 재생성

```cpp
// ❌ 잘못된 방법: 매 프레임 새로운 컴포넌트 생성
void BadTick(float DeltaTime)
{
    FVector CurrentLocation = GetActorLocation();

    // ❌ 메모리 누수! 이전 컴포넌트 정리 안 됨
    UNiagaraComponent* NewComp = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
        GetWorld(),
        SomeNiagaraSystem,
        CurrentLocation
    );
    // 이전에 생성된 수백 개의 컴포넌트는 여전히 메모리에 남음!
}
```

**올바른 방법:**

```cpp
// ✅ 올바른 방법: 컴포넌트 재사용
UPROPERTY()
UNiagaraComponent* CachedComponent;

void BeginPlay()
{
    // ✅ 한 번만 생성
    CachedComponent = NewObject<UNiagaraComponent>(this);
    CachedComponent->SetAsset(SomeNiagaraSystem);
    CachedComponent->SetAutoActivate(false);
    CachedComponent->RegisterComponent();
}

void GoodTick(float DeltaTime)
{
    // ✅ 필요할 때만 활성화
    if (ShouldSpawnEffect())
    {
        CachedComponent->SetWorldLocation(GetActorLocation());
        CachedComponent->ActivateSystem(true);
    }
}
```

---

## 📊 성능 최적화 (Performance Optimization)

### stat 명령어

```
콘솔 명령어 (Play In Editor 또는 Standalone):

stat Niagara              // Niagara 전체 통계
  - Niagara Game Thread Time
  - Niagara Render Thread Time
  - Total Particle Count
  - Total System Count

stat NiagaraEmitters      // 에미터별 상세 통계
  - 각 에미터의 파티클 수
  - 각 에미터의 시뮬레이션 시간
  - 메모리 사용량

stat NiagaraGPU           // GPU 시뮬레이션 통계
  - GPU Dispatch Count
  - GPU Particle Memory
  - Compute Shader Time

stat Particles            // 전체 파티클 시스템 통계
  - Total Particle Instances
  - Total Particle Memory
```

**주요 성능 지표:**

| 지표 | 목표 | 위험 수준 |
|------|------|-----------|
| Niagara Game Thread Time | < 1ms | > 3ms |
| Niagara Render Thread Time | < 2ms | > 5ms |
| CPU Particle Count | < 50만 | > 100만 |
| GPU Particle Count | < 100만 | > 200만 |
| Active System Count | < 100 | > 200 |

### ✅ 해야 할 것

**1. Scalability 설정:**

```cpp
// ✅ Effect Type 생성 및 적용
UNiagaraEffectType* EffectType = NewObject<UNiagaraEffectType>();
EffectType->NumInstancesToActivate = 10;  // 동시 활성 개수 제한
EffectType->MaxDistance = 5000.0f;         // 50m 이상 비활성화

NiagaraSystem->SetEffectType(EffectType);
```

**2. Fixed Bounds 사용 (정적 이펙트):**

```cpp
// ✅ 경계 계산 비용 제거
NiagaraSystem->bFixedBounds = true;
NiagaraSystem->FixedBounds = FBox(
    FVector(-200, -200, 0),
    FVector(200, 200, 400)
);

// 성능 비교:
// Dynamic Bounds: 10만 파티클 = ~2ms
// Fixed Bounds: 10만 파티클 = ~0ms (20% 성능 향상)
```

**3. GPU 시뮬레이션 (대규모 파티클):**

```
에미터 설정:
  Emitter Properties:
    Sim Target: GPU Compute Sim  ← ✅ 10만+ 파티클 시 GPU 사용

성능 비교:
  CPU (VectorVM):
    - 10만 파티클 = ~5ms
    - 50만 파티클 = ~25ms (병렬 처리)

  GPU (HLSL Compute Shader):
    - 100만 파티클 = ~1ms
    - 500만 파티클 = ~5ms (완전 병렬)

  결론: GPU가 20배 이상 빠름 (대규모 시)
```

**4. LOD 및 Scalability:**

```cpp
// ✅ 거리별 퀄리티 조절
UNiagaraSystem* System = ...;
System->EffectType->bUseDistanceBasedScalability = true;

// 거리 임계값 설정:
// - Near (0-10m): Full quality (100% particles)
// - Medium (10-30m): 50% particles
// - Far (30-50m): 25% particles
// - Very Far (50m+): Disabled
```

**5. Warmup 최적화:**

```cpp
// ✅ 루프 이펙트는 Warmup 사용
NiagaraSystem->WarmupTime = 2.0f;           // 2초 워밍업
NiagaraSystem->WarmupTickCount = 60;         // 60틱
NiagaraSystem->WarmupTickDelta = 1.0f / 30.0f;  // 30 FPS

// 주의: 너무 긴 Warmup은 초기 로딩 시간 증가
// 권장: WarmupTime = Particle Lifetime * 1.5배
```

**6. Instanced Niagara Components:**

```cpp
// ✅ 다수의 동일 이펙트 → Instanced 사용
// (예: 100개의 동일한 불 이펙트)

UNiagaraCullProxyComponent* CullProxy = NewObject<UNiagaraCullProxyComponent>(this);
CullProxy->SetAsset(FireSystem);

// 각 인스턴스 추가
for (int32 i = 0; i < 100; ++i)
{
    FNiagaraCullProxyData InstanceData;
    InstanceData.Location = ...;
    CullProxy->AddInstance(InstanceData);
}

// 성능 비교:
// 개별 컴포넌트 100개: ~5ms Tick time
// Instanced 컴포넌트 1개 (100 인스턴스): ~1ms Tick time
```

### ❌ 피해야 할 것

**1. 과도한 Spawn Rate:**

```
❌ 나쁜 예:
  Spawn Rate: 10000 particles/sec
  Lifetime: 5초
  → 총 5만 개 파티클 (CPU 병목)

✅ 좋은 예:
  Spawn Rate: 1000 particles/sec
  Lifetime: 2초
  → 총 2천 개 파티클 (적정 수준)

또는:
  GPU Simulation 사용 (10만+ 파티클 가능)
```

**2. 복잡한 Update Script:**

```
❌ 나쁜 예: Update Script에서 무거운 계산
  - For Loop 1000회
  - 복잡한 Noise 함수 (매 파티클마다)
  - Collision Query (매 프레임)

✅ 좋은 예:
  - 간단한 Force 계산
  - Sample Texture (캐시 활용)
  - Simulation Stage 사용 (필요 시)
```

**3. Dynamic Bounds (대규모):**

```
❌ 나쁜 예: 10만 파티클 + Dynamic Bounds
  → 매 프레임 모든 파티클 위치 검사 (~2ms)

✅ 좋은 예: Fixed Bounds 사용
  → 경계 계산 0ms (20% 성능 향상)
```

**4. 너무 많은 DataInterface:**

```
❌ 나쁜 예: 하나의 에미터에 10+ DataInterface
  - SkeletalMesh x 3
  - StaticMesh x 5
  - Texture x 2
  → 바인딩 오버헤드 증가

✅ 좋은 예: 필요한 것만 사용
  - 주요 DataInterface 2~3개
  - 나머지는 파라미터로 전달
```

---

## 🔧 디버깅 및 트러블슈팅 (Debugging & Troubleshooting)

### Niagara Debugger

**에디터에서 활성화:**

1. **Window → Niagara Debugger**
2. **PIE 실행**
3. 활성 시스템 목록 확인
4. 에미터별 파티클 개수, 메모리, 시간 확인

**주요 기능:**
- 시스템별 Tick Time
- 에미터별 Particle Count
- GPU Memory Usage
- Scalability State
- DataInterface Bindings

### 흔한 에러와 해결 방법

**1. "System is not ready to run"**

```
원인: 컴파일이 완료되지 않음

해결책:
#if WITH_EDITOR
// 에디터에서 컴파일 완료 대기
System->WaitForCompilationComplete(true, true);
#endif

또는:
// 런타임 체크
if (!System->IsReadyToRun())
{
    UE_LOG(LogTemp, Warning, TEXT("System not ready: %s"), *System->GetName());
    return;
}
```

**2. "Script compilation failed"**

```
원인: 그래프 에러 또는 DDC 캐시 문제

해결책:
1. Output Log 확인 (에러 메시지 확인)
2. Niagara 에디터 → Compile 버튼 클릭
3. DDC 재생성:
   - Edit → Project Settings → Derived Data Cache
   - Delete Local Derived Data Cache
4. 에디터 재시작
```

**3. "Effect Type budget exceeded"**

```
원인: Scalability 제한 초과

해결책:
1. Effect Type 설정 확인:
   - NumInstancesToActivate 증가
   - SignificanceBudget 조정

2. 또는 시스템 최적화:
   - Spawn Rate 감소
   - Particle Lifetime 감소
   - GPU Simulation 사용
```

**4. "System not rendering"**

```
원인: Bounds가 카메라 Frustum 밖

해결책:
1. Fixed Bounds 증가:
   System->bFixedBounds = true;
   System->FixedBounds = FBox(FVector(-1000), FVector(1000));

2. 또는 Dynamic Bounds 사용:
   System->bFixedBounds = false;

3. Cull Distance 확인:
   Component->SetCullDistance(10000.0f);  // 100m
```

**5. "DataInterface XXX is not supported on GPU"**

```
원인: GPU 시뮬레이션에서 CPU 전용 DataInterface 사용

해결책:
1. Emitter → Sim Target → CPU Sim으로 변경

또는:

2. GPU 지원 DataInterface로 교체:
   ❌ Collision Query (CPU 전용)
   ✅ Scene Depth Collision (GPU 지원)

   ❌ Skeletal Mesh Socket Sampling (CPU 전용)
   ✅ Skeletal Mesh Sampling (GPU 부분 지원)
```

**6. "Particle count is zero"**

```
디버깅 체크리스트:
✅ Spawn Rate > 0?
✅ Emitter Enabled?
✅ System Activated?
✅ Renderer 추가되었는가? (Sprite, Mesh, etc.)
✅ Particle Lifetime > 0?
✅ Scalability로 비활성화되지 않았는가?
```

### 디버그 드로잉

```cpp
// 시스템 Bounds 표시
#if WITH_EDITOR
if (NiagaraComponent)
{
    FBox Bounds = NiagaraComponent->CalcBounds(FTransform::Identity).GetBox();
    DrawDebugBox(GetWorld(), Bounds.GetCenter(), Bounds.GetExtent(), FColor::Green, false, 1.0f);
}
#endif

// 파티클 위치 표시 (소량만!)
// (실제로는 Niagara Debugger HUD 사용 권장)
```

---

## 🔗 관련 문서 (Related Documents)

이 문서는 UNiagaraSystem 클래스의 상세 분석이며, 다음 문서들과 연계됩니다:

- **[Overview.md](../Overview.md)** - Niagara 시스템 전체 개요
- **[NiagaraEmitter.md](NiagaraEmitter.md)** - 에미터 상세 분석 (버전 시스템, SimTarget, 스크립트 구조)
- **[NiagaraComponent.md](NiagaraComponent.md)** - 런타임 컴포넌트 (파라미터 제어, 생명주기)
- **[DataInterface.md](DataInterface.md)** - DataInterface 시스템 (52+ 타입, 커스텀 DI 제작)
- **[NiagaraScript.md](NiagaraScript.md)** - 스크립트 시스템 (컴파일, VM, HLSL)
- **[Scalability.md](Scalability.md)** - Scalability 시스템 (Effect Type, Budget, LOD)

---

## 📚 참고 자료 (References)

### 소스 파일

- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraSystem.h` (1,167 라인)
  - UNiagaraSystem 클래스 정의
  - FNiagaraSystemCompiledData 구조체
  - FNiagaraEmitterCompiledData 구조체

- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraEmitterHandle.h` (182 라인)
  - FNiagaraEmitterHandle 구조체
  - ENiagaraEmitterMode 열거형

- `Engine/Plugins/FX/Niagara/Source/Niagara/Private/NiagaraSystem.cpp`
  - UNiagaraSystem 구현부
  - 컴파일 시스템 로직
  - Warmup 실행 로직

### 공식 문서

- [Unreal Engine - Creating Systems in Niagara](https://docs.unrealengine.com/5.7/en-US/creating-systems-in-niagara-for-unreal-engine/)
- [Niagara System Overview](https://docs.unrealengine.com/5.7/en-US/overview-of-niagara-effects-for-unreal-engine/)
- [Niagara Effect Type](https://docs.unrealengine.com/5.7/en-US/niagara-effect-type-in-unreal-engine/)

### External 참고 문서 (이 저장소)

- `UnrealSummary/External/나이아가라/나이아가라 분석/2 나이아가라 구성 요소 및 데이터 모델/2 1 Niagara System`
  - 시스템 역할, 기본 구조, 핵심 역할, Effect Type 관계

- `UnrealSummary/External/나이아가라/나이아가라 분석/나이아가라 전체적인 구조`
  - 클래스 흐름도: UNiagaraComponent → FNiagaraSystemInstance → FNiagaraSystemSimulation

- `UnrealSummary/External/나이아가라/나이아가라 분석/5 나이아가라 컴파일 및 스크립트 처리`
  - 컴파일 시스템, DDC 캐싱, HLSL/VM 변환

### 주요 API

```cpp
// UNiagaraSystem
TArray<FNiagaraEmitterHandle>& GetEmitterHandles();
UNiagaraScript* GetSystemSpawnScript();
UNiagaraScript* GetSystemUpdateScript();
FNiagaraUserRedirectionParameterStore& GetExposedParameters();
bool IsReadyToRun() const;
bool NeedsWarmup() const;
float GetWarmupTime() const;

#if WITH_EDITOR
FNiagaraEmitterHandle AddEmitterHandle(UNiagaraEmitter&, FName, FGuid);
FNiagaraEmitterHandle DuplicateEmitterHandle(const FNiagaraEmitterHandle&, FName);
void RemoveEmitterHandle(const FNiagaraEmitterHandle&);
bool RequestCompile(bool bForce, FNiagaraSystemUpdateContext*, const ITargetPlatform*);
void WaitForCompilationComplete(bool bIncludingGPUShaders, bool bShowProgress);
#endif

// FNiagaraEmitterHandle
FGuid GetId() const;
FName GetName() const;
void SetName(FName, UNiagaraSystem&);
bool GetIsEnabled() const;
bool SetIsEnabled(bool, UNiagaraSystem&, bool);
FVersionedNiagaraEmitter GetInstance() const;
FVersionedNiagaraEmitterData* GetEmitterData() const;
bool IsValid() const;

// UNiagaraComponent (관련 API)
void SetAsset(UNiagaraSystem*);
void ActivateSystem(bool bReset = false);
void DeactivateImmediate();
void Deactivate();
void SetFloatParameter(FName, float);
void SetVectorParameter(FName, FVector);
void SetColorParameter(FName, FLinearColor);
bool IsActive() const;
```

---

> 🔄 **작성일**: 2025-11-10
> 📝 **문서 버전**: v1.0
> ✅ **소스 검증**: UE 5.7.0
> 📂 **다음 문서**: [NiagaraEmitter.md](NiagaraEmitter.md)
