---
title: "NiagaraComponent - Niagara 런타임 컴포넌트 (Niagara Runtime Component)"
date: "2025-11-21"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Niagara"
tags: ["unreal", "Niagara", "Core"]
---
# NiagaraComponent - Niagara 런타임 컴포넌트 (Niagara Runtime Component)

## 🧭 개요 (Overview)

**UNiagaraComponent**는 **게임 월드에서 Niagara 시스템을 실행하는 런타임 컴포넌트**입니다. UNiagaraSystem 에셋을 인스턴스화하고, 파라미터를 동적으로 제어하며, 생명주기를 관리하는 핵심 인터페이스입니다.

**핵심 역할:**
- **시스템 인스턴스 생성**: UNiagaraSystem 에셋을 월드에 배치
- **파라미터 제어**: Blueprint/C++에서 동적으로 파라미터 설정
- **생명주기 관리**: Activate, Deactivate, Reset 제어
- **Transform 관리**: 월드 위치, 회전, 스케일 동기화
- **Auto Destroy**: 이펙트 완료 시 자동 제거
- **Pooling**: 컴포넌트 재사용을 통한 성능 최적화
- **Warmup 오버라이드**: 시스템별 워밍업 설정
- **Bounds 오버라이드**: 인스턴스별 경계 설정

**📂 위치:**
- `Engine/Plugins/FX/Niagara/Source/Niagara/Public/NiagaraComponent.h:53` (UNiagaraComponent 클래스 정의)
- `Engine/Plugins/FX/Niagara/Source/Niagara/Public/NiagaraFunctionLibrary.h:24` (유틸리티 함수)

---

## 🎯 설계 철학: 왜 Component 패턴인가?

### 문제: 직접 시스템 실행의 어려움

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    컴포넌트 없이 직접 실행할 경우의 문제                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ❌ 문제 1: Transform 동기화 어려움                                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 파티클 시스템을 액터에 부착하려면?                    │          │
│  │  - 액터가 이동하면 파티클도 따라가야 함                  │          │
│  │  - 수동으로 매 프레임 Transform 동기화 필요               │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ❌ 문제 2: 생명주기 관리                                                │
│  - 언제 시스템을 생성하고 제거해야 하는가?                               │
│  - 이펙트 완료 시 자동으로 정리되지 않음                                 │
│  - 메모리 누수 위험                                                      │
│                                                                         │
│  ❌ 문제 3: 액터 시스템과의 통합                                         │
│  - 액터 생명주기와 파티클 생명주기 불일치                                │
│  - Replication, Visibility, Culling 처리 어려움                         │
│  - Level Streaming 시 처리 복잡함                                       │
│                                                                         │
│  ❌ 문제 4: 파라미터 전달                                                │
│  - Blueprint에서 파티클 제어 어려움                                      │
│  - C++ 코드와 에셋 간 결합도 높음                                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                            ↓
                   UNiagaraComponent 솔루션:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  ✅ 해결 1: Component 아키텍처 통합                                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  AActor                                                  │          │
│  │  ├─ RootComponent (USceneComponent)                     │          │
│  │  └─ NiagaraComponent (UNiagaraComponent)                │          │
│  │      ├─ 자동 Transform 동기화                            │          │
│  │      ├─ AttachToComponent 지원                          │          │
│  │      └─ 액터 생명주기와 통합                             │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ✅ 해결 2: Auto Destroy 시스템                                          │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  bAutoDestroy = true                                     │          │
│  │  └─ 이펙트 완료 시 자동으로 컴포넌트 및 액터 제거        │          │
│  │                                                          │          │
│  │  OnSystemFinished 델리게이트                             │          │
│  │  └─ 완료 시점에 커스텀 로직 실행 가능                    │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ✅ 해결 3: 파라미터 오버라이드 시스템                                   │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  OverrideParameters (FNiagaraUserRedirectionParameterStore)│        │
│  │  ├─ 인스턴스별 독립적인 파라미터 값                       │          │
│  │  ├─ Blueprint/C++에서 동적 제어                          │          │
│  │  └─ SetVariableFloat, SetVariableVector 등               │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ✅ 해결 4: Pooling 시스템                                               │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  ENCPoolMethod::AutoRelease                             │          │
│  │  └─ 사용 완료된 컴포넌트를 풀에 반환                     │          │
│  │                                                          │          │
│  │  장점:                                                   │          │
│  │  - 컴포넌트 생성/제거 비용 절감                          │          │
│  │  - GC 부담 감소                                          │          │
│  │  - 일관된 성능                                           │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ✅ 해결 5: Blueprint 통합                                               │
│  - 모든 주요 함수가 UFUNCTION으로 노출됨                                 │
│  - 디자이너가 코드 없이 이펙트 제어 가능                                 │
│  - 비주얼 스크립팅 완벽 지원                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 설계 결정

| 결정 사항 | 이유 | 트레이드오프 |
|----------|------|-------------|
| **Component 기반** | 액터 시스템 통합, Transform 자동 동기화 | 단독 사용 시 오버헤드 |
| **OverrideParameters** | 인스턴스별 독립 제어, 에셋 공유 | 메모리 오버헤드 (파라미터 복사) |
| **Auto Destroy** | 자동 정리, 메모리 누수 방지 | 수동 제어 필요 시 불편 |
| **Pooling 지원** | 성능 최적화, GC 부담 감소 | 풀 관리 복잡도 증가 |
| **SystemInstanceController** | 스레드 안전성, 간접 접근 | 직접 접근 불가 |

---

## 🏗️ 시스템 아키텍처 (System Architecture)

### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         UNiagaraComponent                               │
│  (Niagara 시스템을 월드에 배치하는 컴포넌트)                             │
├─────────────────────────────────────────────────────────────────────────┤
│  상속: UObject → UActorComponent → USceneComponent →                    │
│        UPrimitiveComponent → UFXSystemComponent → UNiagaraComponent     │
│                                                                         │
│  Private:                                                               │
│    - Asset : UNiagaraSystem*                                           │
│      // 실행할 Niagara 시스템 에셋                                       │
│                                                                         │
│    - TickBehavior : ENiagaraTickBehavior                               │
│      // UsePrereqs: 물리 이후 Tick                                      │
│      // UseComponentTickGroup: 컴포넌트 틱 그룹 사용                     │
│      // ForceTickFirst: 프레임 시작에 Tick                              │
│      // ForceTickLast: 프레임 끝에 Tick                                 │
│                                                                         │
│    - RandomSeedOffset : int32                                          │
│      // 랜덤 시드 오프셋 (동일 시스템 다수 인스턴스 차별화)              │
│                                                                         │
│    - OverrideParameters : FNiagaraUserRedirectionParameterStore        │
│      // 인스턴스별 파라미터 오버라이드 (시스템 ExposedParameters 복사)  │
│                                                                         │
│    - SystemInstanceController : FNiagaraSystemInstanceControllerPtr    │
│      // 실제 시뮬레이션 인스턴스에 대한 스레드 안전 포인터               │
│                                                                         │
│    - AgeUpdateMode : ENiagaraAgeUpdateMode                             │
│      // TickDeltaTime: 일반 Tick (DeltaTime 사용)                       │
│      // DesiredAge: 특정 Age로 Seek                                     │
│      // DesiredAgeNoSeek: Age 설정 without Seek                        │
│                                                                         │
│    - DesiredAge : float                                                │
│      // DesiredAge 모드에서 목표 Age                                    │
│    - SeekDelta : float                                                 │
│      // Seek 시 사용할 Delta                                            │
│    - MaxSimTime : float                                                │
│      // 프레임당 최대 시뮬레이션 시간 (Seek 제한)                        │
│                                                                         │
│    - bAutoDestroy : bool                                               │
│      // 이펙트 완료 시 자동으로 액터 제거                                │
│    - bRenderingEnabled : bool                                          │
│      // 렌더링 활성화 여부 (시뮬레이션은 계속됨)                         │
│    - bForceSolo : bool                                                 │
│      // 배치 시뮬레이션 대신 독립 시뮬레이션 강제                        │
│                                                                         │
│    - bOverrideWarmupSettings : bool                                    │
│      // 시스템의 Warmup 설정 오버라이드 여부                             │
│    - WarmupTickCount : int32                                           │
│      // 오버라이드할 Warmup 틱 횟수                                      │
│    - WarmupTickDelta : float                                           │
│      // 오버라이드할 Warmup 틱 간격                                      │
│                                                                         │
│    - PoolingMethod : ENCPoolMethod                                     │
│      // None: 풀링 안 함                                                │
│      // AutoRelease: 완료 시 자동으로 풀에 반환                          │
│      // ManualRelease: 수동 반환                                        │
│      // ManualRelease_OnComplete: 완료 후 수동 반환                     │
│                                                                         │
│    - CustomTimeDilation : float                                        │
│      // 커스텀 시간 배율 (Solo 모드에서만 사용 가능)                     │
│                                                                         │
│  주요 메서드:                                                            │
│    + SetAsset(UNiagaraSystem*, bool bReset) : void                    │
│      // 실행할 시스템 에셋 설정                                          │
│    + GetAsset() : UNiagaraSystem*                                      │
│      // 현재 에셋 반환                                                   │
│                                                                         │
│    + Activate(bool bReset) : void                                      │
│      // 시스템 활성화 (bReset=true면 처음부터)                           │
│    + Deactivate() : void                                               │
│      // 신규 파티클 생성 중단 (기존 파티클은 유지)                       │
│    + DeactivateImmediate() : void                                      │
│      // 모든 파티클 즉시 제거                                            │
│    + ResetSystem() : void                                              │
│      // 시스템을 초기 상태로 리셋                                        │
│    + ReinitializeSystem() : void                                       │
│      // 에셋 재로드 및 재초기화                                          │
│                                                                         │
│    + IsActive() : bool                                                 │
│      // 활성화 여부 확인                                                 │
│    + IsComplete() : bool                                               │
│      // 완료 여부 확인                                                   │
│                                                                         │
│    + SetVariableFloat(FName, float) : void                             │
│    + SetVariableVec3(FName, FVector) : void                            │
│    + SetVariableLinearColor(FName, FLinearColor) : void                │
│    + SetVariableBool(FName, bool) : void                               │
│    + SetVariableActor(FName, AActor*) : void                           │
│    + SetVariableObject(FName, UObject*) : void                         │
│      // 파라미터 설정 함수들 (OverrideParameters에 저장)                │
│                                                                         │
│    + GetVariableFloat(FName, bool& bIsValid) : float                   │
│    + GetVariableVec3(FName, bool& bIsValid) : FVector                  │
│      // 파라미터 값 읽기                                                 │
│                                                                         │
│    + SetSystemFixedBounds(FBox) : void                                 │
│    + GetSystemFixedBounds() : FBox                                     │
│    + ClearSystemFixedBounds() : void                                   │
│      // 시스템 전체 경계 오버라이드                                      │
│                                                                         │
│    + SetEmitterFixedBounds(FName, FBox) : void                         │
│    + GetEmitterFixedBounds(FName) : FBox                               │
│    + ClearEmitterFixedBounds(FName) : void                             │
│      // 개별 에미터 경계 오버라이드                                      │
│                                                                         │
│    + SetTickBehavior(ENiagaraTickBehavior) : void                      │
│    + GetTickBehavior() : ENiagaraTickBehavior                          │
│      // Tick 타이밍 제어                                                 │
│                                                                         │
│    + SetAutoDestroy(bool) : void                                       │
│    + SetRenderingEnabled(bool) : void                                  │
│    + SetPaused(bool) : void                                            │
│    + IsPaused() : bool                                                 │
│      // 상태 제어                                                        │
│                                                                         │
│    + AdvanceSimulation(int32 Ticks, float Delta) : void                │
│    + AdvanceSimulationByTime(float Time, float Delta) : void           │
│      // 수동으로 시뮬레이션 진행                                         │
│                                                                         │
│    + SetAgeUpdateMode(ENiagaraAgeUpdateMode) : void                    │
│    + GetAgeUpdateMode() : ENiagaraAgeUpdateMode                        │
│    + SetDesiredAge(float) : void                                       │
│    + GetDesiredAge() : float                                           │
│    + SeekToDesiredAge(float) : void                                    │
│      // Age 기반 제어 (시네마틱용)                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 소스 검증:**
- `NiagaraComponent.h:53-649` - UNiagaraComponent 클래스 전체

### SystemInstanceController 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│          FNiagaraSystemInstanceController (스레드 안전 래퍼)             │
├─────────────────────────────────────────────────────────────────────────┤
│  역할: 멀티스레드 환경에서 SystemInstance 안전하게 접근                  │
│                                                                         │
│  UNiagaraComponent                                                      │
│  └─ SystemInstanceController (FNiagaraSystemInstanceControllerPtr)     │
│      │                                                                  │
│      ├─ Game Thread에서 접근 안전                                        │
│      ├─ Concurrent Tick Thread에서 접근 안전                            │
│      └─ Render Thread에서 접근 안전                                     │
│          │                                                              │
│          ↓ 간접 접근                                                    │
│      FNiagaraSystemInstance (실제 시뮬레이션 객체)                       │
│      ├─ FNiagaraSystemSimulation                                       │
│      ├─ EmitterInstances[]                                             │
│      ├─ ParameterStore                                                 │
│      └─ DataInterfaceBindings                                          │
│                                                                         │
│  주요 인터페이스:                                                         │
│    SystemInstanceController->Activate()                                │
│    SystemInstanceController->Deactivate()                              │
│    SystemInstanceController->GetSystemInstance()                       │
│    SystemInstanceController->GetExecutionState()                       │
│                                                                         │
│  이전 직접 접근 (Deprecated):                                            │
│  ❌ GetSystemInstance() → FNiagaraSystemInstance*  (스레드 안전하지 않음)│
│  ✅ GetSystemInstanceController() → Controller  (스레드 안전)            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🧱 핵심 컴포넌트 (Core Components)

### 1. Asset & OverrideParameters

**역할:** 시스템 에셋 설정 및 파라미터 오버라이드

**구조:**

```cpp
// NiagaraComponent.h:86-103
private:
    UPROPERTY(EditAnywhere, Category="Niagara")
    TObjectPtr<UNiagaraSystem> Asset;

    UPROPERTY()
    FNiagaraUserRedirectionParameterStore OverrideParameters;
```

**파라미터 흐름:**

```
파라미터 계층 및 오버라이드:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [1] UNiagaraSystem::ExposedParameters (에셋 기본값)                    │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  User.FireSize = 1.0                                     │          │
│  │  User.FireColor = (1, 0.5, 0)                            │          │
│  │  User.SpawnRate = 100                                    │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 복사 (SetAsset 시)                                            │
│  [2] UNiagaraComponent::OverrideParameters (인스턴스별 오버라이드)       │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  User.FireSize = 1.0  (기본값)                           │          │
│  │  User.FireColor = (1, 0.5, 0)  (기본값)                  │          │
│  │  User.SpawnRate = 100  (기본값)                          │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ SetVariableFloat("User.FireSize", 2.0) 호출                  │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  User.FireSize = 2.0  ← 오버라이드됨! ✅                 │          │
│  │  User.FireColor = (1, 0.5, 0)  (기본값 유지)             │          │
│  │  User.SpawnRate = 100  (기본값 유지)                     │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 런타임 시뮬레이션에 전달                                       │
│  [3] FNiagaraSystemInstance::ParameterStore (실행 시 사용)              │
│  └─ OverrideParameters 값이 우선 적용됨                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**SetAsset 동작:**

```cpp
// SetAsset 내부 동작
void UNiagaraComponent::SetAsset(UNiagaraSystem* InAsset, bool bResetExistingOverrideParameters)
{
    if (Asset == InAsset) return;

    // 기존 시스템 정리
    DeactivateImmediate();
    DestroyInstance();

    // 새 에셋 설정
    Asset = InAsset;

    if (bResetExistingOverrideParameters)
    {
        // ✅ ExposedParameters 복사하여 OverrideParameters 초기화
        OverrideParameters.CopyParametersFrom(Asset->GetExposedParameters());
    }
    // else: 기존 OverrideParameters 유지 (다른 시스템으로 전환 시 유용)

    // 새 인스턴스 초기화
    InitializeSystem();
}
```

### 2. Activate, Deactivate, Reset

**역할:** 시스템 생명주기 제어

**메서드:**

```cpp
// NiagaraComponent.h:223-225, 607-612
UFUNCTION(BlueprintCallable)
void Activate(bool bReset = false);        // 활성화

UFUNCTION(BlueprintCallable)
void Deactivate();                         // 부드러운 비활성화

UFUNCTION(BlueprintCallable)
void DeactivateImmediate();                // 즉시 비활성화

UFUNCTION(BlueprintCallable)
void ResetSystem();                        // 리셋 (Age=0, 파티클 전체 제거)

UFUNCTION(BlueprintCallable)
void ReinitializeSystem();                 // 재초기화 (에셋 다시 로드)
```

**동작 비교:**

```
Activate, Deactivate, Reset 동작 비교:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  Activate(bReset = false)                                               │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 시스템 활성화                                          │          │
│  │  - bReset = false: 현재 상태 유지 (Age 유지)             │          │
│  │  - bReset = true: 처음부터 시작 (Age=0, 파티클 제거)     │          │
│  │  - SystemSpawnScript 실행 (bReset=true 시)               │          │
│  │  - 파티클 생성 시작                                       │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  Deactivate()  (부드러운 종료)                                           │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 신규 파티클 생성 중단                                  │          │
│  │  - 기존 파티클은 Lifetime까지 유지                        │          │
│  │  - 모든 파티클 소멸 후 자동으로 Inactive 상태             │          │
│  │  - OnSystemFinished 델리게이트 호출                       │          │
│  │  - bAutoDestroy=true면 액터/컴포넌트 제거                 │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  DeactivateImmediate()  (즉시 종료)                                      │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 모든 파티클 즉시 제거                                  │          │
│  │  - 화면에서 즉시 사라짐                                   │          │
│  │  - OnSystemFinished 델리게이트 호출                       │          │
│  │  - bAutoDestroy=true면 액터/컴포넌트 즉시 제거            │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ResetSystem()                                                          │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - System Age = 0으로 리셋                                │          │
│  │  - 모든 파티클 제거                                       │          │
│  │  - SystemSpawnScript 재실행                              │          │
│  │  - 활성화 상태 유지 (비활성화되지 않음)                  │          │
│  │  - 루프 이펙트 재시작 시 유용                            │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
│  ReinitializeSystem()                                                   │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  - 에셋 재로드                                            │          │
│  │  - SystemInstance 재생성                                 │          │
│  │  - 에셋이 변경되었을 때 사용                             │          │
│  │  - 에디터에서 에셋 수정 후 런타임 반영 시 유용           │          │
│  └──────────────────────────────────────────────────────────┘          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3. Auto Destroy

**역할:** 이펙트 완료 시 자동 정리

**구조:**

```cpp
// NiagaraComponent.h:172-173
UPROPERTY()
uint32 bAutoDestroy : 1;
```

**동작 흐름:**

```
Auto Destroy 흐름:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [1] 컴포넌트 생성 시                                                    │
│  UNiagaraFunctionLibrary::SpawnSystemAtLocation(                       │
│      SystemTemplate,                                                    │
│      Location,                                                          │
│      Rotation,                                                          │
│      bAutoDestroy = true  ← ✅ 자동 제거 활성화                          │
│  )                                                                      │
│  └─ Actor 생성 (AController 또는 임시 Actor)                            │
│      └─ NiagaraComponent 추가                                           │
│          └─ bAutoDestroy = true 설정                                    │
│                                                                         │
│  [2] 시스템 실행 중                                                      │
│  └─ 매 프레임 Tick                                                      │
│      ├─ 파티클 생성                                                      │
│      ├─ 파티클 업데이트                                                  │
│      └─ 파티클 소멸                                                      │
│                                                                         │
│  [3] 이펙트 완료 감지                                                    │
│  └─ UNiagaraComponent::PostSystemTick_GameThread()                     │
│      └─ if (System->IsComplete())  // 모든 파티클 소멸 확인             │
│          └─ OnSystemFinished.Broadcast(this)  // 델리게이트 발생        │
│              └─ OnSystemComplete(bExternalCompletion = false)          │
│                  │                                                      │
│                  ├─ if (bAutoDestroy)                                   │
│                  │   └─ DestroyComponent()                             │
│                  │       └─ AActor::IsReadyForOwnerToAutoDestroy()     │
│                  │           └─ 모든 컴포넌트가 제거 가능한가?          │
│                  │               └─ Yes → AActor::Destroy()            │
│                  │                   └─ GC에 의해 메모리 해제           │
│                  │                                                      │
│                  └─ PoolingMethod == AutoRelease?                      │
│                      └─ Yes → 풀에 반환 (Destroy 대신)                  │
│                                                                         │
│  [4] 정리 완료                                                           │
│  └─ Actor 및 Component 제거됨 (또는 풀에 반환)                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**주의사항:**

```cpp
// ✅ 올바른 사용: One-shot 이펙트
UNiagaraComponent* HitEffect = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
    World,
    HitEffectSystem,
    HitLocation,
    FRotator::ZeroRotator,
    FVector(1.0f),
    /*bAutoDestroy=*/ true  // ← 이펙트 완료 시 자동 제거
);

// ❌ 잘못된 사용: 루프 이펙트
UNiagaraComponent* FireLoop = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
    World,
    FireLoopSystem,  // Loop 이펙트
    Location,
    FRotator::ZeroRotator,
    FVector(1.0f),
    /*bAutoDestroy=*/ true  // ← 영원히 끝나지 않음! 메모리 누수!
);
// 해결: bAutoDestroy = false 사용, 수동으로 Deactivate 호출
```

### 4. Pooling System

**역할:** 컴포넌트 재사용을 통한 성능 최적화

**ENCPoolMethod:**

```cpp
// NiagaraComponent.h:216
enum class ENCPoolMethod : uint8
{
    None,                      // 풀링 안 함 (기본)
    AutoRelease,               // 완료 시 자동으로 풀에 반환
    ManualRelease,             // 수동으로 풀에 반환
    ManualRelease_OnComplete,  // 완료 후 수동 반환 신호 대기
    FreeInPool,                // 풀에서 대기 중
};
```

**사용 예시:**

```cpp
// ✅ 풀링 사용 (성능 최적화)
UNiagaraComponent* PooledEffect = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
    World,
    ExplosionSystem,
    Location,
    FRotator::ZeroRotator,
    FVector(1.0f),
    /*bAutoDestroy=*/ true,
    /*bAutoActivate=*/ true,
    /*PoolingMethod=*/ ENCPoolMethod::AutoRelease  // ← 풀링 활성화
);

// 이펙트 완료 후:
// 1. Actor/Component가 Destroy되지 않음
// 2. 풀에 반환됨
// 3. 다음 SpawnSystemAtLocation 호출 시 재사용됨

// 성능 비교:
// - 풀링 없음: 매번 Actor + Component 생성 (~0.5ms)
// - 풀링 사용: 풀에서 가져오기 (~0.05ms, 10배 빠름)
```

### 5. Age Update Mode & Seek

**역할:** 시네마틱 및 타임라인 제어

**ENiagaraAgeUpdateMode:**

```cpp
// NiagaraComponent.h:149-156
enum class ENiagaraAgeUpdateMode : uint8
{
    TickDeltaTime,        // 일반 DeltaTime 사용 (기본)
    DesiredAge,           // 특정 Age로 Seek
    DesiredAgeNoSeek,     // Age 설정 without Seek
};
```

**Seek 메커니즘:**

```
DesiredAge 모드 동작:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  [설정]                                                                  │
│  SetAgeUpdateMode(ENiagaraAgeUpdateMode::DesiredAge)                   │
│  SetSeekDelta(1.0f / 30.0f)  // 30 FPS로 Seek                          │
│  SetCanRenderWhileSeeking(false)  // Seek 중 렌더링 안 함               │
│                                                                         │
│  [현재 상태]                                                             │
│  Current Age = 0.0초                                                    │
│                                                                         │
│  [Seek 명령]                                                             │
│  SeekToDesiredAge(5.0f)  // 5초로 이동                                  │
│  └─ DesiredAge = 5.0                                                   │
│      bIsSeeking = true                                                  │
│                                                                         │
│  [Tick 1] (Game Thread)                                                │
│  └─ Current Age = 0.0 < Desired Age = 5.0                              │
│      ├─ Age += SeekDelta = 0.033                                       │
│      ├─ TickComponent(DeltaTime = 0.033)                               │
│      └─ if (bCanRenderWhileSeeking == false)                           │
│          └─ 렌더링 스킵                                                  │
│                                                                         │
│  [Tick 2 ~ 150]                                                        │
│  └─ 계속해서 SeekDelta만큼 증가                                          │
│      Age: 0.033 → 0.066 → 0.099 → ... → 4.95 → 5.0                     │
│      총 150틱 소요 (5.0 / 0.033 ≈ 150)                                  │
│                                                                         │
│  [Tick 151] Age = 5.0 도달                                              │
│  └─ Current Age >= Desired Age                                         │
│      ├─ bIsSeeking = false                                             │
│      ├─ 렌더링 재개                                                      │
│      └─ 정상 Tick 모드로 전환                                            │
│                                                                         │
│  MaxSimTime 제한:                                                        │
│  - 프레임당 최대 시뮬레이션 시간 제한                                     │
│  - 예: MaxSimTime = 0.1초                                               │
│  - 한 프레임에 최대 0.1초까지만 Seek                                     │
│  - 나머지는 다음 프레임에 처리                                           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

**사용 사례:**

```cpp
// ✅ 시네마틱 제어 (Sequencer에서 사용)
NiagaraComp->SetAgeUpdateMode(ENiagaraAgeUpdateMode::DesiredAge);
NiagaraComp->SetSeekDelta(1.0f / 60.0f);  // 60 FPS
NiagaraComp->SetMaxSimTime(0.1f);  // 프레임당 최대 0.1초

// 타임라인 스크러빙
NiagaraComp->SeekToDesiredAge(TimelinePosition);

// ✅ 정확한 Age 제어 (게임플레이)
// 예: 버프 이펙트를 정확히 5초에 특정 상태로 만들기
NiagaraComp->SetAgeUpdateMode(ENiagaraAgeUpdateMode::DesiredAge);
NiagaraComp->SetCanRenderWhileSeeking(true);  // Seek 중에도 표시
NiagaraComp->SeekToDesiredAge(5.0f);
```

### 6. Fixed Bounds Override

**역할:** 인스턴스별 경계 설정

**메서드:**

```cpp
// NiagaraComponent.h:409-442
UFUNCTION(BlueprintCallable)
void SetSystemFixedBounds(FBox LocalBounds);

UFUNCTION(BlueprintCallable)
FBox GetSystemFixedBounds() const;

UFUNCTION(BlueprintCallable)
void ClearSystemFixedBounds();

UFUNCTION(BlueprintCallable)
void SetEmitterFixedBounds(FName EmitterName, FBox LocalBounds);
```

**우선순위:**

```
Bounds 우선순위:
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  1. Component::SetSystemFixedBounds(FBox)  ← 최우선 (인스턴스별)        │
│      └─ 이 컴포넌트에만 적용되는 고정 경계                               │
│                                                                         │
│  2. Component::SetEmitterFixedBounds(EmitterName, FBox)                │
│      └─ 특정 에미터에만 적용되는 고정 경계                               │
│                                                                         │
│  3. UNiagaraSystem::bFixedBounds = true                                │
│      └─ 시스템 에셋의 고정 경계 (모든 인스턴스 공유)                     │
│                                                                         │
│  4. FVersionedNiagaraEmitterData::CalculateBoundsMode = Fixed         │
│      └─ 에미터 에셋의 고정 경계                                          │
│                                                                         │
│  5. Dynamic Bounds  (기본값)                                            │
│      └─ 매 프레임 파티클 위치 기반 계산                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 생명주기 (Lifecycle)

### Component 생명주기 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│              UNiagaraComponent 생명주기                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [1] 생성 (Creation)                                                    │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  AActor::CreateComponent<UNiagaraComponent>()           │          │
│  │  └─ UNiagaraComponent::OnComponentCreated()             │          │
│  │      └─ 기본 초기화만 (Asset은 아직 null)                │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓                                                               │
│  [2] 등록 (Registration)                                                │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  UNiagaraComponent::OnRegister()                        │          │
│  │  ├─ Asset 유효성 확인                                    │          │
│  │  ├─ OverrideParameters 초기화                           │          │
│  │  │   └─ Asset->ExposedParameters 복사                  │          │
│  │  ├─ SystemInstanceController 생성                       │          │
│  │  └─ Scalability Manager에 등록                          │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ Activate() 호출                                               │
│  [3] 활성화 (Activation)                                                │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  UNiagaraComponent::Activate(bReset)                    │          │
│  │  └─ ActivateInternal(bReset, bIsScalabilityCull=false)  │          │
│  │      ├─ if (!SystemInstanceController)                  │          │
│  │      │   └─ InitializeSystem()                          │          │
│  │      │       ├─ FNiagaraSystemInstance 생성             │          │
│  │      │       ├─ OverrideParameters 전달                 │          │
│  │      │       └─ Warmup 실행 (설정 시)                   │          │
│  │      │                                                   │          │
│  │      ├─ SystemInstanceController->Activate(bReset)      │          │
│  │      │   ├─ SystemSpawnScript 실행 (bReset=true 시)    │          │
│  │      │   └─ EmitterSpawnScripts 실행                    │          │
│  │      │                                                   │          │
│  │      └─ if (bAutoActivate)                              │          │
│  │          └─ 즉시 Tick 시작                               │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ 매 프레임                                                     │
│  [4] Tick 루프 (Update Loop)                                            │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  UNiagaraComponent::TickComponent(DeltaTime)            │          │
│  │  ├─ Scalability 체크                                     │          │
│  │  │   └─ Culling되었으면 Tick 스킵                       │          │
│  │  │                                                       │          │
│  │  ├─ AgeUpdateMode 처리                                  │          │
│  │  │   ├─ TickDeltaTime: 일반 DeltaTime 사용             │          │
│  │  │   ├─ DesiredAge: Seek 로직 실행                     │          │
│  │  │   └─ DesiredAgeNoSeek: Age 직접 설정                │          │
│  │  │                                                       │          │
│  │  ├─ SystemInstanceController->Tick(DeltaTime)           │          │
│  │  │   ├─ SystemUpdateScript 실행                         │          │
│  │  │   ├─ EmitterUpdateScripts 실행                       │          │
│  │  │   ├─ ParticleSpawnScripts 실행                       │          │
│  │  │   ├─ ParticleUpdateScripts 실행                      │          │
│  │  │   └─ Rendering 데이터 준비                           │          │
│  │  │                                                       │          │
│  │  └─ PostSystemTick_GameThread()                         │          │
│  │      └─ if (IsComplete() && bAutoDestroy)               │          │
│  │          └─ OnSystemComplete() → 제거 시작              │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓ Deactivate() 또는 Complete                                   │
│  [5] 비활성화 (Deactivation)                                            │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  UNiagaraComponent::Deactivate()                        │          │
│  │  ├─ 신규 파티클 생성 중단                                │          │
│  │  ├─ 기존 파티클 Lifetime까지 유지                        │          │
│  │  └─ 모든 파티클 소멸 시:                                 │          │
│  │      └─ OnSystemFinished.Broadcast(this)                │          │
│  │          └─ bAutoDestroy? → DestroyComponent()          │          │
│  │                                                          │          │
│  │  또는                                                     │          │
│  │                                                          │          │
│  │  UNiagaraComponent::DeactivateImmediate()               │          │
│  │  └─ 모든 파티클 즉시 제거                                │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓                                                               │
│  [6] 등록 해제 (Unregistration)                                         │
│  ┌──────────────────────────────────────────────────────────┐          │
│  │  UNiagaraComponent::OnUnregister()                      │          │
│  │  ├─ DestroyInstance()                                    │          │
│  │  │   └─ FNiagaraSystemInstance 제거                     │          │
│  │  └─ Scalability Manager에서 등록 해제                   │          │
│  └──────────────────────────────────────────────────────────┘          │
│         │                                                               │
│         ↓                                                               │
│  [7] 소멸 (Destruction)                                                 │
│  └─ GC에 의해 메모리 해제                                                │
│      (또는 Pooling 시 풀에 반환)                                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 💡 실전 예시 (Practical Examples)

### ✅ 좋은 예: 기본적인 이펙트 재생

```cpp
UCLASS()
class AMyExplosionActor : public AActor
{
    GENERATED_BODY()

public:
    // ✅ 이펙트 시스템 에셋
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "VFX")
    TObjectPtr<UNiagaraSystem> ExplosionSystem;

    // ✅ 컴포넌트 (재사용)
    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "VFX")
    TObjectPtr<UNiagaraComponent> ExplosionComp;

protected:
    virtual void BeginPlay() override
    {
        Super::BeginPlay();

        // ✅ 컴포넌트 생성
        ExplosionComp = NewObject<UNiagaraComponent>(this, TEXT("ExplosionVFX"));
        ExplosionComp->SetupAttachment(RootComponent);
        ExplosionComp->RegisterComponent();

        // ✅ 에셋 할당
        ExplosionComp->SetAsset(ExplosionSystem);

        // ✅ 자동 활성화 비활성화 (수동 제어)
        ExplosionComp->SetAutoActivate(false);

        // ✅ Auto Destroy 설정 (one-shot 이펙트)
        ExplosionComp->SetAutoDestroy(true);
    }

public:
    UFUNCTION(BlueprintCallable, Category = "VFX")
    void TriggerExplosion(FVector Location, float Power, FLinearColor Color)
    {
        if (!ExplosionComp) return;

        // ✅ 위치 설정
        ExplosionComp->SetWorldLocation(Location);

        // ✅ 파라미터 설정 (Activate 전에!)
        ExplosionComp->SetVariableFloat(FName("User.ExplosionPower"), Power);
        ExplosionComp->SetVariableLinearColor(FName("User.ExplosionColor"), Color);

        // ✅ 활성화 (bReset=true로 처음부터)
        ExplosionComp->Activate(true);
    }
};
```

### ✅ 좋은 예: UNiagaraFunctionLibrary 사용

```cpp
// ✅ 월드 위치에 one-shot 이펙트 생성
void SpawnHitEffect(UWorld* World, FVector HitLocation, FVector HitNormal)
{
    if (!World || !HitEffectSystem) return;

    // ✅ SpawnSystemAtLocation 사용
    UNiagaraComponent* HitEffect = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
        World,
        HitEffectSystem,
        HitLocation,
        HitNormal.Rotation(),    // 법선 방향 회전
        FVector(1.0f),           // 스케일
        /*bAutoDestroy=*/ true,  // 자동 제거
        /*bAutoActivate=*/ true, // 즉시 활성화
        /*PoolingMethod=*/ ENCPoolMethod::AutoRelease,  // 풀링 사용
        /*bPreCullCheck=*/ true  // Scalability 사전 체크
    );

    // ✅ 파라미터 설정 (필요 시)
    if (HitEffect)
    {
        HitEffect->SetVariableFloat(FName("User.ImpactStrength"), 10.0f);
    }
}

// ✅ 컴포넌트에 부착된 이펙트 생성
void AttachBuffEffect(AActor* TargetActor, UNiagaraSystem* BuffSystem)
{
    if (!TargetActor || !BuffSystem) return;

    USceletalMeshComponent* Mesh = TargetActor->FindComponentByClass<USkeletalMeshComponent>();
    if (!Mesh) return;

    // ✅ SpawnSystemAttached 사용
    UNiagaraComponent* BuffEffect = UNiagaraFunctionLibrary::SpawnSystemAttached(
        BuffSystem,
        Mesh,
        FName("spine_03"),       // 소켓 이름
        FVector::ZeroVector,     // 로컬 오프셋
        FRotator::ZeroRotator,   // 로컬 회전
        EAttachLocation::SnapToTarget,  // 부착 방식
        /*bAutoDestroy=*/ false, // 수동 제거 (버프 해제 시까지 유지)
        /*bAutoActivate=*/ true,
        /*PoolingMethod=*/ ENCPoolMethod::None  // 풀링 안 함 (지속 이펙트)
    );

    // 나중에 버프 해제 시:
    // BuffEffect->Deactivate();
}
```

### ✅ 좋은 예: 동적 파라미터 제어

```cpp
UCLASS()
class AFireActor : public AActor
{
    UPROPERTY(VisibleAnywhere)
    UNiagaraComponent* FireComp;

    UPROPERTY(EditAnywhere)
    float BurnDuration = 10.0f;

private:
    float RemainingTime = 10.0f;

protected:
    virtual void Tick(float DeltaTime) override
    {
        Super::Tick(DeltaTime);

        if (!FireComp || !FireComp->IsActive()) return;

        // ✅ 시간에 따라 강도 감소
        RemainingTime -= DeltaTime;
        float Intensity = FMath::Clamp(RemainingTime / BurnDuration, 0.0f, 1.0f);

        // ✅ 파라미터 업데이트
        FireComp->SetVariableFloat(FName("User.FireIntensity"), Intensity);

        // ✅ SpawnRate 동적 조절
        float SpawnRate = FMath::Lerp(0.0f, 500.0f, Intensity);
        FireComp->SetVariableFloat(FName("User.SpawnRate"), SpawnRate);

        // ✅ 색상 변화
        FLinearColor FireColor = FMath::Lerp(
            FLinearColor(0.5f, 0.1f, 0.0f),  // 어두운 주황색
            FLinearColor(1.0f, 0.8f, 0.0f),  // 밝은 노란색
            Intensity
        );
        FireComp->SetVariableLinearColor(FName("User.FireColor"), FireColor);

        // ✅ 불 꺼짐
        if (RemainingTime <= 0.0f)
        {
            FireComp->Deactivate();  // 부드럽게 종료
            UE_LOG(LogTemp, Log, TEXT("Fire extinguished"));
        }
    }
};
```

### ✅ 좋은 예: Seek을 사용한 시네마틱 제어

```cpp
// ✅ Sequencer에서 Niagara 제어
UCLASS()
class UNiagaraCinematicController : public UObject
{
public:
    UPROPERTY()
    UNiagaraComponent* ControlledEffect;

    void Initialize(UNiagaraComponent* InEffect)
    {
        ControlledEffect = InEffect;

        // ✅ DesiredAge 모드 활성화
        ControlledEffect->SetAgeUpdateMode(ENiagaraAgeUpdateMode::DesiredAge);

        // ✅ Seek 설정
        ControlledEffect->SetSeekDelta(1.0f / 60.0f);  // 60 FPS
        ControlledEffect->SetMaxSimTime(0.1f);  // 프레임당 최대 0.1초
        ControlledEffect->SetCanRenderWhileSeeking(true);  // Seek 중에도 표시

        // ✅ 활성화
        ControlledEffect->Activate(false);
    }

    // ✅ 타임라인 위치 업데이트 (매 프레임 호출)
    void UpdateTimelinePosition(float TimelineTime)
    {
        if (!ControlledEffect) return;

        // ✅ Desired Age 설정
        ControlledEffect->SetDesiredAge(TimelineTime);

        // Niagara가 자동으로 Current Age를 Desired Age로 Seek함
    }

    // ✅ 타임라인 스크러빙 (사용자가 타임라인 드래그)
    void SeekToTime(float TargetTime)
    {
        if (!ControlledEffect) return;

        // ✅ Seek 시작
        ControlledEffect->SeekToDesiredAge(TargetTime);

        // 빠르게 Seek하므로 렌더링 비활성화 고려
        ControlledEffect->SetCanRenderWhileSeeking(false);
    }
};
```

### ❌ 나쁜 예: 매 프레임 컴포넌트 재생성

```cpp
// ❌ 잘못된 방법: 매 프레임 SpawnSystemAtLocation 호출
void BadTick(float DeltaTime)
{
    FVector Location = GetActorLocation();

    // ❌ 메모리 누수! 이전 컴포넌트들이 정리되지 않음
    UNiagaraComponent* Effect = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
        GetWorld(),
        SomeSystem,
        Location
    );
    // 수백 개의 Actor/Component가 메모리에 남음!
}
```

**올바른 방법:**

```cpp
// ✅ 올바른 방법: 컴포넌트 재사용
UPROPERTY()
UNiagaraComponent* CachedEffect;

void BeginPlay()
{
    // ✅ 한 번만 생성
    CachedEffect = NewObject<UNiagaraComponent>(this);
    CachedEffect->SetAsset(SomeSystem);
    CachedEffect->SetAutoActivate(false);
    CachedEffect->RegisterComponent();
}

void GoodTick(float DeltaTime)
{
    // ✅ 위치만 업데이트
    CachedEffect->SetWorldLocation(GetActorLocation());

    // ✅ 필요할 때만 활성화
    if (ShouldShowEffect() && !CachedEffect->IsActive())
    {
        CachedEffect->Activate(true);
    }
}
```

### ❌ 나쁜 예: SetAsset 후 바로 파라미터 설정

```cpp
// ❌ 잘못된 방법: SetAsset 후 즉시 파라미터 설정
void BadSetup()
{
    NiagaraComp->SetAsset(NewSystem, /*bResetParams=*/ true);

    // ❌ OverrideParameters가 아직 초기화 안 됨!
    NiagaraComp->SetVariableFloat(FName("User.Size"), 10.0f);  // 무시됨!
}
```

**올바른 방법:**

```cpp
// ✅ 올바른 방법 1: SetAsset 완료 후 파라미터 설정
void GoodSetup1()
{
    NiagaraComp->SetAsset(NewSystem, /*bResetParams=*/ true);

    // ✅ 다음 프레임 또는 Tick 이후에 파라미터 설정
    GetWorld()->GetTimerManager().SetTimerForNextTick([this]()
    {
        NiagaraComp->SetVariableFloat(FName("User.Size"), 10.0f);
    });
}

// ✅ 올바른 방법 2: Activate 전에 파라미터 설정
void GoodSetup2()
{
    NiagaraComp->SetAsset(NewSystem);
    NiagaraComp->SetAutoActivate(false);

    // ✅ Activate 전에 파라미터 설정
    NiagaraComp->SetVariableFloat(FName("User.Size"), 10.0f);
    NiagaraComp->Activate(true);
}
```

---

## 📊 성능 최적화 (Performance Optimization)

### Component 레벨 최적화

**1. Pooling 사용:**

```cpp
// ✅ 풀링으로 성능 향상
UNiagaraComponent* Effect = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
    World,
    System,
    Location,
    FRotator::ZeroRotator,
    FVector(1.0f),
    /*bAutoDestroy=*/ true,
    /*bAutoActivate=*/ true,
    /*PoolingMethod=*/ ENCPoolMethod::AutoRelease  // ← 풀링
);

// 성능 측정:
// - 풀링 없음: ~0.5ms per spawn
// - 풀링 사용: ~0.05ms per spawn (10배 빠름)
```

**2. Tick Behavior 최적화:**

```cpp
// ✅ 물리 이후 Tick (Transform 안정화)
NiagaraComp->SetTickBehavior(ENiagaraTickBehavior::UsePrereqs);

// ✅ 최대한 빨리 Tick (입력 반응성)
NiagaraComp->SetTickBehavior(ENiagaraTickBehavior::ForceTickFirst);

// ✅ 최대한 늦게 Tick (다른 시스템 결과 반영)
NiagaraComp->SetTickBehavior(ENiagaraTickBehavior::ForceTickLast);
```

**3. Scalability Pre-Cull:**

```cpp
// ✅ 생성 전에 Scalability 체크
UNiagaraComponent* Effect = UNiagaraFunctionLibrary::SpawnSystemAtLocation(
    World,
    System,
    Location,
    FRotator::ZeroRotator,
    FVector(1.0f),
    true,
    true,
    ENCPoolMethod::None,
    /*bPreCullCheck=*/ true  // ← Scalability 사전 체크
);

// nullptr 반환 가능 (Culling됨)
if (!Effect)
{
    UE_LOG(LogTemp, Log, TEXT("Effect was culled by scalability"));
}
```

**4. Rendering 선택적 비활성화:**

```cpp
// ✅ 시뮬레이션은 유지, 렌더링만 비활성화
NiagaraComp->SetRenderingEnabled(false);

// 사용 사례:
// - 화면 밖 오브젝트 (소리/로직은 유지, 렌더링 스킵)
// - 디버깅 (시뮬레이션 확인, 시각 제거)
```

### stat 명령어

```
콘솔 명령어:

stat Niagara
  - Component별 Tick Time 확인
  - 총 Active Component 개수

stat NiagaraComponents
  - 개별 컴포넌트 상세 통계
  - Auto Destroy 카운트
  - Pooling 효율

fx.Niagara.Debug.DrawSystemBounds 1
  - 컴포넌트 Bounds 시각화
```

---

## 🔧 디버깅 및 트러블슈팅 (Debugging & Troubleshooting)

### 흔한 에러와 해결 방법

**1. "Component has no asset"**

```
원인: SetAsset 호출 안 됨

해결책:
- BeginPlay에서 SetAsset 호출 확인
- Asset이 null이 아닌지 확인
```

**2. "System not activating"**

```
원인: Scalability Culling

해결책:
1. Effect Type 설정 확인
2. fx.Niagara.QualityLevel 콘솔 변수 확인
3. NumInstancesToActivate 증가
4. bPreCullCheck=false로 강제 생성
```

**3. "Parameters not updating"**

```
원인: SetVariable 호출 시점 문제

해결책:
- SetAsset 이후 최소 1프레임 대기
- Activate 전에 파라미터 설정
- OverrideParameters 직접 확인:
  bool bIsValid;
  float Value = NiagaraComp->GetVariableFloat(FName("User.Size"), bIsValid);
```

**4. "Memory leak with auto destroy"**

```
원인: 루프 이펙트에 bAutoDestroy=true 설정

해결책:
- 루프 이펙트는 bAutoDestroy=false 사용
- 수동으로 Deactivate 호출
- 또는 Duration 설정하여 자동 종료
```

**5. "Component not following actor"**

```
원인: AttachToComponent 안 됨

해결책:
// ✅ 부착 확인
NiagaraComp->SetupAttachment(RootComponent);
NiagaraComp->RegisterComponent();

// 또는
NiagaraComp->AttachToComponent(
    TargetComponent,
    FAttachmentTransformRules::SnapToTargetIncludingScale
);
```

### Niagara Debugger 활용

```
Window → Niagara Debugger

주요 기능:
1. Active Components 목록
2. 컴포넌트별 Particle Count
3. Tick Time (Component vs System)
4. OverrideParameters 값 확인
5. Auto Destroy 상태
6. Pooling 통계
```

---

## 🔗 관련 문서 (Related Documents)

- **[Overview.md](../Overview.md)** - Niagara 시스템 전체 개요
- **[NiagaraSystem.md](NiagaraSystem.md)** - 시스템 에셋 (ExposedParameters, Warmup)
- **[NiagaraEmitter.md](NiagaraEmitter.md)** - 에미터 (SimTarget, Scripts)
- **[DataInterface.md](DataInterface.md)** - DataInterface 시스템

---

## 📚 참고 자료 (References)

### 소스 파일

- `Engine/Plugins/FX/Niagara/Source/Niagara/Public/NiagaraComponent.h` (600+ 라인)
- `Engine/Plugins/FX/Niagara/Source/Niagara/Public/NiagaraFunctionLibrary.h` (144 라인)
- `Engine/Plugins/FX/Niagara/Source/Niagara/Private/NiagaraComponent.cpp`

### 공식 문서

- [Niagara Component Reference](https://docs.unrealengine.com/5.7/en-US/niagara-component-reference/)
- [Spawning Niagara Effects](https://docs.unrealengine.com/5.7/en-US/spawning-niagara-effects/)

### 주요 API

```cpp
// UNiagaraComponent (주요 메서드)
void SetAsset(UNiagaraSystem* InAsset, bool bResetExistingOverrideParameters = true);
UNiagaraSystem* GetAsset() const;

void Activate(bool bReset = false);
void Deactivate();
void DeactivateImmediate();
void ResetSystem();
void ReinitializeSystem();

bool IsActive() const;
bool IsComplete() const;

void SetVariableFloat(FName InVariableName, float InValue);
void SetVariableVec3(FName InVariableName, FVector InValue);
void SetVariableLinearColor(FName InVariableName, const FLinearColor& InValue);
void SetVariableBool(FName InVariableName, bool InValue);
void SetVariableActor(FName InVariableName, AActor* Actor);

float GetVariableFloat(FName InVariableName, bool& bIsValid) const;
FVector GetVariableVec3(FName InVariableName, bool& bIsValid) const;

void SetSystemFixedBounds(FBox LocalBounds);
void SetEmitterFixedBounds(FName EmitterName, FBox LocalBounds);

void SetTickBehavior(ENiagaraTickBehavior NewTickBehavior);
void SetAutoDestroy(bool bInAutoDestroy);
void SetRenderingEnabled(bool bInRenderingEnabled);
void SetPaused(bool bInPaused);

void SetAgeUpdateMode(ENiagaraAgeUpdateMode InAgeUpdateMode);
void SetDesiredAge(float InDesiredAge);
void SeekToDesiredAge(float InDesiredAge);

void AdvanceSimulation(int32 TickCount, float TickDeltaSeconds);
void AdvanceSimulationByTime(float SimulateTime, float TickDeltaSeconds);

FNiagaraSystemInstanceControllerPtr GetSystemInstanceController();

// UNiagaraFunctionLibrary (유틸리티 함수)
static UNiagaraComponent* SpawnSystemAtLocation(
    const UObject* WorldContextObject,
    UNiagaraSystem* SystemTemplate,
    FVector Location,
    FRotator Rotation = FRotator::ZeroRotator,
    FVector Scale = FVector(1.f),
    bool bAutoDestroy = true,
    bool bAutoActivate = true,
    ENCPoolMethod PoolingMethod = ENCPoolMethod::None,
    bool bPreCullCheck = true
);

static UNiagaraComponent* SpawnSystemAttached(
    UNiagaraSystem* SystemTemplate,
    USceneComponent* AttachToComponent,
    FName AttachPointName,
    FVector Location,
    FRotator Rotation,
    EAttachLocation::Type LocationType,
    bool bAutoDestroy,
    bool bAutoActivate = true,
    ENCPoolMethod PoolingMethod = ENCPoolMethod::None,
    bool bPreCullCheck = true
);

static void OverrideSystemUserVariableStaticMeshComponent(
    UNiagaraComponent* NiagaraSystem,
    const FString& OverrideName,
    UStaticMeshComponent* StaticMeshComponent
);

static void OverrideSystemUserVariableSkeletalMeshComponent(
    UNiagaraComponent* NiagaraSystem,
    const FString& OverrideName,
    USkeletalMeshComponent* SkeletalMeshComponent
);

static void SetTextureObject(
    UNiagaraComponent* NiagaraSystem,
    const FString& OverrideName,
    UTexture* Texture
);
```

---

> 🔄 **작성일**: 2025-11-10
> 📝 **문서 버전**: v1.0
> ✅ **소스 검증**: UE 5.7.0
> 📂 **다음 문서**: [DataInterface.md](DataInterface.md)
