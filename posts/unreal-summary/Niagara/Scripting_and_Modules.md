---
title: "나이아가라 스크립팅 시스템 및 모듈"
date: "2025-11-21"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "Niagara"
tags: ["unreal", "Niagara"]
---
# 나이아가라 스크립팅 시스템 및 모듈

## 🧭 개요

나이아가라의 스크립팅 시스템은 **노드 기반 비주얼 프로그래밍 환경**과 **모듈화된 재사용 가능한 함수 시스템**을 제공합니다. 이 시스템은 컴파일 시점에 VectorVM 바이트코드 또는 GPU HLSL로 변환되어 실행됩니다.

**핵심 철학:**
> **그래프(Graph)**는 시각적 스크립팅 인터페이스를 제공하고,
> **스크립트(Script)**는 컴파일된 실행 가능 데이터를 담으며,
> **모듈(Module)**은 재사용 가능한 기능 단위를 제공한다.

**📂 위치:**
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/NiagaraGraph.h`
- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraScript.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/NiagaraNodeFunctionCall.h`

---

## 🧱 아키텍처 계층 구조

### 전체 시스템 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   나이아가라 스크립팅 시스템                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────┐   ┌──────────────────────┐                   │
│  │  에디터 레이어        │   │  런타임 레이어        │                   │
│  ├──────────────────────┤   ├──────────────────────┤                   │
│  │  UNiagaraGraph       │──>│  UNiagaraScript      │                   │
│  │  (비주얼 편집)        │   │  (실행 데이터)        │                   │
│  │                      │   │                      │                   │
│  │  - 노드 연결         │   │  - VM 바이트코드     │                   │
│  │  - 파라미터 관리      │   │  - GPU 셰이더        │                   │
│  │  - 타입 추론         │   │  - 파라미터 바인딩    │                   │
│  └──────────┬───────────┘   └──────────┬───────────┘                   │
│             │                          │                               │
│             │ 컴파일 (Compilation)       │ 실행 (Execution)              │
│             ↓                          ↓                               │
│  ┌─────────────────────────────────────────────────────┐               │
│  │  FNiagaraVMExecutableData (VM) / Shader (GPU)       │               │
│  └─────────────────────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📐 1. 나이아가라 그래프 (UNiagaraGraph)

### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           UNiagaraGraph                                 │
│  (비주얼 스크립팅 그래프 - 노드 기반 편집)                                │
├─────────────────────────────────────────────────────────────────────────┤
│  상속: UEdGraph (언리얼 표준 그래프)                                     │
│                                                                         │
│  Private 핵심 멤버:                                                      │
│    - TArray<UEdGraphNode*> Nodes                  // 그래프 내 노드들   │
│    - FGuid ChangeId                               // 변경 추적 ID       │
│    - TArray<FNiagaraGraphScriptUsageInfo> CachedUsageInfo               │
│                                   // 스크립트 사용 타입별 컴파일 정보     │
│    - TMap<FNiagaraVariable, UNiagaraScriptVariable*> VariableToScriptVariable │
│                                   // 파라미터 메타데이터 저장소          │
│    - TObjectPtr<UHierarchyRoot> ParameterHierarchyRoot                  │
│                                   // 파라미터 계층 구조 (5.7+)          │
│                                                                         │
│  Public 핵심 메서드:                                                     │
│    + FindOutputNode(ENiagaraScriptUsage) : UNiagaraNodeOutput*          │
│                                   // 특정 Usage의 출력 노드 찾기        │
│    + BuildTraversal(TArray<UNiagaraNode*>&, ENiagaraScriptUsage)        │
│                                   // 노드 순회 순서 구축               │
│    + GetParameters(TArray<FNiagaraVariable>& Inputs/Outputs)            │
│                                   // 함수 입출력 파라미터 추출          │
│    + GetCompileDataHash(ENiagaraScriptUsage) : FNiagaraCompileHash      │
│                                   // 컴파일 해시 (DDC 키)              │
│    + AddParameter(FNiagaraVariable) : UNiagaraScriptVariable*           │
│                                   // 새 파라미터 추가                  │
│    + RenameParameter(FNiagaraVariable, FName)     // 파라미터 이름 변경 │
│    + NotifyGraphNeedsRecompile()                  // 재컴파일 요청      │
└─────────────────────────────────────────────────────────────────────────┘
                                  ▲
                                  │ 소유
                                  │
┌─────────────────────────────────────────────────────────────────────────┐
│                      UNiagaraScriptSource                               │
│  (그래프와 스크립트를 연결하는 브리지)                                     │
├─────────────────────────────────────────────────────────────────────────┤
│  멤버:                                                                  │
│    - TObjectPtr<UNiagaraGraph> NodeGraph          // 그래프 참조         │
│                                                                         │
│  핵심 역할:                                                             │
│    + ComputeVMCompilationId() - 컴파일 ID 계산                          │
│    + GetChangeID() - 그래프 변경 추적                                   │
│    + IsSynchronized() - 스크립트와 동기화 상태 확인                      │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/NiagaraGraph.h`

### 그래프의 핵심 책임

| 책임 | 설명 | 예시 |
|------|------|------|
| **노드 연결 관리** | 노드 간 데이터 흐름 추적 | 함수 호출 체인, 파라미터 맵 읽기/쓰기 |
| **파라미터 생명주기** | 변수 생성/이름변경/삭제 | Module 입력, Static Switch 관리 |
| **타입 추론** | 연결된 핀의 타입 일치 검증 | Numeric 타입 자동 변환 |
| **컴파일 해시 생성** | DDC(Derived Data Cache) 키 | 그래프 변경 시 해시 무효화 |

### 그래프 순회 (Traversal)

```
[Output Node] ← [Module 3] ← [Module 2] ← [Module 1] ← [Input Node]
      │                                                        ▲
      │ BuildTraversal()                                      │
      └────────────────────────────────────────────────────────┘
                    역방향 순회 (Reverse Order)
```

**소스 검증:**
```cpp
// NiagaraGraph.h:262
void BuildTraversal(TArray<class UNiagaraNode*>& OutNodesTraversed,
                    ENiagaraScriptUsage TargetUsage,
                    FGuid TargetUsageId,
                    bool bEvaluateStaticSwitches = false) const;
```

---

## 📐 2. 스크립트 유형과 실행 시점

### 스크립트 Usage 타입

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       ENiagaraScriptUsage                               │
│  (스크립트가 실행되는 시점과 컨텍스트를 정의)                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  System Level:                                                          │
│    - SystemSpawnScript          // 시스템 생성 시 1회 실행              │
│    - SystemUpdateScript         // 매 프레임 실행                       │
│                                                                         │
│  Emitter Level:                                                         │
│    - EmitterSpawnScript         // 이미터 생성 시 1회 실행              │
│    - EmitterUpdateScript        // 매 프레임 실행                       │
│                                                                         │
│  Particle Level (CPU/GPU):                                              │
│    - ParticleSpawnScript        // 새 파티클 생성 시                    │
│    - ParticleUpdateScript       // 매 프레임 모든 파티클 업데이트        │
│    - ParticleEventScript        // 이벤트 발생 시 (충돌, 사망 등)       │
│    - ParticleSimulationStageScript  // 커스텀 시뮬레이션 스테이지        │
│    - ParticleGPUComputeScript   // GPU 시뮬레이션 (통합 스크립트)        │
│                                                                         │
│  Standalone (재사용 가능):                                              │
│    - Module                     // 스택에 추가 가능한 모듈               │
│    - Function                   // 헬퍼 함수 (내부 호출용)              │
│    - DynamicInput               // 입력 값 계산 (단일 출력)             │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/Niagara/Public/NiagaraCommon.h`

### 실행 타임라인

```
게임 시작
   │
   ├─> [System Spawn]  ────────────────┐
   │                                   │
   │                        └─> [Emitter Spawn]
   │                                   │
프레임 루프 시작                         │
   │                                   │
   ├─> [System Update] ────────────────┤
   │                                   │
   │                        ├─> [Emitter Update]
   │                        │          │
   │                        │   ┌──────┴──────┐
   │                        │   │             │
   │                        │   ▼             ▼
   │                        [Particle Spawn] [Particle Update]
   │                                   │
   │                                   ├─> [Event Script]
   │                                   │    (조건부 실행)
   │                                   │
   │                                   └─> [Simulation Stage]
   │                                        (다중 패스 가능)
   │
프레임 종료
```

**핵심 차이점:**

| 타입 | 실행 빈도 | 인스턴스 수 | 데이터 접근 |
|------|-----------|------------|------------|
| **System Spawn/Update** | 1회 / 매 프레임 | 1개 | System Parameters |
| **Emitter Spawn/Update** | 1회 / 매 프레임 | Emitter당 1개 | Emitter + System |
| **Particle Spawn/Update** | N번 / 매 프레임 | 파티클 수만큼 | Particle + Emitter + System |
| **Event Script** | 이벤트 발생 시 | 이벤트 수만큼 | Event Data + Particle |

---

## 📐 3. UNiagaraScript - 실행 가능 데이터

### 클래스 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           UNiagaraScript                                │
│  (컴파일된 실행 가능한 스크립트)                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  상속: UNiagaraScriptBase, FNiagaraVersionedObject                      │
│                                                                         │
│  핵심 멤버:                                                             │
│    - ENiagaraScriptUsage Usage                    // 스크립트 타입       │
│    - FGuid UsageId                                // Usage 고유 ID       │
│    - FNiagaraParameterStore RapidIterationParameters                    │
│                                   // 런타임 수정 가능 파라미터           │
│    - FNiagaraVMExecutableData CachedScriptVM                            │
│                                   // CPU VM 바이트코드 + 메타데이터      │
│    - TSharedPtr<FNiagaraShaderScript> ScriptResource                    │
│                                   // GPU 셰이더 리소스                   │
│    - TArray<FVersionedNiagaraScriptData> VersionData                    │
│                                   // 버전별 스크립트 데이터 (에디터 전용) │
│                                                                         │
│  핵심 메서드:                                                           │
│    + GetVMExecutableData() : FNiagaraVMExecutableData&                  │
│    + GetRenderThreadScript() : FNiagaraShaderScript*                    │
│    + CanBeRunOnGpu() : bool                       // GPU 실행 가능 여부  │
│    + IsReadyToRun(ENiagaraSimTarget) : bool       // 실행 준비 완료      │
│    + RequestCompile(FGuid ScriptVersion, bool bForceCompile)            │
│    + SetVMCompilationResults(...)                 // VM 컴파일 결과 설정 │
│    + SetComputeCompilationResults(...)            // GPU 컴파일 결과 설정│
└─────────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ 포함
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     FNiagaraVMExecutableData                            │
│  (VectorVM 실행을 위한 바이트코드 및 메타데이터)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  멤버:                                                                  │
│    - FNiagaraVMExecutableByteCode ByteCode        // VM 바이트코드       │
│    - int32 NumTempRegisters                       // 임시 레지스터 수    │
│    - TArray<uint8> ScriptLiterals                 // 상수 데이터         │
│    - TArray<FNiagaraVariableBase> Attributes      // 읽기/쓰기 속성      │
│    - TArray<FVMExternalFunctionBindingInfo> CalledVMExternalFunctions   │
│                                   // 외부 함수 바인딩 (Data Interface)   │
│    - TArray<FNiagaraScriptDataInterfaceCompileInfo> DataInterfaceInfo   │
│                                   // DI 메타데이터                       │
│    - FNiagaraShaderScriptParametersMetadata ShaderScriptParametersMetadata │
│                                   // GPU 파라미터 정보                   │
│    - ENiagaraScriptCompileStatus LastCompileStatus                      │
│                                                                         │
│  에디터 전용:                                                           │
│    - FString LastHlslTranslation                  // CPU용 HLSL         │
│    - FString LastHlslTranslationGPU               // GPU용 HLSL         │
│    - TArray<FNiagaraCompileEvent> LastCompileEvents // 컴파일 에러/경고  │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraScript.h:774-1475`

### 스크립트 버전 관리 (Versioning)

```
┌───────────────────────────────────────────────────────────────┐
│                  UNiagaraScript (Asset)                       │
├───────────────────────────────────────────────────────────────┤
│  - bVersioningEnabled = true                                  │
│  - ExposedVersion = FGuid(v2.0)                               │
│                                                               │
│  TArray<FVersionedNiagaraScriptData>:                         │
│  ┌─────────────────────────────────────┐                     │
│  │  Version 1.0 (FGuid: xxx-xxx...)    │                     │
│  │  - Category: "Velocity"             │                     │
│  │  - bDeprecated: false               │                     │
│  │  - Source: UNiagaraGraph*           │                     │
│  └─────────────────────────────────────┘                     │
│  ┌─────────────────────────────────────┐                     │
│  │  Version 2.0 (FGuid: yyy-yyy...)  ◄── Exposed            │
│  │  - VersionChangeDescription: "성능 개선" │                 │
│  │  - PythonUpdateScript: "..."        │                     │
│  │  - Source: UNiagaraGraph*           │                     │
│  └─────────────────────────────────────┘                     │
│  ┌─────────────────────────────────────┐                     │
│  │  Version 3.0 (FGuid: zzz-zzz...)    │                     │
│  │  - bExperimental: true              │                     │
│  │  - Source: UNiagaraGraph*  (테스트용)│                     │
│  └─────────────────────────────────────┘                     │
└───────────────────────────────────────────────────────────────┘

    사용자가 모듈 추가 시 → Version 2.0 사용
    기존 Emitter → Version 1.0 유지 (호환성)
```

**버전 업그레이드 프로세스:**
```cpp
// NiagaraNodeFunctionCall.h:146
void ChangeScriptVersion(FGuid NewScriptVersion,
                         const FNiagaraScriptVersionUpgradeContext& UpgradeContext,
                         bool bShowNotesInStack = false);
```

---

## 📐 4. 노드 타입과 역할

### 핵심 노드 클래스 계층

```
                        UEdGraphNode
                             │
                             ▼
                      UNiagaraNode
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                  ▼                  ▼
  UNiagaraNodeInput  UNiagaraNodeOutput  UNiagaraNodeWithDynamicPins
                                                 │
                      ┌──────────────────────────┼──────────────────┐
                      │                          │                  │
                      ▼                          ▼                  ▼
          UNiagaraNodeFunctionCall   UNiagaraNodeParameterMapGet   UNiagaraNodeParameterMapSet
          (모듈/함수 호출)            (파라미터 읽기)               (파라미터 쓰기)
```

### 1) UNiagaraNodeOutput

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      UNiagaraNodeOutput                                 │
│  (스크립트의 출력 노드 - 1개의 그래프에 여러 개 존재 가능)                 │
├─────────────────────────────────────────────────────────────────────────┤
│  멤버:                                                                  │
│    - TArray<FNiagaraVariable> Outputs     // 출력 변수 목록             │
│    - ENiagaraScriptUsage ScriptType       // Particle Spawn/Update 등   │
│    - FGuid ScriptTypeId                   // Usage 인스턴스 ID           │
│                                                                         │
│  역할:                                                                  │
│    - 그래프 순회의 시작점 (역방향)                                      │
│    - 특정 Usage 타입의 출력 정의                                        │
│    - 예: Particle.Position, Particle.Velocity 등을 외부로 노출          │
│                                                                         │
│  예시:                                                                  │
│    [Particle Update Output]                                             │
│      ◄─ Particle.Position (Vector3)                                    │
│      ◄─ Particle.Velocity (Vector3)                                    │
│      ◄─ Particle.Color (LinearColor)                                   │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/NiagaraNodeOutput.h:14-85`

### 2) UNiagaraNodeFunctionCall

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   UNiagaraNodeFunctionCall                              │
│  (모듈/함수 호출 노드 - 재사용 가능한 기능 단위)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  멤버:                                                                  │
│    - TObjectPtr<UNiagaraScript> FunctionScript  // 참조 스크립트         │
│    - FGuid SelectedScriptVersion                // 사용 중인 버전        │
│    - FNiagaraFunctionSignature Signature        // 함수 시그니처         │
│    - TArray<FNiagaraPropagatedVariable> PropagatedStaticSwitchParameters │
│                                   // Static Switch 전파                 │
│    - TMap<FGuid, FName> BoundPinNames           // 오버라이드된 입력     │
│                                                                         │
│  핵심 메서드:                                                           │
│    + GetCalledGraph() : UNiagaraGraph*          // 내부 그래프 접근      │
│    + FindParameterMapDefaultValuePin(FName) : UEdGraphPin*              │
│                                   // 기본값 핀 검색                     │
│    + ChangeScriptVersion(FGuid)                 // 버전 전환            │
│    + CleanupPropagatedSwitchValues()            // Static Switch 정리   │
│                                                                         │
│  예시 (Add Velocity 모듈):                                              │
│    입력 핀:                                                             │
│      ► Velocity (Vector3)  [기본값: (0,0,10)]                          │
│      ► Scale (Float)       [기본값: 1.0]                               │
│    출력 핀: (없음 - Particle.Velocity를 직접 수정)                       │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/NiagaraNodeFunctionCall.h:54-261`

**소스 검증:**
```cpp
// NiagaraNodeFunctionCall.h:62
UPROPERTY(EditAnywhere, Category = "Function")
TObjectPtr<UNiagaraScript> FunctionScript;

// NiagaraNodeFunctionCall.h:150
UNiagaraGraph* GetCalledGraph() const;
```

### 3) Parameter Map 노드

```
┌─────────────────────────────────────────────────────────────────────────┐
│           UNiagaraNodeParameterMapGet / Set                             │
│  (Parameter Map에서 변수 읽기/쓰기)                                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [Map Get 노드]                    [Map Set 노드]                       │
│  ┌─────────────────────┐           ┌─────────────────────┐             │
│  │ ► Map In            │           │ ◄ Map Out           │             │
│  │ ◄ Module.MyValue    │           │ ► Map In            │             │
│  │   ► Default (1.0)   │           │ ► Module.MyValue    │             │
│  └─────────────────────┘           └─────────────────────┘             │
│         │                                      ▲                        │
│         │ 파라미터 읽기                         │ 파라미터 쓰기            │
│         ▼                                      │                        │
│  Parameter Map                                 │                        │
│  {                                             │                        │
│    Module.MyValue = 1.0                        │                        │
│    Particles.Position = (0,0,0)  ◄─────────────┘                        │
│    Emitter.Age = 2.5                                                   │
│  }                                                                     │
└─────────────────────────────────────────────────────────────────────────┘
```

**Parameter Map의 핵심 개념:**
- 모든 변수는 **네임스페이스**를 가짐: `Particles.`, `Emitter.`, `Module.`, `User.` 등
- **Map Get**: 기본값을 제공하며, 값이 없으면 Default 사용
- **Map Set**: 값을 Parameter Map에 기록
- 컴파일 시 실제 데이터셋 읽기/쓰기로 변환

**📂 위치:**
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Private/NiagaraNodeParameterMapGet.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Private/NiagaraNodeParameterMapSet.h`

---

## 📐 5. Dynamic Input과 모듈 재사용성

### Dynamic Input 개념

**Dynamic Input**은 단일 출력 값을 계산하는 재사용 가능한 서브그래프입니다.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      Dynamic Input 예시                                │
│                  "Random Vector in Cone"                                │
├─────────────────────────────────────────────────────────────────────────┤
│  Usage: ENiagaraScriptUsage::DynamicInput                              │
│                                                                         │
│  입력:                                                                  │
│    - Cone Angle (Float)                                                │
│    - Cone Vector (Vector3)                                             │
│    - Random Seed (Int)                                                 │
│                                                                         │
│  출력:                                                                  │
│    - Result (Vector3)  ◄── 단일 출력!                                  │
│                                                                         │
│  내부 그래프:                                                           │
│    [Random Float] → [Sin/Cos] → [Scale] → [Rotate] → [Output]          │
└─────────────────────────────────────────────────────────────────────────┘
```

**사용 예시:**
```
모듈: "Set Velocity"
  입력: Velocity (Vector3)
    └─► 연결: "Random Vector in Cone" (Dynamic Input)
              - Cone Angle = 30.0
              - Cone Vector = (0, 0, 1)
```

**Module vs Dynamic Input vs Function:**

| 타입 | 출력 수 | 스택 표시 | 주 용도 | 예시 |
|------|---------|-----------|---------|------|
| **Module** | 다중 (Map Set) | 스택에 항목으로 표시 | 파티클 상태 변경 | Add Velocity, Scale Color |
| **Dynamic Input** | 단일 값 | 핀 입력으로 연결 | 값 계산 | Random Float, Curl Noise |
| **Function** | 다중 가능 | 직접 노출 안 됨 | 헬퍼 함수 | Vector Cross Product |

---

## 📐 6. Scratch Pad - 인라인 스크립트 편집

**Scratch Pad**는 System/Emitter 내에 임베디드된 스크립트를 편집하는 기능입니다.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        NiagaraScratchPadContainer                       │
│  (System/Emitter에 포함된 임시 스크립트 저장소)                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────────────────────────────────┐                      │
│  │  Scratch Script 1: "Custom Velocity Logic"   │                      │
│  │  - FGuid: xxx-xxx-xxx-xxx                    │                      │
│  │  - UNiagaraScript*                           │                      │
│  │  - Usage: Module                             │                      │
│  └──────────────────────────────────────────────┘                      │
│  ┌──────────────────────────────────────────────┐                      │
│  │  Scratch Script 2: "Debug Color Override"    │                      │
│  │  - FGuid: yyy-yyy-yyy-yyy                    │                      │
│  │  - UNiagaraScript*                           │                      │
│  │  - Usage: DynamicInput                       │                      │
│  └──────────────────────────────────────────────┘                      │
│                                                                         │
│  특징:                                                                  │
│    - 애셋으로 분리하지 않고 인라인 편집                                  │
│    - 빠른 프로토타이핑 및 디버깅                                         │
│    - 나중에 별도 애셋으로 승격 가능                                      │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraScratchPadContainer.h`

---

## 📐 7. Niagara Virtual Machine (VectorVM)

### VectorVM 실행 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        VectorVM 실행 흐름                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  FNiagaraScriptExecutionContext                                         │
│  ┌───────────────────────────────────────────┐                         │
│  │ - VectorVM::Runtime::FVectorVMState*      │                         │
│  │ - TArray<const FVMExternalFunction*>      │  ◄─ 외부 함수 테이블     │
│  │ - FNiagaraScriptInstanceParameterStore    │  ◄─ 파라미터 바인딩     │
│  └─────────────────┬─────────────────────────┘                         │
│                    │ Execute()                                          │
│                    ▼                                                    │
│  ┌─────────────────────────────────────────────────────────┐           │
│  │  VectorVM::Exec()                                       │           │
│  │  ┌───────────────────────────────────────────────────┐  │           │
│  │  │  바이트코드 루프                                   │  │           │
│  │  │  ┌─────────────────────────────────────────────┐  │  │           │
│  │  │  │ EVectorVMOp::add                            │  │  │           │
│  │  │  │   - Reg[2] = Reg[0] + Reg[1]                │  │  │           │
│  │  │  ├─────────────────────────────────────────────┤  │  │           │
│  │  │  │ EVectorVMOp::mul                            │  │  │           │
│  │  │  │   - Reg[3] = Reg[2] * Constant[0]           │  │  │           │
│  │  │  ├─────────────────────────────────────────────┤  │  │           │
│  │  │  │ EVectorVMOp::external_func_call             │  │  │           │
│  │  │  │   → DataInterface->SampleTexture(...)       │  │  │           │
│  │  │  ├─────────────────────────────────────────────┤  │  │           │
│  │  │  │ EVectorVMOp::outputdata_float               │  │  │           │
│  │  │  │   - DataSet[Index].Position = Reg[5]        │  │  │           │
│  │  │  └─────────────────────────────────────────────┘  │  │           │
│  │  └───────────────────────────────────────────────────┘  │           │
│  └─────────────────────────────────────────────────────────┘           │
│                                                                         │
│  특징:                                                                  │
│    - SIMD 최적화 (4-wide 벡터 연산)                                     │
│    - 청크 단위 실행 (배치 처리)                                          │
│    - 외부 함수 호출 (Data Interface)                                    │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Source/Runtime/VectorVM/Public/VectorVM.h`

**OpCode 예시:**
```cpp
// VectorVM.h:52-97
enum class EVectorVMOp : uint8
{
    done,                   // 0 - 실행 종료
    add,                    // 1 - Float 덧셈
    sub,                    // 2 - Float 뺄셈
    mul,                    // 3 - Float 곱셈
    mad,                    // 5 - Multiply-Add
    external_func_call,     // 87 - Data Interface 함수 호출
    outputdata_float,       // 83 - 결과를 DataSet에 쓰기
    // ... 총 178개 OpCode
};
```

### VectorVM 실행 컨텍스트

```
┌─────────────────────────────────────────────────────────────────────────┐
│                FNiagaraScriptExecutionContextBase                       │
│  (스크립트 실행을 위한 런타임 컨텍스트)                                  │
├─────────────────────────────────────────────────────────────────────────┤
│  멤버:                                                                  │
│    - UNiagaraScript* Script                       // 실행할 스크립트    │
│    - VectorVM::Runtime::FVectorVMState* VectorVMState                   │
│                                   // VM 실행 상태                       │
│    - TArray<const FVMExternalFunction*> FunctionTable                   │
│                                   // 외부 함수 포인터 테이블             │
│    - TArray<void*> UserPtrTable                   // Data Interface 인스턴스 │
│    - FNiagaraScriptInstanceParameterStore Parameters                    │
│                                   // 파라미터 데이터                    │
│    - TArray<FNiagaraDataSetExecutionInfo> DataSetInfo                   │
│                                   // 입출력 DataSet 정보                │
│                                                                         │
│  핵심 메서드:                                                           │
│    + Execute(FNiagaraSystemInstance*, float DeltaSeconds, uint32 NumInstances) │
│    + BindData(int32 Index, FNiagaraDataSet&)      // DataSet 바인딩     │
│    + PostTick()                                   // Data Interface 정리 │
└─────────────────────────────────────────────────────────────────────────┘
```

**📂 위치:** `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraScriptExecutionContext.h:128-191`

**소스 검증:**
```cpp
// NiagaraScriptExecutionContext.h:132
VectorVM::Runtime::FVectorVMState* VectorVMState = nullptr;

// NiagaraScriptExecutionContext.h:135
TArray<const FVMExternalFunction*> FunctionTable;
```

---

## 📐 8. GPU 스크립트와 HLSL 변환

### GPU 컴파일 파이프라인

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   GPU 스크립트 컴파일 파이프라인                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────────┐                                                  │
│  │  UNiagaraGraph   │  (노드 기반 그래프)                              │
│  └────────┬─────────┘                                                  │
│           │                                                            │
│           │ FNiagaraHlslTranslator                                     │
│           ▼                                                            │
│  ┌─────────────────────────────────────────────────────────┐           │
│  │  HLSL 소스 코드 생성                                    │           │
│  │  ┌───────────────────────────────────────────────────┐  │           │
│  │  │  ComputeShader MainComputeShader(...)             │  │           │
│  │  │  {                                                │  │           │
│  │  │    // 파라미터 선언                                │  │           │
│  │  │    cbuffer Constants { ... }                     │  │           │
│  │  │                                                   │  │           │
│  │  │    // Data Interface 함수 호출                    │  │           │
│  │  │    Texture2D MyTexture;                          │  │           │
│  │  │    SamplerState MySampler;                       │  │           │
│  │  │                                                   │  │           │
│  │  │    // 메인 연산                                   │  │           │
│  │  │    [numthreads(64, 1, 1)]                        │  │           │
│  │  │    void MainCS(uint3 DispatchThreadId : SV_DispatchThreadID) │  │
│  │  │    {                                              │  │           │
│  │  │      uint Index = DispatchThreadId.x;            │  │           │
│  │  │      float3 Position = InputData.Position[Index]; │  │           │
│  │  │      float3 Velocity = InputData.Velocity[Index]; │  │           │
│  │  │                                                   │  │           │
│  │  │      // 모듈 로직                                 │  │           │
│  │  │      Position += Velocity * DeltaTime;           │  │           │
│  │  │                                                   │  │           │
│  │  │      OutputData.Position[Index] = Position;      │  │           │
│  │  │    }                                              │  │           │
│  │  │  }                                                │  │           │
│  │  └───────────────────────────────────────────────────┘  │           │
│  └──────────────────┬──────────────────────────────────────┘           │
│                     │                                                  │
│                     │ FShaderCompilerEnvironment                       │
│                     ▼                                                  │
│  ┌─────────────────────────────────────────────────────────┐           │
│  │  FNiagaraShaderScript (Compiled Shader)                │           │
│  │  - FNiagaraShaderMap (RHI 독립적 셰이더 맵)            │           │
│  │  - Compute Shader Code (DX12, Vulkan, ...)             │           │
│  └──────────────────┬──────────────────────────────────────┘           │
│                     │                                                  │
│                     │ 런타임 실행                                       │
│                     ▼                                                  │
│  ┌─────────────────────────────────────────────────────────┐           │
│  │  FNiagaraComputeExecutionContext                       │           │
│  │  - Dispatch(NumGroups, NumThreads)                     │           │
│  │  - GPU 버퍼 바인딩                                      │           │
│  └─────────────────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────────────────┘
```

### GPU vs CPU 실행 비교

| 항목 | CPU (VectorVM) | GPU (Compute Shader) |
|------|----------------|----------------------|
| **실행 단위** | 청크 (64~1024 파티클) | 스레드 (1 파티클 = 1 스레드) |
| **병렬화** | SIMD (4-wide) | 수천~수만 스레드 동시 |
| **메모리** | 메인 메모리 접근 | VRAM (고대역폭) |
| **Data Interface** | 함수 포인터 호출 | Texture/Buffer 직접 접근 |
| **디버깅** | CPU 브레이크포인트 가능 | RenderDoc 등 GPU 디버거 필요 |
| **최적화** | 캐시 친화적 청크 처리 | occupancy, 메모리 coalescing |

**GPU 스크립트의 제약사항:**
- **Dynamic Branching 제한**: Static Switch로만 분기 가능
- **Recursion 불가**: 그래프는 DAG여야 함
- **Data Interface 제약**: GPU 지원 DI만 사용 가능 (예: Texture, Grid)

**📂 위치:**
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Private/NiagaraHlslTranslator.h`
- `Engine/Plugins/FX/Niagara/Source/NiagaraShader/Public/NiagaraShared.h`

---

## 💡 실전 예시

### 예시 1: 커스텀 모듈 생성

**목표:** "Spiral Motion" 모듈 만들기

```
1. 콘텐츠 브라우저에서 우클릭 → Niagara → Module Script 생성
2. Usage Bitmask 설정: "Particle Update" 체크
3. 그래프 편집:

[Parameter Map Get]                [Output]
   ◄─ Particles.Position             ◄─ Particles.Position
   ◄─ Particles.Age
   ◄─ Module.Radius (Float)
   ◄─ Module.Speed (Float)
        │
        ▼
   [Multiply]  ──┐
        │        │
        ▼        │
   [Sin/Cos]    │
        │        │
        ▼        │
   [Make Vector3]│
        │        │
        ▼        │
   [Add] ◄───────┘
        │
        ▼
   [Parameter Map Set]
      ► Particles.Position
```

**컴파일 결과:**
- CPU: VectorVM 바이트코드 (Sin/Cos 최적화)
- GPU: HLSL Compute Shader (병렬 실행)

### 예시 2: Dynamic Input 체인

**시나리오:** "Random Perturbed Value"

```
모듈: "Scale Velocity"
  입력: Scale Factor (Float)
    └─► Dynamic Input: "Add Noise"
          입력: Base Value (Float) = 1.0
                Noise Strength (Float) = 0.1
            └─► Dynamic Input: "Random Float"
                  입력: Seed (Int) = ParticleID
```

**그래프 펼침 (Inline Expansion):**
```
[ParticleID] → [Random Float] → [Multiply: 0.1] → [Add: 1.0] → [Scale Velocity]
```

---

## 🔧 디버깅 및 최적화

### 일반적인 함정

**❌ 하지 말아야 할 것:**

```cpp
// 1. Static Switch를 파라미터로 전달 (불가능!)
// Static Switch는 컴파일 시점에 결정되어야 함
UPROPERTY()
bool bUseCustomLogic;  // ❌ 런타임 변경 불가
```

**✅ 올바른 방법:**
```cpp
// Static Switch를 모듈 내부에서 직접 사용
[Static Switch: UseCustomLogic]
  ├─ True: [Custom Logic Graph]
  └─ False: [Default Logic Graph]
```

**❌ 과도한 Dynamic Input 중첩:**
```
[Dynamic Input A]
  └─► [Dynamic Input B]
        └─► [Dynamic Input C]
              └─► [Dynamic Input D]  // 너무 깊음!
```
- 컴파일 시간 증가
- 스택 오버플로우 위험
- 일반적으로 3단계 이내 권장

**✅ 성능 팁:**

| 기법 | 설명 | 효과 |
|------|------|------|
| **Static Switch 활용** | 런타임 분기 대신 컴파일 타임 분기 | 분기 비용 제거 |
| **Vectorized Operations** | 스칼라 연산 대신 벡터 연산 | CPU SIMD 활용 |
| **GPU 시뮬레이션** | 대량 파티클(10,000+) | 100배+ 성능 향상 |
| **Data Interface 캐싱** | 반복 호출 시 결과 재사용 | 외부 함수 호출 감소 |

### 디버깅 도구

```bash
# VM 바이트코드 확인 (에디터 로그)
LogNiagara: Verbose: ByteCode Size: 1024 bytes
LogNiagara: Verbose: NumTempRegisters: 32
LogNiagara: Verbose: ExternalFunctions: 5

# GPU HLSL 확인
NiagaraEditor -> Script Asset -> Generated Code 패널
  - LastHlslTranslationGPU 확인
  - Compile Errors/Warnings 확인

# 런타임 디버깅
obj list class=NiagaraComponent
obj refs name=MyNiagaraSystem
```

---

## 🔗 참고 자료

**공식 문서:**
- [Niagara Scripting Overview](https://docs.unrealengine.com/5.7/en-US/overview-of-niagara-effects-for-unreal-engine/)
- [Creating Modules](https://docs.unrealengine.com/5.7/en-US/creating-modules-in-niagara-for-unreal-engine/)
- [VectorVM Documentation](https://docs.unrealengine.com/5.7/en-US/vector-vm-in-unreal-engine/)

**소스 파일:**
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/NiagaraGraph.h` (L209-583)
- `Engine/Plugins/FX/Niagara/Source/Niagara/Classes/NiagaraScript.h` (L774-1553)
- `Engine/Plugins/FX/Niagara/Source/NiagaraEditor/Public/NiagaraNodeFunctionCall.h` (L54-261)
- `Engine/Source/Runtime/VectorVM/Public/VectorVM.h` (L20-300)

**관련 문서:**
- `UnrealSummary/Niagara/Compiler.md` - 컴파일 프로세스 상세
- `UnrealSummary/Niagara/VectorVM.md` - VectorVM 실행 구조
- `UnrealSummary/Niagara/SimulationPipeline.md` - 시뮬레이션 흐름

---

## 📝 버전 정보

> **작성 버전:** Unreal Engine 5.7
> **작성일:** 2025-11-21
> **마지막 검증:** D:\UnrealEngine5.7\Engine\Plugins\FX\Niagara\Source
