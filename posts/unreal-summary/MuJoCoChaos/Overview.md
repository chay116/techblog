---
title: "MuJoCoChaos Plugin"
date: "2026-02-18"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "MuJoCoChaos"
tags: ["unreal", "MuJoCoChaos"]
---
# MuJoCoChaos Plugin

> Updated: 2025-12-17 — Initial documentation for MuJoCoChaos plugin implementation

## 🧭 Overview

MuJoCoChaos는 MuJoCo/Brax 스타일의 **Generalized Coordinates 기반 물리 시뮬레이션**을 Unreal Engine의 **Chaos Physics** 시스템에 통합하는 플러그인입니다.

### 핵심 목적

1. **RL 학습용 배치 시뮬레이션** - 다중 환경 병렬 실행
2. **Articulated Body Dynamics** - 관절체 순동역학 (Forward Dynamics)
3. **Chaos 통합** - UE의 Physics Thread와 동기화

### 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         MuJoCoChaos Architecture                        │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Game Thread                         Physics Thread                     │
│  ┌─────────────────────┐            ┌─────────────────────────────┐    │
│  │ URlArticulation     │            │ FMuJoCoSimCallback          │    │
│  │ Component           │───Push────▶│ - OnPreIntegrate (ABA)      │    │
│  │ (Blueprint 노출)     │            │ - OnPreSolve (PGS)          │    │
│  └─────────────────────┘            │ - OnPostIntegrate (Sync)    │    │
│           ▲                         └──────────────┬──────────────┘    │
│           │                                        │                    │
│           │                                        ▼                    │
│           │                         ┌─────────────────────────────┐    │
│           └────────Pull────────────│ FArticulatedBodyPhysicsProxy│    │
│                                     │ (qpos, qvel ↔ Maximal)      │    │
│                                     └─────────────────────────────┘    │
│                                                                         │
│  RL Environment Layer                                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ FRLPhysicsWorld (nworld 배치 시뮬레이션)                          │   │
│  │ - Step(actions) → observations, rewards                         │   │
│  │ - Reset() → initial observations                                │   │
│  │ - Gymnasium 호환 API                                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🧱 Structure

### 플러그인 디렉토리 구조

```
Plugins/MuJoCoChaos/
├── MuJoCoChaos.uplugin                    # 플러그인 정의
├── Source/MuJoCoChaos/
│   ├── MuJoCoChaos.Build.cs               # 빌드 설정
│   ├── Public/
│   │   ├── MuJoCoChaosModule.h            # 모듈 인터페이스
│   │   ├── MuJoCoTypes.h                  # 핵심 타입 정의
│   │   ├── ArticulatedBodyPhysicsProxy.h  # Physics Proxy
│   │   ├── MuJoCoSimCallback.h            # SimCallback 확장
│   │   ├── Dynamics/
│   │   │   └── ForwardDynamics.h          # 순동역학 알고리즘
│   │   ├── Solver/
│   │   │   └── PGSSolver.h                # 제약 솔버
│   │   └── RL/
│   │       └── RLPhysicsWorld.h           # RL 배치 환경
│   └── Private/
│       ├── MuJoCoChaosModule.cpp
│       ├── ArticulatedBodyPhysicsProxy.cpp
│       ├── MuJoCoSimCallback.cpp
│       ├── Dynamics/
│       │   └── ForwardDynamics.cpp
│       ├── Solver/
│       │   └── PGSSolver.cpp
│       └── RL/
│           └── RLPhysicsWorld.cpp
```

### 모듈 의존성

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Module Dependencies                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│                         ┌─────────────┐                                 │
│                         │ MuJoCoChaos │                                 │
│                         └──────┬──────┘                                 │
│                                │                                        │
│          ┌─────────┬──────────┼──────────┬─────────┐                   │
│          ▼         ▼          ▼          ▼         ▼                   │
│    ┌─────────┐ ┌───────┐ ┌─────────┐ ┌──────┐ ┌─────┐                  │
│    │  Core   │ │ Chaos │ │PhysCore │ │GeomCo│ │RHI  │                  │
│    └─────────┘ └───────┘ └─────────┘ └──────┘ └─────┘                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Key Components

### 1. MuJoCoTypes.h - 핵심 데이터 구조

**📂 위치:** `Plugins/MuJoCoChaos/Source/MuJoCoChaos/Public/MuJoCoTypes.h`

#### 좌표계 비교

| 구분 | Generalized (MuJoCo) | Maximal (Chaos) |
|------|---------------------|-----------------|
| **위치** | qpos (nq 차원) | 각 바디별 Position (3D) |
| **속도** | qvel (nv 차원) | 각 바디별 Linear/Angular Velocity |
| **장점** | 제약 자동 만족, 낮은 차원 | 직관적, 충돌 처리 용이 |
| **단점** | 좌표 변환 필요 | 제약 drift, 높은 차원 |

#### 핵심 구조체

```cpp
// Joint 타입 정의
enum class EJointType : uint8
{
    Fixed,      // 0 DOF
    Revolute,   // 1 DOF (회전)
    Prismatic,  // 1 DOF (이동)
    Spherical,  // 3 DOF (구면 조인트)
    Free,       // 6 DOF (자유 조인트)
};

// Joint 기술자
struct FJointDesc
{
    EJointType Type;
    int32 ParentBody;
    int32 ChildBody;
    FVector3d Axis;           // 회전/이동 축
    uint32 QposStart;         // qpos 배열 시작 인덱스
    uint32 QvelStart;         // qvel 배열 시작 인덱스
    uint32 NumDof;            // 자유도
    FReal LimitLower, LimitUpper;  // 조인트 한계
    FReal Damping, Stiffness, Friction;
    FReal Armature;           // 회전 관성 증강
};

// 물리 모델 (정적 데이터)
struct FMuJoCoModel
{
    uint32 Nq, Nv, Nu, Nbody, Njnt;
    TArray<FBodyInertia> BodyInertia;
    TArray<FJointDesc> Joints;
    TArray<FActuatorDesc> Actuators;
    FSimOptions Options;
};

// 시뮬레이션 상태 (동적 데이터)
struct FMuJoCoState
{
    TArray<FReal> Qpos;       // 일반화 좌표
    TArray<FReal> Qvel;       // 일반화 속도
    TArray<FReal> Qacc;       // 가속도
    TArray<FReal> Ctrl;       // 제어 입력
    TArray<FReal> QfrcBias;   // 편향력 (C + G)
    TArray<FReal> QfrcApplied;// 적용 토크
    TArray<FBodyTransform> Xpos;  // 월드 변환
    TArray<FBodyVelocity> Xvel;   // 월드 속도
};
```

### 2. ArticulatedBodyPhysicsProxy - Chaos 통합

**📂 위치:** `Plugins/MuJoCoChaos/Source/MuJoCoChaos/Public/ArticulatedBodyPhysicsProxy.h`

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    FArticulatedBodyPhysicsProxy                         │
│  (IPhysicsProxyBase 상속 - Chaos 통합 인터페이스)                        │
├─────────────────────────────────────────────────────────────────────────┤
│  Private:                                                               │
│    - Model : FMuJoCoModel*         // 물리 모델 (공유, 읽기 전용)        │
│    - State : FMuJoCoState          // 시뮬레이션 상태                    │
│    - LinkHandles : TArray<FParticleHandle*>  // Chaos 파티클 핸들       │
│    - PendingCtrl : TArray<FReal>   // GT에서 전달된 제어 입력            │
│    - PendingForces : TArray<FPendingForce>  // 외부 힘                  │
│                                                                         │
│  Public:                                                                │
│    + SetControl(ctrl) : void       // 제어 입력 설정 (GT)               │
│    + GetJointPositions() : TArray  // 조인트 위치 반환                  │
│    + GetLinkTransform(idx) : FTransform  // 링크 변환 반환              │
│    + PushToPhysicsState() : void   // GT → PT 데이터 전송               │
│    + PullFromPhysicsState() : bool // PT → GT 결과 수신                 │
│    + SyncMaximalFromGeneralized()  // qpos → Chaos 파티클 동기화        │
│    + ComputeForwardKinematics()    // 순운동학 계산                      │
└─────────────────────────────────────────────────────────────────────────┘
```

#### GT ↔ PT 동기화 시퀀스

```
    Game Thread              Physics Thread              Chaos Solver
         │                         │                          │
         │  SetControl(ctrl)       │                          │
         ├─────────────────────────┤                          │
         │                         │                          │
         │  PushToPhysicsState()   │                          │
         ├────────────────────────▶│                          │
         │                         │  OnPreIntegrate          │
         │                         ├──────────────────────────┤
         │                         │  ComputeABA() ────────▶  │
         │                         │                          │
         │                         │  OnPreSolve              │
         │                         ├──────────────────────────┤
         │                         │  SolvePGS() ──────────▶  │
         │                         │                          │
         │                         │  OnPostIntegrate         │
         │                         ├──────────────────────────┤
         │                         │  SyncMaximal() ───────▶  │
         │                         │                          │
         │  PullFromPhysicsState() │                          │
         │◀────────────────────────┤                          │
         │                         │                          │
```

### 3. MuJoCoSimCallback - Physics Thread 콜백

**📂 위치:** `Plugins/MuJoCoChaos/Source/MuJoCoChaos/Public/MuJoCoSimCallback.h`

```cpp
// 콜백 옵션 설정
constexpr Chaos::ESimCallbackOptions MuJoCoSimCallbackOptions =
    Chaos::ESimCallbackOptions::PreIntegrate |   // ABA 계산
    Chaos::ESimCallbackOptions::PostIntegrate |  // 좌표 동기화
    Chaos::ESimCallbackOptions::PreSolve;        // PGS 제약 해결

class FMuJoCoSimCallback : public Chaos::TSimCallbackObject<
    FMuJoCoSimCallbackInput,
    FMuJoCoSimCallbackOutput,
    MuJoCoSimCallbackOptions>
{
protected:
    // 순동역학 계산 (적분 전)
    virtual void OnPreIntegrate_Internal() override;

    // 제약 해결 (Chaos 솔버 전)
    virtual void OnPreSolve_Internal() override;

    // 좌표 동기화 (적분 후)
    virtual void OnPostIntegrate_Internal() override;
};
```

### 4. ForwardDynamics - 순동역학 알고리즘

**📂 위치:** `Plugins/MuJoCoChaos/Source/MuJoCoChaos/Public/Dynamics/ForwardDynamics.h`

#### Spatial Math 기초

```cpp
// 6D 공간 벡터 (Plucker 좌표)
struct FSpatialVector
{
    FVector3d Angular;  // 회전 성분
    FVector3d Linear;   // 이동 성분

    // 운동량 교차곱: v1 x v2
    FSpatialVector CrossMotion(const FSpatialVector& Other) const;

    // 힘 교차곱: v x* f
    FSpatialVector CrossForce(const FSpatialVector& Force) const;
};

// 6x6 공간 관성 행렬
struct FSpatialInertia
{
    FReal Mass;
    FVector3d CoM;
    FMatrix RotationalInertia;

    // I * v 계산
    FSpatialVector Apply(const FSpatialVector& Motion) const;
};
```

#### 알고리즘 파이프라인

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Forward Dynamics Pipeline                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  입력: qpos, qvel, ctrl, τ_ext                                          │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ Pass 1: Forward Pass (루트 → 리프)                               │   │
│  │   - 조인트 변환 X_J 계산                                         │   │
│  │   - 링크 속도 V[i] 계산                                          │   │
│  │   - 코리올리 항 C[i] = v_j × V[i] 계산                           │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                            ↓                                            │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ Pass 2: Backward Pass (리프 → 루트)                              │   │
│  │   - 관절 관성 I_A[i] 계산 (Articulated Body Inertia)             │   │
│  │   - 편향력 p_A[i] = I_A * c + v ×* I_A * v 계산                  │   │
│  │   - 유효 관성 D[i] = S^T * I_A * S 계산                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                            ↓                                            │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ Pass 3: Forward Pass (루트 → 리프)                               │   │
│  │   - 조인트 가속도 qacc = (τ - U) / D 계산                        │   │
│  │   - 링크 가속도 전파                                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  출력: qacc (일반화 가속도)                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 주요 함수

```cpp
namespace Dynamics
{
    // Articulated Body Algorithm (O(n) 순동역학)
    void ComputeABA(
        const FMuJoCoModel& Model,
        FMuJoCoState& State,
        FForwardDynamicsContext& Context,
        const FVector3d& Gravity
    );

    // Recursive Newton-Euler Algorithm (역동역학)
    void ComputeRNEA(
        const FMuJoCoModel& Model,
        const FMuJoCoState& State,
        FForwardDynamicsContext& Context,
        const FVector3d& Gravity,
        TArray<FReal>& OutTorque
    );

    // Composite Rigid Body Algorithm (질량 행렬)
    void ComputeCRBA(
        const FMuJoCoModel& Model,
        const FMuJoCoState& State,
        FForwardDynamicsContext& Context,
        TArray<FReal>& OutMassMatrix
    );
}
```

### 5. PGSSolver - 제약 솔버

**📂 위치:** `Plugins/MuJoCoChaos/Source/MuJoCoChaos/Public/Solver/PGSSolver.h`

#### 제약 타입

```cpp
enum class EConstraintType : uint8
{
    Equality,       // 양방향 제약 (=0)
    Inequality,     // 단방향 제약 (>=0)
    Friction,       // 마찰 원뿔
    JointLimit,     // 조인트 한계
    Contact,        // 접촉 제약
};
```

#### PGS 알고리즘

```
┌─────────────────────────────────────────────────────────────────────────┐
│              Projected Gauss-Seidel Algorithm                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  문제: J · M⁻¹ · Jᵀ · λ = -J · v - bias                                 │
│                                                                         │
│  for iteration = 1 to MaxIterations:                                    │
│      for each constraint c:                                             │
│          1. Cdot = J[c] · v                    // 제약 속도              │
│          2. Δλ = -(Cdot + bias) · effectiveMass                        │
│          3. λ_new = λ_old + Δλ                                         │
│          4. λ_new = Project(λ_new, bounds)     // 투영 (>=0 등)         │
│          5. v += M⁻¹ · Jᵀ · (λ_new - λ_old)    // 속도 업데이트         │
│      end                                                                │
│      if converged: break                                                │
│  end                                                                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### 솔버 설정

```cpp
struct FSolverConfig
{
    uint32 VelocityIterations = 10;  // 속도 반복 횟수
    uint32 PositionIterations = 3;   // 위치 보정 횟수
    FReal Tolerance = 1e-6;          // 수렴 허용치
    FReal Baumgarte = 0.2;           // Baumgarte 안정화 계수
    FReal DefaultCFM = 1e-6;         // 제약력 혼합 (정규화)
    bool bWarmStart = true;          // 워밍 스타트 사용
};
```

### 6. RLPhysicsWorld - RL 배치 시뮬레이션

**📂 위치:** `Plugins/MuJoCoChaos/Source/MuJoCoChaos/Public/RL/RLPhysicsWorld.h`

#### Gymnasium 호환 인터페이스

```cpp
class FRLPhysicsWorld
{
public:
    // 초기화
    bool Initialize(FMuJoCoModel* Model, const FWorldConfig& Config);

    // 환경 리셋
    bool Reset(FBatchObservation& OutObservations);

    // 스텝 실행 (배치)
    bool Step(
        const FBatchAction& Actions,
        FBatchObservation& OutObservations,
        FBatchStepResult& OutResults
    );

    // 커스텀 보상 함수 등록
    void SetRewardFunction(FRewardFunction Func);

    // 커스텀 종료 조건 등록
    void SetTerminationFunction(FTerminationFunction Func);
};
```

#### 배치 시뮬레이션 데이터 플로우

```
┌─────────────────────────────────────────────────────────────────────────┐
│                   Batch Simulation Data Flow                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Actions [NumWorlds × ActionDim]                                        │
│         │                                                               │
│         ▼                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    ParallelFor (NumWorlds)                       │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐       ┌──────────┐│   │
│  │  │ World 0   │  │ World 1   │  │ World 2   │  ...  │ World N  ││   │
│  │  │           │  │           │  │           │       │          ││   │
│  │  │ ApplyAct  │  │ ApplyAct  │  │ ApplyAct  │       │ ApplyAct ││   │
│  │  │ SimStep   │  │ SimStep   │  │ SimStep   │       │ SimStep  ││   │
│  │  │ ComputeObs│  │ ComputeObs│  │ ComputeObs│       │ComputeObs││   │
│  │  │ CheckTerm │  │ CheckTerm │  │ CheckTerm │       │CheckTerm ││   │
│  │  └───────────┘  └───────────┘  └───────────┘       └──────────┘│   │
│  └─────────────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         ▼                                                               │
│  Observations [NumWorlds × ObsDim]                                      │
│  Rewards [NumWorlds]                                                    │
│  Terminated [NumWorlds]                                                 │
│  Truncated [NumWorlds]                                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 💡 Tips & References

### 성능 최적화

**✅ 권장:**
```cpp
// 배치 시뮬레이션에서 병렬 처리 활성화
FWorldConfig Config;
Config.NumWorlds = 64;
Config.bParallelWorlds = true;

// 프레임 스킵으로 시뮬레이션 주파수와 제어 주파수 분리
Config.FrameSkip = 4;  // 4 physics steps per control step
```

**❌ 피해야 할 것:**
```cpp
// 매 프레임 State 복사 (비효율적)
for (int i = 0; i < NumWorlds; ++i)
{
    TArray<FReal> StateCopy = World->GetWorldState(i)->Qpos;  // 불필요한 복사
}
```

### 디버깅 팁

```cpp
// 에너지 보존 확인 (통합 안정성 검증)
FMuJoCoSimCallbackOutput& Output = GetProducerOutputData_Internal();
UE_LOG(LogTemp, Log, TEXT("Total Energy: %f"), Output.Energy);

// 솔버 수렴 확인
UE_LOG(LogTemp, Log, TEXT("PGS Iterations: %d, Residual: %f"),
    Output.SolverIterations, Output.SolverResidual);
```

### 사전 정의 모델 사용

```cpp
// Pendulum 모델 생성
FMuJoCoModel PendulumModel = Models::Pendulum(
    100.0,  // 길이 (cm)
    1.0     // 질량 (kg)
);

// Cart-Pole RL 환경 생성
auto CartPoleWorld = Environments::CreateCartPole(
    64,     // 병렬 환경 수
    1.0,    // 카트 질량
    0.1,    // 폴 질량
    100.0   // 폴 길이
);
```

### 🔗 References

| 참조 | 설명 |
|------|------|
| Featherstone, R. (2008) | "Rigid Body Dynamics Algorithms" - ABA, CRBA 알고리즘 원본 |
| MuJoCo Documentation | [mujoco.org](https://mujoco.org) - Generalized Coordinates 기반 시뮬레이션 |
| Chaos Physics | `Engine/Source/Runtime/Experimental/Chaos/` - UE Chaos 소스 |
| ISimCallbackObject | `Chaos/SimCallbackObject.h` - 콜백 인터페이스 |

---

## 📐 Design Philosophy

### 왜 Generalized Coordinates인가?

| 이유 | 설명 | 효과 |
|------|------|------|
| **제약 만족** | 조인트 제약이 좌표계에 내재됨 | 제약 drift 없음, 안정적 시뮬레이션 |
| **낮은 차원** | N개 조인트 = N 자유도 | 효율적 연산, 빠른 솔버 |
| **RL 친화적** | 관측/행동 공간이 직관적 | qpos/qvel이 직접 상태 |

### 왜 Chaos 통합인가?

| 이유 | 설명 | 효과 |
|------|------|------|
| **UE 생태계** | 렌더링, 에디터, BP 연동 | 시각화, 디버깅 용이 |
| **충돌 시스템** | Chaos의 충돌 감지 재사용 | 추가 구현 불필요 |
| **스레드 모델** | Physics Thread 활용 | 게임 스레드 블로킹 없음 |

### 핵심 설계 원칙

> **SimCallback**이 "순동역학 + 제약 해결"을,
> **PhysicsProxy**가 "좌표 변환 + 동기화"를,
> **RLPhysicsWorld**가 "배치 관리 + API"를 담당한다.

---

## 🗓️ Version History

| 버전 | 날짜 | 변경 사항 |
|------|------|----------|
| v1.0 | 2025-12-17 | 초기 구현 - Phase 1~3 완료 |

### 향후 계획

- **Phase 4**: MJCF/URDF 모델 로더
- **Phase 5**: GPU Compute Shader 최적화
- **Phase 6**: Python 바인딩 (Gymnasium 연동)
