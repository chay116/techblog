---
title: "게임 프레임워크 (GameFramework)"
date: "2026-02-18"
status: "stable"
project: "UnrealEngine"
lang: "ko"
category: "unreal-summary"
track: "GameFramework"
tags: ["unreal", "GameFramework"]
---
# 게임 프레임워크 (GameFramework)

## 🧭 개요

**게임 프레임워크(GameFramework)**는 언리얼 엔진에서 게임플레이를 구성하는 핵심 클래스들의 집합입니다. 이는 플레이어, AI, 게임 규칙, UI 등 게임의 모든 요소를 관리하는 표준화된 아키텍처를 제공합니다.

**핵심 구성 요소:**
- **UGameInstance** - 게임 인스턴스 (앱 생명주기)
- **AGameModeBase** - 게임 모드 (게임 규칙, 서버 전용)
- **AGameStateBase** - 게임 상태 (복제된 게임 정보)
- **APlayerController** - 플레이어 컨트롤러 (입력 처리)
- **APawn / ACharacter** - 폰/캐릭터 (플레이어/AI가 제어하는 액터)
- **APlayerState** - 플레이어 상태 (복제된 플레이어 정보)
- **AHUD** - HUD (UI 표시)
- **ULocalPlayer** - 로컬 플레이어 (로컬 입력/뷰포트)

**생명주기 계층:**
```
게임 시작
    ↓
[1] UGameInstance::Init()           // 가장 오래 살아남음 (앱 전체)
    ↓
[2] AGameModeBase::InitGame()       // 맵 로드 시 생성
    ↓
[3] APlayerController::Login()      // 플레이어 접속
    ↓
[4] APawn::PossessedBy()            // 폰 빙의
    ↓
[5] BeginPlay() - 게임 시작         // 모든 액터
    ↓
    ... 게임플레이 진행 ...
    ↓
[6] APlayerController::Logout()     // 플레이어 로그아웃
    ↓
[7] 맵 전환 (Travel)
    ↓
[8] UGameInstance::Shutdown()       // 게임 종료
```

**네트워크 존재:**
| 클래스 | 서버 | 클라이언트 (자신) | 클라이언트 (다른 사람) |
|--------|------|------------------|---------------------|
| **UGameInstance** | ✅ | ✅ | ✅ |
| **AGameModeBase** | ✅ | ❌ | ❌ |
| **AGameStateBase** | ✅ (Authority) | ✅ (Replicated) | ✅ (Replicated) |
| **APlayerController** | ✅ (모든 플레이어) | ✅ (자기 것만) | ❌ |
| **APlayerState** | ✅ (Authority) | ✅ (Replicated) | ✅ (Replicated) |
| **APawn** | ✅ (Authority) | ✅ (Replicated) | ✅ (Replicated) |

**모듈 위치:**
- `Engine/Source/Runtime/Engine/Classes/Engine/GameInstance.h`
- `Engine/Source/Runtime/Engine/Classes/GameFramework/GameModeBase.h`
- `Engine/Source/Runtime/Engine/Classes/GameFramework/PlayerController.h`
- `Engine/Source/Runtime/Engine/Classes/GameFramework/Pawn.h`
- `Engine/Source/Runtime/Engine/Classes/GameFramework/Character.h`

**엔진 버전:** Unreal Engine 5.7 (2025년 기준)

---

## 🧱 구조

### 게임 프레임워크 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    GameFramework Architecture                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [레벨 1: 게임 인스턴스 - 앱 생명주기]                                    │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  UGameInstance                                                   │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  • 가장 상위 레벨 게임 오브젝트                                   │  │
│  │  • 앱 시작부터 종료까지 유지 (맵 전환에도 살아있음)                │  │
│  │  • 로컬 플레이어 관리 (LocalPlayers[])                            │  │
│  │  • 온라인 서브시스템 (OnlineSession)                               │  │
│  │  • 타이머, Latent Action 관리                                     │  │
│  │  • PIE: 각 PIE 인스턴스마다 별도 생성                              │  │
│  │  • 네트워크: 서버/클라이언트 모두 존재                             │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                             ↓ (소유)                                    │
│  [레벨 2: 월드 - 레벨/맵 생명주기]                                       │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  UWorld                                                          │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  • 게임 월드 (레벨/맵)                                            │  │
│  │  • 맵 전환 시 소멸 및 재생성                                      │  │
│  │  • NetDriver (네트워크 드라이버)                                   │  │
│  │  • PhysicsScene (물리 시뮬레이션)                                  │  │
│  │  • AudioDevice (오디오)                                           │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                             ↓                                           │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │  [서버 전용]          │  [복제됨 - 모두에게 존재]              │      │
│  │                       │                                       │      │
│  │  AGameModeBase        │  AGameStateBase                       │      │
│  │  ────────────────     │  ────────────────────────────         │      │
│  │  • 게임 규칙          │  • 복제된 게임 정보                    │      │
│  │  • 스폰 관리          │  • 점수, 경과 시간 등                  │      │
│  │  • 플레이어 로그인     │  • 모든 클라이언트가 볼 수 있음         │      │
│  │  • 승패 판정          │  • UPROPERTY(Replicated)              │      │
│  │  • 서버에만 존재       │  • ServerFrame, ReplicatedScore ...   │      │
│  │                       │                                       │      │
│  └───────────────────────┴───────────────────────────────────────┘      │
│                             ↓                                           │
│  [레벨 3: 플레이어 - 플레이어별 생명주기]                                 │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  ULocalPlayer                                                    │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  • 로컬 플레이어 (스플릿스크린 지원)                              │  │
│  │  • ViewportClient                                                │  │
│  │  • ControllerId / PlatformUserId                                 │  │
│  │  • 클라이언트에만 존재                                            │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                             ↓                                           │
│  ┌─────────────────────────────────────────────────────────────┐      │
│  │  APlayerController                                          │      │
│  │  ────────────────────────────────────────────────────────   │      │
│  │  • 플레이어 입력 처리                                        │      │
│  │  • 카메라 제어 (PlayerCameraManager)                        │      │
│  │  • HUD 소유                                                  │      │
│  │  • RPC (Server/Client 함수)                                  │      │
│  │  • 네트워크:                                                 │      │
│  │    - 서버: 모든 플레이어의 PC 존재                            │      │
│  │    - 클라이언트: 자기 PC만 존재                               │      │
│  │                                                             │      │
│  │  소유:                                                       │      │
│  │    • PlayerCameraManager                                    │      │
│  │    • PlayerInput                                            │      │
│  │    • AHUD                                                    │      │
│  │    • CheatManager                                           │      │
│  └─────────────────────────────────────────────────────────────┘      │
│       ↓ (Possess)                      ↓ (소유)                        │
│  ┌─────────────────────┐   ┌────────────────────────────────────┐    │
│  │  APawn / ACharacter │   │  APlayerState                      │    │
│  │  ────────────────   │   │  ────────────────────────────      │    │
│  │  • 플레이어/AI 제어  │   │  • 복제된 플레이어 정보             │    │
│  │  • 물리적 존재       │   │  • PlayerName, Score, Ping        │    │
│  │  • 이동, 충돌       │   │  • UPROPERTY(Replicated)          │    │
│  │  • 애니메이션       │   │  • 모든 클라이언트가 볼 수 있음     │    │
│  │  • 복제됨 (모두 봄) │   │  • 맵 전환 시 유지 가능             │    │
│  │                     │   │    (SeamlessTravel)               │    │
│  │  컴포넌트:           │   └────────────────────────────────────┘    │
│  │  • MovementComp     │                                             │
│  │  • MeshComp         │                                             │
│  │  • CapsuleComp      │                                             │
│  └─────────────────────┘                                             │
│                                                                         │
│  [레벨 4: UI]                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │  AHUD                                                            │  │
│  │  ────────────────────────────────────────────────────────────    │  │
│  │  • Canvas 기반 UI (레거시)                                        │  │
│  │  • UMG 위젯 관리                                                  │  │
│  │  • DrawHUD() - 매 프레임 호출                                     │  │
│  │  • 클라이언트 전용 (복제 안 됨)                                   │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### UGameInstance 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          UGameInstance                                  │
│  (게임 인스턴스 - 최상위 게임 오브젝트)                                   │
├─────────────────────────────────────────────────────────────────────────┤
│  📂 위치: Engine/Classes/Engine/GameInstance.h:150                      │
│                                                                         │
│  상속: UObject, FExec                                                   │
│                                                                         │
│  Public Members:                                                        │
│    - WorldContext : FWorldContext*          // 월드 컨텍스트            │
│    - LocalPlayers : TArray<ULocalPlayer*>   // 로컬 플레이어 목록        │
│    - OnlineSession : UOnlineSession*        // 온라인 세션               │
│    - ReferencedObjects : TArray<UObject*>   // GC 루트                  │
│    - TimerManager : FTimerManager*          // 타이머 관리자             │
│    - LatentActionManager : FLatentActionManager*  // Latent Action     │
│                                                                         │
│  Lifecycle Methods:                                                     │
│    + Init()                                 // 초기화                   │
│    + Shutdown()                             // 종료                     │
│    + StartGameInstance()                    // 게임 시작                │
│    + OnWorldChanged(OldWorld, NewWorld)     // 월드 전환 콜백           │
│                                                                         │
│  LocalPlayer Management:                                                │
│    + CreateLocalPlayer(UserId, OutError, bSpawnPC) : ULocalPlayer*     │
│    + AddLocalPlayer(NewPlayer, UserId) : int32                         │
│    + RemoveLocalPlayer(ExistingPlayer) : bool                          │
│    + GetNumLocalPlayers() : int32                                      │
│    + GetFirstGamePlayer() : ULocalPlayer*                              │
│    + GetFirstLocalPlayerController(World) : APlayerController*         │
│                                                                         │
│  Travel Methods:                                                        │
│    + ClientTravelToSession(ControllerId, SessionName) : bool           │
│    + PreloadContentForURL(InURL)           // 맵 전환 전 준비           │
│    + CreateGameModeForURL(InURL, World) : AGameModeBase*               │
│                                                                         │
│  Subsystem Access:                                                      │
│    + GetSubsystem<T>() : T*                // 서브시스템 가져오기        │
│    + ForEachSubsystem<T>(Operation)        // 서브시스템 순회           │
│                                                                         │
│  Network/Online:                                                        │
│    + GetOnlinePlatformName() : FName       // 플랫폼 이름               │
│    + EnableListenServer(bEnable, PortOverride) : bool                  │
│    + IsDedicatedServerInstance() : bool    // 전용 서버 여부            │
│                                                                         │
│  Replay:                                                                │
│    + StartRecordingReplay(Name, FriendlyName, AdditionalOptions)       │
│    + StopRecordingReplay()                                             │
│    + PlayReplay(Name, WorldOverride, AdditionalOptions) : bool         │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### AGameModeBase 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        AGameModeBase                                    │
│  (게임 모드 - 게임 규칙 및 관리, 서버 전용)                               │
├─────────────────────────────────────────────────────────────────────────┤
│  📂 위치: Engine/Classes/GameFramework/GameModeBase.h:46                │
│                                                                         │
│  상속: AInfo                                                            │
│                                                                         │
│  네트워크: 서버에만 존재! (클라이언트에는 없음)                           │
│                                                                         │
│  Class References (UPROPERTY):                                          │
│    - GameSessionClass : TSubclassOf<AGameSession>                      │
│    - GameStateClass : TSubclassOf<AGameStateBase>                      │
│    - PlayerControllerClass : TSubclassOf<APlayerController>            │
│    - PlayerStateClass : TSubclassOf<APlayerState>                      │
│    - HUDClass : TSubclassOf<AHUD>                                      │
│    - DefaultPawnClass : TSubclassOf<APawn>                             │
│    - SpectatorClass : TSubclassOf<ASpectatorPawn>                      │
│                                                                         │
│  Instance References:                                                   │
│    - GameSession : AGameSession*            // 세션 관리                │
│    - GameState : AGameStateBase*            // 게임 상태                │
│                                                                         │
│  Initialization:                                                        │
│    + InitGame(MapName, Options, ErrorMessage)  // 가장 먼저 호출        │
│    + InitGameState()                        // GameState 초기화         │
│    + StartPlay()                            // BeginPlay 전환           │
│                                                                         │
│  Player Management:                                                     │
│    + PreLogin(Options, Address, UniqueId, ErrorMessage)  // 접속 승인   │
│    + Login(NewPlayer, RemoteRole, Portal, Options, UniqueId, Error)    │
│      : APlayerController*                   // PC 생성 및 로그인        │
│    + PostLogin(NewPlayer)                   // 로그인 후처리             │
│    + Logout(Exiting)                        // 로그아웃                 │
│    + SpawnPlayerController(RemoteRole, Options) : APlayerController*   │
│                                                                         │
│  Pawn Spawning:                                                         │
│    + ChoosePlayerStart(Player) : AActor*    // 스폰 지점 선택           │
│    + FindPlayerStart(Player, IncomingName) : AActor*  // 스폰 지점 찾기 │
│    + RestartPlayer(NewPlayer)               // 플레이어 리스폰           │
│    + RestartPlayerAtPlayerStart(NewPlayer, StartSpot)                  │
│    + SpawnDefaultPawnFor(NewPlayer, StartSpot) : APawn*                │
│                                                                         │
│  Game Flow:                                                             │
│    + HasMatchStarted() : bool               // 매치 시작 여부           │
│    + HasMatchEnded() : bool                 // 매치 종료 여부           │
│    + SetPause(PC, CanUnpauseDelegate) : bool  // 일시정지              │
│    + ResetLevel()                           // 레벨 리셋                │
│    + ReturnToMainMenuHost()                 // 메인 메뉴로 복귀         │
│                                                                         │
│  Server Travel:                                                         │
│    + ProcessServerTravel(URL, bAbsolute)    // 서버 여행                │
│    + GetSeamlessTravelActorList(bToTransition, ActorList)  // 유지 액터 │
│    + HandleSeamlessTravelPlayer(C)          // Seamless 플레이어 처리   │
│                                                                         │
│  Blueprint Events:                                                      │
│    + K2_PostLogin(NewPlayer)                // BP: OnPostLogin          │
│    + K2_OnLogout(ExitingController)         // BP: OnLogout             │
│    + K2_OnChangeName(Other, NewName, bNameChange)  // BP: OnChangeName │
│    + K2_OnRestartPlayer(NewPlayer)          // BP: OnRestartPlayer      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### APlayerController 구조

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       APlayerController                                 │
│  (플레이어 컨트롤러 - 입력 처리 및 플레이어 제어)                          │
├─────────────────────────────────────────────────────────────────────────┤
│  📂 위치: Engine/Classes/GameFramework/PlayerController.h:259           │
│                                                                         │
│  상속: AController                                                      │
│                                                                         │
│  네트워크:                                                               │
│    - 서버: 모든 플레이어의 PlayerController 존재                         │
│    - 클라이언트: 자기 것만 존재                                          │
│    - IsLocalController() - 자기 PC인지 확인                              │
│                                                                         │
│  Public Members:                                                        │
│    - Player : UPlayer*                      // ULocalPlayer 또는 NetConnection │
│    - AcknowledgedPawn : APawn*              // 서버 확인된 폰            │
│    - MyHUD : AHUD*                          // HUD                      │
│    - PlayerCameraManager : APlayerCameraManager*  // 카메라 관리자      │
│    - PlayerInput : UPlayerInput*            // 입력 시스템               │
│                                                                         │
│  Possession (빙의):                                                      │
│    + Possess(InPawn)                        // 폰 빙의                  │
│    + UnPossess()                            // 빙의 해제                │
│    + GetPawn() : APawn*                     // 현재 폰                  │
│    + OnPossess(PossessedPawn)               // BP: 빙의 시                │
│    + OnUnPossess()                          // BP: 빙의 해제 시          │
│                                                                         │
│  Input Processing:                                                      │
│    + SetupInputComponent()                  // 입력 바인딩              │
│    + ProcessPlayerInput(DeltaTime, bGamePaused)  // 입력 처리           │
│    + InputKey(Key, EventType, AmountDepressed, bGamepad) : bool        │
│    + InputAxis(Key, Delta, DeltaTime, NumSamples, bGamepad) : bool     │
│                                                                         │
│  Input Modes:                                                           │
│    + SetInputMode(InData : FInputModeDataBase)                         │
│      - FInputModeUIOnly          // UI만 입력                           │
│      - FInputModeGameOnly        // 게임만 입력                         │
│      - FInputModeGameAndUI       // 게임 + UI 입력                      │
│                                                                         │
│  Camera:                                                                │
│    + SetViewTarget(NewViewTarget, TransitionParams)  // 카메라 타겟     │
│    + GetViewTarget() : AActor*              // 현재 카메라 타겟          │
│    + SetControlRotation(NewRotation)        // 제어 회전 설정           │
│    + GetControlRotation() : FRotator        // 제어 회전 가져오기        │
│    + PlayerCameraManager->SetCameraMode(CameraMode)  // 카메라 모드     │
│                                                                         │
│  RPC (Remote Procedure Call):                                           │
│    + Server_XXX() - 클라이언트 → 서버                                    │
│      UFUNCTION(Server, Reliable, WithValidation)                       │
│    + Client_XXX() - 서버 → 클라이언트                                    │
│      UFUNCTION(Client, Reliable)                                       │
│    + Multicast_XXX() - 서버 → 모든 클라이언트                            │
│      UFUNCTION(NetMulticast, Reliable)                                 │
│                                                                         │
│  HUD:                                                                   │
│    + GetHUD() : AHUD*                       // HUD 가져오기             │
│    + ClientSetHUD(NewHUDClass)              // HUD 설정                 │
│                                                                         │
│  Cheats:                                                                │
│    + CheatManager : UCheatManager*          // 치트 관리자              │
│    + EnableCheats()                         // 치트 활성화              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔬 설계 철학: 왜 이런 계층 구조인가?

### 단일 클래스 vs 분리된 프레임워크

```cpp
// ❌ 단일 클래스 접근 (불가능)

class AMonolithicPlayer : public AActor
{
    // 모든 것을 한 곳에
    void ProcessInput();           // 입력
    void UpdateCamera();           // 카메라
    void MoveCharacter();          // 이동
    void UpdateScore();            // 점수
    void ReplicateData();          // 네트워크
    void DrawUI();                 // UI

    // 문제점:
    // 1. 네트워크 복제 불가능 (어떤 정보를 누구에게?)
    // 2. 역할 분리 불가 (입력 vs 표현 vs 규칙)
    // 3. 재사용 불가 (AI는 입력이 없음)
    // 4. 맵 전환 시 유지 불가
    // 5. 스플릿스크린 불가
};
```

```cpp
// ✅ 언리얼 방식 - 역할별 분리

// [1] GameInstance - 앱 전체 생명주기
UGameInstance* GI = CreateGameInstance();
GI->Init();  // 게임 시작
// ... 여러 맵 로드/언로드 ...
GI->Shutdown();  // 게임 종료

// [2] GameMode - 게임 규칙 (서버 전용)
AGameModeBase* GM = SpawnGameMode();
GM->InitGame("MyMap", "?SpectatorOnly=1");
GM->Login(NewPlayer);  // 플레이어 입장 허용

// [3] PlayerController - 입력 및 제어 (네트워크 분리)
APlayerController* PC = GM->SpawnPlayerController();
PC->SetupInputComponent();  // 입력 바인딩
PC->Possess(Pawn);          // 폰 제어

// [4] Pawn - 물리적 표현 (복제됨)
APawn* Pawn = SpawnPawn();
Pawn->AddMovementInput(Direction);  // 이동

// [5] PlayerState - 복제된 플레이어 정보
APlayerState* PS = PC->PlayerState;
PS->Score += 10;  // 모든 클라이언트에게 복제

// ✅ 장점:
// - 각 역할이 명확히 분리
// - 네트워크 복제 최적화
// - AI와 플레이어가 동일한 Pawn 사용
// - 맵 전환 시 필요한 것만 유지 (GameInstance, PlayerState)
```

### 프레임워크 분리 이점

| 문제 | 단일 클래스 | 분리된 프레임워크 |
|------|------------|------------------|
| **네트워크 복제** | ❌ 모든 것을 복제? 비효율 | ✅ 역할별 복제 (GameState, PlayerState만) |
| **AI 지원** | ❌ 입력 처리가 포함되어 있음 | ✅ AI는 Controller만 교체 |
| **맵 전환** | ❌ 모든 것이 소멸 | ✅ GameInstance, PlayerState 유지 가능 |
| **스플릿스크린** | ❌ 불가능 | ✅ LocalPlayer 여러 개 |
| **서버 권한** | ❌ 규칙 검증 불가 | ✅ GameMode는 서버에만 존재 |
| **역할 분리** | ❌ 모든 것이 한 곳에 | ✅ 입력/표현/규칙/상태 분리 |

---

## 🔄 생명주기 상세

### 게임 시작부터 플레이어 스폰까지

```
┌─────────────────────────────────────────────────────────────────────────┐
│              Game Start → Player Spawn Sequence                         │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [Phase 1: 엔진 초기화]                                                  │
│  ────────────────────────────────────────────────────────────────────  │
│  FEngineLoop::PreInit()                  // 엔진 부팅                   │
│  FEngineLoop::Init()                     // 렌더링, 물리 등 초기화       │
│      ↓                                                                  │
│  [Phase 2: GameInstance 생성]                                            │
│  ────────────────────────────────────────────────────────────────────  │
│  UGameInstance* GI = NewObject<UGameInstance>()                        │
│  GI->Init()                              // 📂 GameInstance.h:217       │
│    - OnlineSession 초기화                                                │
│    - Subsystem 로드                                                      │
│    - TimerManager, LatentActionManager 생성                             │
│      ↓                                                                  │
│  GI->StartGameInstance()                 // 📂 GameInstance.h:301       │
│    - 초기 맵 로드 트리거                                                  │
│      ↓                                                                  │
│  [Phase 3: 맵 로드]                                                      │
│  ────────────────────────────────────────────────────────────────────  │
│  UEngine::LoadMap(URL)                                                 │
│    - 기존 World 정리 (있다면)                                            │
│    - 새 UWorld 생성                                                      │
│    - 레벨 스트리밍                                                        │
│      ↓                                                                  │
│  [Phase 4: GameMode 생성]                                                │
│  ────────────────────────────────────────────────────────────────────  │
│  AGameModeBase* GM = World->SpawnActor<AGameModeBase>()                │
│  GM->InitGame(MapName, Options, ErrorMessage)  // 📂 GameModeBase.h:62 │
│    - OptionsString 파싱 (?Spectator=1 등)                               │
│    - GameSession 생성                                                    │
│      ↓                                                                  │
│  GM->PreInitializeComponents()           // AActor 생명주기             │
│      ↓                                                                  │
│  [Phase 5: GameState 생성]                                               │
│  ────────────────────────────────────────────────────────────────────  │
│  AGameStateBase* GS = World->SpawnActor<AGameStateBase>()              │
│  GM->GameState = GS                                                    │
│  GM->InitGameState()                     // 📂 GameModeBase.h:69        │
│    - GameState 초기값 설정                                               │
│      ↓                                                                  │
│  [Phase 6: 플레이어 로그인 (네트워크 또는 로컬)]                           │
│  ────────────────────────────────────────────────────────────────────  │
│  (서버) 클라이언트 접속 또는 (로컬) CreateLocalPlayer()                   │
│      ↓                                                                  │
│  GM->PreLogin(Options, Address, UniqueId, ErrorMessage)                │
│                                          // 📂 GameModeBase.h:293       │
│    - 접속 승인 여부 결정                                                  │
│    - 인원수 제한 확인                                                     │
│    - 밴 여부 확인                                                        │
│    - ErrorMessage 설정 시 거부                                           │
│      ↓                                                                  │
│  APlayerController* PC = GM->Login(NewPlayer, RemoteRole, Portal,      │
│                                     Options, UniqueId, Error)          │
│                                          // 📂 GameModeBase.h:323       │
│    - SpawnPlayerController() 호출                                        │
│    - PlayerState 생성 및 할당                                            │
│    - InitNewPlayer() - 이름, 옵션 설정                                   │
│      ↓                                                                  │
│  GM->PostLogin(PC)                       // 📂 GameModeBase.h:326       │
│    - HandleStartingNewPlayer() 호출                                      │
│    - RestartPlayer() 호출 (조건 충족 시)                                  │
│      ↓                                                                  │
│  [Phase 7: Pawn 스폰]                                                    │
│  ────────────────────────────────────────────────────────────────────  │
│  GM->RestartPlayer(PC)                   // 📂 GameModeBase.h:441       │
│      ↓                                                                  │
│  AActor* StartSpot = GM->FindPlayerStart(PC, IncomingName)             │
│                                          // 📂 GameModeBase.h:422       │
│    - ChoosePlayerStart() 호출                                            │
│    - PlayerStart 액터 검색                                               │
│      ↓                                                                  │
│  APawn* NewPawn = GM->SpawnDefaultPawnFor(PC, StartSpot)               │
│                                          // 📂 GameModeBase.h:458       │
│    - DefaultPawnClass 인스턴스 생성                                       │
│      ↓                                                                  │
│  PC->Possess(NewPawn)                    // Controller → Pawn 빙의      │
│    - PC->Pawn = NewPawn                                                │
│    - NewPawn->Controller = PC                                          │
│    - NewPawn->PossessedBy(PC)           // BP: OnPossessed             │
│    - PC->OnPossess(NewPawn)             // BP: OnPossess               │
│      ↓                                                                  │
│  [Phase 8: BeginPlay]                                                   │
│  ────────────────────────────────────────────────────────────────────  │
│  GM->StartPlay()                         // 📂 GameModeBase.h:159       │
│    - World->BeginPlay()                                                │
│      - 모든 Actor->BeginPlay() 호출                                      │
│                                                                         │
│  ✅ 플레이어 게임 진입 완료!                                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 맵 전환 (Travel)

```cpp
// [1] Non-Seamless Travel (전통적인 맵 전환)
// ─────────────────────────────────────────────────────────────

// 클라이언트 여행
PC->ClientTravel("/Game/Maps/NewMap", TRAVEL_Absolute);

// 서버 여행 (모든 플레이어 이동)
World->ServerTravel("/Game/Maps/NewMap");

// 프로세스:
// 1. 현재 World 소멸 (모든 Actor 파괴)
// 2. 연결 끊김 (네트워크 재연결 필요)
// 3. 새 World 로드
// 4. GameMode, GameState 재생성
// 5. 플레이어 재접속 (Login 다시 호출)
// 6. Pawn 재스폰

// ❌ 단점:
// - 로딩 시간 김
// - 네트워크 재연결
// - 플레이어 정보 손실 가능
```

```cpp
// [2] Seamless Travel (끊김 없는 맵 전환)
// ─────────────────────────────────────────────────────────────

// GameMode에서 활성화
GameMode->bUseSeamlessTravel = true;

// 서버 여행
World->ServerTravel("/Game/Maps/NewMap?bSeamless=true");

// 프로세스:
// 1. 전환 맵으로 이동 (/Engine/Maps/Transition)
// 2. GetSeamlessTravelActorList() 호출
//    - PlayerController, PlayerState, GameState 등 유지
// 3. 백그라운드에서 새 맵 로드
// 4. 새 GameMode 생성
// 5. 유지된 Actor들 새 World로 이동
// 6. HandleSeamlessTravelPlayer() 호출
// 7. PostSeamlessTravel() 호출

// ✅ 장점:
// - 연결 유지
// - PlayerState 유지 (점수, 인벤토리 등)
// - 끊김 없는 전환

// 유지되는 Actor 지정
void AMyGameMode::GetSeamlessTravelActorList(bool bToTransition, TArray<AActor*>& ActorList)
{
    Super::GetSeamlessTravelActorList(bToTransition, ActorList);

    // 커스텀 Actor 유지
    for (TActorIterator<AMyInventoryManager> It(GetWorld()); It; ++It)
    {
        ActorList.Add(*It);
    }
}
```

---

## 🧩 주요 패턴 및 사용법

### 1. GameInstance - 글로벌 데이터 관리

```cpp
// MyGameInstance.h
UCLASS()
class UMyGameInstance : public UGameInstance
{
    GENERATED_BODY()

public:
    virtual void Init() override;
    virtual void Shutdown() override;

    // 맵 전환에도 유지되는 데이터
    UPROPERTY(BlueprintReadWrite)
    int32 PlayerTotalScore = 0;

    UPROPERTY(BlueprintReadWrite)
    FString PlayerName;

    // 세이브/로드
    UFUNCTION(BlueprintCallable)
    void SaveGame();

    UFUNCTION(BlueprintCallable)
    void LoadGame();
};

// MyGameInstance.cpp
void UMyGameInstance::Init()
{
    Super::Init();

    // 게임 시작 시 한 번만 실행
    UE_LOG(LogTemp, Log, TEXT("GameInstance Initialized"));

    LoadGame();  // 저장된 데이터 로드
}

void UMyGameInstance::Shutdown()
{
    SaveGame();  // 종료 시 저장

    Super::Shutdown();
}

// 사용 예시 - 어디서든 접근 가능
UMyGameInstance* GI = Cast<UMyGameInstance>(GetGameInstance());
if (GI)
{
    GI->PlayerTotalScore += 100;
}
```

### 2. GameMode - 게임 규칙 구현

```cpp
// MyGameMode.h
UCLASS()
class AMyGameMode : public AGameModeBase
{
    GENERATED_BODY()

public:
    virtual void InitGame(const FString& MapName, const FString& Options, FString& ErrorMessage) override;
    virtual void PreLogin(const FString& Options, const FString& Address, const FUniqueNetIdRepl& UniqueId, FString& ErrorMessage) override;
    virtual void PostLogin(APlayerController* NewPlayer) override;
    virtual void Logout(AController* Exiting) override;

    // 커스텀 규칙
    UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
    int32 MaxPlayers = 4;

    UPROPERTY(EditDefaultsOnly)
    float RespawnDelay = 3.0f;

    UFUNCTION(BlueprintCallable)
    void CheckWinCondition();
};

// MyGameMode.cpp
void AMyGameMode::PreLogin(const FString& Options, const FString& Address, const FUniqueNetIdRepl& UniqueId, FString& ErrorMessage)
{
    Super::PreLogin(Options, Address, UniqueId, ErrorMessage);

    // 인원수 제한
    if (GetNumPlayers() >= MaxPlayers)
    {
        ErrorMessage = TEXT("Server is full");
        return;
    }

    // 밴 확인 (예시)
    if (IsBanned(UniqueId))
    {
        ErrorMessage = TEXT("You are banned");
        return;
    }
}

void AMyGameMode::PostLogin(APlayerController* NewPlayer)
{
    Super::PostLogin(NewPlayer);

    // 플레이어 입장 알림
    AMyPlayerState* PS = NewPlayer->GetPlayerState<AMyPlayerState>();
    if (PS)
    {
        UE_LOG(LogTemp, Log, TEXT("Player %s joined the game"), *PS->GetPlayerName());
    }

    // 게임 시작 체크
    if (GetNumPlayers() >= 2)
    {
        StartMatch();
    }
}

void AMyGameMode::CheckWinCondition()
{
    // 승리 조건 확인
    for (FConstPlayerControllerIterator It = GetWorld()->GetPlayerControllerIterator(); It; ++It)
    {
        AMyPlayerState* PS = (*It)->GetPlayerState<AMyPlayerState>();
        if (PS && PS->Score >= 100)
        {
            EndMatch(PS);
            return;
        }
    }
}
```

### 3. PlayerController - 입력 및 RPC

```cpp
// MyPlayerController.h
UCLASS()
class AMyPlayerController : public APlayerController
{
    GENERATED_BODY()

public:
    virtual void SetupInputComponent() override;
    virtual void OnPossess(APawn* InPawn) override;

    // 입력 액션
    void MoveForward(float Value);
    void MoveRight(float Value);
    void Jump();
    void Fire();

    // RPC (서버 함수)
    UFUNCTION(Server, Reliable, WithValidation)
    void ServerFire(FVector Location, FRotator Rotation);

    // RPC (클라이언트 함수)
    UFUNCTION(Client, Reliable)
    void ClientShowMessage(const FString& Message);

    // RPC (멀티캐스트)
    UFUNCTION(NetMulticast, Reliable)
    void MulticastPlayEffect(FVector Location);
};

// MyPlayerController.cpp
void AMyPlayerController::SetupInputComponent()
{
    Super::SetupInputComponent();

    // 입력 바인딩
    InputComponent->BindAxis("MoveForward", this, &AMyPlayerController::MoveForward);
    InputComponent->BindAxis("MoveRight", this, &AMyPlayerController::MoveRight);
    InputComponent->BindAction("Jump", IE_Pressed, this, &AMyPlayerController::Jump);
    InputComponent->BindAction("Fire", IE_Pressed, this, &AMyPlayerController::Fire);
}

void AMyPlayerController::MoveForward(float Value)
{
    if (APawn* ControlledPawn = GetPawn())
    {
        FVector Direction = ControlledPawn->GetActorForwardVector();
        ControlledPawn->AddMovementInput(Direction, Value);
    }
}

void AMyPlayerController::Fire()
{
    // 클라이언트 → 서버 RPC
    FVector Location = GetPawn()->GetActorLocation();
    FRotator Rotation = GetControlRotation();

    ServerFire(Location, Rotation);
}

void AMyPlayerController::ServerFire_Implementation(FVector Location, FRotator Rotation)
{
    // 서버에서 실행
    // 발사 로직 (히트스캔, 데미지 등)

    // 모든 클라이언트에게 이펙트 표시
    MulticastPlayEffect(Location);

    // 특정 클라이언트에게만 메시지
    ClientShowMessage(TEXT("You fired!"));
}

bool AMyPlayerController::ServerFire_Validate(FVector Location, FRotator Rotation)
{
    // 치팅 방지 검증
    return true;  // 유효한 요청이면 true
}

void AMyPlayerController::ClientShowMessage_Implementation(const FString& Message)
{
    // 클라이언트에서만 실행
    if (GEngine)
    {
        GEngine->AddOnScreenDebugMessage(-1, 3.0f, FColor::Green, Message);
    }
}

void AMyPlayerController::MulticastPlayEffect_Implementation(FVector Location)
{
    // 모든 클라이언트 (서버 포함)에서 실행
    UGameplayStatics::SpawnEmitterAtLocation(GetWorld(), MuzzleFlashEffect, Location);
}
```

### 4. Pawn/Character - 플레이어 표현

```cpp
// MyCharacter.h
UCLASS()
class AMyCharacter : public ACharacter
{
    GENERATED_BODY()

public:
    AMyCharacter();

    virtual void Tick(float DeltaTime) override;
    virtual void SetupPlayerInputComponent(UInputComponent* PlayerInputComponent) override;
    virtual void PossessedBy(AController* NewController) override;

    // 복제된 프로퍼티
    UPROPERTY(Replicated, BlueprintReadOnly)
    int32 Health = 100;

    UPROPERTY(Replicated)
    bool bIsAiming = false;

    // 서버 함수
    UFUNCTION(Server, Reliable)
    void ServerSetAiming(bool bNewAiming);

    UFUNCTION(BlueprintCallable)
    void TakeDamageFromServer(int32 Damage);

    // 복제 설정
    virtual void GetLifetimeReplicatedProps(TArray<FLifetimeProperty>& OutLifetimeProps) const override;
};

// MyCharacter.cpp
#include "Net/UnrealNetwork.h"

AMyCharacter::AMyCharacter()
{
    // 복제 활성화
    bReplicates = true;
    SetReplicateMovement(true);
}

void AMyCharacter::GetLifetimeReplicatedProps(TArray<FLifetimeProperty>& OutLifetimeProps) const
{
    Super::GetLifetimeReplicatedProps(OutLifetimeProps);

    // Health 복제 (모든 클라이언트)
    DOREPLIFETIME(AMyCharacter, Health);

    // bIsAiming 복제 (소유자 제외 - 소유자는 이미 알고 있음)
    DOREPLIFETIME_CONDITION(AMyCharacter, bIsAiming, COND_SkipOwner);
}

void AMyCharacter::ServerSetAiming_Implementation(bool bNewAiming)
{
    bIsAiming = bNewAiming;  // 복제됨
}

void AMyCharacter::TakeDamageFromServer(int32 Damage)
{
    // 서버에서만 실행되어야 함
    if (!HasAuthority())
    {
        return;
    }

    Health -= Damage;

    if (Health <= 0)
    {
        // 사망 처리
        Destroy();
    }
}
```

### 5. GameState / PlayerState - 복제된 정보

```cpp
// MyGameState.h
UCLASS()
class AMyGameState : public AGameStateBase
{
    GENERATED_BODY()

public:
    // 복제된 게임 정보
    UPROPERTY(Replicated, BlueprintReadOnly)
    int32 RemainingTime = 300;  // 5분

    UPROPERTY(Replicated, BlueprintReadOnly)
    int32 TeamAScore = 0;

    UPROPERTY(Replicated, BlueprintReadOnly)
    int32 TeamBScore = 0;

    // 서버 전용 함수
    UFUNCTION(BlueprintCallable)
    void AddScore(int32 Team, int32 Score);

    virtual void GetLifetimeReplicatedProps(TArray<FLifetimeProperty>& OutLifetimeProps) const override;
};

// MyGameState.cpp
void AMyGameState::GetLifetimeReplicatedProps(TArray<FLifetimeProperty>& OutLifetimeProps) const
{
    Super::GetLifetimeReplicatedProps(OutLifetimeProps);

    DOREPLIFETIME(AMyGameState, RemainingTime);
    DOREPLIFETIME(AMyGameState, TeamAScore);
    DOREPLIFETIME(AMyGameState, TeamBScore);
}

void AMyGameState::AddScore(int32 Team, int32 Score)
{
    if (!HasAuthority())
    {
        return;  // 서버에서만
    }

    if (Team == 0)
    {
        TeamAScore += Score;  // 자동으로 모든 클라이언트에 복제
    }
    else
    {
        TeamBScore += Score;
    }
}

// MyPlayerState.h
UCLASS()
class AMyPlayerState : public APlayerState
{
    GENERATED_BODY()

public:
    UPROPERTY(Replicated, BlueprintReadOnly)
    int32 Kills = 0;

    UPROPERTY(Replicated, BlueprintReadOnly)
    int32 Deaths = 0;

    UPROPERTY(Replicated, BlueprintReadOnly)
    int32 Level = 1;

    virtual void GetLifetimeReplicatedProps(TArray<FLifetimeProperty>& OutLifetimeProps) const override;
};

// 사용 예시 - 스코어보드 UI
void UMyScoreboardWidget::UpdateScoreboard()
{
    for (FConstPlayerControllerIterator It = GetWorld()->GetPlayerControllerIterator(); It; ++It)
    {
        AMyPlayerState* PS = (*It)->GetPlayerState<AMyPlayerState>();
        if (PS)
        {
            // 모든 클라이언트에서 접근 가능
            FString Entry = FString::Printf(TEXT("%s: %d Kills, %d Deaths"),
                *PS->GetPlayerName(), PS->Kills, PS->Deaths);

            AddScoreboardEntry(Entry);
        }
    }
}
```

---

## 🚨 일반적인 함정

### ❌ GameMode를 클라이언트에서 사용

```cpp
// ❌ 나쁨: 클라이언트에서 GameMode 접근
void AMyActor::Tick(float DeltaTime)
{
    AMyGameMode* GM = Cast<AMyGameMode>(GetWorld()->GetAuthGameMode());

    if (GM)  // 클라이언트에서는 항상 nullptr!
    {
        GM->CheckWinCondition();  // 실행 안 됨
    }
}

// ✅ 좋음: GameState 사용 (복제됨)
void AMyActor::Tick(float DeltaTime)
{
    AMyGameState* GS = GetWorld()->GetGameState<AMyGameState>();

    if (GS)  // 모든 클라이언트에서 유효
    {
        int32 TimeLeft = GS->RemainingTime;
        // UI 업데이트 등
    }
}

// ✅ 또는: 서버에서만 실행
void AMyActor::CheckWin()
{
    if (!HasAuthority())
    {
        return;  // 클라이언트는 실행 안 함
    }

    AMyGameMode* GM = GetWorld()->GetAuthGameMode<AMyGameMode>();
    if (GM)
    {
        GM->CheckWinCondition();  // 서버에서만 실행
    }
}
```

### ❌ PlayerState와 PlayerController 혼동

```cpp
// ❌ 나쁨: PlayerController에 복제 데이터 저장
UCLASS()
class AMyPlayerController : public APlayerController
{
    UPROPERTY(Replicated)  // ❌ PC는 자기 것만 존재!
    int32 Score;  // 다른 플레이어가 볼 수 없음!
};

// ✅ 좋음: PlayerState에 복제 데이터 저장
UCLASS()
class AMyPlayerState : public APlayerState
{
    UPROPERTY(Replicated)  // ✅ 모든 클라이언트에 복제
    int32 Score;  // 모든 플레이어가 볼 수 있음
};

// 사용 예시
void ShowAllPlayerScores()
{
    // 모든 플레이어 순회
    for (FConstPlayerControllerIterator It = GetWorld()->GetPlayerControllerIterator(); It; ++It)
    {
        APlayerState* PS = (*It)->GetPlayerState();
        if (PS)
        {
            // PlayerState는 모든 클라이언트에서 접근 가능
            UE_LOG(LogTemp, Log, TEXT("%s: %f"), *PS->GetPlayerName(), PS->GetScore());
        }
    }
}
```

### ❌ 로컬 함수에서 서버 함수 호출

```cpp
// ❌ 나쁨: Authority 체크 없이 서버 로직
void AMyCharacter::TakeDamage(int32 Damage)
{
    Health -= Damage;  // 클라이언트에서 실행 시 동기화 안 됨!
}

// ✅ 좋음: Authority 체크
void AMyCharacter::TakeDamage(int32 Damage)
{
    if (!HasAuthority())
    {
        return;  // 클라이언트는 실행 안 함
    }

    Health -= Damage;  // 서버에서만 실행, 자동 복제
}

// ✅ 또는: 서버 RPC 사용
void AMyCharacter::ClientRequestDamage(int32 Damage)
{
    // 클라이언트 → 서버 요청
    ServerTakeDamage(Damage);
}

void AMyCharacter::ServerTakeDamage_Implementation(int32 Damage)
{
    Health -= Damage;  // 서버에서 실행, 복제됨
}
```

---

## 🔍 디버깅 팁

### 콘솔 명령어

```bash
# 네트워크 디버깅
showdebug net              # 네트워크 통계
stat net                   # 네트워크 상세
netprofile                 # 네트워크 프로파일

# GameFramework 정보
showdebug game             # 게임 정보
showdebug player           # 플레이어 정보
showdebug pawn             # 폰 정보

# 복제 디버깅
net.ReplicationVisualize 1 # 복제 시각화

# Authority 확인
show Collision             # 충돌 표시 (Authority는 보라색)
```

### 로깅

```cpp
// Authority 확인
UE_LOG(LogTemp, Log, TEXT("HasAuthority: %s"), HasAuthority() ? TEXT("true") : TEXT("false"));
UE_LOG(LogTemp, Log, TEXT("Role: %d, RemoteRole: %d"), (int32)GetLocalRole(), (int32)GetRemoteRole());

// 네트워크 역할
// ROLE_None           = 0
// ROLE_SimulatedProxy = 1  // 다른 클라이언트의 복제본
// ROLE_AutonomousProxy= 2  // 자기 클라이언트
// ROLE_Authority      = 3  // 서버

// GameMode 존재 확인
AGameModeBase* GM = GetWorld()->GetAuthGameMode();
UE_LOG(LogTemp, Log, TEXT("GameMode exists: %s"), GM ? TEXT("true") : TEXT("false"));

// PlayerController 개수
int32 NumPCs = 0;
for (FConstPlayerControllerIterator It = GetWorld()->GetPlayerControllerIterator(); It; ++It)
{
    NumPCs++;
}
UE_LOG(LogTemp, Log, TEXT("PlayerController count: %d"), NumPCs);
```

---

## 🔗 참고자료

- [Gameplay Framework](https://docs.unrealengine.com/gameplay-framework-in-unreal-engine/)
- [GameMode and GameState](https://docs.unrealengine.com/game-mode-and-game-state-in-unreal-engine/)
- [PlayerController](https://docs.unrealengine.com/player-controllers-in-unreal-engine/)
- [Pawn](https://docs.unrealengine.com/pawns-in-unreal-engine/)
- [Networking](https://docs.unrealengine.com/networking-and-multiplayer-in-unreal-engine/)
- [GameInstance.h Source](Engine/Source/Runtime/Engine/Classes/Engine/GameInstance.h)
- [GameModeBase.h Source](Engine/Source/Runtime/Engine/Classes/GameFramework/GameModeBase.h)
- [PlayerController.h Source](Engine/Source/Runtime/Engine/Classes/GameFramework/PlayerController.h)

**연관 문서:**
- [Core/EngineInitialization.md](../Core/EngineInitialization.md) - 엔진 초기화 및 GameInstance 생성
- [CoreUObject/PackageAndLinker.md](../CoreUObject/PackageAndLinker.md) - 맵 로딩

---

## 🔗 크로스 레퍼런스

### Actor 생명주기와 관련 시스템

```
SpawnActor (World.md)
     ↓
Actor 생성자 (Actor.md)
     ↓
PostActorCreated (Actor.md)
     ↓
Component 등록 (Component.md)
     ↓
BeginPlay (Actor.md)
     ↓
Tick 시작 (TickSystem.md)
     ↓
Possession (PlayerController.md, Pawn.md)
     ↓
게임플레이 (GameMode.md)
     ↓
EndPlay (Actor.md)
     ↓
Destroy (Actor.md)
```

### 입력 처리 흐름

```
UGameViewportClient::InputKey (PlayerController.md)
     ↓
PlayerController::InputKey (PlayerController.md)
     ↓
PlayerInput (PlayerController.md)
     ↓
InputComponent (Pawn.md, Actor.md)
     ↓
Bound Functions (Pawn.md)
     ↓
Pawn->AddMovementInput (Pawn.md)
     ↓
ControlInputVector 누적 (Pawn.md)
     ↓
MovementComponent->TickComponent (Component.md, TickSystem.md)
     ↓
ConsumeMovementInputVector (Pawn.md)
     ↓
CalcVelocity (Pawn.md)
```

### Transform 업데이트 흐름

```
SetActorLocation (Actor.md)
     ↓
SetWorldLocation (Component.md)
     ↓
UpdateComponentToWorld (Component.md)
     ↓
PropagateTransformUpdate (Component.md)
     ├─ UpdateBounds (Component.md)
     ├─ MarkRenderTransformDirty (Component.md)
     └─ UpdateChildTransforms (Component.md)
     ↓
DoDeferredRenderUpdates_Concurrent (Component.md, TickSystem.md)
     └─ SendRenderTransform_Concurrent (Component.md)
```

### 네트워크 복제 흐름

```
GameMode (서버만) (GameMode.md)
     ↓
GameState (복제) (GameMode.md)
     ↓ 모든 클라이언트
PlayerState (복제) (PlayerController.md, Pawn.md)
     ↓ 모든 클라이언트
Actor (복제 가능) (Actor.md)
     ↓ Relevancy 체크
Component (복제 가능) (Component.md)
```

---

## 🎯 실전 활용 가이드

### 새로운 게임플레이 Actor 만들기

1. **클래스 상속 선택**
   - 단순 개체: AActor
   - 제어 가능: APawn
   - 이족 보행: ACharacter
   - 차량/특수: APawn + Custom MovementComponent

2. **Component 추가**
   - Transform: USceneComponent
   - 렌더링: UStaticMeshComponent, USkeletalMeshComponent
   - 충돌: UCapsuleComponent, UBoxComponent, USphereComponent
   - 기능: Custom UActorComponent

3. **Tick 설정**
   - bCanEverTick: true로 설정
   - TickGroup: 물리와의 관계 고려
   - TickInterval: 성능 최적화 필요 시
   - Prerequisites: 의존성 설정

4. **생명주기 이벤트 오버라이드**
   - PostInitializeComponents(): 초기 설정
   - BeginPlay(): 게임 시작 로직
   - Tick(): 매 프레임 업데이트
   - EndPlay(): 정리 작업

### 네트워크 게임 만들기

1. **GameMode 설정 (서버)**
   - Login: 플레이어 접속 처리
   - RestartPlayer: Pawn 스폰
   - 규칙 검증: CanDamage 등

2. **GameState 설정 (복제)**
   - 게임 상태 정보 (점수, 시간 등)
   - UPROPERTY(Replicated) 사용

3. **PlayerState 설정 (복제)**
   - 플레이어별 정보 (킬, 데스, 팀 등)
   - UPROPERTY(Replicated) 사용

4. **Actor 복제 설정**
   - bReplicates = true
   - UPROPERTY(Replicated) 사용
   - GetLifetimeReplicatedProps 오버라이드

### 성능 최적화

1. **Tick 최적화**
   - TickInterval 사용 (AI는 0.2초마다)
   - SetActorTickEnabled로 동적 제어
   - LOD 기반 TickInterval 조정

2. **Component 최적화**
   - bAutoRegister: 불필요하면 false
   - Mobility: Static/Stationary/Movable 적절히 사용
   - Attachment 최소화

3. **Level 최적화**
   - Level Streaming 사용
   - Actor 가시성 체크
   - Occlusion Culling

---

## 📚 문서 읽기 순서 추천

### 초급 (GameFramework 기초)

1. **[World.md](World.md)** - 게임 월드의 구조 이해
2. **[Actor.md](Actor.md)** - Actor 생명주기와 기본 개념
3. **[Component.md](Component.md)** - Component 시스템
4. **[GameMode.md](GameMode.md)** - 게임 규칙과 진행

### 중급 (플레이어 제어)

5. **[PlayerController.md](PlayerController.md)** - 입력과 카메라
6. **[Pawn.md](Pawn.md)** - Pawn과 Character
7. **[TickSystem.md](TickSystem.md)** - Tick 시스템 이해

### 고급 (심층 분석)

8. **[Actor_Lifecycle_And_Replication_Deep_Dive.md](Actor_Lifecycle_And_Replication_Deep_Dive.md)** - Actor 생명주기 및 리플리케이션 심층 분석
9. **[World_Tick_Pipeline_Deep_Dive.md](World_Tick_Pipeline_Deep_Dive.md)** - World Tick 파이프라인 심층 분석
10. **모든 문서 재독** - 크로스 레퍼런스 따라가며 통합 이해
11. **실제 UE5 Engine 소스 읽기** - 최신 변경사항 확인

---

## 🛠️ 트러블슈팅

### 자주 하는 실수

1. **Tick 관련**
   - bCanEverTick = false인데 Tick()이 호출 안 됨
   - Prerequisites 순환 참조
   - bRunOnAnyThread = true인데 UObject 접근 (크래시)

2. **Component 관련**
   - RegisterComponent() 누락
   - AttachToComponent의 Transform 규칙 잘못 사용
   - RootComponent가 nullptr

3. **Possession 관련**
   - Possess 전에 Pawn에 접근
   - PlayerState가 nullptr (아직 복제 안 됨)
   - InputComponent가 nullptr (PawnClientRestart 전)

4. **네트워크 관련**
   - GameMode를 클라이언트에서 접근 (nullptr)
   - Replicated Property 업데이트 안 됨 (GetLifetimeReplicatedProps 누락)
   - RPC 호출 안 됨 (서버/클라이언트 구분 실수)

### 디버깅 팁

```cpp
// Actor 상태 확인
UE_LOG(LogTemp, Warning, TEXT("Actor: %s, HasAuthority: %d, Role: %d"),
    *GetName(), HasAuthority(), GetLocalRole());

// Component 등록 확인
UE_LOG(LogTemp, Warning, TEXT("Component: %s, IsRegistered: %d"),
    *MyComponent->GetName(), MyComponent->IsRegistered());

// Tick 활성화 확인
UE_LOG(LogTemp, Warning, TEXT("TickEnabled: %d, TickInterval: %f"),
    PrimaryActorTick.IsTickFunctionEnabled(), PrimaryActorTick.TickInterval);

// Possession 확인
UE_LOG(LogTemp, Warning, TEXT("Controller: %s, Pawn: %s"),
    *GetController()->GetName(), *GetPawn()->GetName());
```

---

## 📋 핵심 개념 요약

### Tick 시스템

**FTickFunction**
- 매 프레임 업데이트 단위
- TickGroup (TG_PrePhysics, TG_PostPhysics 등)
- Prerequisites (의존성)
- TickInterval (쿨다운)

**FTickTaskLevel**
- Level별 Tick 관리
- AllEnabledTickFunctions (매 프레임)
- AllCoolingDownTickFunctions (TickInterval 대기)
- AllDisabledTickFunctions (비활성화)

**FTickTaskSequencer**
- TaskGraph 통합
- TGraphTask로 변환 및 스케줄링
- Completion Event 관리

**FTickTaskManager**
- 최상위 Tick 관리자
- World::Tick의 진입점
- StartFrame → RunTickGroup → EndFrame

**관계:** World::Tick → FTickTaskManager → FTickTaskLevel → FTickFunction → Actor/Component::Tick

---

> 📅 생성: 2025-10-21 — 게임 프레임워크 문서화 (UE 5.7 검증 완료)
> 🔄 Updated: 2026-02-18 — README.md를 Overview.md에 통합, 관련 문서 교차 참조 추가
